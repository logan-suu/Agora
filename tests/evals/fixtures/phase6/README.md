# Phase 6 eval fixtures

This versioned internal fixture set exercises Agora's production contracts without copying any
public benchmark data. The task catalog pins this directory as `phase6-fixture-v1`; task
fingerprints capture the complete per-task definition.
