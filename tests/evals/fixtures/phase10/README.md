# Phase 10 benchmark fixtures

The Docker image contains evaluation dependencies only. It is not a product runtime dependency. The completed OpenCode Go comparisons are documented in `docs/evals/phase10-opencode-go-final-report.md`: the retained v11 public comparison has 36 attempts, and the v14 internal comparison has 18 attempts. Their results and frozen source versions remain separate.

```sh
docker build -t agora-benchmark:task105 -f tests/evals/fixtures/phase10/Dockerfile tests/evals/fixtures/phase10
pnpm eval:phase10:preflight
docker run --rm --network none agora-benchmark:task105 cat /opt/benchmark/package-lock.json > .data/evals/phase10-preflight/environment-package-lock.json
pnpm eval:phase10:freeze
pnpm eval:phase10:model
pnpm eval:phase10:report
```

The public adapter pins Aider Polyglot exercises at `7e0611e77b54e2dea774cdc0aa00cf9f7ed6144f`. Only four JavaScript exercises are downloaded to the ignored `.data/evals/phase10-preflight/sources` directory. Source manifests classify original file purposes; the frozen group's `modelSeedFiles` records actual model visibility. Protocol v3 supplies instructions, stubs and the upstream public test text as non-executable `.spec.txt` contracts. The private source and independent Jest verifier retain the original `.spec.js` filename. Reference answers and independent verifier configuration never enter agent workspaces. All original `xtest` cases are activated without changing assertions, following Aider's helper at `5dc9490bb35f9729ef2c95d00a19ccd30c26339c`. The upstream license is MIT; individual LICENSE files and hashes are retained with each downloaded task.

`holdout.ts` collects versioned original contracts, fixed DAGs, independent acceptance tests, and preflight references. The v14 matrix selects `thermal-inspection` and `daily-availability`; older internal fixtures remain development and historical evidence. Drivers receive only the contract, seed allowlist, and declared Leader events. References and acceptance sources run only inside independent verifier containers. Once a model sees a holdout, changing its contract creates a different experiment, not another attempt in the same group.

The current frozen group is `.data/evals/phase10-final-v14/group.json`; all 18 attempts are final and must not be silently rerun. The model entry additionally requires the explicit matching `AGORA_GO_FORMAL_AUTHORIZATION` and existing authorized ledger; the commands above do not grant new paid-run authorization. `AGORA_BENCHMARK_GROUP=phase10-final-v1 pnpm eval:phase10:report` reads the retained historical group. Group IDs are restricted versioned basenames. Actual `EvalResult` directories use the existing canonical `.data/evals` runner root and are linked from this registry. Code, source, image, seed, task and model fingerprints must match on resume. Budget trial identities include the group. Started attempts are retained; the command never silently reruns them. Interrupted execution locks and unknown-cost reservations require an explicit audit. An operator may conservatively charge the entire pre-request reservation for irrecoverable usage, retaining the audit and distinguishing the upper bound from measured cost.

To stop admitting attempts, create `stop-requested.json` inside the selected group directory. The runner checks the marker before another attempt and before subsequent model/tool admission. An already active model stream is not canceled; its step closes and cleanup is retained. Review the original stop reason, accounting, source identity and cleanup before resuming any pending entries. Removing the marker permits a later resume within the original group time/budget limits. A stopped incomplete experiment exits nonzero and retains the pending list. Do not send a process signal during a model stream.

Leader approved v2 after v1 exposed missing interface contracts: public tests specify exact error messages and duplicate-student behavior absent or inconsistent in the prose. V1 retains 11 normally completed samples, two operator-interrupted records and 41 unstarted entries. Its USD0.489798432 remains charged to the original formal pool. The corrected group runs a new 54-attempt group inside the unchanged USD20 total/17 formal/3 diagnostic budget. V2 then exposed an adapter defect: the production Node validator automatically collects `.spec.js`, so the readable Jest document became an executable test. Its three completed records and USD0.3067079 are retained; the active attempt finished naturally before the stop marker prevented the fourth. V3 changes only the model-side document filename to `.spec.txt` and records a new task/protocol/input fingerprint, with a real multi-role regression for the collector boundary. No model has seen either internal holdout before v3. Neither historical group is pooled with v3. All 16 historical records plus the new 54 remain auditable.

The historical official-DeepSeek budget remains USD 20 total: USD 17 formal and USD 3 diagnostic/regression. Current OpenCode Go formal groups share a separate original USD 5 subscription-quota-equivalent cap, including historical Go groups; the independent Go diagnostic budget is separate. Each formal attempt remains capped at USD 2. Current single/multi/parallel/sparse variants use `deepseek-v4-flash`; mixed PM/ARCHITECT/REVIEWER use `deepseek-flash`, with other roles using `deepseek-v4-flash`. The model command stops starting work when the remaining budget cannot cover another request, or after eight hours. It does not cancel an in-flight token stream. Failed model attempts produce a nonzero model-command exit; report generation remains available. Default `pnpm test` excludes `.eval.ts` files.

The 65536 context limit uses the locked Harness TokenMeter heuristic, including messages, system text and tool definitions. This is not an exact provider tokenizer or a byte limit. Each call records the estimate and actual provider usage separately. Monetary admission reserves against a conservative serialized-byte upper bound and the full output allowance before issuing a request.

Existing live regressions can share the audit with:

```sh
AGORA_EVAL_BUDGET_FILE="$PWD/.data/evals/phase10-budget.json" pnpm run test --maxWorkers=1
```

This preserves the configured model credential and the original tests. Single test-worker execution prevents unrelated processes from treating each other's in-flight reservations as interrupted requests. Raw response bytes are forwarded unchanged; only usage and bounded metadata are audited. No credential is written to the budget ledger or mounted into a container.

Public results are a fixed-subset evaluation, not an official Aider leaderboard score. Agent workspaces use `type=module` for production Node/TAP checks; independent Jest verification restores upstream configuration. The sole-solver prompt, multi-role workflow, independent grading, budgets and retries differ from Aider's native protocol. Completion-gate restoration does not imply a worker Fork: D17 never reopens done/failed workers. Actual paused-worker Fork coverage comes from the separate real integration regressions.

The single solver uses the same HarnessExecutor D1 agent-scoped SystemPrompt section: every request receives the current structured goal/file references separately from official session history. Pre-step admits one fixed start per turn, tool exchanges and bounded format repair; it does not append another projection user message after each tool. Official assistant/tool history and automatic compaction remain enabled, while raw group-chat logs are never admitted. Its sole-solver system prompt and lack of role handoffs are part of the declared single/multi treatment. The sparse treatment applies only after the multi-role production Channel projection; authoritative system slices remain intact.
