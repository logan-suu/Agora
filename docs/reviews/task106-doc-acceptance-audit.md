# T10.6 优化与出口验收文档核对

日期：2026-09-12（本地时间）。范围：Leader在T10.6中提出、已实现并验证的优化，以及新版视频接受后10.7应使用的文档预期。本轮只同步文档与任务索引，不修改生产代码或测试，不执行10.7。

## 结论

大多数优化已有来源定义和测试证据，但散落于历史追加段，10.7必读列表没有纳入这些具体条目。确有可能误判的表述，已从蓝图开始依次同步到详细设计、架构/选型、开发计划、task-status和AGENTS导航。当前出口对照入口为[详细设计§11.10](../详细设计方案.md#1110-t106-优化的-phase-10-出口验收对照任务-107-必读)，E01–E12分别指向来源及验证入口。对照表不是另立架构真相源，也不是通过报告。

## 已纠正的歧义

| 原表述或索引缺口 | 与批准/实现证据的关系 | 同步处理 |
| --- | --- | --- |
| D9“正常输入框及录制不用命令/JSON”、§11.9“只通过自然语言及真实确认控件” | 新需求变更确实如此；已获接受的视频在返工及最终批准使用既有`/resolve-gate`，本轮没有实现任意自然语言控制或通用gate按钮 | 蓝图D9及§11.9收窄为需求变更；保留D16独立裁决与旧命令。10.7不能要求尚未承诺的全命令替代 |
| §11.9开头“不新增生产能力” | 最初仅做材料，此后Leader批准了Markdown、草案确认和执行链修复 | 明确后续批准的生产契约有效，最初范围不能排除这些优化 |
| 历史“无自动跳转末尾”“暂停录制”及120秒Go idle | 后续已实现按阅读位置跟随；新片已接受；Go现行等待为300s/300s | 开发计划纠正无条件滚动表述，标注旧等待为中间版本；当前状态指向新片和§11.10 |
| 旧65K/8192演示容量、USD0.25/0.35/1等临时限制与冻结Eval夹具混杂 | Leader已取消T10.6临时限额并恢复正常容量；旧任务绑定、用户自定义值与10.5冻结协议仍应保留 | 明确已知新配置、旧绑定、自定义/测试/Eval三种范围。容量与费用改变不放宽重试、安全点或新任务授权边界 |
| “executionPlan不另建测试节点”容易读成硬解析规则 | 已修改的是ARCHITECT角色指导，录制9仍有偏离；现有schema未按节点标题拒绝，累计路径保护有效 | 三处来源明确指导与程序强制边界。偏离保留记录；越权、删除测试、漏验收等实际缺陷继续失败 |
| 10.7只有通用五条标准，未指向本轮具体优化 | 容易只复用10.2–10.4验收或把旧DOM/通知文案当固定契约 | 增加§11.10及本核对记录为必读；“自主完成”明确仍需Leader终审，不将无限上下文/任意角色自主调度写成已实现承诺 |

## 已有覆盖与仍待出口验证的部分

静态对照了当前工作区实现、新增及既有测试，覆盖可读交付/Markdown、侧栏、滚动、发送/刷新、自然语言草案、模型容量、Go会话/推理/取消/超时、当前需求与波次范围、完成反馈、输出格式恢复、英文Tier识别及终态回收。来源和测试路径逐项放入§11.10，避免只在一段长notes中列修复经过。

现有`phase10-model-settings.test.ts`明确保存本地测试provider的32768/4096，再验证不可变绑定与实际请求，属于合法自定义配置；它不应改成1M/384000。`phase10-local-startup.test.ts`的小容量同样是测试输入。已知DeepSeek容量预设另有`model-capacity.test.ts`和`model-settings.test.ts`覆盖，两者应并存。未发现需要为了这次文档同步修改这些断言的依据。

`tests/integration/phase10/phase10-exit.test.ts`尚不存在；10.7仍pending，依赖10.6开发交付。滚轮、滑块、键盘、阅读位置、作用域切换和异步草稿等浏览器行为不能靠SSR标记断言替代，应在出口计划中安排实际交互验证。现有1,246项全量回归、合成自然语言真实Go、录制与归档检查属于各自记录的实测，不冒称本轮重跑或已经完成出口。

新版已接受视频、候选、裁决、源码清单与媒体hash见[演示说明](../demo/task106-natural-chat-demo.md)，自然语言实测见[功能证据](../demo/task106-natural-language-evidence.md)，失败/修复与回归链见[正式录制证据](../demo/task106-english-recording-evidence.md)。10.5冻结结果和DEF-016/017没有修改。

## 本轮验证

文档/索引检查涵盖E01–E12完整性、实现/测试引用路径、10.6/10.7依赖与状态、允许的task-status字段、文档内新链接及`git diff --check`。本轮不新增测试、不skip、不放宽断言、不调用模型；后续10.7仍须执行适用的完整回归和真实G5。检查结果以本轮任务notes为准。


## PR73后续修复

以上为文档核对当时的快照。随后PR评审查出实际边界缺陷及三份历史文档的当前状态遗漏；修复及独立验证见[PR73修复记录](task106-pr73-repair.md)。E01–E12继续生效，不将本轮缺陷改写为正确；原文引用的1,246项为当时基线，本轮新回归结果单独记录。
