import fs from 'node:fs';
import { createHash } from 'node:crypto';
import { join } from 'node:path';
import { spawnSync } from 'node:child_process';
const root='/private/tmp/agora-task123-validation-RE6wOy';
const repo='/Users/logansu/Documents/Dev/Agora';
const report=join(repo,'docs/reviews/task123-command-evidence/local-download-timeout-cleanup.json');
const sha=b=>createHash('sha256').update(b).digest('hex');
const identity=fs.lstatSync(root), owner=JSON.parse(fs.readFileSync(join(root,'state/.desktop-owner')));
const registry=JSON.parse(fs.readFileSync(join(root,'state/local-workspaces/registry.json')));
const log=fs.statSync(join(repo,'docs/reviews/task123-command-evidence/local-authorized-download-native.log'));
if(identity.ino!==128508231 || identity.uid!==process.getuid() || fs.realpathSync(root)!==root || !identity.isDirectory() || identity.isSymbolicLink() || owner.pid!==86486 || identity.birthtimeMs<log.birthtimeMs || identity.birthtimeMs>log.mtimeMs || registry.snapshot.grants[0]?.actions.join(',')!=='read,edit,install' || fs.readFileSync(join(root,'project/sentinel')).toString('hex')!=='00ff802a') throw Error('fixture_provenance_changed');
const dead=spawnSync('/bin/ps',['-p',String(owner.pid),'-o','pid='],{encoding:'utf8'});
const handles=spawnSync('/usr/sbin/lsof',['-nP','+D',root],{encoding:'utf8'});
const mounts=spawnSync('/sbin/mount',[],{encoding:'utf8'});
if(dead.status!==1 || dead.stdout || handles.status!==1 || handles.stdout || handles.stderr || mounts.status!==0 || mounts.stdout.includes(root)) throw Error('fixture_quiescence_unproven');
const entries=[];
function visit(path,prefix='') {for(const name of fs.readdirSync(path).sort()){const full=join(path,name),rel=prefix?`${prefix}/${name}`:name,s=fs.lstatSync(full);if(s.isSymbolicLink()) entries.push({path:rel,link:fs.readlinkSync(full)});else if(s.isDirectory())visit(full,rel);else if(s.isFile())entries.push({path:rel,bytes:s.size,sha256:sha(fs.readFileSync(full))});else throw Error('special_file');}}
visit(root);
const before=fs.statfsSync('/private/tmp');
const data={root,identity:{dev:identity.dev,ino:identity.ino,uid:identity.uid,birthtime:identity.birthtime},owner,source:'tests/integration/phase12/phase12-3-grant.test.ts',sourceAtCleanup:sha(fs.readFileSync(join(repo,'tests/integration/phase12/phase12-3-grant.test.ts'))),failureLogHash:sha(fs.readFileSync(join(repo,'docs/reviews/task123-command-evidence/local-authorized-download-native.log'))),scriptHash:sha(fs.readFileSync(import.meta.filename)),noProcess:true,noHandles:true,noMounts:true,entries,logicalBytes:entries.reduce((n,e)=>n+(e.bytes??0),0),deleted:false};
fs.writeFileSync(report,JSON.stringify(data,null,2)+'\n',{flag:'wx'});
const check=fs.lstatSync(root);if(check.dev!==identity.dev||check.ino!==identity.ino)throw Error('identity_changed');
fs.rmSync(root,{recursive:true});data.deleted=!fs.existsSync(root);const after=fs.statfsSync('/private/tmp');data.availableBytesDelta=after.bavail*after.bsize-before.bavail*before.bsize;fs.writeFileSync(report,JSON.stringify(data,null,2)+'\n');console.log(JSON.stringify({deleted:data.deleted,logicalBytes:data.logicalBytes,availableBytesDelta:data.availableBytesDelta}));
