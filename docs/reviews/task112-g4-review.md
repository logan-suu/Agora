# 11.2 修复分支全面审查与完整 G4

日期：2026-09-14。验证提交：`cd9e99aa4be92320ea3abc5bbf768b14ae9c2060`；相对 `dev-1.0.0` 的 50 个变更文件已审查。**本轮完整 G4 通过，未发现新的必须修复问题。** 本轮没有修改产品、测试断言或执行代码，仅新增观测证据并更新任务追踪。

规格依据：蓝图 §21 同日 Leader 测试模型选择要求“当前通用真实回归固定OpenCode Go的`deepseek-v4-flash`，缺少Go凭据或失败必须报错，不自动转官方或V4.1、不skip。”以及“原测试断言、原生容量/思考及期限不变，所选路径仍须完整G1–G7。”

## 验证结果

执行 `pnpm run test --config .data/diagnostics/agora112-full-g4-review/vitest.config.mjs`，退出码 0，用时 201.86 秒。配置继承原 `vitest.config.ts` 的全部 include、setup 和默认并发；仅为三个实网文件添加脱敏观测及 JSON reporter。没有排除文件、跳过测试、关闭思考、降低容量、扩大期限或增加重试。

| 范围 | 文件数 | 通过 | 失败／跳过 |
| --- | ---: | ---: | ---: |
| packages（含 LRU 与 Harness 实网） | 86 | 827 | 0 / 0 |
| apps | 25 | 204 | 0 / 0 |
| 测试路由 helper | 1 | 2 | 0 / 0 |
| Eval 离线 fixture 单元测试 | 29 | 139 | 0 / 0 |
| Phase 0–10 集成测试 | 29 | 111 | 0 / 0 |
| cross-phase | 1 | 1 | 0 / 0 |
| **Vitest 合计** | **171** | **1284** | **0 / 0** |
| 同一命令中的 Node 查询工具测试 | — | 7 | 0 / 0 |
| 另行执行的桌面启动／关闭回归 | 1 | 3 | 0 / 0 |

`pnpm typecheck`、`pnpm lint` 均通过。逐阶段数量、测试文件与源码 SHA-256、三个实网用例的请求与沙箱耗时见 [机器可读报告](task112-g4-review.json) 和 [证据目录](task112-g4-evidence/README.md)。

## 审查结论

- PR #79 的历史清单计数问题已由原始结果归档、历史范围说明和新运行的精确输入快照解决；本轮复核当前 manifest 的 27 项 SHA-256 全部一致，未倒改历史 16 项为当前数量。
- 启动／关闭竞态修复共用 startup/shutdown 状态，停止后不再报告 ready，仅关闭已初始化资源；父进程等待退出、按期限回收自己的非模型探针进程组，强制回收明确记失败。三项真实子进程回归通过；此前真实 Electron/Next/Keychain 的 16 项 G5 证据仍与当前脚本哈希一致。
- Go adapter 的 `stream` 和 `prepareCall` 均复用已有工具身份归一化；工具参数保持原样，身份冲突拒绝，先自然读完模型流再发布工具块。实网与离线协议回归均通过。
- 模型更名及 reasoning 回放识别与当前规格一致；产品公开接口、State 写入、投影、角色权限与安全点路径没有新增改变。正式 Benchmark 仍关闭，实网请求未使用 V4.1 或官方后备。
- cross-phase 目录的结构化 Ledger／handoff 链与各阶段出口共同覆盖已实现的数据流；后续 Phase 8–10 出口另含持久化 Fork、并行验证、Leader 返工及累计归档链。本轮未发现因该差异引入的跨阶段覆盖缺口。Phase 11 产品与发布出口尚未实现，不把本轮累计回归称为 Phase 11 出口验收。
- 对当前变更文件扫描两个已配置真实凭据，匹配数为 0。诊断只保留白名单字段、计数和会话哈希，不落原始模型输入、输出或工具参数。

## 超时判断及交付边界

本轮 LRU 通过耗时 200.198 秒：17 次请求均 HTTP 200 并收到 DONE，模型累计耗时 196.190 秒；11 次沙箱命令累计 3.365 秒，最长 0.609 秒。CODER 与 TESTER 均正常完成。两个短实网用例分别为 14.387 秒和 9.427 秒；三者都保持原生 thinking enabled、maxTokens 256000 和原测试期限。

这证明当前完整 G4 已通过，但**没有复现或确定历史 600 秒 LRU／120 秒摘要超时的根因**，不能据此宣称间歇性延迟已消除。[历史诊断](task112-lru-timeout-diagnosis.md)及失败结果继续保留。本轮没有为获得绿色结果修改限额或重跑失败用例。

任务 11.2 从 blocked 恢复 in_progress，后续仍须把修复与新增证据通过功能 PR 交付并由人类合并；11.3 继续 pending。已合并的 PR #79 只包含旧实现，两条旧审查线程仍未解决，待后续交付关联证据；本轮没有提交、推送、回复或合并 PR。
