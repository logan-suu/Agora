# Task10.5 公开题契约复核

2026-09-09，正式v1运行中发现；Leader已确认“修正公开测试可见性并重跑（推荐）”。以下计划进入实施，旧冻结实验输入不修改。

## 实测问题

- `grade-school`首9次均为上游9/10，唯一失败为同名学生跨年级：题面要求拒绝重复添加，测试要求旧年级移除学生。原版本没有附加说明文件。
- `wordy`single首轮为15/23；已查看实际失败消息，非法表达式被拒绝，但错误文本为`Invalid syntax`、`Unsupported operation: cubed`等，上游要求精确`Syntax error`/`Unknown operation`。题面仅要求reject，没有这两个精确文本；该版本也没有附加说明文件。
- 上述样本的其余适用Process/Safety检查通过。原测试、独立镜像和输入哈希无漂移，不能通过弱化断言“修复”这些失败。

问题在已批准的公开测试隐藏策略：上游公开测试同时承载精确接口契约；只给题面/stub会迫使模型猜测未声明约定。继续当前组仍能报告原协议结果，但无法把低分直接解释为通用代码能力。原组不得删样本、改输入后混算或事后移除失败任务。

## 建议的新组

1. 当前组在没有在途模型请求的安全边界暂停，保留全部已启动/完成/未启动记录、费用和镜像/源码指纹。原费用留在原预算分类，不暗中归零或转移。
2. 公开四题增加固定上游`.spec.js`作为Agent可读文件及fileRef；保留原题面/stub，明确公开测试用于消解接口歧义。原测试无需成为生产Node/TAP入口；Agent仍写自身Node测试。参考解仍不可访问，独立Jest verifier仍只使用宿主私有原始测试/config和候选源文件，模型改写工作区文件不能改判定器。
3. 将新协议和模型seed可见文件清单写入新manifest/结果。新groupId隔离v1，不合并不同输入配置的成绩；预算trial身份增加group前缀，保证跨组attempt费用隔离。
4. 新组仍为四个公开任务×三变体×三重复，加两个尚未暴露的新内部holdout×三变体×三重复，共54次。holdout的隐藏验证和任务内容保持原定；新运行会使本轮总attempt数超过初始54次。
5. 维持USD20总上限、正式17/诊断3分池，旧请求全部计入已有支出，不增加费用上限。补充外部可控的“安全停止新工作”检查，避免长期实验必须靠中断模型才能暂停。
6. 先用独立诊断或确定性输入可见性用例验证公开测试可读、参考解隔离及原Verifier不变；运行必要定向检查和G3/G4后冻结新组。新协议依然不是原生Aider排行榜协议，差异继续报告。

备选为完整执行原54次，仅在报告中声明公开接口契约缺口。该选择不修改既有模型输入、代码、预算或判定。

## v2真实验证发现的落盘缺陷

公开测试可读策略不变，但`.spec.js`文件名同时匹配生产可信Node验证器的TEST_FILE规则。v2 mixed首次实际可信测试包含grade-school.spec.js并失败；内容哈希与seed一致，不能归因于模型篡改。原单solver可读测试未覆盖生产多角色自动收集接缝。

修复属于已批准可读契约的实现纠正：模型侧以`.spec.txt`保存相同激活文本，独立Jest侧继续原文件名/断言；新增从实际taskDefinition seed进入真实多角色验证的回归。v2等待当前attempt自然闭合再停，记录和正式费用保留；修正版另冻结完整54次，不混算，不增加预算，不改生产收集器或要求模型跳过测试。
