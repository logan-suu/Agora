# PR #87 全面修复与复验

本轮对应 Leader 的“进行全面修复”，继续在 `codex/fix-human-gate-replay` 更新 [PR #87](https://github.com/logan-suu/Agora/pull/87)，不合并。当前阶段仍为12；原12.3的done和Phase14全部pending保持。

## 修复范围与规格

蓝图§21 D4/D9原文：“Phase8无per-worker计划的旧receipt维持兼容，不得将已存在的per-worker证据删除后冒充旧格式。”

- 历史重放兼容校验不再只看会变动的当前worker。旧共享组合最多一个safePoint；当前checkpoint和历史Leader receipt/Coordinator marker保留的worker/source绑定均参与核验。来源指向原动作per-worker child时，原计划缺失拒绝；真实旧共享session保留兼容。来源经Harness既有解析器校验，跨任务拒绝。
- 来源必须与其worker配对，不能把所有worker和所有来源交叉组合。合法ID包含冒号时，新回归复现这种组合造成的误拒绝；已改成完整的worker/source身份比较。
- 新增6项回归覆盖单worker、多worker全部经过后续两次gate、删除原计划、真正legacy后续恢复，以及带冒号身份的正常重放。旧请求不重新执行的约束及首次paused准入保持。
- DEF-016的`target_phase`同步为25；恢复四份来源中的macOS26.5验证说明及SDK26.5；补齐详设身份演进表、四批范围和蓝图20→21阶段/110→117任务的历史与当前边界。未改写历史验收文件或平台版本证据。

## 失败、修正与边界

1. 新回归首次为19通过/2失败，确认原缺陷；身份配对新增反例为21通过/1失败。最终D9定向4文件63项通过，含真实JSON/HTTP重放与原Phase8官方JSONL/Fork集成。
2. 首次完整回归为226文件通过、1文件失败，1768通过/5失败，另7项脚本通过。四项本机验证共用无效字符串`fixed-fixture-safe-point`，被新增校验正确拒绝，未走到原`file_version_conflict`断言。把该确定性夹具改为格式及scope有效的模拟游标，并明确R11原因；没有改变生产校验或放宽原断言，真实Harness仍由独立G5验证。
3. 同轮batch超过原5000ms期限；单独复验为4041ms通过。安装、验证和两种终态释放共四项也通过。该5项定向只用于定位复验，筛选掉的30项不冒充全量通过；最终全量必须无筛选、无skip、无提供方后备。
4. 首轮全量开始后仅修正上述夹具及身份配对代码/测试，不把该失败运行说成冻结来源验收。第三轮全量冻结614份源码/配置且无漂移，仍因batch超时失败；最终交付候选另冻结源码及本机helper二进制，结果及hash见下方证据。

5. 第二轮全量安装场景出现独立的`workspace_command_recovery_required`，不归因于D9或静默重试为成功。该夹具原来在重放之后才保存初次命令结果，失败只保留hash，无法追出首次命令为何未闭合；补充重放前的初次结果与原生journal留证，原断言和期限不变。补充诊断后的单独安装用例通过：命令exitCode=0、quiescent=true，native journal记录discoveryFailed=false且登记进程均已退出。原失败因首次结果/journal未保留，根因明确记为inconclusive；该次通过不能反推原失败原因，也不宣称修复了本机执行器缺陷。后续全量与最终处置见验证记录。

6. 第三轮全量为1773通过/1失败，batch为5055ms，原5000ms期限未改变。该fixture业务断言及清理最终完成，但Vitest失败仍保留。单独计时又两次触及时限；临时仪表显示编译约197–314ms，事务应用约1819ms，准备/读取约2080ms，清理约290ms，不能把全部开销归因于编译，也无证据可以删除生产保护。
7. 夹具改用`pnpm build:sandbox-native`从当前源码构建的真实root inspection/initialization/file transaction helper，逐项记录二进制hash，去除每case重复编译；不改变生产实现、任何业务断言或期限。单项4502ms通过；四项与typecheck同跑时仍有1项超时，该次保留为失败。停止并行检查后独立运行四项全部通过：batch3766ms、partial3442ms、read-drift3033ms、stale2220ms。最终完整回归不同时运行其他构建/检查/测试。辅助计时源码未进入交付；两次超时导致Vitest退出前未清理的专用目录已保全文件hash并复核身份/无句柄/无挂载后删除，不能把fixture的`passed`字段冒充Vitest通过。

## 最终交付验证

最终冻结候选：typecheck通过；lint589文件零违规；原生helper从当前源码重新构建。`pnpm test`为227文件/1774项全部通过，另7项脚本通过，无skip，818.78秒；batch3681ms低于原5000ms。614份源码/配置及6份原生二进制在运行前后无hash漂移。真实模型使用OpenCode Go `deepseek-v4-flash`；官方Harness/Fork、MCP、Docker、本机授权/安装/验证及D16链路均包含在本轮适用回归中。正式Benchmark未运行。

任务索引178任务/32阶段/26决策/69历史通过；94个迁移任务的依赖、状态及测试路径核验通过。最终敏感信息检查结果以验证JSON为准。

TEST-CLEANUP：本轮835份本机测试报告对应的专用根均已不存在，清理证明已核对。精简834份重复报告（10680244逻辑字节）及75份辅助原始文件（1546103字节）；之前已删除隔离审查/旧审查目录54文件（334918字节），另删除两次计时超时退出的专用目录104文件（351794字节）。未明安装失败原件及第二轮全量失败日志保留在忽略目录；当前交付草稿/hash核验输入在使用完毕前保留。未动用户项目、正常依赖、全局缓存、已安装应用或产品数据。逐项来源/hash及空间观察见JSON。

## 独立审查与证据

CodeRabbit CLI 0.7.6，仅上传Leader明确允许的六份D9代码/测试与AGENTS.md，隔离仓库无remote。首次上传被自动审批拒绝，Leader随后明确确认限定范围后才执行。首轮遗漏未跟踪的新helper/测试，只审四份代码；提出两项“旧marker后重建执行”的建议，与蓝图§21已定“不用旧source重新执行”冲突，均不采纳。用intent-to-add登记新增文件后完整复审为0 issues；身份配对修正后再次完整复审，最终仍为0 issues，完成清单核验包含全部7路径。此范围不含D19设计或本机测试夹具/失败诊断；这些由本地规格核对和回归验证。

最终G3/G4/G5、提供方、源码/日志hash、迁移核验与TEST-CLEANUP见[验证记录](pr87-review-fixes-20260917.json)。历史初次交付/定稿结果继续保留其发生时点，不用本轮数字改写旧报告。PR合并与发布仍由人类执行。
