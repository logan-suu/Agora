# T10.6 自然语言需求变更验证（录制前历史快照）

范围：下文“未commit/push”“尚未录制”均指本次功能验证结束时。后续quote-en-take-9已录制并获接受、PR #73已建立，当前入口见[新演示说明](task106-natural-chat-demo.md)。历史验证结果与费用保持原值。

日期：2026-09-12。任务仍为10.6/in_progress；未commit/push，未重新录制。录制6已撤出正式入口，历史成功产物和诊断媒体保留。本文覆盖自然语言交互修复，不把合成输入验证视为新演示任务完成。

## 用户交互与控制边界

Leader选择“直接用自然语言聊天，例如把票价改成每人900分，系统识别后展示变更供确认”。此前普通文字只记chat/action none，录制脚本靠JSON命令改需求，因此需要补齐产品能力。

- 当前main输入先持久化，经冻结的Coordinator模型连接与同一个GlobalScheduler，由独立无工具Harness解释本条文字、结构化需求/决策、目标与必要的前一草案/澄清。普通worker投影未开放原始聊天；保留正常模型容量和独立官方session。
- 模型只生成草案、澄清或回复。草案Message记录来源和事实指纹；原需求在确认前不变。普通模型step禁止伪造解释/确认信封。
- 群聊提供前后对照、展开验收与非目标、确认及取消按钮；刷新从持久事实恢复，失效草案禁用确认。JSON不进入输入框。
- 浏览器确认仅提交proposalId/action及稳定msgId。服务端加载规范草案，在安全点前后检查来源、当前性和事实指纹；一个requirements_change批次原子更新所有关联需求，重试复核已提交效果。D16完成终审保持独立。

## 检查范围

新增15项测试覆盖批量更新/重放、草案持久化和来源、过期/gate/done拒绝、确认与取消幂等、客户端伪造内容拒绝、解释失败后同ID恢复、澄清上下文、模型写入边界、真实Harness工具隔离/格式恢复/独立session、可读确认卡片。既有消息/SSE、WorkerRuntime、D9并行安全点与累计验证回归继续执行。

Browser plugin not available，使用已有Chrome/Playwright。最终构建UI测试在1440×1000及390×844验证实际页面、自然文字输入、前后对照、刷新恢复、重复确认复用ID、保留未发送草稿、过期禁用、无横向溢出和无应用异常。HTTP脚本响应只用于可重复的UI错误/重试条件，明确不作为G5。截图首次揭示深色主题下浅色卡片对比度不足，已复用既有主题变量修复并复验。

## 真实合成输入验证

隔离任务natural-input-g5只包含本次创建的票价/场地/报价合成需求，没有仓库文件、worker、subtask或历史会话。使用正式pnpm start、本机HTTP/SSE、真实Go deepseek-v4-flash、官方Harness与官方JSONL。当前连接容量1M/384K；无临时费用/请求数门限，继续记录用量并在服务异常时停止。

首次英文输入明确900 cents per person，真实模型同时提出tickets与quotes变更；确认前1000/17000不变，真实按钮确认后900与1800/15000/16800一次生效，场地和非法输入规则保留。刷新后草案恢复；取消操作已通过真实入口验证。

歧义输入“Make it 850.”首轮被猜成票价草案，未执行并显式取消。排除原始聊天或已处理草案误准入、以及解析器改写kind后，收紧提示：缺少明确对象/单位的代词加数值先澄清，不根据原始目标与当前需求差异猜指代。相同输入复验生成澄清，需求保持900。

中文输入能解析为850及报价16700，但首轮说明沿用前一英文澄清；随后明确summary/text以当前leaderInput.text的语言为准，既有需求未改字段保留原语言。最终相同中文输入返回中文说明，原有英文需求仅修改必要数值；真实确认后票价850、报价1700/15000/16700，其他约束保留。

## 环境失败与授权

首次完整回归在沙箱内DNS解析失败，26ms内未取得HTTP响应，计量保留unknown，不伪造零费用结算。只读对照确认沙箱ENOTFOUND、允许联网后服务返回HTTP401；已获准的完整回归改在允许联网的环境执行，保留所有真实模型测试及异常保护。

自动审批两次拒绝Go实测服务启动，认为独立验证超出先前演示授权；未绕过限制。Leader随后明确批准“上述合成输入实测”，才启动真实服务。此前本地UI检查使用空目录、禁止外网与脚本HTTP响应，不外传测试内容。

## 证据位置

私有审计：.data/demos/task106-20260911/audit/natural-input-validation/，含独立官方回归账本、Go请求元数据、真实浏览器输入/确认回执及截图。仅当前合成任务的官方session保存于product/projects/agora/tasks/natural-input-g5/harness-sessions；不把原始session发布到README或演示视频。

临时复现脚本和日志位于/private/tmp/agora106-qa/natural-*；新测试进入正式测试目录。该合成任务只验证需求交互和空cohort控制提交，不声称它执行了编码、动态抢占或完成归档；动态安全点与累计产物失效由既有真实Phase9集成测试覆盖，新正式录制仍须独立验证其实际执行。


收尾兼容性：已有Requirement schema允许澄清中的空acceptance数组；草案before快照原本错误复用了新upsert的非空校验。最小回归先复现预览报错，改为按既有读模型验证后通过；拟提交变更的完整性限制和全部既有断言保留。最终英文输入再次返回英文草案，真实确认及刷新后为900与1800/15000/16800。合成任务共17条规范消息、3次确认、2次取消及1次澄清；6次Go均HTTP200，用量已知，保守折算USD0.006169800。State SHA-256：f239270ce10007bf589e918116aadef4f3cfad394fcaf17dc6cb5aac96907924。


产品边界：本次实现自然语言需求草案，不承诺任意复杂多意图都可正确解析；模型建议必须经过用户审阅。自然语言不能代替D16完成批准或自动管理角色。正式英文视频将使用新的自然语言题面与确认交互，本轮没有重新录制。移动端正式会话的即时缩放截图捕获了侧栏过渡动画，仅保留为诊断；移动端布局验收使用页面重新加载、布局稳定后的独立UI检查，不把过渡帧作为交付截图。


最终生产读取器复核：从落盘State与官方session重建6个Trace会话，全部turn/step completed、工具调用0、omittedEventCount=0；6条解释事实中5条具有规范确认/取消记录，另一条为澄清；无待确认草案。未调用模型、未改变State。源码与6个已解压官方会话分别执行Gitleaks，均无命中；原始session仅在私有审计范围内处理。

一次回归进程运行期间补入了兼容性用例，Vitest仍使用先前缓存的readBasis模块，出现旧错误栈；该轮明确记录为失败，不作为最终门禁。独立13项通过后固定420个源码/配置文件，在新进程重跑完整套件。冻结manifest SHA-256：2dc1092eda79199d7bfcb7e84150de344d2a1349118ffad044ea1e5a2feccf89；最终门禁结果见下节。


## 最终验证结果

- 固定源码的新进程完整回归：166文件、1244项全部通过、0skip，398.10秒；包含全部既有真实模型/Docker/Git/MCP/跨阶段测试。日志SHA-256：1be7def5e79a3d81e29aa0863988ef55e524382d92ba4cb05dcea3e7e5f10e9b。
- 最终typecheck、Biome（418文件）、生产build均exit 0；git diff --check通过。源码manifest复核无变化。
- 真实Go：6请求全部HTTP200、6官方会话正常闭合且工具0、保守折算USD0.006169800。中英文明确改价、歧义澄清、确认前不变、取消、原子确认及刷新均有持久证据。
- 本次官方回归累计81条记录，80条用量已知、保守折算USD0.055830792；另1条为最初沙箱DNS失败，保留unknown。最终冻结回归17请求均成功、USD0.009311082。历史已发生费用不改写，也未重跑冻结Benchmark。
- 已停止正式验证服务和浏览器，3106/3107无监听。合成任务未生成worker/worktree/container，未伪造done或归档。

T10.6仍in_progress；自然语言功能修复及所列验证完成，未commit/push。旧JSON录制继续停用，新的全英文真实用户录制尚未进行，新产物完成须另经其自身D16 Leader终审。
