# T10.6 终版演示执行计划（历史过程记录）

当前状态（2026-09-12，PR73修复时）：quote-en-take-9已获Leader接受，成片及产物见[当前演示](task106-natural-chat-demo.md)。本文件下方题面、额度、暂停与启动安排均为当时的历史快照，不表示仍待录制；T10.6已建PR #73，保持in_progress等待人类合并，10.7尚未执行。

历史状态（2026-09-12，录制6之后）：Leader指出录制6输入框直接发送JSON不符合真实用户体验；该视频撤出正式演示入口，保留原片/成片作为诊断证据及已批准产物。代码审查确认普通自然语言当前仅记chat/action none，不会更新需求；需先补齐真实用户需求变更交互、同步D9及验证后再录制。T10.6保持in_progress。此前技术验证和归档结果仍有效，不等于用户体验验收通过。

历史执行状态（2026-09-12，录制1–3之后）：录制1/2/3全部失败录像已删除，保留失败State与审计。Leader批准继续超时审查后的修复：Go SDK/idle恢复300秒，补齐私有逐请求时序，小范围合成Go验证及完整回归通过后先英文预演，再全新录制。当前尚未开始新预演/录制，T10.6保持in_progress。以下旧状态及额度仅为历史；发布准备已取消临时费用/请求数限制，保留用量审计及异常停查。

历史执行快照：2026-09-12。Leader在英文预演3完成归档后明确“开始录制吧”，当时已启动下述正式英文录制。T10.6保持in_progress。以下题面、额度和执行记录保留为历史，不表示已启动新的录制。来源：[详细设计§11.9](../详细设计方案.md)、[规格评审](../reviews/task106-spec-review.md)。Leader已要求开始任务；Leader已分别授权Go演示最多2次/USD1配额折算，以及官方完整回归累计USD0.30（后续从USD0.25增加USD0.05）；两者独立，未继承10.5账本。

**[2026-09-11 历史安排]** 官方完整回归累计上限后续已获授权提高到USD0.35；Go独立上限仍USD1，历史额外诊断及第3次尝试见文末。以上历史额度不表示新的演示尝试已获授权。下一次终版录屏全部使用英文，使用[English recording script](task106-english-script.md)准备题面、需求变更及媒体文案；下方首次中文题面仅作历史记录。保持暂停录制，等待优化全部完成。

## 交付与文件

[2026-09-11 全英文预演授权] 两项录制前优化验证通过后，Leader回复“好的进行下一步”，对应上一轮明确建议的全英文预演。本次准入一个新任务quote-demo-en-1（历史attempt索引4），沿用Go deepseek-v4-flash、8192输出上限和独立累计USD1预算；最多80个请求，旧48请求/USD0.098492676账本完整保留，不回退官方模型。先核验真实英文交付/并行/需求变更/完成候选；本次不捕获视频，不启动正式成片制作。新候选仍须单独Leader终审，额外重试不由此自动授权。

1. `README.md`：当前架构/能力/限制、正式Quick Start、最终Eval摘要和媒体入口；保留历史录屏身份。
2. `docs/portfolio/engineering-notes.md`：简历事实、取舍和源码/测试/报告引用。
3. `docs/demo/`：终版可播放视频及脱敏证据索引；原片、运行State/session和工具审计留私有`.data`，公开索引关联hash。
4. `docs/task-status.json`：记录实际检查、费用/尝试、未完成项和交付状态；保持10.7独立。

## 录制方式与环境

正式`pnpm run setup`、`pnpm run doctor`、`pnpm start --port 3106`启动当前源码；用独立`AGORA_DATA_ROOT`隔离演示数据。复用这台Mac现有Node24/pnpm9.15.9/Docker/钥匙串，不称为全新机器安装。预检发现的实际缺陷先修复并验证；录制辅助工具不进入产品依赖。

Browser plugin not available：会话没有Browser技能，使用已安装Playwright及其FFmpeg录制实际浏览器WebM，并保存桌面/窄屏静态QA。不以截图序列假装一条连续录屏。录屏前先验证工具能输出、播放并定位时间轴；录制范围为Agora页面，不捕获其他应用。

## 候选场景

实现一个无外部依赖的JavaScript“活动费用报价”模块API。初步拆分为两个独立模块（人数票价、场地费用）和依赖两者的汇总模块，均有明确正常/边界输入、整数金额与Node测试。在独立CODER执行期间，Leader通过正式消息入口修改一个明确的报价规则；随后观察更新、累计测试/评审、Leader完成终审、产物与刷新恢复。

付费前需定稿具体题面/接口/调整值并验证生产Tier2入口。是否实际并行由真实模型计划与Trace验证，不能向State写入伪造DAG或为了镜头插入sleep。若模型未产生可验收计划，按失败记录，不宣称完成并行录屏。

已获授权使用Go `deepseek-v4-flash`，最多2次演示尝试，总计USD1配额折算（含失败请求）。每次尝试使用独立taskId，明确请求/工具/时间上限；模型计量先预留后请求，unknown或异常停后续准入并让在途流自然闭合。最终参数/计价和计量接缝在预检完成时补充，不修改既有冻结Benchmark账本。

## 镜头与验证顺序

| 段落 | 实际动作 | 所需证据 |
| --- | --- | --- |
| 本机入口/设置 | 展示当前页面和模型配置说明，配置演示团队；保存不主动调用模型 | URL/标题、页面非空、设置确认；不录真实密钥 |
| 任务启动 | 浏览器填写taskId与完整goal，点击启动 | 同任务创建请求与持久进展，真实Harness/MCP/Docker/Git |
| 并行与改需求 | 展示两个同role泳道重叠，提交正式需求或决策调整 | Trace实际时间重叠、canonical Leader消息与效果；不把reproject称为Fork |
| 验证与审阅 | 等待累计测试与REVIEWER完成候选 | 规范validation receipt/HEAD/current review，不拿历史passed标签替代 |
| Leader终审 | 人工查看候选证据后通过界面批准或退回 | 本次gate对应的Leader裁决；不自动替人裁决 |
| 归档/刷新 | 展示done/归档引用，刷新并恢复同任务 | 归档产物身份与测试证据、刷新状态/消息、资源清理 |

主片不强求sub、离职、阻塞Fork等全部机制在同一任务出现；已有真实验证可在备答中单列。允许加速/剪辑等待时间，但保留同运行原片，明确剪辑点和播放速度，不拼不同任务伪装一次完成。若只能产出脚本/截图，录屏交付仍未完成。

## 检查与停止条件

- README本地链接、命令映射、图与当前规格一致；定向核验启动/停止和模型设置交互。
- 页面身份、非空、无框架错误覆盖层、相关控制台/网络错误说明、桌面与窄屏布局、实际按钮结果。
- [2026-09-11 群聊滚动验证补充] 后续截图/录制应保留真实原生滚动条；Playwright无头模式默认带`--hide-scrollbars`时，显式省略该参数，再检查滚轮、滑块拖动和键盘回看，避免测试配置隐藏已实现的滚动条。此条只约束后续录制，不解除当前暂停重录安排。
- [2026-09-11 跟随验证补充] 检查初次载入定位最新、底部收到新消息自动跟随、回看时位置保留及未读msgId去重；Back to latest支持点击/键盘，任务或Channel切换清计数。新派发通知只保留角色/动作/必要subtask引用，完整goal仍从侧栏展开查看；不修改历史消息来制造紧凑旧录屏。
- 检查视频播放/可读性与关键镜头，核对媒体hash和同任务事件/验证/归档证据；排除密钥、原始推理与无关项目内容。
- 逐项核验全英文：浏览器可见区域/产品标签/系统通知、任务目标、需求/验收、Leader消息、Agent交付说明、展示的代码注释与测试名称、封面/字幕/片尾及旁白（如有）。新任务goal明确要求英文且保留角色JSON契约；核对真实输出，语言偏离先停查，不翻译旧State/SSE冒充新运行。录制仅捕获Agora页面，避免中文系统弹窗或无关浏览器内容入镜。
- 异常先停受影响操作，审查根因；实现缺陷按常驻授权最小修复、同步规格并执行相应G1–G7。只有文档/媒体变更时，不为新数字重跑Benchmark或源码全量付费回归。
- 没有实际媒体、未触达关键链路、缺Leader最终裁决或费用尚未结清时如实记未完成，不标10.6 done。

## 预检与修复记录

- 本机setup/doctor/正式启动停止已跑通，未称为干净机器验证。
- Team列表新增Model按钮后，旧last-child选择器不再命中名称/状态容器；补充文字容器CSS规则后真实Chrome桌面/390px窄屏通过，名称/状态均为grid分行、无页面异常或横向溢出。
- 完整回归最初被临时计量器的32K限制拦截，外发0；实际Harness请求上限256K，修正为按实际请求及官方调用时段预留。第二次真实LRU回归暴露失败反馈先进入State、交接才报错的问题；先停查，4个畸形失败对象的reducer测试红灯，补充边界校验与TESTER格式后145项领域/编排定向回归通过。真实失败原始条目未保留，不能断言具体缺哪个字段。
- Go模型连接通过正式设置接口保存到隔离项目加密存储；contextWindow65536/maxTokens8192，不点击收费连通测试。费用观察器不改请求或响应，按Go峰值费率预留/结算，保守高估非峰时费用；[费率来源](https://opencode.ai/docs/go/)，2026-09-11核对。无usage或HTTP/传输失败立即停后续请求，在途流自然结束。

## 首次录制题面

实现一个无外部依赖的 JavaScript 活动费用报价模块 API，使用 .mjs 和 Node 内置 node:test。请保留需求 ID，按 A/B 独立、C 依赖 A/B 的执行计划安排编码；所有输入仅接受非负整数，否则抛 RangeError，0 合法。req-ticket：A 在 ticket.mjs 导出 ticketCost(attendees)，返回 attendees * 1000（单位为分）。req-venue：B 在 venue.mjs 导出 venueCost(hours)，返回 hours * 5000（分）。req-quote：C 在 quote.mjs 导出 quote(attendees,hours)，导入前两者并返回 {tickets,venue,total}，total=tickets+venue。各模块都需正常/0/负数/小数的测试，累计运行 node --test。仅实现这些文件和必要测试，不加依赖、CLI、服务端或 UI。工作中以 Leader 后续正式需求为准；完成后交给 TESTER 和 REVIEWER 验证，并等待 Leader 最终确认。

计划中途经`/requirement req-ticket`把票价从每人1000分改为900分；只有在真实State确认该需求ID以及实际运行中的CODER后才发送，保留其余验收。

实际PM把quote的正常例子固化为旧票价，录制时连同`req-quote`的派生金额一起更新为tickets=1800、venue=15000、total=16800，保留其他验收，避免两条需求互相矛盾。追加诊断2次和第3次录制已获Leader明确授权，总Go预算仍为USD1。
## 全英文预演状态记录（2026-09-11）

`quote-demo-en-1` 在 Leader 批准返工、900分变更 applied、累计18项通过后，被辅助守卫自定80请求阈值中断后续C波次。未最终评审/完成/归档，未录制；累计Go USD0.223482864/1。服务已停止、证据保留。辅助指令/计量器已修正并离线验证，下一轮仅为待授权提案。详细证据与恢复边界见[全英文预演记录](task106-english-rehearsal-evidence.md)。下文中的首次预演授权为历史记录，不代表可自动新增尝试。

后续Leader已批准并执行`quote-demo-en-2`：两条正式需求更新applied、TESTER识别旧值并返工，但A连续TIMEOUT耗尽两次重试，仍未完成/录屏。仅Go的idle等待从30秒修正为120秒，慢响应与超限边界测试通过；完整158文件1207项0skip、typecheck/lint/生产构建通过。累计Go USD0.274659984/1，官方USD0.185457218/0.35，全部结算；服务已停止、运行容器0、历史与失败证据保留。修正后尚未新增真实Go预演，不自动追加尝试。


## 2026-09-11 正常容量恢复

Leader批准产品恢复模型正常容量，当前Go项目六角色已由65,536更新为1,000,000，输出8192不变，正式设置接口save/keep及刷新验证完成。上文65,536是已发生任务的历史配置，后续新预演应读取最新1M连接并另记来源；不修改旧任务或冻结评测，也未授权/启动新预演。详细证据见task106-english-rehearsal-evidence.md末节。


## 2026-09-11 发布阶段完整输出配置

Leader明确取消发布准备阶段为控制费用设置的限制。当前Go项目六角色通过正式设置页更新为1,000,000 context / 384,000 max output，revision 2→3，新连接e8d08363-2317-4142-b9f7-03e031c4205d；原8192为历史演示配置。后续新任务采用该连接，旧任务继续保留冻结版本。新连接预设与Use model limits同时恢复两项模型能力，手动设置分别保留。旧USD1/官方USD0.35及请求数实验限制不继续用于发布准备；费用仅计量，服务异常先停查。历史实验和账本不可改写，本次没有启动新的预演或录屏。iterationCount 8、有限重试、安全点与Leader完成终审是运行正确性约束，继续保留。


## 2026-09-12 当前预演授权

Leader回复“好的开始预演”，准入新任务quote-demo-en-3。沿用已通过160文件1212项回归的源码、Go deepseek-v4-flash与1M/384K当前连接；取消历史费用/请求数临时门限，独立追加计量并保留异常停止。正式浏览器英文题面→A/B并行→两条900分需求更新→累计验证→REVIEWER→本次Leader终审→归档刷新。保留旧任务和账本，不录视频；只在实际门禁形成后请求该候选的完成裁决。Browser plugin不可用，沿用已有Chrome/Playwright及原生滚动条配置。私有证据在audit/english-rehearsal-3。


## 2026-09-12 正式英文录制授权

Leader明确开始录制，准入新任务quote-demo-en-recording-1，16:23:30Z正式浏览器启动返回202/running。采用预演修复后已通过160文件1214项完整回归的源码，418文件指纹513f2eb7f495c5400dfac0fd96ce0571bade6af9c222a8e3c8c923f33fac01d2；当前Go六角色1M/384K、无临时费用/请求数门限，逐请求计量并在服务异常时停止准入。复用现有本机环境，正式doctor/start通过，录像预检1440×1000/4.04秒可播放与seek。正式视频只捕获英文Agora页面，原生滚动条保留，Browser plugin not available，使用既有Chrome/Playwright。本次候选仍须Leader单独完成裁决，不能复用预演3批准。私有原片和来源/指令/用量记录在audit/english-recording-1，最终成片按同任务原片剪辑并标注时序，未形成候选前不声明录制闭环通过。


[2026-09-12 正式录制1冲突与修复] quote-demo-en-recording-1两条需求更新均applied，TESTER 11/12识别旧票价。A/B返工同基线f6eb4009，A修900为b42d7746，B越界也改ticket.mjs为3286623c，真实Git冲突ticket.mjs；abort成功、无MERGE_HEAD、当前集成树clean，持久integration gate保留。B官方request/header确认自身B和范围[A,B]并存，触发assignment范围歧义；最小回归先红，收紧CODER subtaskIds并明确累计失败不扩大写入责任后42项通过。Leader要求修复后删除旧录制、重新录制，故不resolve当前gate、不继续旧任务；正式服务与录像已停止，原视频待验证结束后按授权删除，保留审计。后续完整回归和新录制结果另记。


[2026-09-12 assignment修复验证与重录] 完整160文件1215项0skip全部通过，500.81s，日志SHA b6cd3dd4ed056ba7a79bf7b1136e4ae5989f5ccadcbf1dbfa421d46925c48813；typecheck/lint/生产构建/G7通过。官方新增37请求USD0.021248580，累计354/USD0.229460768全settled。按Leader要求删除录制1的3个WebM（正式原片及副本、预检片），保留hash/失败State/session/审计，不resolve其gate。新录制quote-en-take-2于16:42:49Z正式202/running，独立audit/english-recording-2；当前418文件来源e167fc9341632709692064a8d230c7df50dfe4ce8f238863faab012d1585d8e3。辅助浏览器初始化第一次误把Node变量放入page.evaluate，启动任务前已修正（0模型调用），不涉及产品源码。新任务六角色1M/384K，PM保留English要求，当前运行与媒体未宣称完成，后续须本次Leader终审。


## 2026-09-12 自然语言交互修复后的录制门槛

Leader明确选择“自然语言聊天→系统识别→展示变更供确认”。后续录制以英文脚本的自然语言题面和中途改价消息为准，不要求用户知道Requirement ID、JSON schema或控制命令；草案由真实无工具Harness生成，UI对照前后需求，按钮确认后通过同一POST入口原子应用关联需求。旧录制6已撤出正式入口，其成功产物和历史证据不被改写。新能力须通过持久化/幂等/过期/安全点回归、真实模型解释验证及桌面/手机UI验证，才重新录制。未完成的真实验证不得以脚本HTTP响应代替；脚本UI检查单独标记。新产物仍须独立D16 Leader完成批准。
