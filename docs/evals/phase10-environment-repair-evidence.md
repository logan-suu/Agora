# Task10.5 运行环境说明修复

2026-09-10。v7首个multi再次因TESTER的两次shell git调用退出127触发自动停止；模型供应商15次请求均完整。原v7保留1pass/1fail/52pending，无后续调用。停止机制工作正常。

先前将单次git CLI误用记录为模型工具使用失败；跨样本重复后复审，所有配置应显式获取固定镜像的环境限制，不能只在宿主规格中说明Git/MCP边界。新增共同环境前缀到public和holdout的goal/TASK.md：Node20、无网络/包安装、无Git CLI，Git只经当前角色获授的MCP工具，工具名不是shell命令；fs_write后可用空patch提交。该前缀不构成新增产品需求，也不扩大权限或在容器安装Git。

真实Docker预检探测Node v20.20.2且git不可用，环境证据绑定image ID与前缀hash，正式freeze/run入口拒绝漂移。99公开断言/4负例、12PM配置投影、2个holdout正反例通过；相关16文件50项、typecheck、lint389文件通过。此前生产代码修复的151文件1143项全量回归保持记录，本次仅调整Eval任务输入/预检，不重复付费全回归。

公开包装version6，holdout包装version2。holdout原始任务文件与隐藏验题未改变，单元测试逐字证明仅共同环境前缀不同；公开题原测试和行为澄清不变。v8冻结360份源码，组db30e8c75738ee474bd1296104303698fd06e65753276fd6597810e43e27b589，源码5bd08463d7bc55cb3594b57c285e80688e77e1c151a436ca6c04dc17d2552838。旧组结果、源码快照和费用均保留。

修复阶段无模型调用。恢复前正式账本累计USD0.154388832，剩余USD4.845611168，无未结算；继续沿用USD5上限。随后按原授权恢复测评，任意异常/非pass仍先停审查。

[2026-09-10 v8重复输出审查] single-1独立验题pass；multi-1的REVIEWER第6请求持续重复同一段1167次，官方codec确认33380唯一事件序号，排除读取重复；两个离线流转发验证无文本复制，360冻结与工作源码一致。该请求自然以max-tokens/32768输出结束，usage完整，单次0.020568264；整attempt21请求/0.037081158，正式累计0.194319048/136请求，零未结算。发现后立即写停止标记，未新增请求；现有异常finish策略同样会停组。trace/routing/cleanup有效，独立Docker检查无关联容器；本次未完成独立验题，不当代码判错。审查暂归供应商响应重复退化，无产品实现缺陷证据，不改配置/题面/评分；保留1pass/1fail及费用，按既有授权继续下一pending。审查记录.data/evals/phase10-final-v8/stop-reviews/grade-school-multi-1/；只读监控已增加重复段落与shell126/127统计。
