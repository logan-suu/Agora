# Agora 技术选型文档

**版本**：v1.6
**日期**：2026-09-05
**状态**：已定稿（同步至 Phase 9 / D17 worker 构造、worktree 身份与职责边界；技术选型未变化，当前进度以 `docs/task-status.json` 为准）


## 1. 选型总览

| 类别              | 技术选型                       | 版本/规格                              |
| :---------------- | :----------------------------- | :------------------------------------- |
| **语言**          | TypeScript                     | 5.9+                                   |
| **运行时**        | Node.js                        | 24 LTS（宿主；用户代码沙箱仍为 Node 20） |
| **包管理器**      | pnpm                           | 9.x + workspaces                       |
| **单 Agent 内核** | DeepSeek Harness               | v0.1 (Developer Preview)               |
| **编排层**        | 自研轻量编排                   | —                                      |
| **工具协议**      | MCP (Model Context Protocol)   | SDK v1 (v1.30.0)                       |
| **沙箱**          | Phase 0: 本地临时目录 (Mock)   | Node.js `fs.mkdtemp` + `child_process` |
| **沙箱**          | Phase 1+: Docker               | dockerode                              |
| **版本控制**      | simple-git                     | 3.36.x                                 |
| **前端框架**      | Next.js                        | 15.x                                   |
| **UI 框架**       | React                          | 19.x                                   |
| **实时通信**      | SSE + HTTP POST                | —                                      |
| **状态持久化**    | Phase 0: 文件系统 (JSON/JSONL) | —                                      |
| **测试框架**      | Vitest                         | 3.x                                    |
| **代码质量**      | Biome                          | 2.x                                    |


## 2. 语言与运行时

### 2.1 TypeScript (5.9+)

**选型理由**：

1. **全栈统一**：前后端 + Agent 运行时统一使用 TypeScript，减少上下文切换成本，类型定义可在全栈共享。
2. **类型安全**：State Schema、MCP 工具接口、RoleSpec 等核心数据结构均可获得完整的类型推导和编译时校验。
3. **生态成熟**：所有核心依赖（Harness、MCP SDK、Next.js）均为 TypeScript 原生或提供一流支持。
4. **单人项目友好**：统一的工具链（格式化、Lint、测试）降低维护成本。

**约束**：
- 启用 `strict: true`
- 启用 `moduleResolution: "bundler"` 或 `"node16"`
- 启用 `esModuleInterop: true`

### 2.2 Node.js (24 LTS host)

**选型理由**：

1. **Harness 持久化要求**：Phase 8 锁定的 `@deepseek-ai/dsh-session-persistence-jsonl` `0.1.1-rc.2` 在包入口直接导入 Node 原生 Zstandard API；Node 22.14/20 无法加载，Node 24.19 实测可用，因此宿主基线必须为 Node 24+。
2. **长期支持**：Node 24 为当前 LTS 基线；`package.json#engines` fail-fast 要求 `>=24`。
3. **原生 Fetch/WebStream/Zstandard**：宿主同时满足 MCP 流式传输与 Harness JSONL 默认 checksummed Zstandard frames。Docker 中执行用户产出仍固定 `node:20-slim`，不把宿主升级扩散为用户代码运行时变更。

> **[2026-09-03 实装记录] 任务 8.1：** `packages/runtime/executor` 已直接声明并接入 `@deepseek-ai/dsh-session-persistence-jsonl@0.1.1-rc.2`，生产组合根按项目/任务建立 session 根目录；Node 24.19 下的集成测试已真实执行 flush、cold load、lineage child create/resume。当前受限环境无法联网重跑 pnpm 9 安装，依赖声明与 lockfile 已同步，测试使用仓库现有 pnpm store 中同一锁定版本。
4. **事件循环适配并发模型**：Node 的事件循环 + Promise 天然适配"多 Worker 各自长跑、到边界让出"的并发形态。


## 3. 包管理器与 Monorepo

### 3.1 pnpm (9.x) + Workspaces

**选型理由**：

1. **磁盘效率**：pnpm 使用硬链接和符号链接，在 monorepo 下能极大节省磁盘空间并加快安装速度。
2. **严格依赖管理**：pnpm 的依赖提升策略更严格，能有效避免"幽灵依赖"，确保项目结构清晰。
3. **Monorepo 原生支持**：`pnpm-workspace.yaml` 提供一流的 monorepo 支持。
4. **Catalog 特性**：pnpm 9.x 的 catalog 特性可统一管理 TypeScript 等关键依赖的版本。

**目录结构**：

```
agora/
├── packages/
│   ├── core/               # 领域模型 + 编排 (L1+L2)
│   ├── runtime/            # 执行器 + 沙箱 (L3+L4)
│   ├── comm/               # 通信总线
│   ├── tools/              # MCP 服务器
│   └── roles/              # 角色定义 (数据)
├── apps/
│   └── web/                # Next.js 前端 (Phase 5+)
├── pnpm-workspace.yaml
├── package.json
└── tsconfig.base.json
```


## 4. 单 Agent 内核

### 4.1 DeepSeek Harness (v0.1, MIT)

**选型理由**：

1. **官方开源，活跃维护**：DeepSeek 于 2026-08-13 正式开源 Harness (DSH) v0.1 开发者预览版，以 MIT 协议开源。
2. **插件化架构**：Harness 的核心理念是"Everything is a plugin"——模型适配器、工具注册、Session Log、甚至 Agent Loop 本身都是可替换的插件。这与 Agora 的微内核架构高度契合。
3. **四个扩展点精确映射**：
   - `agent/pre-step` → 投影注入 / 覆写消息队列
   - `agent/request` → 模型路由
   - `agent/turn-stopping` → 配合式抢占落点
   - `agent/request-error` → 失败重试
4. **事件溯源会话日志**：天然支持 checkpoint、fork、resume、replay，是配合式抢占的基础设施。
5. **`ctx.agents` / `ctx.subagents` 边界**：`ctx.agents.create()` + `followup()`/`whenIdle()` 可由 HarnessExecutor 映射为受控 turn 边界，并已接入 JSONL checkpoint/Fork；`ctx.subagents.start()` 的锁定 spawn provider 是 one-shot，只适合 worker 内边界清晰的一次性委派，不作为 Phase 9 顶层长跑 worker 构造器。
6. **`ctx.compaction`**：内置单 Agent 历史压缩引擎，省去自研。
7. **TypeScript 原生**：整个 Harness 是大型 TypeScript 代码库。
8. **社区活跃**：发布后在 GitHub 上星数迅速增长。

**约束**：
- 当前为 Developer Preview 阶段，后续迭代中可能出现破坏兼容性的修改。
- Phase 0 仅使用 Harness 的薄执行器能力，厚 Agent 支持延后。

> **[2026-09-03 选型同步] D4 Harness 会话持久化：** Harness core `SessionStore` 本身是进程内事件源，仅有 `sessionId` 不能在 context dispose 后恢复。Phase 8 直接采用与 Harness 同版本的 `@deepseek-ai/dsh-session-persistence-jsonl` `0.1.1-rc.2`，不自研 session codec；它按配置 root/规范化 cwd/session id 写持久 header + append-only events。`saveSafePoint()` 必须 await `ctx.sessions.flush()` 且确认持久 listener 参与；`loadSafePoint()` 在全新 context 通过 `ctx.sessionPersistence.load()` 读取经校验的 source log，截取 completed-turn prefix，再调用 `ctx.agents.create()` 以新 sessionId、seed 和 `parentSession/seedLength` lineage 原子创建 child Agent + Session。直接 `ctx.sessions.fork()` 只创建 live child Session，公开 Agent API 不能再把它原子接入 loop，因此不选；`ctx.agents.resume()` 复用旧 sessionId，也不满足新 lineage。该引入是 D4 真恢复所需的 Harness 官方配套插件，不替代 TaskStateStore，不将原始群聊注入 agent，也不改变 D8 单实例部署边界。

> **[2026-09-05 选型同步] D17 顶层 worker 构造：** 本地锁定 `@deepseek-ai/dsh-subagent-spawn-in-process@0.1.1-rc.2` 的官方类型与 README 显示 provider 只有 one-shot `start()`、没有 continuable creation；返回契约是最终 result/dispose，不提供 Agora 所需的逐 turn 控制、D4 持久 suspend/Fork 与 canonical step commit 接缝。因此 Phase 9 复用现有、已实测的 HarnessExecutor 工厂，为每个稳定 workerId 建独立 Context/Agent/session，再由 WorkerRuntime 并发调度；不新增库、不自研 agent loop，也不把 one-shot child 冒充可恢复 worker。

**备选方案（已排除）**：

| 框架                        | 排除理由                                                     |
| :-------------------------- | :----------------------------------------------------------- |
| **Agent Forge**             | 依赖装饰器（`experimentalDecorators`），与 Agora 的纯数据驱动角色设计理念不符；框架较重 |
| **BeeAI Framework**         | IBM 开源，Python/TS 双语言，但设计偏向企业级通用 Agent，与 Agora 的"编码协作"垂直场景匹配度低 |
| **LlamaIndex Workflows TS** | TypeScript 包已弃用（`@llamaindex/workflow-core` 已 deprecated），官方建议使用 Python 版本 |
| **AutoGen (Microsoft)**     | 群聊=全量历史广播、无角色投影（官方 issue #4648/#1006 自认缺陷）；人类是被动参与者、无否决权；MagenticOne 用 LLM 当 orchestrator。仅借鉴其 TerminationCondition/CodeExecutor/Ledger 模式，不引入框架（详见《框架调研与借鉴决策》） |
| **AgentScope (Alibaba)**    | MsgHub 扁平广播无投影、v2.0 已移除（转向 leader-worker）；定位为角色扮演/仿真而非编码编排；人类=普通参与者。仅借鉴其 reply/observe 接口与写所有权不变量，不引入框架（详见《框架调研与借鉴决策》） |


## 5. 编排层

### 5.1 自研轻量编排

**选型理由**：

1. **4 个通用节点**：编排拓扑极小（entry → coordinator → worker → integrate → humanGate → finalize），无需引入重量级框架。
2. **角色是数据，不是节点**：编排层不包含任何角色节点，路由由 Coordinator 数据驱动。增删角色不改代码。
3. **Harness 已提供底层能力**：事件溯源、checkpoint、inbox steering 已由 Harness 提供，编排层只需"粘合"这些能力。
4. **避免框架锁定**：自研层仅 ~500-800 行核心代码，未来替换成本极低。

**设计参考**：参考 LlamaIndex Workflows 的**轻量、事件驱动、100% 类型安全**的设计理念，但自研实现以确保完全可控。


## 6. 工具协议

### 6.1 MCP SDK v1 (`@modelcontextprotocol/sdk` v1.30.0)

**选型理由**：

1. **稳定优先**：v1 是经过验证的生产就绪版本，最新为 1.30.0（2026-07-28）。v2 于 2026-07-28 随新规范发布，但生态尚在建立中。
2. **生态成熟**：v1 的文档、示例和社区支持更丰富。
3. **向后兼容承诺**：v1.x 在 v2 发布后至少继续接收 6 个月的 bug 修复和安全更新。
4. **协议标准化**：MCP 是连接 AI 应用到数据与工具系统的开放标准，与"工具走 MCP"的架构决策一致。
5. **Zod 集成**：SDK 内置 Zod schema 校验（支持 v3/v4），工具输入输出类型安全。

**约束**：
- Phase 0 所有工具服务器使用 v1 SDK 实现。
- 未来可平滑迁移至 v2，迁移指南已由官方提供。

**MCP 工具服务器清单**：

| 服务器           | 路径                      | 功能                              |
| :--------------- | :------------------------ | :-------------------------------- |
| `fs-server`      | `packages/tools/fs/`      | 文件读写（限 worktree 内）        |
| `git-server`     | `packages/tools/git/`     | git 操作（worktree、diff、merge） |
| `test-server`    | `packages/tools/test/`    | 运行测试、返回结果                |
| `lint-server`    | `packages/tools/lint/`    | 静态检查                          |
| `sandbox-server` | `packages/tools/sandbox/` | 沙箱内执行命令                    |


## 7. 沙箱与隔离

### 7.1 Phase 0: 本地临时目录 (Mock)

**选型理由**（阶段 0 快速迭代策略）：

1. **零依赖**：使用 Node.js 原生 `fs.mkdtempSync` + `child_process.spawn`，无需安装 Docker。
2. **快速验证**：1 周内可跑通"状态流转 + Harness 接入 + 测试回环"完整链路。
3. **可替换性**：`SandboxManager` 接口抽象完善，Phase 1 可无缝切换至 Docker 实现，编排层零改动。

**接口定义**：

```typescript
interface SandboxManager {
  createWorktree(taskId: string, role: string): Promise<Worktree>;
  read(worktree: Worktree, path: string): Promise<string>;
  write(worktree: Worktree, path: string, content: string): Promise<void>;
  run(worktree: Worktree, cmd: string, timeout?: number): Promise<RunResult>;
  integrate(base: string, branches: string[]): Promise<IntegrationResult>;
  teardown(taskId: string): Promise<void>;
}
```

### 7.2 Phase 1+: Docker (dockerode)

**选型理由**：

1. **生产级隔离**：每任务一容器，限网络与资源。
2. **生态成熟**：dockerode 是 Node.js 操作 Docker 的事实标准。
3. **可平滑升级**：后续可切换至 E2B / Firecracker microVM。

**[2026-08-27 架构决策更新] `DockerSandbox`（阶段 1 实现类）落地参数**（`packages/runtime/sandbox/src/docker-sandbox.ts`）：

- **一任务一容器**：首次 `createWorktree(taskId, key)` 惰性创建容器，同 taskId 复用；任务 host 根目录 bind mount 至容器 `/workspace`。Phase 0–8 的顺序退化可令 key=role；Phase 9 真并行必须令 key=稳定 workerId，使同 role 多 worker 位于不同子目录。
- **Worktree.path 语义不变**：仍返回 host 绝对路径（组合根/模型零改动，R9）；`read/write` 走 host 侧 bind mount 共享文件，`run` 经 `docker exec`（`/bin/sh -c`，WorkingDir 映射容器路径）。
- **限网络与资源**（默认值，均可配置）：`NetworkMode: 'none'`（默认禁网，可选 `bridge`）；`Memory: 512MiB`（MemorySwap 同量禁 swap）；`CpuShares: 512`；`SecurityOpt: ['no-new-privileges:true']`；`Cmd: ['sleep','infinity']` 保活。
- **默认镜像**：`node:20-slim`（对齐 Node 20 LTS 运行时），可经配置替换；容器创建前检查镜像存在性，缺失自动 pull。
- **run 超时**：默认 30s（R7）；Docker API 无 `POST /exec/{id}/kill`（实测 404），改为容器内 coreutils `timeout -s KILL` 包装命令（`Cmd: ['timeout','-s','KILL',sec,'/bin/sh','-c',cmd]`，argv 数组免二次 shell 解析），超时仅杀 exec 进程组、容器存活可复用；JS 侧 timer 兜底（超时 +10s 强杀整容器，防无 coreutils 镜像挂死）。
- **integrate**：显式 not-implemented（Git 语义归 `packages/tools/git`，Phase 9 集成检查点实现），不静默。
- **teardown**：stop + remove(force) 容器，host 根目录移动至 `agora-sandbox-trash`（移动而非删除，对齐文件保护）。

**配置切换**：`packages/runtime/sandbox/src/factory.ts` 提供 `createSandbox(config)`：

```ts
SandboxConfig = { kind: 'local' } | { kind: 'docker', image?, networkMode?, memoryBytes?, cpuShares?, docker?, baseDir? }
```

默认 `{ kind: 'local' }`（Phase 0 行为不变，R9 向后兼容）；Phase 1 出口测试（1.6）用 `{ kind: 'docker' }` 验证双模式切换。

> [2026-09-01 选型同步] 宿主侧 `simple-git` 与文件 API 只负责 Git 元数据和 artifact 快照管理，不是代码执行后门。Agent 生成的源码/测试/脚本，包括从 `.data/.../artifacts/` 恢复后的独立复验，一律进入新的 DockerSandbox/等价隔离容器后再运行；验收必须检查真实 `RunResult`，不得以宿主 `node --test` 或确定性 adapter 硬编码的通过结果替代。

**[2026-08-27 架构决策更新] 路径防护统一**：抽共享 `path-guard.ts`（`assertInside`，realpath 加固防 symlink 逃逸，含 dangling symlink 拒绝），`LocalTempSandbox` 与 `DockerSandbox` 共用，解决 DEF-001；`dirnameOf/basenameOf` 手写拆分替换为 `node:path`，解决 DEF-002。


## 8. 版本控制

### 8.1 simple-git (3.36.x)

**选型理由**：

1. **轻量**：纯 Node.js 实现，无需额外 CLI 依赖。
2. **TypeScript 支持**：原生类型定义。
3. **功能完备**：支持 clone、branch、checkout、merge、diff 等所有 Phase 0-9 所需操作。

**用途**：
- 每个稳定 `workerId` 绑定独立 git worktree/分支；同一 role 的并行 Worker 不复用目录
- 集成检查点时顺序合并分支
- 冲突检测与升级

> **[2026-09-05 架构决策更新] D17 同步：** simple-git 只负责 9.3 的 worktree/merge 基础设施，不负责并行额度、任务 State 合并或 worker 生命周期。9.1 的 GlobalScheduler lease 与 canonical commit queue 均为自研 L2/L3 控制面，不新增依赖；`SandboxManager.createWorktree(taskId, role)` 保持 R9 的字符串签名，Phase 9 将第二参数作为稳定 workerId 隔离 key 使用。


## 9. 前端与实时通信

### 9.1 Next.js (15.x) + React (19.x)

**选型理由**：

1. **全栈同构**：与后端（Node.js + TypeScript）共享类型定义和工具函数。
2. **App Router**：Next.js 15 的 App Router 支持 Server Components 和 Streaming，适合群聊消息的实时渲染。
3. **部署灵活**：前端可部署至 Vercel；Phase 5–9 的状态与实时后端使用单实例自托管，完整 Serverless 后端须先落地外部耐久存储与跨实例事件传输（D8）。

### 9.2 SSE + HTTP POST（混合模式）

**选型理由**：

1. **HTTP 协议友好**：SSE + HTTP POST 无需 WebSocket 长连升级，前端可在 Vercel 上消费自托管后端的 HTTP/SSE 协议；这不等于当前 `.data` + 进程内订阅后端可部署到无状态 Serverless。
2. **场景匹配**：群聊场景下，接收消息（服务器推送）是主要负载 → SSE；发送消息（用户发言）是低频操作 → HTTP POST。
3. **实现简单**：无需维护独立的 WebSocket 服务。

**协议设计**：
- `GET /api/stream?projectId={projectId}&taskId={taskId}&channelId={id}` → SSE 连接，接收实时展示信封
- `POST /api/messages` → Leader 发送消息或指令（含 `projectId`, `taskId`, `channelId`, 稳定 `msgId`, `display`；浏览器提交的 `msgId` 必须满足 `[A-Za-z0-9][A-Za-z0-9._:-]*`，受信任单用户边界内发送者由服务端固定为 `leader`，结构化意图与 agent `payload` 由服务端生成）
- `POST /api/tasks` → Phase 5 单实例生命周期入口（含 `projectId`, `taskId`, 稳定 `requestId`, `goal`），异步启动既有编排循环；同 scope 不同 goal或另一 scope 已有活动 run 时返回 `409`
- `GET /api/tasks?projectId={projectId}&taskId={taskId}` → 读取持久化任务摘要与进程内 run 状态；进程重启后未完成快照明确返回 `interrupted`

> [2026-09-01 选型同步] D10：任务生命周期入口与 D9 的任务内消息入口分离，但共享同一个 State/MessageBus 事实链；SSE 与 `POST /api/messages` 不再为未知 scope 自动创建占位 State。Phase 5 全实例最多一个活动 run，避免在 GlobalScheduler/Phase 9 前抢跑多任务并行。生产组合根使用 DockerSandbox + HarnessExecutor + MCP 工具目录 + 六角色 roster；Docker bind-mount worktree 由宿主侧 simple-git 初始化，容器内命令执行与宿主 Git 元数据共享同一目录。run 终态时把 worktree 产物快照复制到任务 `.data/.../artifacts/`，持久化引用后释放 Harness/MCP/Git/Docker，刷新只依赖 JSON State 与耐久 artifact。

> **[2026-09-05 选型同步] D17 容量升级：** Phase 9 不引入外部队列或 Redis；同一 Node 后端内由 TaskCompositionFactory 闭包共享自研 GlobalScheduler。D10 的全实例单 run 闸门自 9.1 起退役，不同 task 可并发进入运行注册表；真正执行模型/工具前仍必须取得默认 cap=3 的全局 lease。TaskStateStore 继续按 task 串行，Web canonical join 显式 load 最新 JSON 快照。完整多实例部署仍受 D8 约束。

> [2026-08-31 选型同步] D9：Leader 普通发言与指令共用 `POST /api/messages`；退役绕过 State 的独立 `POST /api/commands` 临时投递面。Phase 5 仅执行经校验的开头单一 `@ROLE` 定向激活，其余阶段能力返回结构化 `rejected/deferred`。消息与有效动作在同任务串行区内经一次 State commit 原子、幂等落盘后才投递。planned message 服务只允许一个主消息，动作 mutations 禁止写 `messages`；多消息路径必须使用会逐条校验并投递的批量服务。

> [2026-09-03 选型同步] D9/D12 Phase 7：角色离职不新增 command API，Leader 使用同一 `POST /api/messages` 提交 `/role remove <ROLE> [to <SUCCESSOR>]`，稳定 `msgId` 作为 actionId。离职横跨现有 JSON `ProjectCollaborationStore` 与 `TaskStateStore`，采用持久阶段 + revision CAS 的可恢复 saga，不引入分布式事务、队列或新依赖。重试先从持久 departure 阶段续办，不被已有 Leader Message 短路；派生 handoff id 命中非规范事实时 fail-closed。Phase 7 只等待目标角色当前 Harness Step 到安全点并保存游标；全局 Preemptor、terminate/fork、inbox 注入与 resume 仍按 Phase 8/9 落地。

> [2026-09-03 选型同步] D9/D4 Phase 8：humanGate 裁决不新增 endpoint，Leader 继续经 `POST /api/messages` 提交 `/resolve-gate <gateId> <option> [argument]`，稳定 `msgId=actionId`。只有 Harness JSONL session 全部 flush 后才提交含 `safePointRefs` 的完整 active gate；一次 Task State commit 写 canonical `leader_intent`、原因特定 mutations、清 gate，并在消息 resolution receipt 中复制 checkpoint refs 与从 actionId 派生的 `resumeSessionId`。新 composition 从 receipt load source prefix，经 Agent factory 创建或接管 lineage child；提交稳定 `human-gate-resumed:<actionId>` 控制事实后才运行。旧 `/approve`/`/reject` 因不绑定 gateId 而拒绝，Phase 9 才接全局抢占。
>
> [2026-09-03 选型同步] D13 Phase 7：项目成员登记继续复用 `ProjectRosterService`/`ProjectCollaborationStore`，不新增模板目录依赖或跨文件事务。任务接手复用 `POST /api/messages`，激活 `/role onboard <ROLE> [from <HANDOFF_MSG_ID>[,<HANDOFF_MSG_ID>...]]`，符合 D9 安全 token 语法的稳定 `msgId` 作为 actionId；目标角色由服务端从最新持久 roster 验证 enabled，Leader 主消息、`nextRole` 与既有 `leader_intent` payload 内的 onboarding receipt 在一次 Task State commit 中落盘。同 ID applied 重放在返回前重新校验既存 canonical 消息、完整 receipt 与当前 State 引用事实。`onboardingContext` 由既有 JSON State 读时投影，零新依赖；SSE 仍只发送不含 payload 的展示信封。模板目录和任意 RoleSpec 编辑 UI 不在 Phase 7 声称范围内。
>
> [2026-08-31 选型同步] D6/D7：SSE 只承载已提交消息的 `{msgId,channelId,fromRole,type,display,ts}` 展示信封，不传 `payload`，也不充当 State 事实源。Message 先写任务 State 的 JSON 快照，再经 MessageBus→SSE 投递；不同项目/任务可复用 `channelId='main'` 而不碰撞。SSE 建连必须无缝覆盖快照与实时尾流，UI 按 `msgId` 去重；同一逻辑发送的网络重试必须复用原 `msgId`。
>
> [2026-09-02 选型同步] D8/D12：Phase 5–9 的全栈运行形态为受信任单用户边界内的单实例自托管后端，Vercel 仅部署前端。完整 Serverless/水平扩展后端需外部耐久 `TaskStateStore` + `ProjectCollaborationStore`（含 `ProjectChannelStore` 视图）+ 跨实例事件传输，由 Phase 10 部署任务根据需求选型。
>
> [2026-09-01 选型同步] D7 Phase 6 历史形态：当时新增零外部依赖的 JSON `ProjectChannelStore`，快照位于 `.data/projects/{projectId}/channels.json`，初始化幂等，更新按项目串行、以 `revision` 防止丢失更新，并通过同目录临时文件 + rename 原子替换。自 Phase 7/D12 起该文件仅作迁移输入，现行唯一事实源见下方 `collaboration.json` 规则；inbox 仍是项目快照与任务 `state.json` 的可重建读模型。
>
> [2026-09-01 选型同步] D7 Phase 6 历史升级恢复：当时入口发现已有任务 State 且 `channels.json` 为 ENOENT 时，以合法 roster 幂等补建 main。自 Phase 7/D12 起禁止在 `collaboration.json` 已存在时重建 legacy 文件；两文件都缺失才由统一 store 初始化 collaboration 快照。解析失败或不变量失败保持 fail-fast；对象属性顺序通过确定性 canonical JSON 比较消除伪变更，数组顺序不改写。

> [2026-09-02 选型同步] D12：Phase 7 继续采用零外部依赖 JSON，不引入数据库；项目 roster 与 Channel 改由 `.data/projects/{projectId}/collaboration.json` 的 `ProjectCollaborationSnapshot` 同 revision 原子保存。现有 `ProjectChannelStore` 签名不变并作为 Channel 视图，底层与新的协作写端口共享同一项目串行器和 CAS。旧 `channels.json` 仅作一次性迁移输入：原子写入后从正式路径重新读取并核对 collaboration 快照，成功后才清理；崩溃遗留的同内容双文件可由 initialize/load 幂等完成恢复清理，内容冲突或任一损坏均 fail-fast，不运行长期双写。


## 10. 状态持久化

### 10.1 Phase 0–5+: 文件系统 (JSON / JSONL，默认且必须可用)

**选型理由**：

1. **零依赖**：直接读写 JSON 文件，快速启动。
2. **与 Harness 契合**：Harness 的事件溯源日志本质是事件流，JSONL 是自然延续。
3. **可读性强**：调试时可直接查看 State 快照。

**存储结构**：
```
.data/
├── projects/
│   └── {projectId}/
│       ├── collaboration.json         # Phase 7+ roster + Channel 原子协作快照（D12）
│       ├── channels.json              # 仅 Phase 6 legacy 迁移输入；迁移完成后删除
│       ├── tasks/
│       │   └── {taskId}/
│       │       ├── state.json          # AppState 快照
│       │       ├── harness-sessions/     # Phase 8+ Harness JSONL session 持久根
│       │       │   └── ...                # plugin 管理 header/events/lineage
│       │       └── artifacts/          # 终态产物快照；刷新恢复不依赖活沙箱
│       └── knowledge/
│           └── kb.json                 # 知识库 (Phase 3+)
└── global/
    └── scheduler.json                  # 全局调度器状态
```

### 10.2 Phase 5+: SQLite（可选项）

当需要复杂查询（如按时间范围检索历史任务、跨任务搜索）时，可无缝迁移至 SQLite。

> [2026-08-31 选型同步；2026-09-03 D4 校准] 澄清“Phase 5+”不等于强制 SQLite：Phase 5 单实例 MVP 采用 `.data/projects/{projectId}/tasks/{taskId}/state.json` 原子快照；早期 `events.jsonl` 只是未落地的 Harness 事件占位命名，自 Phase 8 起由任务目录下 `harness-sessions/` 中锁定版本的官方 JSONL persistence plugin 取代。SQLite 仅在复杂查询出现时替换适配器。State 提交完成后才发布实时事件，进程内 SSE 接缝不构成持久化。

> **[2026-09-03 选型同步] D4 恢复校验与失败清理：** JSONL plugin 的 session header 只证明声明的 lineage，不能单独证明 child 内容；接管既有 child 前必须加载其事件并把 source completed-turn seed 前缀逐项比对。`RecoverableSandboxManager.resume()` 除公共 baseDir/path guard 外还必须验证路径属于当前 task 的规范 root。composition factory 的恢复流程采用 acquire/cleanup 作用域，identity、roster、Executor、safe-point load 或 inbox 注入任一步失败，都释放本次已经取得的运行资源。gate 已耐久提交后的 suspend 清理失败仍是 `needs_attention`，不触发终态 artifact 归档。

> [2026-08-31 选型同步] `.data` 是单实例自托管后端的默认真实实现，不是 Serverless 实例本地持久化方案。SQLite 同样不自动解决多实例共享；若 Phase 10 选择 Serverless/水平扩展，必须单独选定外部耐久存储与事件传输。

> [2026-09-01 选型同步] Phase 5→6 历史升级采用惰性 create-if-absent：任务 State 已存在且项目 `channels.json` 缺失时补建 main。该规则只描述 Phase 6 迁移点；Phase 7+ 由 D12 的 `collaboration.json` 初始化/迁移规则替代，禁止据此重建 legacy 文件。损坏 JSON 或身份/不变量错误始终 fail-fast。

> [2026-09-03 选型同步] D4 将“Task State 快照”与“Harness session 事件源”分开：前者仍由 `JsonTaskStateStore` 原子替换，后者由 Harness JSONL persistence plugin 管理追加、flush、load 与 lineage。`safePointRef` 是作用域化的 opaque 引用，不是进程内 sessionId。gate 等待期保留 State/worktree/session 且不归档终态 artifact；冻结的 `SandboxManager.teardown()` 会移动 worktree，只能用于终态，暂停/重建由不改变基接口的 `RecoverableSandboxManager.suspend()/resume()` companion capability 停止容器并重新挂载原 host root。仅当裁决事实已提交、Sandbox 与 Agent factory 已恢复或接管 lineage child，且稳定 resumed 控制事实已持久化后才能宣称 resumed。


## 11. 测试与代码质量

### 11.1 Vitest (3.x)

**选型理由**：

1. **Vite 生态**：与 TypeScript 项目集成顺滑。
2. **速度快**：利用 ES Module 原生特性，测试执行速度快。
3. **兼容 Jest API**：学习成本低。

### 11.2 Biome

**选型理由**：

1. **All-in-One**：集格式化、Lint、Import 排序于一体。
2. **速度极快**：Rust 实现，比 ESLint + Prettier 快 10-50 倍。
3. **TypeScript 原生**：无需额外配置。

### 11.3 工程 Eval Suite（D11）

**选型原则**：

1. **复用任务生态**：优先复用 [SWE-bench Verified](https://openai.com/index/introducing-swe-bench-verified/) 的真实仓库 Issue/测试判定，以及 [Terminal-Bench/Harbor](https://github.com/harbor-framework/terminal-bench) 的容器任务格式与 Outcome Grader；不从零采集通用 Coding Benchmark，也不为排行榜定制生产 Agent。
2. **保持运行时纯 TS**：Phase 6 的统一任务 schema、运行器、四类 Grader 与结果汇总使用仓库既有 TypeScript/Node/Vitest/Docker 能力，不新增产品运行时依赖。
3. **外部适配而非侵入**：公开 Benchmark 通过独立 adapter 调用 Agora；Harbor/Python/`uv` 等只可作为 Phase 10 的可选开发评测工具，实施时必须重新核对版本、许可证、网络/镜像成本与技术选型，禁止进入 L1/L2 或默认产品部署。
4. **固定版本可审计**：记录数据集名称、release/commit、任务 ID/version/canonical fingerprint、runner/adapter 版本、模型 provider/model/参数、预算、环境指纹与 attempt；公开数据不复制入仓库，原始运行产物默认 gitignored。
5. **四维结果与 fail-closed**：Outcome（测试/补丁）、Process（协作不变量）、Efficiency（token/费用/时延/调用/编排轮次与返工）、Safety（沙箱/权限/泄漏/回收）。统一 check 状态为 `pass | fail | unknown` 并附证据引用；Outcome/Process/Safety 未观测不得通过，Efficiency 缺失才可显式 `unknown`。`maxIterations` 对应编排轮次，不混同返工。LLM-as-judge 只补充主观 rubric，确定性结果仍用测试和结构化不变量。
6. **隔离、生命周期与 profile**：每次运行使用经真实路径校验的 `.data/evals/<runId>/` 独立 data root/workspace，允许生产组合根写评测专用 State，但禁止访问用户 `.data/projects/` 与 KB。结果按 provisional→清理→final 两阶段原子留档，只有 final 可计入通过。Phase 6 同一任务目录提供分开汇总的 deterministic（脚本化 adapter）与 model（真实 provider）profile；前者不算模型能力，后者不承诺逐字节确定。
7. **配置与驱动同源**：实际模型/环境实例和结果元数据由同一冻结配置构造；禁止未记录环境覆写。任务 schema 必须完整运行时校验，未映射 driver 与缺凭据的专用 model 命令明确失败，不静默 skip 或回退。

Phase 6 先以 8–12 个小型内部版本化任务打通 Eval 基础设施；保持运行时纯 TypeScript，不新增产品依赖，指标通过 Eval wrapper/observer 和既有 trace 采集，无法可靠取得时写 `unknown`。Phase 10 再接公开 Benchmark 的小型固定子集并做重复对照。Eval 允许统计波动，不替代 Vitest、阶段出口集成测试和 G5 真实执行链路。


## 12. 依赖版本锁定（Recommendation）

| 依赖                        | 推荐版本  | 备注                           |
| :-------------------------- | :-------- | :----------------------------- |
| `typescript`                | `^5.9.0`  | 使用 pnpm catalog 统一版本     |
| `@deepseek-ai/dsh`          | `latest`  | 跟踪官方最新，Harness 迭代较快 |
| `@deepseek-ai/dsh-session-persistence-jsonl` | `0.1.1-rc.2` | Phase 8 D4；必须与当前 Harness RC 版本对齐 |
| `@modelcontextprotocol/sdk` | `1.30.0`  | 锁定 v1 稳定版                 |
| `next`                      | `^15.0.0` | —                              |
| `react`                     | `^19.0.0` | —                              |
| `react-dom`                 | `^19.0.0` | Next.js 15 必需的 React DOM 运行时 |
| `@types/react`              | `^19.0.0` | React 19 TypeScript 类型       |
| `@types/react-dom`          | `^19.0.0` | React DOM 19 TypeScript 类型   |
| `dockerode`                 | `^4.0.0`  | Phase 1+                       |
| `simple-git`                | `^3.36.0` | —                              |
| `vitest`                    | `^3.0.0`  | —                              |
| `@biomejs/biome`            | `^2.0.0`  | —                              |


## 13. 选型决策记录

| 决策点               | 决策                                    | 日期       |
| :------------------- | :-------------------------------------- | :--------- |
| 单 Agent 内核        | DeepSeek Harness v0.1                   | 2026-08-24 |
| 编排层               | 自研轻量编排                            | 2026-08-24 |
| MCP SDK              | v1 (`@modelcontextprotocol/sdk` 1.30.0) | 2026-08-24 |
| 沙箱 (Phase 0)       | 本地临时目录 (Mock)                     | 2026-08-24 |
| 沙箱 (Phase 1+)      | Docker + dockerode                      | 2026-08-24 |
| 实时通信             | SSE + HTTP POST                         | 2026-08-24 |
| 包管理器             | pnpm workspaces                         | 2026-08-24 |
| 状态持久化 (Phase 0) | 文件系统 (JSON/JSONL)                   | 2026-08-24 |
| Harness session 持久化 (Phase 8) | 官方 `dsh-session-persistence-jsonl` + Agent factory seed/lineage 真分叉 | 2026-09-03 |
| 框架借鉴（AutoGen）  | 借鉴 TerminationCondition/CodeExecutor/Ledger 模式，不引入框架 | 2026-08-24 |
| 框架借鉴（AgentScope）| 借鉴 reply/observe/写所有权不变量/interrupt()，不引入框架       | 2026-08-24 |
| 工程 Eval（D11）      | 渐进式 Agora Eval + 成熟 Benchmark 外部 adapter；不自建通用任务生态，不新增默认产品运行时依赖 | 2026-09-01 |
