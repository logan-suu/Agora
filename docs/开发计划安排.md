# Agora 开发计划安排

**[2026-09-12 T10.6 Go 推理回放兼容修复]** opencode.ai 的 deepseek-v4-flash/pro 自定义路由启用锁定 PiAi/Harness 原生 reasoningEfforts 与 requiresReasoningContentOnAssistantMessages：非空推理原样回放，无推理的 assistant 消息由原生序列化补空 reasoning_content。thinkingFormat=deepseek；不提供 off 映射，不显式关闭思考，保留提供方默认模式及1M/384K容量。其他提供方/模型不变，不改上游依赖或自研循环。原始推理不进入群聊/Trace。此为协议兼容补齐；录制2原HTTP400未能以合成对照复现，不宣称该次失败根因已确认。验证及限制见 https://github.com/logan-suu/Agora/blob/83fe416c1015d86616acdead7af2ebbad82e8384/docs/demo/task106-english-recording-evidence.md。

**版本**：v2.2
**日期**：2026-09-07
**状态**：已定稿（同步至 Phase 9 / D17 精确 linked-worktree 成员身份、不可变 artifact receipt、当前波次投影与补偿式清理；当前进度以 `docs/task-status.json` 为准）


## 1. 总览

### 1.1 开发原则

1. **先跑通，再优化**：每个阶段优先交付可演示的最小闭环，而非完美实现。
2. **接口先行**：模块间依赖通过接口定义解耦，允许并行开发和替换实现。
3. **AI-Native 开发**：充分利用 Cursor/Windsurf 的 Composer 能力，将详细设计文档作为上下文输入，让 AI 生成 90% 的样板代码。
4. **每阶段可演示**：每个阶段结束时必须有可运行/可演示的产出，便于秋招展示进度。
5. **风险前置**：最不确定的技术点（Harness 接入、投影覆写）在 Phase 0 优先验证。
6. **评测随能力演进**：确定性测试/G5 继续作为门禁；自 Phase 6 起维护渐进式工程 Eval，成熟 Benchmark 复用任务与结果判定，Agora 只补协作语义 Grader，未实测数据不得对外宣称。

### 1.2 时间估算

| 阶段     | 内容                 | 预估时间 | 累计时间 |
| :------- | :------------------- | :------- | :------- |
| Phase 0  | 骨架 + 单 Agent 闭环 | 1 周     | 1 周     |
| Phase 1  | 沙箱与工具           | 1 周     | 2 周     |
| Phase 2  | 完整角色与回环       | 1.5 周   | 3.5 周   |
| Phase 3  | 上下文工程           | 1 周     | 4.5 周   |
| Phase 4  | 自适应编排           | 1 周     | 5.5 周   |
| Phase 5  | 群聊界面 MVP         | 1.5 周   | 7 周     |
| Phase 6  | 多形态通信 + Eval 骨架 | 1.5 周   | 8.5 周   |
| Phase 7  | 角色热插拔           | 1 周     | 9.5 周   |
| Phase 8  | 人工卡点与裁决       | 1 周     | 10.5 周  |
| Phase 9  | 真并行升级           | 1.5 周   | 12 周    |
| Phase 10 | 产品化 + 最终 Benchmark | 2 周     | 14 周    |

**总计约 14 周（3 个多月）**。Phase 5 已提供秋招可演示 MVP；后续 Eval 采用阶段增量，避免为了最终报告阻塞产品主线。


## 2. 技术栈版本锁定

基于《技术选型文档》的决策，各阶段使用的版本如下：

| 依赖             | 版本        | 备注                       |
| :--------------- | :---------- | :------------------------- |
| TypeScript       | 5.9+        | pnpm catalog 统一管理      |
| Node.js          | 24 LTS      | 宿主运行时；用户代码沙箱仍为 Node 20 |
| pnpm             | 9.x         | 含 Catalogs 特性           |
| DeepSeek Harness | 最新 rc     | 跟踪官方最新版本           |
| MCP SDK          | v1 (1.30.0) | v2 稳定后评估迁移          |
| Next.js          | 15.x        | React 19 最小版本          |
| React            | 19.x        | Server Components 稳定     |
| Vitest           | 3.x         | 原生 ESM + TypeScript 支持 |
| Biome            | 2.x         | 统一 Lint + Format         |
| dockerode        | 4.x/5.x     | Phase 1+ 引入              |
| simple-git       | 3.36.x      | TypeScript-first           |


## 3. Phase 0：骨架 + 单 Agent 闭环（1 周）

### 3.1 目标
搭建工程骨架，跑通“Coordinator → Coder → Tester”的最小闭环，验证 State 传递、Harness 接入、投影覆写三个核心机制。

### 3.2 里程碑
- [ ] **Day 1-2**：pnpm monorepo 初始化，TypeScript 配置，Biome 配置
- [ ] **Day 3-4**：State 接口定义 + Reducer 合并函数 + 真实 LocalTempSandbox 临时目录沙箱
- [ ] **Day 5-6**：Harness Executor 接入 + `agent/pre-step` 投影覆写
- [ ] **Day 7**：跑通“单文件 LRU 缓存”任务闭环

### 3.3 具体任务

**工程初始化**：
- 创建 `pnpm-workspace.yaml`，配置 `packages/*` 和 `apps/*`
- 创建 `tsconfig.base.json`（`strict: true`, `moduleResolution: "bundler"`）
- 配置 Biome（`biome.json`，含 Lint + Format + Import 组织）
- 配置 Vitest（`vitest.config.ts`）
- 配置 pnpm catalog（统一 TypeScript、Harness 等版本）

**核心模块（`packages/core/`）** ：
- `domain/state.ts`：AppState 接口定义（仅 Phase 0 必需字段）
- `domain/reducer.ts`：`applyMutations()` 合并函数（append/mergeById/set）
- `domain/roster.ts`：RoleSpec 类型定义 + COORDINATOR/CODER/TESTER 三条静态规格
- `orchestration/orchestrator.ts`：主循环（entry → coordinator → worker → coordinator → finalize，CODER 写码 → TESTER 跑测 → 失败回 CODER）
- `orchestration/coordinator.ts`：路由函数（固定返回 CODER）

**沙箱（`packages/runtime/sandbox/`）** ：
- `SandboxManager` 接口定义
- `LocalTempSandbox` 实现（`fs.mkdtempSync` + `child_process.spawn`）

**执行器（`packages/runtime/executor/`）** ：
- `Executor` 接口定义（`step`, `saveSafePoint`, `loadSafePoint`, `injectInbox`）
- `HarnessExecutor` 实现：
  - 复用 `ctx.subagents.start`（`inheritsParentContext=false`）
  - `agent/pre-step` 覆写 `messages` 为 `project()` 返回值
  - `agent/request` 固定模型路由
  - 自行驱动 `runStep`（而非 subagent 的 one-shot）

> **[2026-09-05 实现校准]** 上述 Phase 0 条目保留最初计划原文；锁定 Harness 版本并不存在可逐 step 驱动的 subagent `runStep`。实际已交付实现及 Phase 9 D17 顶层 worker 都使用 `HarnessExecutor` 内的独立 `Context` + `ctx.agents.create()` + `followup()` / `whenIdle()`；`ctx.subagents.start()` 仅可用于 worker 内边界清晰的 one-shot 委派，不作为顶层 WorkerRuntime 构造器。

**验证任务**：
- 创建 `packages/core/__tests__/e2e/lru-cache.test.ts`
- 任务描述：“实现一个带 TTL 的 LRU 缓存类，包含 get/set/delete 方法，并编写单元测试”
- 预期流程：Coordinator 创建任务 → Coder 写代码 → Tester 跑测试 → 通过则 finalize

### 3.4 技术风险与应对

| 风险                                        | 应对                                        |
| :------------------------------------------ | :------------------------------------------ |
| Harness `ctx.subagents` API 不稳定          | 跟踪官方更新，锁定具体版本                  |
| `agent/pre-step` 覆写与 compaction 顺序冲突 | 约定顺序：先 compaction 后 injectProjection |
| 本地临时目录权限问题                        | 使用 `os.tmpdir()` 保证跨平台兼容           |


## 4. Phase 1：沙箱与工具（1 周）

### 4.1 目标
将真实 LocalTempSandbox 临时目录 升级为完整的工具链，接入 MCP 协议，为 Phase 2 的多角色协作做准备。

### 4.2 里程碑
- [ ] **Day 1-2**：MCP SDK v1 接入，实现 `fs-server`（read/write/list）
- [ ] **Day 3-4**：实现 `test-server`（运行测试、返回结果）
- [ ] **Day 5**：实现 `git-server`（worktree 创建、diff）
- [ ] **Day 6-7**：Docker 沙箱集成（dockerode），替换 LocalTempSandbox

### 4.3 具体任务

**MCP 工具服务器（`packages/tools/`）** ：
- 使用 `@modelcontextprotocol/sdk` v1 实现
- `fs-server`：read/write/list，路径限定在 worktree 内
- `test-server`：run 命令，返回 `{passed, total, failed, failures, coverage}`
- `git-server`：createWorktree、diff、merge（Phase 1 仅实现基础操作）

**沙箱升级**：
- 保留 `SandboxManager` 接口不变
- 新增 `DockerSandbox` 实现（dockerode）
- Phase 1 支持通过配置切换 LocalTemp / Docker

**工具-角色权限矩阵**：
- 在 `RoleSpec.tools` 中声明白名单
- Harness Executor 通过 `toolFilter` 限制可用工具

`[2026-08-27 实测校准]` 任务 1.5 落地：白名单数据化装载（`RoleSpec.tools` → 桥目录展开 → executor ctx 注册 + agent 级 `restrict` 作用域化，Phase 1 toolFilter 等价物）；fs/test/git MCP server 经 InMemoryTransport 进程内桥接为 Harness 工具；超时（`timeoutMs` + timeout-policy 插件，R7 30s）/审批门（`tools/pre-execute`，Phase 8 humanGate 缝）/限流（`guard` 每 turn 预算）三治理就位。`lint` server 无 Phase 1 任务，立项 DEF-005 延期（台账 docs/deferred-items.json），Phase 1 出口测试不覆盖 lint。

`[2026-08-28 实测校准]` 任务 1.6 出口测试落地，三点决策留档供 Phase 2 组合根参考：① 循环内工具面仅取角色白名单实有项（CODER/TESTER 的 §2 白名单不含 `fs.list`/`test.run` → 出口测试循环内只跑 fs_write/sandbox_run，fs/test/git 全部 wire 工具在直链目录链验证）；② `git` 组不进循环：in-loop git 流需组合根在 `git_createWorktree` 后切换 getWorktree 指针（Phase 2 组合根机制），且 DEF-006（TESTER git 粒度）open，故 git 全链（createWorktree→applyPatch→diff→merge）在直链以真实 git 二进制验证；③ 双模式切换以「同一脚本化循环分别跑 LocalTemp 与 Docker」验证，Docker 组按 daemon 探测 skip（对齐 DEEPSEEK_API_KEY skip 模式）。出口测试 13 例全绿，typecheck/lint/test（183 通过 2 跳过）无回归。

### 4.4 依赖关系
- 依赖 Phase 0 的 Executor 接口
- MCP Server 独立于编排层，可并行开发


## 5. Phase 2：完整角色与回环（1.5 周）

### 5.1 目标
补齐全部 6 个角色（PM/Architect/Coder/Tester/Reviewer），实现 coding↔testing↔review 完整回环。

### 5.2 里程碑
- [ ] **Day 1-3**：实现 PM、Architect、Reviewer 的 RoleSpec 和执行器
- [ ] **Day 4-6**：Coordinator 路由升级，支持多角色条件路由
- [ ] **Day 7-9**：测试-编码回环 + 评审回环 + 迭代上限控制

### 5.3 具体任务

**角色定义（`packages/roles/definitions/`）** ：
- 6 个角色的 YAML/TS 定义（systemPrompt、tools、projection 切片）
- `roster` 注册表加载逻辑

**Coordinator 路由升级**：
- 从固定返回 CODER 升级为条件路由：
  - `goal` 含糊 → PM
  - 需求已定 → Architect
  - 设计完成 → Coder
  - 测试失败 → Coder（回环）
  - 测试通过 → Reviewer
  - 评审通过 → 完成候选（Phase 2–7 保留历史直接 finalize 退化；Phase 8/D16 起必须经 Leader completion gate）

**回环控制**：
- `iterationCount` 硬上限（默认 8）
- 超限自动置 `humanGate`

**角色投影细化**：
- 各角色的 `projection` 切片按详细设计第 7 节实现
- CODER 只投 subtask + 失败测试 + 相关 fileRefs
- REVIEWER 投 pendingDiff + conventions + architecture

### 5.4 依赖关系
- 依赖 Phase 1 的 MCP 工具（fs/test/git）
- 依赖 Phase 0 的投影机制


## 6. Phase 3：上下文工程（1 周）

### 6.1 目标
实现结构化交接包、决策台账（含权威级别）、Handoff 机制，打牢上下文地基。

### 6.2 里程碑
- [ ] **Day 1-2**：`decisionLedger` 实现（含 authority 字段）
- [ ] **Day 3-4**：`handoffPackets` 结构化交接包
- [ ] **Day 5-6**：角色投影视图完善（按角色精确切片）
- [ ] **Day 7**：跨 Agent 摘要压缩（自研，非 Harness compaction）

### 6.3 具体任务

**决策台账（`packages/core/domain/ledger.ts`）** ：
- `Decision` 接口：id, topic, decision, rationale, authority, by, supersedes, ts
- `addDecision()`：追加并检查与既有决策的冲突
- 权威级别：`'leader'` > `'agent'`

**交接包（`packages/core/domain/handoff.ts`）** ：
- `HandoffPacket` 接口：fromRole, toRole, done, keyDecisions, openIssues, fileRefs
- 生成逻辑：在角色切换时由 Coordinator 触发

> [2026-08-31 实装记录] 任务 4.4 在 Coordinator 的统一决策后置接缝实现通用生成：仅当既有 `nextRole` 与新 worker role 不同时追加 `HandoffPacket`，同角色重派与首次派发不生成；所有路径继续通过 reducer 的 shape/决策引用完整性门禁。实现覆盖当前及未来角色切换，不只硬编码旧五条 flip；Phase 2 真实 Harness/MCP/LocalTempSandbox 链已在无手工 seed 情况下验证连续交接，DEF-012 据此解除。

**投影完善（`packages/core/domain/projection.ts`）** ：
- 按详细设计第 7 节的表格精确实现各角色投影切片
- 三条铁律：不投原始群聊 log、代码传引用不传全文、理由随决策走

**自研摘要**：
- 跨 Agent 投影切片超阈值时触发
- 用摘要替换旧条目，保留 msgId/fileRef 检索入口

### 6.4 依赖关系
- 依赖 Phase 2 的完整角色集


## 7. Phase 4：自适应编排（1 周）

### 7.1 目标
实现复杂度路由 + 反馈式升级，让参与规模随任务自然伸缩。

### 7.2 里程碑
- [ ] **Day 1-2**：复杂度评估（Tier 0/1/2 分类逻辑）
- [ ] **Day 3-4**：分层拓扑路由（Tier 0 跳 PM/ARCH，Tier 2 全队）
- [ ] **Day 5-6**：反馈式升级（测试连续失败 → 加 REVIEWER → 拉 ARCHITECT）
- [ ] **Day 7**：4.5 代表性端到端场景（简单任务 vs 复杂任务）→ 4.6 Phase 4 累计出口门禁

### 7.3 具体任务

**复杂度评估（`packages/core/orchestration/complexity.ts`）** ：
- 输入：`goal`、文件数估算、依赖数
- 输出：`{ tier: 0|1|2, signals: Record<string, unknown> }`
- Tier 0：单文件/单函数 → 直接 CODER
- Tier 1：小型多文件 → PM(条件)→ARCH→CODER↔TESTER
- Tier 2：复杂多模块 → 全队 + 多子任务拆分（Phase 4 顺序调度；真并行 Phase 9）

**Coordinator 路由升级**：
- 读 `complexity.tier` 决定起始拓扑
- 每轮回到 Coordinator 检查升级条件：
  - 测试连续失败 N 轮且未加 REVIEWER → 加 REVIEWER
  - REVIEWER 指出设计问题 → 拉 ARCHITECT
  - iterationCount >= 8 → humanGate

**Coordinator 双层进度台账**：
- 外层 `task`：确定性维护 confirmed facts / hypotheses / role plan；内层 `progress`：维护 satisfied/loop/progress/next speaker/instruction
- Ledger 作为严格校验的结构化 Coordinator message append-only 持久化，不增加 AppState/Mutation 字段
- worker 仅投影 confirmed facts、本角色 plan 与当前指令；Coordinator 投影完整台账，禁止纯文本全员广播
- 普通完成只标 `completionCandidate`；`isRequestSatisfied` 归 leader authority，Phase 8 以前不由 LLM/Coordinator 自动置真
- 连续 stall 上限 3，达到上限后 revision++ 并从结构化 State 重建 facts/plan

**4.5 代表性正向场景验收（端到端验证）**：
- 测试用例 1：“实现一个函数”（预期 Tier 0，跳过 PM/ARCH）
- 测试用例 2：“实现一个 REST API 服务”（预期 Tier 2，全队出动）

> [2026-08-31 实测口径校准] Phase 4 的 Tier 2“全队出动”指 PM/ARCHITECT/CODER/TESTER/REVIEWER 全角色拓扑到位、Coordinator 路由与 Ledger 在场，并将多模块拆为多个子任务顺序调度；真并行批调度仍属 Phase 9。4.5 采用确定性真实执行链集成验证：仅脚本化非确定性的外部 LLM 响应，Harness、WorkerRuntime、角色投影、MCP、LocalTempSandbox、Reducer、Coordinator 与 Ledger/handoff 均使用真实实现。

**4.6 Phase 4 出口集成测试**：
- 定位：Phase 4 的累计发布门禁，不重复实现 4.5 的两个完整正向场景；4.5 的通过结果作为“参与规模随任务伸缩”的前置证据
- 覆盖 4.1–4.5 的阶段级组合行为：Tier 路由正确性、连续测试失败升级至 REVIEWER、架构问题升级至 ARCHITECT、`iterationCount >= 8` 进入 humanGate、Ledger 连续 stall 达上限后的 revision/replan 节奏，以及 leader-only completion 语义
- 执行 Phase 0–3 累进回归与 G1–G7 门禁，确认 Phase 4 没有破坏既有真实 Harness/MCP/沙箱/投影/交接链路
- 对照 Phase 4 `exit_criteria` 留存证据；仅在 4.6 合并并完成收尾后，Phase 4 才可标 `done` 并推进 Phase 5

> [2026-08-31 任务边界澄清] 4.5 是代表性功能场景验收，4.6 是阶段出口门禁；二者是“前置证据 → 累计放行”的关系，不是两份重复的端到端测试。

### 7.4 依赖关系
- 依赖 Phase 3 的决策台账（升级时写决策）


## 8. Phase 5：群聊界面 MVP（1.5 周）

### 8.1 目标
构建 Next.js 前端，实现群聊式界面，Agent 消息实时流展示，Leader 可发送指令。

### 8.2 里程碑
- [ ] **Day 1-3**：Next.js 15 + React 19 项目初始化，SSE 服务端
- [ ] **Day 4-6**：群聊 UI 组件（消息列表、输入框、角色头像）
- [ ] **Day 7-9**：消息总线接入，Leader `@ROLE` 指令 → 可持久验证的编排目标映射

### 8.3 具体任务

**前端项目（`apps/web/`）** ：
- Next.js 15 App Router + React 19
- 使用 Server Components + Client Components 混合渲染
- Turbopack 稳定版用于开发

**实时通信与消息提交**：
- SSE 端点：`GET /api/stream?projectId={projectId}&taskId={taskId}&channelId={id}`
- HTTP POST：`POST /api/messages` 发送 Leader 消息；服务端在受信任单用户边界内固定发送者为 `leader`
- 应用服务先以 `applyMutations()` 提交消息并通过 JSON TaskStateStore 原子持久化，再发布 `MessageCommitted`
- SSE 只推 `{msgId,channelId,fromRole,type,display,ts}` 展示信封，不推 `payload`
- SSE 建连必须无缝衔接持久快照与实时尾流：可重复、不可漏消息，UI 按 `msgId` 去重
- `msgId` 按逻辑发送稳定；超时/断网/响应丢失后重试同一条消息时复用原值
- Phase 5 每个活动任务只支持固定 `channelId='main'`；项目/任务作用域防止同名 main 群碰撞
- [2026-09-09 架构决策更新] 当前产品仅面向用户本机单用户、单实例运行；Phase 10 延续该范围，不提供 Vercel/云服务器/Serverless 路线（D8/D12）

> [2026-08-31 实装记录] 任务 5.1 已落 Next.js 15 App Router、SSE 心跳/断连清理、按 `channelId` 隔离的进程内传输接缝，以及 `POST /api/messages` / `POST /api/commands` 的初始 HTTP 接缝。`commands` 在该任务只传输不解释，意图映射仍归后续任务；正式 MessageBus 接入仍归 5.3。进程内接缝只用于单实例 MVP，State 持久化缺口见 DEF-014，须在 5.6 前核对。服务端身份与 `Channel.participants` 授权尚未落地，当前端点只允许受信任的单用户部署；公开暴露前或最迟 Phase 6 出口前按 DEF-015 解决。该历史接缝已由 D9 判定在 5.4 退役，Leader 发言与指令统一收敛到 `POST /api/messages`。

**UI 组件**：
- 消息列表（按时间排序，角色头像 + 名称 + 内容）
- 输入框（支持 @ 提及角色）
- 侧栏（当前任务状态、活跃 Worker 列表）

> [2026-08-31 实装记录] 任务 5.2 按 Leader 确认采用默认暗色的极客工作台视觉：桌面为频道/团队、群聊时间线、任务状态三栏，移动端收起两侧栏并通过互斥抽屉访问；消息组件只渲染 `display`，不渲染 `payload`。输入框已实现本地 @ 角色筛选、插入与 Leader 消息追加，用于验证 UI 交互；正式 SSE/MessageBus 接线仍归 5.3，Leader 意图解释仍归 5.4，不在本任务抢跑。

> [2026-08-31 架构决策更新] 任务 5.3 同步承担 D6/D7 的 Phase 5 退化落地：新增 TaskStateStore 端口与 JSON 快照适配器，消息经 reducer 提交/持久化后才由 MessageBus 投递到 SSE；客户端接入 EventSource，刷新/重启可由 State 恢复。该任务不实现动态 Channel、participants 或角色 inbox，后者归 Phase 6。

> [2026-08-31 架构决策更新] 任务 5.3 的“刷新/重启恢复”必须包含建连竞态与网络重试：新 SSE 连接不得遗漏快照读取与订阅建立之间提交的消息，客户端必须对同一逻辑发送复用 `msgId`。部署验证仅针对单实例自托管后端，不把本地 `.data`/进程内 SSE 宣称为 Vercel Serverless 后端能力。

**[2026-08-31 架构决策更新] Leader 指令映射（D9，`apps/web/src/lib/intent.ts`）** ：
- 服务端从 `display` 解析，不信任浏览器自报 `payload.intent`；普通发言与指令统一经 `POST /api/messages`
- Phase 5：开头单一 `@ROLE` + 已启用角色 + 非 `done`/非 `humanGate` → 消息与 `nextRole=ROLE` 在同任务串行区内经一次 State commit 原子、幂等落盘
- Coordinator 只消费最新 applied assignment 一次：追加 `sourceMsgId` 确认事件，定向派发，并把 instruction 写入本轮 Coordination Ledger；更早指令由最新指令覆盖
- 普通文本只写 chat；未知/禁用角色或非法状态返回 `rejected`，不得绕过终态与人工卡点
- 显式命令首 token：`/requirement|/decision|/priority` → `deferred(targetPhase=9)`；`/approve|/reject|/resolve-objection` → `deferred(targetPhase=8)`；`/channel` → `deferred(targetPhase=6)`；未知 slash command 返回 `rejected`
- HTTP 响应显式携带动作状态；退役只向进程内 SSE 发送 opaque command 的 `/api/commands` 旁路

**[2026-09-01 规格修订] Web 编排执行桥接与 Demo 真实性门禁（任务 5.5）**：
- 在受信任单用户、单实例自托管边界内，UI 必须能提交一个新的任务目标并显式启动该任务；任务创建/启动属于生命周期操作，任务内 Leader 发言与指令仍统一遵守 D9 的 `POST /api/messages` 契约
- 服务端组合根复用既有 `runOrchestration()`、HarnessExecutor、沙箱与端口实现，不复制第二套编排循环；Phase 5 全实例最多一个活动 run，相同 scope 重复启动必须幂等返回既有 run/终态，另一 scope 已运行时明确返回容量冲突
- Coordinator/Worker 的人类可读进度消息必须先经 `applyMutations()` + TaskStateStore 持久化，再由既有 MessageBus→SSE 展示；UI 不得把静态种子消息、测试脚本输出或客户端自造消息冒充实时 Agent 执行
- 最终 State、消息与产物快照必须可刷新恢复；run 终态先把产物归档到任务 `.data/.../artifacts/` 并持久化引用，再释放 Harness/MCP/Git/Docker 资源，刷新不得依赖活容器；进程重启后的活动 run 自动恢复不在 Phase 5 承诺内，但不得把已中断 run 显示为仍在执行
- 任务 5.5 同时产出 §17 Demo 录屏；任务 5.6 必须在 5.5 完成后验证浏览器触发的真实闭环，不能只分别证明 Web 消息链和编排测试链

### 8.4 依赖关系
- 依赖 Phase 4 的编排层（提供状态查询 API）
- 前后端通过 API 路由通信
- 完整配合式抢占不在 Phase 5 验收范围，归 Phase 9；5.4 不得以空 preemption 接缝伪装已支持

### 8.5 Phase 5 出口验收矩阵（任务 5.6）

> [2026-09-01 验收规格修订] 任务 5.6 是 Phase 5 的累计出口审计，不新增产品能力。固定自动化入口为 `tests/integration/phase5/phase5-exit.test.ts`；该入口应组合生产模块验证服务端消息事实链与 Web→编排→执行跨包契约，不复制实现逻辑，也不得把两个彼此分离的服务端/执行测试冒充同一条端到端链路。纯客户端确定性契约（如 pending submission 复用 `msgId`、SSE 读侧去重、动作提示格式）可由实际生产 helper/component 测试承担，再由浏览器证据验证接线，不要求为形式上的“单链”把前端纯函数人工拼入服务端集成测试。

**A. 可重复自动化门禁（必须全绿）**：
- D6/D7：消息及同批状态变更先经 `applyMutations()` + TaskStateStore 原子持久化，成功后才发布 `MessageCommitted`；SSE/UI 展示信封不含 `payload`；`projectId/taskId/channelId='main'` 作用域隔离，未知 scope 不创建占位 State
- SSE 生命周期：快照与实时尾流建连竞态允许重复但不得漏消息；客户端按稳定 `msgId` 去重并在逻辑重试时复用原值（由生产 helper/component 测试 + 浏览器证据组成门禁）；心跳不携带业务 payload；abort/cancel 后释放订阅；页面刷新与后端进程重启后可由 JSON 快照恢复已持久化事实
- D9：服务端只从 `display` 解析意图且忽略客户端伪造 `payload.intent`；开头单一 `@ROLE` 的合法/非法路径、消息与 `nextRole` 单次原子幂等提交、Coordinator 按 `sourceMsgId` 只消费最新 applied assignment 一次、普通 chat 无动作、Phase 6/8/9 deferred、非法状态 rejected，以及 `/api/commands` 旁路退役均有回归
- D10：生产组合根复用既有 `runOrchestration()`、HarnessExecutor、SandboxManager 与 MCP 工具；全实例最多一个活动 run；同 scope 重试幂等、冲突返回明确 `409`；终态产物先归档并持久化引用再释放执行资源；未完成快照在进程丢失后显示 `interrupted` 而非伪 active
- 自动化 G5 必须实跑 DockerSandbox、Harness、MCP、宿主 Git、容器内 Node 测试、JSON State 与 MessageBus→SSE；生产链中的 `sandbox_run` 结果必须被验收逻辑实际读取并断言 `exitCode===0`、`timedOut===false` 与有效测试输出，确定性 adapter 不得无视工具失败后硬编码 `testResults.passed=true`。宿主 Git/文件 API 仅管理元数据与快照，不得执行 Agent 代码；仅外部 LLM 边界允许使用文件头注明原因的确定性 adapter，且不得把该 adapter 声称为 live-model 证据
- `pnpm typecheck`、`pnpm lint`、`pnpm test` 与 `pnpm --filter @agora/web build` 全部通过，既有 Phase 0–4 回归不得退化

**B. 浏览器与 live-model G5（必须有可审计证据）**：
- 从浏览器创建并启动同一个真实任务，连续贯穿 Tier 路由、Coder→Tester→Reviewer、State/TaskStateStore、MessageBus→SSE、`done`、产物独立测试与刷新恢复；静态种子消息、客户端自造 Agent 消息或两条分离链路不计通过。归档产物的“独立测试”必须在不同于原任务容器的 fresh DockerSandbox/等价隔离容器中执行并检查真实结果，禁止宿主运行时直接执行 Agent 产出
- 桌面与 390px 移动视口均验证：只渲染 `display`、实时角色进展可见、`rejected/deferred` 反馈可见、终态/测试结果/产物入口可恢复、无框架错误层与横向溢出
- 任务 5.5 已合并的 live DeepSeek 浏览器闭环、独立产物测试和 `https://github.com/logan-suu/Agora/blob/83fe416c1015d86616acdead7af2ebbad82e8384/docs/demo/Agora-Phase5-TTL-LRU-Live-Demo-2026-09-01.gif` 可作为 5.6 的基线证据；若 5.6 或其审查修复改动任何生产执行路径，则必须重新实跑并更新证据，不能仅沿用旧录屏

**C. 边界与收尾**：
- Phase 5 只承诺受信任单用户边界内的单实例自托管后端。动态 Channel/participants 授权归 Phase 6，humanGate/objection 裁决归 Phase 8，完整配合式抢占与真并行归 Phase 9，活动 run 自动续跑与完整 Serverless/水平扩展后端均不在本阶段验收范围
- `docs/deferred-items.json` 必须逐项核对：DEF-014 应保持 resolved；DEF-015 在当前受信任单用户且未公开暴露的边界内不阻塞 Phase 5，但公开暴露前或最迟 Phase 6 出口前必须解决
- 全部门禁通过且证据写入任务 notes 后，才可由收尾流程将 5.6、Phase 5 与 M5 标为 `done`，把 `current_phase` 推进到 6，并幂等翻转 6.1 为 `ready`；PR 创建但尚未由人类合并时，5.6 仍保持 `in_progress`


## 9. Phase 6：多形态通信 + 工程 Eval 骨架（1.5 周）

### 9.1 目标
实现统一的 Channel 原语——main 群 + 动态 sub 群，agent 与 Leader 均可发起，小群结论冒泡回大群；在已跑通的 Phase 0–6 生产链上建立 D11 Eval runner、统一任务/结果 schema 与首批基线任务。

### 9.2 里程碑
- [ ] **Day 1-2**：可持久恢复的项目级 Channel 注册表 + participants/角色 inbox 路由（复用既定 MessageBus 端口，inbox 不存第二份消息）
- [ ] **Day 3-4**：sub 群生命周期（agent/Leader 发起、幂等去重、通信、权限关闭、main 生命周期公告）
- [ ] **Day 5**：小群 ChannelContext 读时派生与角色隔离
- [ ] **Day 6-7**：小群关闭 + 结论冒泡回 main 群
- [ ] **Day 8-9**：Eval runner + 8–12 个 Phase 0–6 固定任务 + Outcome/Process/Efficiency/Safety 基线

### 9.3 具体任务

**Channel 管理（`packages/comm/channels/`）** ：
- `Channel` 接口：channelId, kind ('main'|'sub'), sub.taskId/threadId/topic/createdBy, participants, closed, sub.bubbledSummaryRef；不持久 localContext 或摘要正文
- 不变量：`'leader' ∈ participants` 恒成立
- 项目内恰有一个 `channelId='main'`，main 恒 `closed=false` 且 participants=当前已启用 roster+leader；sub `channelId` 项目内唯一且必须绑定当前任务
- `ProjectChannelStore`：`.data/projects/{projectId}/channels.json`，幂等 initialize + revision 校验 + 按项目串行 + 原子替换；进程重启可恢复
- Phase 5→6 升级：已有任务 State、缺失 `channels.json` 时惰性补建 main；损坏/非法快照 fail-fast，不自动覆盖
- no-op 语义：普通对象属性顺序不构成 Channel 变更；participants 是集合并标准化为 leader 在前、其余按 enabled roster 顺序，成员顺序不同不触发 revision；Channel 注册表顺序保留

**Channel/Inbox 路由升级（`packages/comm/channels/`）** ：
- `Project.channels` 是 Channel 注册表唯一真相源，`AppState.messages` 继续保存任务级消息事实
- `inboxFor(projectSnapshot, task, role)`：只返回当前任务中、Channel 作用域匹配且该角色为 participant 的 `InboxItem`；保留 `AppState.messages` 顺序，`to` 只标注 direct 优先级
- 提交前校验 Channel 存在/sub task 绑定/发送者 participant/closed；失败时不写 State、不 publish
- MessageBus 接口与“先提交、后投递”顺序不变；事件可唤醒 worker，但 inbox 每次从 Channel 快照 + 任务 State 重建
- `ChannelContextBuilder(projectSnapshot, task, role)` 同样读时派生：按 project/task/participants default-deny，main 只投允许的共享结构化事件，sub 只投群内允许的结构化条目；永不复制 display，未知 payload 只留 ref
- 每个 Channel 的 `{entries,omittedRefs,omittedRefCount}` 共用 4000 字符动态消息预算：较新 entries 保留完整结构化内容，剩余空间按事实顺序保留最近的旧 refs，计数记录全部遗漏；不在 pre-step 调 LLM，不向 Channel 回写消息索引

**Sub 群生命周期**：
- 动作契约：agent 最终回复末尾至多一个严格 `<agora-channel-action>{JSON}</agora-channel-action>`，open 可显式给安全稳定 threadId；Harness 校验/剥离 display 并以 assistant msgId 绑定 actionId 后通过现有 `StepResult.output` 交付，纯控制回复保留审计 Message 并生成非空 display；Leader 使用 `POST /api/messages` 的 `/channel open ROLE[,ROLE...] <topic>` / `/channel close <channelId>`；组合根绑定可信 actor，不修改 Executor/Store/MessageBus 签名
- 创建：稳定 actionId（Leader=msgId）以 taskId 长度前缀派生 `channelId='sub-'+taskId.length+'-'+taskId+'-'+actionId`，缺省 threadId=actionId；participants 自动包含 leader、agent 发起者与请求角色，并按 roster 标准化
- 去重：同 `taskId+threadId` 同 topic/participants 返回既有群，语义不同明确冲突，closed thread 不静默复活
- 通信：群内消息仅 participants 可见
- 发现/UI：`GET /api/channels?projectId&taskId` 按字段白名单只读返回 main + 当前 task sub 元数据（不返回 bubbledSummaryRef 指向的内容或 payload）；UI 初始化/动作完成刷新一次，仅 active run 持续短轮询，刷新错误独立且恢复后清除；可切群，SSE/POST 复用所选 channelId
- 关闭：仅 leader/participant 可置 `closed=true`，main 不可关闭，重复 close 为 no-op；6.2 写 main 生命周期公告，6.3 经 L3 生成器端口复用无工具薄 Harness，从目标 sub 的全部白名单结构化条目生成 conclusion/keyDecisions+rationale/openQuestions/sourceMsgIds；完整 entry 预算内装箱，超大单条用保留 ref 的结构化 fragment，中间输出有界并以每轮输入不超过 4000 字符的批次多轮归并；以稳定 channel-bubble msgId 先写 main，再只在 Channel 写 bubbledSummaryRef
- summary 消息固定由 COORDINATOR 发到 main 并沿用 sub.threadId；空 sub 不调模型而写确定性 “No conclusion recorded.”；输出严格拒绝空 conclusion/rationale、多余字段与不属于该 sub 的 sourceMsgIds
- 冒泡恢复：main 的结构化 channel_summary 消息是唯一摘要事实；close 后与运行时恢复入口都 reconcile closed-without-ref，重试先复用 first-write-stays 消息，再以 revision CAS 有限重试补引用，不依赖常驻内存扫描器
- schema 升级：旧 localContext 校验 shape 后移除；旧 bubbledSummary 字符串必须先转成稳定 main summary 消息再写引用，禁止静默丢失
- 一致性：生命周期服务按 project 串行；Leader 入口按 task 串行并在任何生命周期副作用前检查重复 msgId；先提交 ProjectChannelStore，再以按 Channel 实例稳定的 msgId 经 D6 补原消息/announce，失败不回滚 Channel，重试只补缺失展示事实
- 升级：6.1 legacy sub 若完整缺少 threadId/topic/createdBy，首次初始化按 `legacy-${channelId}` / `Legacy channel ${channelId}` / leader 通过正常 revision commit 一次性补齐；部分缺失 fail-fast

**工程 Eval 基础设施（任务 6.4）**：
- 定稿版本化 `AgoraEvalTask` / `EvalResult` schema 与可重跑入口；记录 task/runner/adapter 指纹、attempt/profile、模型、预算、环境和带 SHA-256 的相对 artifact 引用
- 首批 8–12 个任务覆盖 Phase 0–6 已解锁能力：编码闭环、测试修复、消息 commit-before-publish、main/sub 作用域、非 participant 隔离、派生 ChannelContext 与可恢复结论冒泡；每个任务声明支持的 profile
- Grader 分 Outcome/Process/Efficiency/Safety，统一输出 `pass | fail | unknown` + detail + evidenceRefs；确定性结果用测试/State 不变量，LLM-as-judge 不得替代可执行验收
- `deterministic` profile 复用 Harness adapter 注入缝隙验证 runner/Process/Safety；`model` profile 走真实 provider 测量实际 Outcome/Efficiency/协作行为；两者分开汇总，前者不得宣称模型能力
- 复用既有 DockerSandbox/Harness/MCP/JSON/SSE 生产链，不新建第二套 Agent 或沙箱；指标只经 Eval wrapper/observer 与既有 trace 采集，不修改冻结端口；公开 Benchmark adapter 延后 Phase 10
- 每个 run 只在 `.data/evals/<runId>/` 隔离 data root/workspace 内写评测专用 State/产物，不访问用户 `.data/projects/`、不写 KB；原始结果 gitignored，仓库只提交无敏感内容的任务/运行清单、指纹与聚合摘要
- schema/driver/eval root 必须完整校验并 fail-fast；`overallStatus` 由 Grader 推导，Outcome/Process/Safety 不接受 unknown 假绿，Efficiency 仅不可采集项可 unknown；`iterations` 对应 `maxIterations`，与 `repairIterations` 分开
- result 按 provisional→注册清理→final 两阶段原子留档，资源回收与其他 pass 只接受真实观察证据；模型执行与记录元数据共用冻结配置，专用 model 命令缺凭据明确失败
- Eval 不替代 Phase 6 出口集成测试；任务 6.5 才是阶段阻断门禁

> [2026-09-02 架构决策更新·任务 6.4 规格评审] “可重复”校准为版本化输入与环境可重跑、可追溯，不承诺真实模型输出逐字节一致；修正“Eval 不修改产品 State”的过度表述为“允许隔离 run root 内的评测专用 State，禁止触碰用户产品 State/KB”；补齐结构化 grader evidence、显式 unknown 指标、task/runner/environment fingerprint，并拆分 deterministic/model profile。该修订不新增依赖、不修改生产端口、不改变 6.5/G5 门禁。
>
> [2026-09-02 架构决策更新·PR #51 复评] 补齐 Eval fail-closed 与证据生命周期：总状态由 check 推导，核心类别 unknown 不得假绿；编排 `iterations` 与 `repairIterations` 分离；结果以 provisional→cleanup→final 留档；schema/driver/canonical eval root fail-fast；pass 必须来自实际运行证据；执行模型/环境与审计元数据同源，专用 model 命令缺凭据不静默跳过。
>
> [2026-09-01 架构决策更新] 任务边界重新锁定：6.1 落地 Channel schema/不变量、`ProjectChannelStore` JSON 适配器、提交前策略与 inbox 纯读模型；6.2 落地 sub 创建/通信/关闭状态转换；6.3 增加不改变前三参语义的 `project(...,channelContext?)`，由 `ChannelContextBuilder` 读时派生角色切片，并落结构化、可恢复的冒泡 saga。6.1 已持久化的 localContext 在 6.3 迁移撤销，避免消息索引双写。DEF-015 在 Phase 6 的验收范围是固定 leader 的 Channel 完整性校验 + worker participant 隔离；完整账号认证仍为非目标，系统仍仅允许受信任单用户部署。
>
> [2026-09-01 实装记录·任务 6.1] Day 1-2 地基已完成：项目级 Channel schema/不变量、原子 JSON 注册表、revision 并发保护、重启恢复、participants/任务作用域 inbox 派生、提交前完整性策略，以及 Web main 初始化与已注册 sub 动态寻址均已落地。任务级消息仍只在 `AppState.messages`，MessageBus 端口未扩权，投影调用面未修改。sub 创建/关闭 API 仍归 6.2，localContext 投影与冒泡仍归 6.3。
>
> [2026-09-01 架构决策更新·PR #47 规格复评] 6.1 增补旧 Phase 5 State 的 main backfill、enabled roster 排除保留身份/重复项、对象属性顺序无关的 no-op commit，以及 D9 planned message 的“单主消息 + 非消息动作 mutations”边界。该修订解决升级恢复与附加消息绕过问题，不引入账号认证、不提前实现 sub 生命周期或 localContext 投影。
>
> [2026-09-01 架构决策更新·任务 6.2 规格评审] Leader 确认不机械沿用旧规格：participants 从“数组顺序有语义”校准为集合标准化；sub 增 `threadId/topic/createdBy`；新增可信 actor、稳定 actionId、同 thread 去重、权限关闭和 project 串行规则。Channel 与任务消息分属两个真相源，故采用“Channel 先提交、D6 消息后补齐”的可重试 saga，不伪造跨文件事务。6.2 只关闭并公告，6.3 才冒泡摘要，Phase 8 才处理 humanGate/resume。
>
> [2026-09-01 架构决策更新·任务 6.3 规格评审] Leader 批准“派生上下文 + 单份结构化冒泡事实”：Channel 不再持久 localContext/摘要正文；ChannelContextBuilder 从注册表 + Task State 按成员资格读时派生、类型白名单过滤并按 4000 字符预算裁剪。closed sub 由无工具薄 Harness 生成结构化 channel_summary，先以稳定 msgId 写 main，再以 revision CAS 写 bubbledSummaryRef；恢复时复用 first-write-stays 消息并补引用。任务必读范围扩展到蓝图 §10/§11/§21、详细设计 §1/§5/§7、架构 §4.3/§5 与本节。
>
> [2026-09-01 实装记录·任务 6.3] 已撤销 Channel 持久 `localContext`/摘要字符串并实现旧字段校验迁移；`DerivedChannelContextBuilder` 按 project/task/participants 读时派生、类型白名单过滤且每群 4000 字符裁剪，`project` 第四参和 WorkerRuntime 每 Step 刷新已接线。closed sub 通过可注入 `ChannelSummaryGenerator` 与生产无工具 `HarnessExecutor` 生成严格解析的结构化摘要；`ChannelSummaryReconciler` 已实现稳定 main 消息 first-write-stays、重新读取规范事实、revision CAS 有限重试、close/恢复触发及 legacy 字符串无损迁移。锁定 Harness 无 provider-native response schema，故严格性落在纯 JSON prompt + 服务端 exact-key/来源校验，不宣称模型侧原生强制。

### 9.4 依赖关系
- 依赖 Phase 5 的前端 UI（展示多群聊列表）
- Eval 骨架依赖 6.3 完成派生 ChannelContext 与冒泡恢复语义，并复用 Phase 5 已有 G5 fixture 与指标来源


## 10. Phase 7：角色热插拔（1 周）

### 10.1 目标
实现 Leader 管理项目成员、移除角色、离职强制交接，以及把 enabled 成员入职到具体任务并读取可验证交接接手；项目成员登记与任务接手按 D13 分离。

### 10.2 里程碑
- [ ] **Day 1-2**：角色注册表动态管理（增删改）
- [ ] **Day 3-4**：离职流程（departing 阻断 → 目标 step/end drain → 确定性交接/责任处理 → departed）
- [ ] **Day 5-6**：任务入职流程（稳定 Leader Intent → 校验/认领离职交接引用 → 持久 onboardingContext 投影 → 激活接手）
- [ ] **Day 7**：护栏验收（Coordinator 不可移除、`departing` 阻断新工作、当前 Step 安全排空后再交接）

### 10.3 具体任务

**Roster 管理（`packages/core/domain/roster.ts`）** ：
- D12：`RoleSpec` 只描述能力；项目成员使用 `RosterEntry { spec, status, departure? }`，`status = enabled | disabled | departing | departed`，departure 是离职 saga 的持久恢复/审计元数据
- 项目 roster 与 Channel 共同进入 `.data/projects/{projectId}/collaboration.json`；`ProjectCollaborationStore` 以 revision CAS 原子提交，既有 `ProjectChannelStore` 签名不变并作为兼容视图
- `addRole(spec)`：规范化并注册 enabled 新成员；仅已有 enabled 且同 spec 的重放 no-op，同 id 异 spec或已有 disabled/departing/departed（即使同 spec）均冲突，复职必须显式 `enableRole`
- `enableRole/disableRole`：只允许 enabled↔disabled，同态 no-op；Coordinator 与 departing/departed 均拒绝普通启停
- `removeRole` 不属于 7.1 的底层数组操作，移到 7.2 离职状态机；禁止硬删除历史身份
- roster 变更与 main participants 重算一次原子提交；历史 sub 保留已知非 enabled 成员，但新建、发言、路由和普通执行只允许 enabled，departing 仅允许离职服务完成受限 drain；消息入口缺少持久 roster 权威源时 fail-closed，已应用指派消费前失效则进入 Leader humanGate
- Phase 6 `channels.json` 确定性迁移到 collaboration 快照；正式路径重新读取核对成功后才删除 legacy，同内容双文件由 initialize/load 幂等清理，损坏或冲突 fail-fast，迁移后禁止双写

`[2026-09-02 实装记录]` 7.1 已完成 Code+Check+Verify：`ProjectCollaborationStore` 与 JSON CAS 适配器、冻结 Channel 视图、`ProjectRosterService`、全入口 enabled 门控、step 边界停用和旧 Channel 迁移均已落地；测试覆盖角色状态转换、原子 main 同步、重启/CAS/双文件冲突、历史 sub 合法性、显式自定义角色指派及真实 Harness 通用 worker 链。任务保持 `in_progress`，待 `$agora-commit` 建 PR 与人工合并后再标 done。

`[2026-09-02 PR 复评加固]` PR #53 先复评规格再修实现：明确 addRole 非 enabled 冲突、迁移写后读回、同内容双文件恢复清理、消息权限 fail-closed 与失效指派 humanGate；新增对应领域/存储/Coordinator 回归并把所有 MessageService 组合根接到必需的持久 collaboration 权威源。任务继续 `in_progress`，等待人工合并。

**离职流程**：
1. Leader 经 `POST /api/messages` 发 `/role remove <ROLE> [to <SUCCESSOR>]`，稳定 `msgId=actionId`
2. collaboration revision CAS 写 `departing` 与持久离职阶段，立即阻断目标角色的新指派/发言/普通执行
3. Phase 7 目标角色 drain 端口等待其当前 Harness Step 完成、mutations 提交并保存 safePoint；无活动 Worker 时立即 quiescent
4. 从重新加载的已提交 State 确定性生成现有 `HandoffPacket`；同一次 TaskStateStore commit 写稳定 handoff Message，并转移未完成 subtask
5. 有 enabled 接任者或无未完成责任时 CAS 为 `departed/completed`；否则 subtask 保持原 owner 并标 blocked、写 humanGate、roster 保持 `departing/awaiting_replacement`
6. 重试先按 roster 离职阶段 + Task State handoffRef 从断点继续，不被已有 Leader Message 短路；派生 handoff id 只能命中完整匹配的规范交接事实，否则 fail-closed；同 actionId 参数不一致明确冲突

`[2026-09-03 架构决策更新]` Phase 7 不调用 Phase 9 的 task-scoped pause epoch `requestPause()/complete()/abort()` 生命周期，不广播全局暂停、不额外启动 LLM 交接 Step、不 inject inbox/terminate/fork/resume。系统在目标 step/end 后从结构化 State 固化交接：`done`=当前进展，`keyDecisions`=决策引用，`openIssues`=未完成事项及 `Recommendation:` 后跟一个空格与建议正文，`fileRefs`=路径行号；无人接手时 `toRole=leader` 托管，但只升级人工不等于责任已转移，不能先 departed。

**入职流程**：
1. 项目级 `addRole` 只把已选择/自定义的 `RoleSpec` 登记为 enabled 成员并原子更新 main participants；Phase 7 不实现或宣称模板目录/任意 RoleSpec 编辑 UI
2. Leader 在目标任务 main 群经 `POST /api/messages` 发 `/role onboard <ROLE> [from <HANDOFF_MSG_ID>[,<HANDOFF_MSG_ID>...]]`；浏览器提交的稳定 `msgId=actionId` 与显式 handoff ID 均使用 `[A-Za-z0-9][A-Za-z0-9._:-]*` 安全 token，角色必须由最新持久 roster 验证 enabled
3. 系统自动选择当前任务中未认领、规范离职 handoff Message 且 `packet.toRole=ROLE` 的直接交接；`from` 仅可显式认领同任务 `packet.toRole=leader` 的托管交接。引用保存 `{taskId,msgId}`，Message/payload/actionId/packet/handoffPackets 任一不一致、跨任务或重复认领均 fail-closed
4. Leader 主消息作为可见入职公告，与 `nextRole=ROLE` 和既有 `leader_intent` payload 内的 `onboarding` receipt 在一次 Task State commit 中原子持久化；不把 addRole 与 onboard 包成跨 collaboration/Task State 事务
5. `project()` 从最新 applied onboarding 事实派生系统级 `onboardingContext`，与当前角色声明投影和 ChannelContext 一起进入 D1 pre-step；不投原始群聊、不受 Channel 4000 字符窗口影响，刷新/重启/新 Harness executor 可恢复
6. 普通角色切换但没有稳定 handoff Message 的 packet 不参与本协议；同任务后续 onboard 取代旧上下文，但已认领交接不能转授另一角色。同 ID applied 重放必须先复核既存 canonical 消息、完整 receipt 与当前 State 引用事实，畸形/漂移 fail-closed；提交后消费前角色失效沿用 `required_role_unavailable:<role>` humanGate

**自定义角色路由边界（D12 / DEF-016）**：
- Phase 7 保证自定义角色可持久注册、投影、工具作用域化，并由 Leader 显式 `@ROLE` 指派给通用 worker
- 默认六角色继续使用现有 Coordinator 自动拓扑
- 任意自定义角色的自主调度须先定义多候选优先级、产出 mutation/output 契约与失败回退，不纳入 Phase 7，不得对外宣称已支持

**护栏验收（任务 7.4）**：
- 本任务只补齐并集中验证 D12 护栏，不重新实现 7.1/7.2 已落地的 roster 与离职状态机
- Coordinator 的 disable/remove 必须在真实入口被拒绝；项目管理操作不得改变 collaboration，Leader `/role remove` 除按 D9 持久化并投递规范 `rejected` 审计消息外，不得改变 roster、Channel、subtask、handoff 或 humanGate
- 目标进入 `departing` 后必须阻断新指派、新发言和新 Step；同任务后续 Leader 指派可在单一 `MessageRuntime` 的 D9 串行队列中等待，但排队期间不得提交消息、改写 `nextRole` 或启动 Worker，离职 action 收敛后须重读 roster 并经同一 `POST /api/messages` 返回规范 `rejected` 审计消息。Agent 发言与 Worker 新 Step 看到 `departing` 后立即 fail-closed；唯一写入例外是已登记活动 handle 的当前在途 Step 经 role-bound 接缝提交 mutations 后保存安全点
- 当前在途 Step 必须先完整提交再完成 drain，离职目标不得 resume；Phase 7 不设置全局暂停，也不调用 Phase 9 Preemptor
- 无 enabled 接任者且仍有未完成责任时，subtask 保持原 owner 并转为 blocked，写 humanGate，roster 保持 `departing/awaiting_replacement`，不得进入 `departed`
- 集中验收文件为 `tests/integration/phase7/phase7-guardrails.test.ts`；复用单一组合根中的真实 HTTP、同一组 JSON stores、MessageService 与 WorkerRuntime，不得创建第二个 `MessageRuntime` 绕过 Leader 串行队列；外部付费 LLM 可按 R11 在文件头注明后使用脚本适配器

**阶段 Eval 增量（任务 7.5）**：
- 在固定 Eval 集上追加角色加入、离职强制交接、接手、孤儿职责升级与 Coordinator 不可删除场景
- 结果同时验证最终产物与 handoff/roster 过程不变量，以及 Leader 经 D9 发起离职/入职、Coordinator 恒 enabled、孤儿职责升级 humanGate 这些 Phase 7 权威边界；项目级 `addRole` Eval 从受信任的单用户项目管理服务边界开始，只验证 roster+main 原子提交，不虚构尚未定义的 `/role add` 消息协议或账号鉴权；不提前验收 Phase 8 的 authority 覆盖、blocking/advisory 异议裁决或 Terminate & Fork；仍由 Phase 7 出口集成测试承担阻断门禁

`[2026-09-03 规格澄清]` 本节原“authority”表述范围不明确，现按《详细设计方案》§11.4 的阶段边界收窄为 Phase 7 已落地的成员生命周期权威事实；完整 Leader authority 与异议裁决仍归 Phase 8。

`[2026-09-03 实装记录]` 7.5 已新增 5 个 Phase 7 版本化 deterministic Eval 场景，并与 Phase 6 的 10 个任务组成 15 项累积目录；每个场景通过真实 HTTP、JSON stores、collaboration CAS、roster/离职/入职领域路径与 Eval runner 生成 fail-closed 检查和哈希证据，只有外部付费 LLM stream 使用文件头注明的脚本适配器。`phase7-exit.test.ts` 另以真实 WorkerRuntime/HarnessExecutor 串联角色登记、在途 Step、safePoint、确定性交接、稳定 actionId 重放、重启后 onboardingContext 接手和 Coordinator/孤儿职责负向护栏。deterministic 基线 15/15（含真实 Docker），独立 model profile 1/1；出口与 Eval 仍分开统计，生产代码和冻结端口未修改。

`[2026-09-03 PR 复评加固]` PR #57 的离职 Eval 与出口测试现旁路观测真实 `TaskStateStore.commit`、Harness safePoint 和 collaboration CAS，强制验证 step commit → safePoint → handoff commit → departed commit，并在 departed 提交时重读 Task State 确认职责已转移；接手投影检查改为排除本次持久化的全部原始 `display`，覆盖离职命令、handoff 公告与入职命令。

`[2026-09-03 CodeRabbit 复审裁定]` 角色注册场景补充受信任项目管理服务边界说明；Phase 7 没有 `/role add` D9 协议，故不伪造 Leader event。蓝图 §10 的 D9/D13 与 §14 已明确完整 authority/异议裁决归 Phase 8，本次仅澄清 Eval 口径，不新增 §21 决策。`POST /api/messages` 继续遵守蓝图 D10 的受信任单用户组合根，本阶段不引入项目明确排除的账号级鉴权。

### 10.4 依赖关系
- 依赖 Phase 3 的 HandoffPacket 结构
- 依赖 Phase 5 的 Leader 指令 UI


## 11. Phase 8：人工卡点与冲突裁决（1 周）

### 11.1 目标
实现 human-in-the-loop 卡点、异议双轨（阻塞/建议）、决策权威级别、Leader 终审确认与结构化 trace 面板。

### 11.2 里程碑
- [ ] **Day 1-2**：humanGate 持久生命周期（safe-boundary suspend → Leader resolve → 全新 context 真 Fork resume）
- [ ] **Day 3-4**：异议双轨（blocking/advisory 分类 + 处理）
- [ ] **Day 5**：决策权威级别校验（leader 可覆盖 agent）+ D16 终审确认闭环
- [ ] **Day 6-7**：trace 面板（事件流可视化）

### 11.3 具体任务

**humanGate（`packages/core/orchestration/human-gate.ts`）** ：
- **[2026-09-03 架构决策更新]** Phase 8 仅处理已到达并提交 `step/end` 的回合制单任务：gate 生产者先交瞬态请求，生命周期服务 flush 项目/任务级 Harness JSONL session，全部成功后一次提交完整 `{gateId,...,safePointRefs}`，不得写半成品 gate → 销毁进程内 executable composition 并进入 `needs_attention`（保留 State/worktree/session，不归档终态 artifact；冻结的 `SandboxManager.teardown()` 仅用于终态，暂停/重建经 companion `RecoverableSandboxManager.suspend()/resume()` 停容器并重新挂载原 host root）→ Leader 经 `POST /api/messages` 以 `/resolve-gate <gateId> <option> [argument]` 和稳定 `msgId=actionId` 裁决 → 一次 Task State commit 写含 checkpoint refs + 确定性 resumeSessionId 的 resolution receipt/原因特定变更/清 gate → 新建 composition，在全新 Harness context 中从 source prefix 经 Agent factory 创建或接管 lineage child → 暂存 D1 投影并提交稳定 resumed 控制事实后继续编排；此前 run 始终为 `needs_attention`。
- **[2026-09-03 架构决策更新]** gate 可见到旧 composition 释放完成之间必须按 task 线性化：早到的裁决可先耐久提交，但 resume 等待旧 run 收敛并重读 State。gate 提交后的清理失败保持 `needs_attention`、禁止归档；恢复工厂失败释放已取得资源。验收还须覆盖 resolution actionId 跨 intent 冲突、既有 child 的事件前缀校验、worktree 跨 task 拒绝，以及离职 handoff-first/gate-second 部分提交的可重试恢复。
- `saveSafePoint()/loadSafePoint()` 签名不变，但前者必须 flush 并返回 opaque 持久 ref，后者必须在全新 context 执行真 load + Harness fork；仅 sessionId/no-op 不计通过。后端重启后仍能从 gate/checkpoint 事实恢复。
- Leader 已确认 Phase 8 resolver 收敛为：`iteration_limit→continue`（重置本轮计数）、`required_role_unavailable:<ROLE>→retry`（最新 roster 已 enabled 才放行）、`role_departure_requires_replacement:<ROLE>→assign_enabled_successor <ROLE>`（原子转移 blocked subtasks后续办 saga）；旧含混 option 与无持久 outcome 的 abort 不实现。
- Phase 8 只释放本地运行注册表容量，不实现 GlobalScheduler 或多 worker 广播抢占；二者仍属 Phase 9。AutoGen `TerminationCondition` 已由现有 `iterationCount` 硬上限 + humanGate 路由具体化，8.1 不再新建重复框架。
- **[2026-09-03 实装记录] 8.1 已完成实现与本地门禁：** 完整 gate schema、官方 Harness JSONL flush/load、确定性 lineage child、Leader `/resolve-gate` 原子 receipt、非终态 composition/sandbox suspend、持久 worktree 重挂载、resumed marker、三种 resolver 与离职 saga 续办已接通；`tests/integration/phase8/human-gate-lifecycle.test.ts` 真实运行上述链路（仅付费 LLM 响应使用确定性 adapter）。任务状态在 PR 人工合并前仍保持 `in_progress`。

**异议双轨（`packages/core/domain/objection.ts`）** ：
- **[2026-09-04 架构决策更新]** `ObjectionDraft` 用 `{kind:'decision'|'requirement',id}` 区分目标命名空间，并以 `claim='contradiction'|'concern'` 承载 Coordinator 已完成的语义判断；`classifyObjection(draft,decisionLedger,requirements)` 只做确定性引用/当前性校验并计算 track，不用关键词或隐藏 LLM 猜冲突。未知、错种类或已 supersede 的引用 fail-fast；拿不准由上游声明 concern，归 advisory。
- `objections` 是 append-only 事实集；同 id 的 `type='objection'` Message 与结构化事实一次 Task State commit 后才投递。advisory 继续工作；blocking 事实提交后以 `blocking_objection:<id>` 交 D4 持久 suspend。事实已提交但 gate 尚未完成时可从持久 State 幂等重试。
- 薄 Worker 只可在 assistant 正文末尾输出一个严格 `<agora-objection>` JSON 控制块，且与 Channel action 互斥；模型只给 `claim/target?/argument`，Harness/WorkerRuntime 分别绑定持久 assistant `msgId` 与当前角色，L2 用完整 State 分类并把 objection+Message mutations 合入同一次 worker-step commit。畸形、混合或自报身份/track 明确拒绝。
- 8.2 不为新 reason 建临时 clear-gate 旁路；8.3 负责 blocking 的 gate-bound `accept_objection/reject_objection`、advisory 的 `/resolve-objection`、确定性最高权威 resolution Decision、accept blocking target 撤回、未决视图及完整重放校验。Leader rationale 为必填自由文本；decision target 以 supersedes 撤回，requirement target 以 `withdrawnByDecisionId` 撤回，旧 Requirement 缺字段视为 active。Phase 9 才处理普通决策/需求变更与多 worker 广播抢占。`[2026-09-04 架构决策更新]`
- **[2026-09-05 架构决策更新]** Decision `supersedes` 只允许指向同 topic 的 current Decision；跨 topic 写入 fail-fast。D14 的 decision-target resolution 继承目标 topic，关闭 DEF-011 时须同步 L1 边界与回归，不得只改读侧压缩。

**终审确认（D16，任务 8.5）**：
- REVIEWER `approved` 只产生 completion candidate；Coordinator 只接受本轮 dispatch 后唯一、满足 `[A-Za-z0-9][A-Za-z0-9._:-]*` 且未跨轮复用的 verdict id，并以它打开 `completion_confirmation` D4 gate，不得直接 finalize。
- Leader 经 `/resolve-gate` 选择 `approve_completion [rationale...]` 或 `request_changes <rationale...>`；同 commit 写规范 Leader 消息、最高权威 completion Decision、resolution receipt 并清 gate。approve 在真 Fork resume 后令 `isRequestSatisfied=true` 并 finalize；request changes 保持 false、重开返工并路由 CODER。
- resolution 必须与当前 review 交叉验证且只消费一次；旧 review、跨轮复用、缺 Decision/receipt 或 actionId 冲突均 fail-closed。普通阶段切换不逐段审批。

**Trace 面板**：
- **[2026-09-04 架构决策更新] D15**：唯一来源为当前任务锁定版本官方 Harness JSONL session；通过 L3 `TraceReader` + L4 `HarnessTraceReader` 调用官方 `list()/inspect()` 读时派生，不解析物理 JSONL，不复制进 State/MessageBus/第二份日志
- 严格白名单 DTO 只含 role/session lineage、turn/step 时间与状态、工具名/状态/错误码；禁止 projection、prompt、assistant/reasoning 内容、工具 arguments/results、环境变量与文件正文
- fork child 只投影 `seq >= seedLength` 的 native 事件并保留 parent 引用；parent 图必须无环。官方 inspect 后仍由 Trace 读侧校验 turn/step 连续序号、唯一开放父节点、已启动 tool 配对与先子后父闭合；官方 `TOOL_NOT_STARTED` synthetic result 是无 call 的唯一合法例外且不投影。合法开放尾部显示 running，闭合后代、重复结束、未闭合子项或 lineage/事件损坏均 fail-closed。响应默认最多 500、硬上限 2000 个白名单事件，裁剪以 `omittedEventCount` 显式报告
- `GET /api/traces` 提供独立只读快照；右侧栏按 session/role→turn→step→tool 展示，运行中短轮询、终态最终刷新，不扩张 D6 消息 SSE，不使用静态假 trace
- 单元测试覆盖安全裁剪、连续 turn/step、开放尾部、先子后父闭合、关闭后代/重复结束、tool 配对、失败状态、lineage 去重与环拒绝、截断和损坏；Phase 8 集成测试用真实 Harness + 官方 JSONL + HTTP handler 验证刷新恢复与无敏感字段泄漏

**阶段 Eval 增量（任务 8.5）**：
- 交付 `tests/integration/phase8/phase8-exit.test.ts` 的跨包阻断门禁，以及 `tests/evals/phase8/` 的累计任务、driver、测试、runner、fixture README 与 fingerprint-bound baseline manifest；根脚本增加 Phase 8 deterministic/model 入口
- 新增 completion approval/rework、iteration-limit replay、blocking/advisory、Leader authority、跨重启真 Fork、资源释放与 trace sanitation/integrity 场景；关键安全失败路径必须 fail-closed
- Leader 模拟事件必须由固定脚本驱动并记录触发 phase/step；不得让 LLM 自行扮演最终裁决者。Phase 8 新任务强制 deterministic profile，model profile 只运行显式声明支持的场景
- 出口逐条处置 `docs/deferred-items.json` 的 DEF-007/009/011；实现与回归证据通过后才标 resolved。宿主测试固定 Node 24+，不得以 Node 22/20 下 skip Harness persistence 伪装通过

> **[2026-09-05 实装记录] 8.5 Code/Verify：** D16 已从 REVIEWER candidate 接通到 review-bound D4 gate、D9 canonical Leader Message/Decision/receipt、真 Fork resume 后交叉验证及 approve→finalize / request-changes→CODER 两条路径；Decision 写侧同步收紧为同 topic current-only。独立 Phase 8 出口矩阵真实串起退回、新 review、终审、资源释放、JSONL lineage 与安全 Trace；累计 deterministic Eval 23/23、独立 model profile 1/1，DEF-007/009/011 均有证据并关闭。宿主为 Node 24.20.0；真实 Docker 用户代码仍在 `node:20-slim`。

### 11.4 依赖关系
- 依赖 Phase 5 的 UI/单实例任务运行注册表与 TaskStateStore，Phase 7 的持久 worktree/role drain 安全点接缝，以及 Harness 同版本 JSONL session persistence plugin
- 依赖 Phase 3 的决策台账


## 12. Phase 9：真并行升级（1.5 周）

### 12.1 目标
将退化的单 Worker 顺序执行升级为独立异步 Worker 真并行 + 配合式抢占 + worktree 隔离 + 集成检查点。

### 12.2 里程碑
- [ ] **Day 1-3**：GlobalScheduler lease + `WorkerRuntime.runParallel()`（完整 batch 有界队列 + canonical commit join）
- [ ] **Day 4-5**：配合式抢占完整实现（task-scoped epoch → cohort 安全点 → reproject 或 D4 持久 suspend/Fork resume）
- [ ] **Day 6-7**：worktree 隔离 + 集成检查点（合并分支、冲突处理）
- [ ] **Day 8-9**：Coordinator 拓扑波次接线 + 端到端并行任务验证

### 12.3 具体任务

**全局槽位 + 并行 Worker 池（任务 9.1）** ：
- 单实例 `GlobalScheduler.acquire(projectId,taskId,workerId)/release` 是跨项目唯一 worker 执行额度来源，默认全局 cap=3；按项目轮转排队，同一进程内同 lease capability 幂等释放，克隆/伪造/错配 fail-closed且不保留永久强引用 tombstone
- 退役 Phase 5–8 的全实例单活动 run 容量闸门：不同 task 可同时 running，但所有生产组合共享一个 GlobalScheduler；同 scope 幂等与 goal 冲突规则保持。另以默认 3 个的 lifecycle admission cap 限制 active executable composition，满载时在重资源初始化前返回可重试 429；该 admission 不授予 worker 执行额度
- `runParallel(state, batch)` 处理全部合法 assignment，不用 `slice` 丢尾部；逻辑身份 `{projectId,taskId,workerId}`，workerId 由任务内 dispatch/index 稳定派生并经 canonical WorkerState 绑定 subtask；每个 worker 使用独立薄 Harness Executor/session
- 模型/工具调用并行，step 输出处理与 TaskStateStore 写入经 per-task canonical commit queue 串行；并行 agent step mutation 只写稳定 append，WorkerState 注册/status/safePoint 仅由 WorkerRuntime 内部按自身分区提交，当前 Subtask 全字段均由串行控制面更新，禁止并行全局 set
- 使用 all-settled 失败收敛，等待已启动 worker 到自然边界；join 排空提交队列并 reload canonical State，禁止 `mergeStates(AppState[])`
- 9.1 的 G5 限于无文件副作用的真实并发/槽位/提交链；独立 worktree 与文件写入真并行由 9.3 验收

**配合式抢占（`packages/core/preemption/preemptor.ts`）** ：
- **[2026-09-06 架构决策更新]** `requestPause({scope,actionId,reason,mode})` 以 task-scoped pause epoch 固定 active cohort；同参数重放共用结果，异参数冲突，不使用可覆盖的全局 `paused` 布尔值
- 未取得 lease 的队列 assignment 取消 acquire 但保持 pending；已进入 Step 的 worker 必须先 canonical commit 当前 Step，再 flush checkpoint 并由 WorkerRuntime 写自身 safePoint/status。done/failed 不被 paused 覆盖
- Phase 9 解锁三条仅限 main/非 done/无 active humanGate 的 Leader 命令：`/requirement <requirementId> <JSON>` 完整 upsert `{story,acceptance,nonGoals}` 且拒绝复活已撤回项；`/decision <topic> <JSON>` append `{decision,rationale,supersedes?}`，已有 topic 必须显式 supersede 唯一 current Decision；`/priority <subtaskId> <0-100>` 只更新存在且未 done 的 subtask。id/topic、JSON 精确 shape、字符串/数组/整数范围全部在建立 pause epoch 前 fail-closed 校验
- 三类命令复用 `msgId=actionId`，先计划唯一 Leader 主消息+控制 mutations，空 cohort 可立即提交；屏障后在 canonical queue 重读前置条件并一次 commit，非重放才 publish/reproject。同 ID 重放交叉验证消息与状态效果，异语义或漂移冲突；所有角色从规范 applied action 取得不含 display/raw log 的结构化 `leaderDirective`
- 非阻塞 reproject 在屏障后以一次 TaskStateStore canonical commit 写 Leader 主消息+控制 mutations，然后 `inbox.inject` 新 D1 投影；复用原 context/session/lease/admission，禁止 `(AppState)=>AppState` 整体写法
- D4 blocking/humanGate 在所有目标 checkpoint 与完整 gate 持久后先释放已静止 worker lease，再收尾 composition；清理失败保留 admission/needs_attention 但不占 worker 执行额度
- Phase 9 receipt 增加按 workerId 稳定排序的 per-worker `{workerId,sourceSafePointRef,resumeSessionId}` 计划并兼容 Phase 8 单 worker receipt；每个 paused worker 真 Fork 后重新排队获取新 lease，旧 lease 不跨 Fork

**集成检查点（`packages/core/orchestration/integrate.ts`）** ：
- `SandboxManager.createWorktree` 六个公开签名不变，Phase 9 以稳定 workerId 作为第二参数逻辑 isolation key；复合 workspace adapter 对 `{projectId,taskId,workerId}` 做确定性 Git-safe 防碰撞编码，canonical workerId 不改名
- 将受信 seed 物化到 `.data/projects/{projectId}/tasks/{taskId}/repository/` 的 task-owned canonical repo；同一个真实 linked worktree 路径同时作为 Git 工作区与 Docker bind mount，禁止 Git/Docker 双目录，也禁止 checkout/merge 或自动 push 用户源仓
- 新 worker 持久化结构化 `WorktreeRef{path,branch,baseCommit,headCommit?}`；Phase 0–8 字符串路径只作兼容读，须由真实 Git 查询升级后才可进入文件并行。删除无 writer/消费者的 `pendingPatch` 与旧 `branchOrPatch` 投影，TESTER/REVIEWER 统一读取 `branchOrIntegration` 引用
- 按 `subtask.dependsOn` 拓扑波次执行，在专用 integration worktree 中按 topologicalRank、稳定 workerId 顺序合并；稳定 `Integration` 快照记录 integrationId/base/有序输入/已合并进度/conflicts/resultCommit，每步用 branch HEAD、integration HEAD 与 ancestry 保证可恢复重放
- `mergedBranches` 必须是 pending 的完整身份前缀；丢失回执只补记一个可证明的 fast-forward/精确双亲 merge，目标含后续分支或额外提交即 fail-closed。恢复除 Git common-dir 外，还须以 canonical `git worktree list --porcelain -z` 唯一匹配规范 path/branch/HEAD 并拒绝主工作区；创建/bind/回收失败闭合 worktree metadata 与临时 branch，冲突 abort 未确认不得开 gate
- TESTER/REVIEWER 有 Integration 时只投当前 pending 波次；无 Integration 的顺序兼容仅允许候选收敛到同一 WorktreeRef。并发终态归档从 canonical State 确定来源，使用直接 code-unit 稳定排序；首次物化写不可变 source→archived receipt，重试只校验并复用首次映射。禁止使用最后完成 worker 的进程内变量或根据后来 State 重算；多个未集成分支必须确定性全部保留或 fail-closed
- 冲突先收集结构化贡献者/文件并 `merge --abort`，再持久化 `integration.status='conflict'` 与 `integration_conflict:<integrationId>` gate；Phase 9 最小裁决仅允许 Leader `request_rework <workerId>`，命中冲突贡献者后将其 subtask 退回 todo，并从原 base 重建本轮集成
- DEF-004 保持 open：9.3/9.5 必须实测同 worktree 内宿主 fs 与并发进程的 TOCTOU，出口前落修复或书面安全边界；DEF-010 仅在 schema/RoleSpec/投影迁移与回归测试落地后关闭

> **[2026-09-07 task 9.3 实装]** 已落 `WorkspaceAdapter`、结构化 `WorktreeRef/Integration`、WorkerRuntime 分配/HEAD 刷新、专用 integration service、冲突 `request_rework` resolver、legacy Git 实查迁移与 Web 组合根显式 integrate hook。真实测试覆盖同 role 两 worker 的单一路径 Docker bind、稳定合并顺序、逐步持久化、终态恢复引用、add/add 冲突 abort 与 gate；DEF-010 按条件关闭。Coordinator 自动拓扑波次、集成后单一 TESTER 与完整 E2E 仍属 9.4，DEF-004 仍 open 至 9.5。

**Coordinator 拓扑波次 + E2E（任务 9.4）** ：
- **[2026-09-07 架构决策更新]** 先落严格 `architecture.executionPlan` DAG 与 `parallelExecution` 波次控制；未知引用/自环/环/重复项 fail-closed，legacy modules 只显式退化为顺序依赖链。只激活依赖均 done 的全部就绪 subtask，按 priority 降序、id code-unit 破平局；禁止无就绪任务却直接完成
- 消费稳定非空 batch Route，持久化 waveId/attempt/base/完整 workerIds；首波全部 CODER 从 initialBase、后续波次从上一已验证累计 HEAD 创建独立 linked worktree，assignment 作用域投影覆盖初始执行、D9 reproject 和 D4 resume。恢复不扫描历史 worker 猜当前 batch
- 本版本 TESTER preparation 固定关闭、不提供启用入口；保留将来最多一个只读准备 worker 的契约，启用前须独立验收。所有测试文件与真实执行由 validation TESTER 负责
- Integration 合并完成即冻结 resultCommit；单一 TESTER 在独立 validation worktree 写测试并对精确提交执行真实沙箱验证。receipt 绑定当前 dispatch/integration/HEAD/需求决策指纹和实际命令证据，禁止消费旧 test-results 文件；通过后原子闭合 wave 并推进 accepted 基线。单 CODER wave 也不跳过集成验证
- worker 失败不硬杀同批 LLM，保留成功分支，失败/未启动 assignment 用新 workerId 有界重试；冲突经 Leader request_rework 保持原 base；测试失败以失败验证提交为新 attempt 修复基线，但不推进 accepted receipt。达到 iterationCount=8 使用既有 gate
- REVIEWER 只读最终累计验证产物并获得全计划 subtask 索引；合法 refs 重开目标及所有传递后继（含 done），缺 refs 才显式 degraded 全量返工，坏 refs fail-closed。D16 approve/request_changes 必须绑定同一 validation receipt/HEAD；DEF-013 保持 open 至实现与出口证据齐全
- 同一 Web 生产链呈现 D15 JSONL 的同 role 不同 session lineage 泳道、真实时间重叠/Fork/截断/刷新恢复；不能用静态消息或 WorkerState 标签冒充 trace
- G5 以 A/B 并行→依赖 A/B 的 C→累计验证→REVIEWER→Leader 终审→归档复验为最小闭环，包含 TESTER 测试产物跨波继承、旧证据拒绝、冲突重做、done 后继返工与 D4 validation Fork。详细负向矩阵见详细设计 §11；确定性 LLM adapter 与 live model 证据分开报告
- [2026-09-07 实测修复] D9/D14 累计复验显式绑定 sourceReceiptId 并继承其验证 HEAD；真实回归检查旧测试文件逐字保留，拒绝未知/前向/身份漂移的源回执。
- [2026-09-07 审查修复] 补齐失败 A/B 波次的 root-cause 定向返工：仅重写 A、B 随修复产物累计验证，通过后再启动 C；单 CODER 执行失败同样有界重试，基础设施失败明确拒绝自动重试。补证据已落盘而 State 未提交的幂等恢复、中文/空格等合法测试文件全集、嵌套 suite 失败统计，以及新波次不继承已修复失败的投影回归。架构级返工明确全计划重开，验证 replanSourceReceiptId 后保留累计 HEAD；失败批未启动成员和失败者统一使用新 workerId，D4 暂停恢复仍保留原身份。正式测试及实际运行结果记 task-status，不以旧成功闭环替代这些边界验收。
- [2026-09-07 PR #66 修复] 补原生暂停消费/abort、继承测试删除/改名拒绝、CODER 忽略缓存与提交状态分离、非 gate 并行异常 suspend/清理重试、Fork 标识一致性；新增对应负向回归及真实 Docker/Harness 链路。preparation 当前固定关闭，首波与后续 base 分支明确区分。
- [2026-09-07 实装] 已在 Leader 确认计划后接入生产编排；自动波次、真实 Node/TAP 验证回执、D4 真 Fork、D9 重验、D16 返工/批准及 Trace 泳道均有独立测试。完整门禁、live model 证据及合并后当前状态以 task-status 为准；9.5 的阶段性能对照与 DEF-004/013 出口处理不提前宣称完成

**阶段 Eval 增量（任务 9.5）**：

[2026-09-08 9.5 检查点] 独立出口与完整116文件982项回归已通过，正式文件边界修复及DEF-004/010/013验收见延期台账；真实Flash固定九次对照及浏览器证据单独留档，任务状态以task-status为准。
- **[2026-09-08 架构决策更新]** 本任务的完整出口契约统一见详细设计 §11.7；先核对现有证据、确定 DEF-004 修复或经 Leader 明确接受的后续边界，再补独立出口链与 Eval。文档修订本身不表示代码/安全修复或模型实验已完成
- 串行/并发 2/3 共用相同 D17 多角色、DAG、波次、集成与验证协议，仅切换 GlobalScheduler cap=1/2/3；不用旧顺序 fallback。主任务至少 4 个独立模块加依赖后继，A/B→C 只保留为正确性证据；跨项目公平与顺序依赖控制另设确定性场景
- 固定任务/模型/预算/镜像/源码及 warm/cold 策略，真实 model 主任务每组至少 3 次独立 attempt、轮换运行顺序。预先冻结调用/时间/可取得时的费用预算，保留所有失败与 provisional 尝试；报告成功率及分母、时延分布、token/费用、冲突、返工与分层资源指标。3 次仅作探索性基线，不设必须提速的出口门槛，单次快照不构成统计结论
- 独立 phase9-exit 真实跨包链配合现有正式回归覆盖表，不复制所有单元用例；G5、deterministic 与 model 结果分开。新增 Phase 9 tasks/scenarios/runner、fixture/聚合摘要与显式 eval:phase9:deterministic/model 脚本，复用 Phase 0–8 累计目录。上述新增项已实现，验证结果与当前源码适用范围见任务 notes 及 docs/evals/phase9-model-repair.md；默认回归不触发独立 Eval
- 追加全 batch 不丢任务、全局 cap/跨项目公平、all-settled 失败收敛、canonical State join、pause epoch 重放/冲突、队列 acquire 取消保持 pending、Step commit 先于 checkpoint、done/failed 不被 paused 覆盖、reproject 保留 lease、humanGate 释放 lease 但清理失败保留 admission、per-worker Fork 恢复、未满足依赖不得激活、同 role 的 Git/Docker 单一路径 worktree 隔离、legacy WorktreeRef 迁移、专用 integration worktree 确定性重放、merge abort、`request_rework` 冲突裁决和 mutation 所有权/幂等场景；单次快照不构成统计结论
- 出口逐条处置 DEF-004/010/013：DEF-004 需正式读/写/目录遍历及后台进程、路径替换、恢复/归档证据，跨 worker 隔离或风险复现不算关闭；仅 Leader 另行接受具体后续边界时保留 open 并记录强制限制/残余风险/复评条件。DEF-010 已 resolved，复核删除及 legacy 迁移证据，不解锁 writer。DEF-013 在定向闭包含 done 后继、缺 refs 全量、坏 refs 拒绝均实测后关闭

### 12.4 依赖关系
- 依赖 Phase 1 的 git-server（worktree/merge）
- 依赖 Phase 4 的 Coordinator（决定何时触发并行）
- 依赖 Phase 5 的 TaskStateStore 串行提交、Phase 7 的 enabled roster、Phase 8 的 D4 checkpoint/Fork 生命周期与 D15 trace


## 13. Phase 10：产品化打磨 + 最终 Benchmark（2 周）

### 13.1 目标
统一基于 DeepSeek Harness 的自研 Agent，完善错误恢复、模型调优、界面细节与本地安装/一键启动；接入成熟 Coding Agent Benchmark 小型固定子集，完成系统变体对照、失败分析和可审计报告，再据实更新 README、简历与终版演示。

### 13.2 里程碑
- [ ] **Day 1-5**：沿用 Harness 执行路线完成错误恢复增强、日志完善（任务 10.2）
- [ ] **Day 6-7**：逐 Agent 模型配置界面、OpenAI 兼容服务连接、配置持久化与真实路由验收（10.3）
- [ ] **Day 8-9**：本地安装、启动与配置（10.4）：依赖检查、本机启动/停止、自动主密钥管理、配置说明与数据保留；不开展云端部署
- [ ] **Day 10-12（原估算，预检后重估）**：成熟 Benchmark 兼容性预检 → 固定子集/adapter 与验证器校验 → 四组对照运行 → 统计/失败分类报告；不以三天排期强迫省略实验或门禁
- [ ] **Day 13-14（原估算，演示预检后重估）**：核验现有 README、准备简历事实/面试备答、录制并验证当前版本产品演示；不以排期省略真实执行和证据

### 13.3 具体任务

 [2026-09-10 Go在线诊断] Leader授权小规模诊断后按0.50美元Go配额折算/累计20次请求上限执行；发现缺缓存usage、诊断工具render签名及Go空工具身份覆盖问题均停止后排查，修复后最后14次请求完成两个Flash模型真实工具调用及各一次官方自动压力压缩。含停止运行累计19次请求，保守额度USD0.078770214，零未结算，未重置预算。11文件31项离线回归、typecheck、lint377文件通过；正式54次入口保持暂停，完整Docker/Git单模型/混合流程试跑未执行。证据docs/evals/phase10-go-diagnostic-evidence.md。

 [2026-09-10 Go完整流程结果] Leader继续授权后，single与mixed自有answer42完整链路均final/pass：single 8请求/10工具/20.380秒，mixed 19请求/24工具/129.991秒，独立Docker验题通过；mixed第一轮完成、2项波次测试通过、D16绑定有效、返工0、gate/最终lease均0。保留首次single未知bash工具失败1请求；本阶段累计28请求、新增额度USD0.015258036，总诊断47请求/保守USD0.094028250，无未结算。修复Docker标签inspect404兼容及Docker Desktop挂载前缀漏计；本次历史containerPeak标unknown，修复后无模型真实容器检查1→0且三个run无遗留容器。离线14文件37项、typecheck、lint384文件通过，未跑完整付费回归/正式54次。详见docs/evals/phase10-go-flow-evidence.md与metrics.json。

**[2026-09-10 OpenCode Go配置]** Leader确认后续测试改用OpenCode Go DeepSeek。已按Leader最终选择配置deepseek-flash/deepseek-v4-flash和独立额度计量：mixed的PM/ARCHITECT/REVIEWER用前者，其余角色及非mixed组均用后者，保留64K输入/32K输出，预留v4但不冻结/执行。当前modelRequestsEnabled=false；只做离线验证，原54次结果与USD8.472523904账本不变。新诊断/批量评测/额度预算不从剩余旧预算推定授权。另更正上一轮typecheck漏报，详见docs/evals/phase10-opencode-go-config.md。

[2026-09-10 Go正式组授权] Leader确认新54次正式测评独立USD5订阅配额折算总上限，formal=5/diagnostic=0，不合并旧官方USD20或Go诊断USD0.094028250账本。仅phase10-final-v4配合AGORA_GO_FORMAL_AUTHORIZATION=phase10-final-v4-usd5显式入口可执行；默认modelRequestsEnabled=false仍防误运行。冻结模型/任务/源码/镜像及预算后顺序执行；每次请求前检查持久停止标记，请求异常、缺有效usage、异常finish或系统性工具错误立即禁止后续请求，在途流自然结束；任一attempt非pass先停止整组审查，原失败与费用保留，源码修复须重新冻结新组。非空未知工具名仅允许一次原生纠正。预算不足请求上界时提前停止，绝不扩额。

[2026-09-10 Eval公开契约修正] Leader要求修复PM无法读取公开测试导致的需求歧义。四个公开题的接口和行为澄清从固定公开starter/测试/题面提炼，以固定SHA256绑定原始来源；澄清进入各变体相同goal投影，对冲突原题面明确优先，不投原测试代码全文，不读取参考解或holdout。PM保持无工具，生产RoleSpec/投影接口/异议解析不改。grade-school仅说明公开断言证明的事实：两次跨年级添加后原年级为空、不抛错，add返回值未断言；不得补造false/异常/名册不变要求。正式public task version升4，协议与组升v5；旧v4四次失败和费用保留，新54次共用现有USD5正式账本而非重置。修复先离线回归、真实Docker预检，保持异常停组。

[2026-09-10 Eval工具循环修复] Leader授权发现问题后先审查并直接修复，不对既定范围内的修复重复询问。v5停止原因已留档；Git MCP工具说明明确空patch只stage/commit现有fs_write变更，单模型与多角色获得一致工具语义，不改接口或权限。Eval每个attempt允许一次普通工具错误纠正，第二个独立toolCallId的工具错误（含shell126/127，普通业务测试退出1除外）在下次provider I/O/费用预留前持久停组；历史重放幂等，基础设施错误仍立即停止。single在成功/失败时均从已闭合官方session保存安全trace，缺trace的路由指标标unknown而非已证失败。先离线故障回放及真实Docker验证，再另冻新组，不覆盖v5结果或重置USD5账本。

[2026-09-10 Eval年级排序澄清] v6第二轮multi的PM将grade()顺序写为nonGoal，公开独立验题仅此断言失败。原公开澄清排序条目落在roster()句下，工具为空的PM可误读为只限定roster()；直接修正为逐API明确：grade(grade)同样按姓名字母序返回，即使输入顺序不同。原始公开测试和评分不变，所有变体同用该事实，公开version升5/组v7，holdout/version不变；v6两pass两fail及费用保留，不混算成功率。复核其余wordy/book-store/forth公开测试后未发现同类API范围歧义；仅进行明确行为澄清，不增加未被公开证据约束的行为。

[2026-09-10 Eval运行环境说明] v6/v7中TESTER多次尝试容器内git log/status，固定镜像没有Git CLI；单次先记模型使用错误，重复证据表明应向所有配置明确共同环境约束。为public和holdout的goal/TASK.md统一增加Node20/no-network/no-Git-CLI说明：Git仅经已授予MCP工具操作，git_*不是shell命令，fs_write后可用空patch提交；不新增产品验收要求，不改变权限、不在容器装Git。真实Docker预检必须证实Node20与Git CLI缺失并绑定镜像/说明hash。公开version6/holdout包装version2、组v8；holdout任务本体/隐藏验题仍原样，只有共同环境前缀改变。旧v7一pass一fail及全部费用保留，同一USD5账本，不放宽错误停止阈值。

[2026-09-10 ARCHITECT交付边界修复] v8的mixed第三次将executionPlan错放顶层，旧解析器静默丢弃该字段，先写入缺计划的architecture，再在编排legacy modules校验时报错；此为严格交付边界缺口。ARCHITECT最终对象只接受architecture/conventions两个顶层键；显式计划仅允许architecture.executionPlan并复用领域DAG校验。无显式计划时，交付入口复用领域executionPlanFromArchitecture验证legacy modules（仅非空字符串数组；空/缺省保留原单任务退化语义），对象modules必须配有效显式计划。错误在纯validateTurnOutput阶段拒绝并走既有最多两次、无工具、同turn的官方格式修复，不由宿主搬动字段或猜补计划；通过后才读取mutations并提交一次。提示同时明确legacy modules字符串限制，接口与D17/D16不变。旧v8结果/费用/源码快照保留，修复验证后另冻新组并共用USD5账本。

[2026-09-10 D1 输入修复] v9 在 29/54 final（24 pass、5 fail、25 pending）处停止。离线真实 Harness/MCP/Go 序列化确认旧 pre-step 每次工具结果后重复追加完整投影 user 消息；工具历史虽然保留，重复任务指令仍是输入构造缺陷，不能继续将相关循环归为纯模型故障。改用 agent-scoped 官方 SystemPrompt.section/variable 每次请求提供当前投影，turn 仅一次固定启动消息，工具历史与压缩仍由官方 Harness 管理。变量一次插值保留任务字面量花括号；初始、重投影、Fork 同路。旧 v9 快照与失败不覆盖，修复后须另冻新组，沿用原 Go USD5 账本；已结算 USD0.911844144/717 请求，0 未结算。当前核心 5 文件45项离线通过；集成与完整门禁仍在验证，旧官方真实回归漏记费用缺口仍待核实，不再新增官方付费回归。

**[2026-09-10 Go 压缩请求修正]** v9 的 forth-multi-1 中一次官方压缩请求以 tool-calls 结束，随后官方 compact 报 summarization produced no text summary content；停止标记在该在途摘要请求发出后写入，原流自然闭合。锁定 Harness 允许 adapter 按 GenerateOptions.purpose 区分辅助请求；Go Eval 计量适配器仅对 purpose=compaction 去掉可调用 tools，保留原 system/messages（含历史工具证据）、模型、session/purpose 与费用门禁，由官方引擎继续生成和提交纯文本摘要。普通任务请求保留原工具清单；计量记录增加安全 purpose 分类，实际发送参数与输入估计同源。不重写 compaction loop，不把旧空摘要改报成功；Go wire 正反例及真实官方压力压缩离线验证后方可重新冻结。 正式组另校验摘要请求必须正常stop并形成非空文本；tool-calls、空白或空摘要在当前流自然闭合和费用结算后持久停组，后续官方压缩重试在provider I/O前被拒绝，不让无效摘要继续消耗。

[2026-09-10 Go正式v4自动停止] 完成真实Docker正反例预检、15文件42项离线测试、typecheck/lint386文件和357份源码冻结后开始USD5正式组。首题grade-school-single-1第2次请求出现STREAM_CLOSED（响应提前结束，无DONE/usage）；请求级停止生效，只有1个final/fail，其余53个pending，未继续批量消耗。共2请求，已结算USD0.000365100，未知请求保留USD0.058982400预留，总预算占用上界USD0.059347500，未伪造usage/清除unknown。输入估算3418/65536，未见context-limit错误；无法确定上游或中间连接中断原因，保持停组等待连接问题审查。安全点自然闭合，cleanup通过，独立Docker检查零遗留。旧v3全部349份快照哈希保持。证据docs/evals/phase10-go-formal-evidence.md、metrics.json。未运行完整pnpm test、未commit/push，10.5仍in_progress。

**[2026-09-10 Task10.5 修复进展]** Leader 已授权修复评测暴露的问题；已实现 REVIEWER advisory 有界续评与评测适配器容量元数据补齐，最终143文件/1114测试、lint及真实模型advisory续评诊断通过；当时typecheck误报已更正，测试夹具修复后重新通过typecheck，预算累计USD8.472523904，证据见 docs/evals/phase10-repair-evidence.md。v3 的 25/54 与既有费用为修复前测量，保持原值；当前源码已变更，不把旧分数声明为修复后结果，不自动追加新的正式对照组。

**[2026-09-09 Task10.5实施补记]** Leader已确认公开四题与两个新holdout的54次正式对照及USD20总上限；脚本eval:phase10:preflight/freeze/model/report显式运行，默认回归保持完整。v3冻结组54/54次已归档，公开23/36与holdout 2/18整体通过；最终报告与逐次指标见docs/evals/phase10-final-benchmark-report.md。历史16条独立保留，总预算占用USD8.352819564（含保守上界），最新完整回归1105测试通过。实验暴露上下文限额与合法advisory后的评审续行缺口，不宣称产品全量验收完成；任务保持in_progress，未commit/push或推进依赖。完成gate全员done不虚报Fork，另由真实paused验证恢复链覆盖。

**[2026-09-08 架构决策更新] D2 / 原任务 10.1 取消记录**：Leader 明确取消“ExternalAgentExecutor 厚执行器接入”，统一使用基于 DeepSeek Harness 的自研 Agent。原任务依赖为 9.5，取消前为 ready、未开始实现；现从 `task-status.json` 活动任务表及下游依赖中移除，编号 10.1 保留为历史标识、不复用，不标记为实现 done，不以取消计入完成率。活动任务数由 62 调整为 61，Phase 10 保留 10.2–10.7 共 6 项。D1/D4/D15/D17 与冻结 Executor/SandboxManager 契约继续有效；旧 external 类型/命令字段仅作为兼容表示，本轮未执行源码迁移。D11 的外部 Benchmark 任务/环境/Outcome Grader adapter 继续保留，单 Agent/多 Agent 等被测变体均使用 Harness 内核。

**错误恢复与日志（任务 10.2）**：
- **[2026-09-08 架构决策更新] 先评审再实施**：以详细设计 §6.1 作为本任务精确契约；官方 retry 实际是同一 open Step 内的 provider attempt，不按其 README 的新 turn 口径实现
- 复用官方有限 normal 重试并验证预算不能由外层 worker 重派/新 context 绕过；永久/额度/协议错误与 unknown 不泛化为瞬态
- 有 scope 的 worker 失败、规范 failed 提交失败、lease/suspend/finalization 失败分别保留；进程退出不承诺临终写 State或自动续跑
- D15 只增加最小同 Step retry 列表，显示等待/退避完成/未启动即闭合；不猜下一请求已发送或成功，不投原始错误正文
- 保留真实沙箱默认 30s、D4/Fork、8 轮上限、KB Write-Block 与既有阶段回归；顺序失败诊断产物不等于完成验收，并行未验证产物仍 suspend 保留
- Task 10.2 独立验收 `tests/integration/phase10/phase10-resilience.test.ts`，按架构 §6 覆盖矩阵复用已有证据并追加新组合链；规格评审记录见 `docs/reviews/task102-spec-review.md`，静态评审与运行证据分开，后续实现/G5/model/浏览器验收记录见 `docs/evals/phase10-resilience-evidence.md`（待 PR 人工合并）

**用户逐 Agent 模型配置（任务 10.3）**：
- **[2026-09-09 架构决策更新·Leader 范围确认]** 提供产品界面入口，让用户独立配置各 Agent 的 Base URL、API Key 与模型名，接入兼容 OpenAI API 的服务
- 提供“为全部 Agent 设置同一模型”快捷入口：一次填写连接和模型，批量应用到当前项目团队；之后仍可单独调整某个 Agent，验证批量保存一致性及冲突反馈
- 用户保存后持久化，并通过 Harness `agent/request` 与模型适配器用于真实执行；刷新恢复、错误反馈、凭据不泄漏与配置生效规则须验收
- 规划/评审用强推理、编码用代码模型仅为建议，不固定用户选择；全 Flash/全 Pro/角色混合预设与九次对照实验不是本任务交付，10.5 最终 Benchmark 保留
- API Key 不得明文落盘或进入群聊、投影、Trace；Leader 已批准 Chat Completions/SSE/function tools、AES-256-GCM 凭据存储、任务启动固定模型绑定与同源/revision CAS。验收覆盖批量原子性、逐 Agent 覆盖、旧任务 Fork、真实兼容路径及桌面/手机 UI，证据见 docs/evals/phase10-model-settings-evidence.md

**本地安装与启动（任务 10.4）**：
- **[2026-09-09 架构决策更新] Leader 范围确认**：Agora 程序、数据、Git 工作树及 Docker 沙箱均在用户电脑；浏览器访问本机服务，不部署云端
- 提供可复现的安装与启动流程，检查 Node.js 24、pnpm、Git、Docker、原生 helper；**[2026-09-09 架构决策更新·Leader 平台范围确认] 10.4 本轮仅适配 macOS，Linux 产品启动与自动密钥适配移出本轮范围（DEF-017），不阻塞 10.4 或本轮 Phase 10 出口；既有底层 macOS/Linux 沙箱支持不变**
- **[2026-09-09 架构决策更新] 自动主密钥管理为必交付项**：首次启动自动生成并安全保存主密钥（macOS 钥匙串），后续自动读取复用；普通用户只填写 Base URL、API Key 和模型名，不要求手动设置 AGORA_CREDENTIALS_KEY，该变量保留为高级兼容配置
- 系统安全存储异常、访问拒绝或旧密钥缺失/不匹配须提供明确恢复提示，不静默换新密钥或改用明文文件；停止/重启保留密钥与 `.data`，补充备份/迁移中密文与主密钥分离说明
- 依详细设计 §3 验收干净环境首次使用、保存后重启解密与实际连接消费、已有密钥复用/重复初始化、系统访问失败、旧密钥缺失/错误及高级配置有效/无效情形，并检查秘密不进入日志/浏览器/普通文件/沙箱。本轮须留 macOS 真实钥匙串及产品启动证据，不要求或宣称 Linux 产品验收；既有密钥接管与高级配置优先级已按详细设计 §3 的批准计划实装
- 10.3 的历史实现依赖后端环境注入；10.4 已在本地分支完成 macOS 产品入口及自动钥匙串管理，实测记录见 docs/evals/phase10-local-startup-evidence.md，待交付/人类合并；开发 Mac 历史临时脚本不替代该产品证据
- 启动入口默认仅回环访问；正式组织已选本机Node启动脚本 + Next custom server + 现有Docker逐worktree沙箱，不要求前后端迁往云端，也不改现有逐 worktree 沙箱架构
- 验收本机安装/启动/停止、浏览器操作、已有恢复契约与错误反馈；Vercel、云服务器、云沙箱、Serverless、多实例水平扩展均不在当前目标中

**最终工程 Benchmark（任务 10.5）**：
- **[2026-09-10 入口校准]** v12首项发现新题包装仅进入Tier1，停于1fail/17pending；补生产Tier2冻结前检查，运费题转开发。v13新内部两题为inventory-restock/daily-availability，18项，继续引用v11公开36项且共享原Go USD5。
- **[2026-09-10 修复与独立分组]** 保留v11公开36项完整对照，内部2项首次探查失败暴露全局工具错误计数缺陷，停止剩余16项。修正为session内失败步骤计数后，以两道新内部任务单独冻结18项，所有旧费用继续占原Go USD5。两份版本绑定结果分别报告，不跨版本合并总分；具体恢复与证据边界见详细设计§11.8。
- **[2026-09-09 实施修正]** 可读公开契约以`.spec.txt`进入工作区，避免现有可信Node入口自动执行Jest `.spec.js`。增加真实taskDefinition→多角色验证回归，契约内容和独立验收不变；受此缺陷影响的v2记录与费用独立保留，修正组重新冻结54次，继续受原USD20/17/3约束。
- **[2026-09-09 Leader确认]** 公开题精确接口依赖上游公开测试，新组提供其可读文件；隐藏验证/参考解仍隔离。保留旧组13条已启动记录（其中2条受操作暂停影响），原费用不变；在原USD20总预算内新冻结54次，使用新groupId和group-scoped费用身份，不混算不同可见性协议。
- **[2026-09-09 架构决策更新]** 精确契约见详细设计 §11.8，评审见 `docs/reviews/task105-spec-review.md`。先核对候选与本机沙箱、产物及官方 verifier 的兼容性，至少接通一种成熟生态的固定小型分层子集；SWE-bench Verified、Terminal-Bench/Harbor 是候选而非双重强制安装，不复制公开数据进仓库
- 上游 verifier 必须经隔离的已知正确/错误结果预检，绑定真实 patch/产物；参考解/隐藏测试不进模型上下文或工作区。当前 Node/TAP 波次验收不能冒充外部任务判定，所需生产能力扩展先列计划
- 保留新的 Agora holdout，专测角色上下文、Channel、Leader、交接、持久 suspend/resolve/真 Fork resume、抢占和并行一致性；旧 Phase 6–9 开发 fixture 只作回归，不重新命名为 holdout
- 完成单 Agent/多 Agent、合规结构化上下文保留策略、同 D17 DAG cap=1/3、固定模型/角色路由四组比较；按适用任务分组，不做全组合穷举。每组只改变目标因素，模型路由组明确改变模型映射；上下文组不突破 R2/assignment 或移除最新权威事实，单 Agent 是 Harness Eval driver、不伪造产品完成
- 冻结任务数量/分层、模型/工具、预算、超时/环境和轮换顺序，每个纳入比较的 task×variant 至少三次独立 model attempt；保留所有失败、中断及未启动原因，报告样本量、均值/样本方差、成功率、机制触达与失败分类。配置变更另立组，旧 9.5 结果不冒充当前版本结果
- 汇总 Outcome/Process/Efficiency/Safety，官方 Outcome 与 D16 完成独立表述；公开任务、开发集和 holdout 分开报告。低成功率或无提速可如实交付探索性结论；适配错误、权限/安全失败、缺实验或未知关键证据不能宣称完成
- Harbor/Python/`uv` 等若成为实际工具依赖，实施前重新核对版本、许可证与网络/镜像成本；不得进入默认产品运行时依赖。本次文档修订不授权安装、付费实验或源码实现；具体模型/费用及实现文件计划仍待确认

**README 与终版演示（任务 10.6）**：

**[2026-09-12 展示精简]** 10.6已合并完成；README不再展示Historical Phase5 Demo，docs/demo仅保留当前成片材料，旧验证链接固定到Git历史83fe416。当前GitHub视频入口使用H.264附件播放器，保留原剪辑及产物身份；10.7仍ready，未执行。

**[2026-09-12 当前交付状态]** quote-en-take-9已获Leader接受，当前成片见`docs/demo/task106-natural-chat-demo.md`。PR #73修复消息身份冲突、跨作用域异步响应、失效工作树回收及测试结果一致性，证据见`docs/reviews/task106-pr73-repair.md`。下方暂停/待录制/等待旧额度等段落是各自日期的历史过程，不作为现行状态；10.6仍等待人类合并，10.7保持独立出口。

**[2026-09-11 T10.6 根因复核]** 已修复Request克隆链在GC后取消不传播、任务错误未显示及超时提示误导；真实HTTP红绿与浏览器检查见 `https://github.com/logan-suu/Agora/blob/83fe416c1015d86616acdead7af2ebbad82e8384/docs/demo/task106-english-rehearsal-evidence.md`。900需求已进入模型请求，旧票价被真实测试拦截；不能以此宣称新全英文闭环成功。新源码完整回归等待必要费用授权，无新录屏。

- **[2026-09-11 规格评审修订]** 按蓝图 §18、详细设计 §11.9执行：10.4已提供真实仓库地址及macOS Quick Start，本任务核验现有步骤并更新当前能力、架构图、限制和演示/Eval入口；不重新实现启动器
- 交付当前版本真实浏览器闭环录屏与证据索引：并行Trace、中途指令、生产测试/评审、Leader完成终审、归档和刷新恢复；设置/sub/交接/Fork等补充能力可引用单列证据，不要求在同一次任务穷举全部机制。剪辑明确标注并可追溯同一运行原片，不以静态消息或Phase5历史录屏代替
- 简历事实/面试备答区分机制、验证与收益；最终比较引用10.5公开v11和内部v14，分版本保留样本量、失败、方差和unknown。单次演示事实可标明范围后记录，不据此推导通用速度/成功率；不新增Benchmark要求
- 实施前固定演示题面、环境、模型及当前有效授权范围；有显式次数/费用上限时遵守，已撤销的T10.6临时上限不作为产品限制，10.5历史冻结额度/结果不变。材料检查覆盖命令/链接、播放可读性、同任务证据与脱敏；发现代码问题按既有修复授权及G1–G7处理
- 10.6依赖10.5；录屏与材料交付不替代10.7产品出口。具体交付与验收以详细设计§11.9为准
- [2026-09-11 Leader确认] 当前视频已撤下，待优化完成再录。群聊先完成结构化交付可读呈现，再统一支持Markdown标题/列表/引用/链接/代码/表格/只读任务清单，保留原文及@角色语义；依赖锁定与呈现边界见技术选型§12、详细设计§11.9。
- [2026-09-11 任务侧栏优化] Current task分离任务ID/状态与三行目标预览，完整目标按需展开为Markdown；真实桌面与移动抽屉检查进度首屏可见、完整内容保留及切换任务重置展开状态，仍不重新录制。
- [2026-09-11 群聊滚动优化] 保留消息列表独立纵向滚动，增加清晰原生滚动条/稳定轨道与键盘聚焦；实测滚轮、滑块拖动、Home/End及输入框固定。回看历史时不强制跳底；后续已批准的底部跟随及Back to latest按详细设计§11.9/§11.10验收。此处暂停录制仅为当时记录。

- [2026-09-11 Leader确认·英文演示] 下次录屏全部使用英文，覆盖产品/系统文案、题面、Leader消息、Agent交付、展示的代码说明与字幕/旁白/封面/片尾。修正Coordinator中文展示模板，保留payload及历史消息；新任务在英文goal中要求PM保留语言验收，再经currentRequirements投影传递。英文脚本见https://github.com/logan-suu/Agora/blob/83fe416c1015d86616acdead7af2ebbad82e8384/docs/demo/task106-english-script.md，实际模型输出仍须新录制预检；继续暂停录制，不增加尝试或费用授权。

- [2026-09-11 Leader确认·录制前优化] 已授权底部新消息跟随、回看位置保留、按msgId去重的未读提示/Back to latest及作用域切换重置；同时精简Coordinator派发通知，不重复完整goal/title，详细契约见§11.9。继续暂停录制，完成浏览器及完整回归后再评估。

- [历史中间版本·2026-09-11 Go等待修正；当前Go为300s/300s，见选型§4.1] 新获准全英文预演quote-demo-en-2已真实提交900分更新，TESTER识别旧值后进入返工；A在同Step连续TIMEOUT耗尽两次重试，未完成。31秒延迟流最小复现驱动仅Go的stream-idle从30秒调整至120秒，其他服务/SDK请求配置/重试数不变；须验证慢响应成功与停滞仍失败，并执行原完整回归。失败记录保留，不自动重开任务或录屏，细节见https://github.com/logan-suu/Agora/blob/83fe416c1015d86616acdead7af2ebbad82e8384/docs/demo/task106-english-rehearsal-evidence.md。

### 13.4 依赖关系

**[2026-09-12 架构决策更新·10.7验收入口]** 10.6新版英文视频已获Leader接受；优化后当前产品行为以蓝图§18/§21和详细设计§11.10的E01–E12对照表为出口编写/评审依据。上面的“暂停录制”、旧费用上限及Go等待中间版本只记录历史，不再充当当前验收要求。Markdown/折叠原文、紧凑侧栏、原生滚动/按需跟随、发送及刷新、自然语言需求确认、当前需求/波次范围/完成反馈、正常模型容量、Go兼容恢复、角色指令与归档证据均须映射到适用测试；不能只核对旧10.2–10.4入口。

**Phase10出口（10.7）的判定方式：** 五条成功标准保持，其中“自主完成全流程”指正常阶段可推进、普通缺陷有界返工，最终完成仍须D16 Leader终审；“上下文不崩”应验收投影/官方压缩/容量配置与有界错误处理，不作为无限上下文质量保证；“可扩展”不扩大DEF-016/017范围。既有回归和真实跨包/G5继续运行，不将录屏或已有测试数字代替本次执行；新增`phase10-exit.test.ts`仍须独立计划与实现。10.7的documents_required纳入§11.10和本轮文档核对记录。依赖和done规则不变，视频接受不能越过10.6代码交付及人类PR合并。

- Phase 9 出口 9.5 完成后，10.2、10.3 ready；10.4 依赖 10.2；10.5 依赖 10.3、10.4；10.6 依赖 10.5；10.7 依赖 10.2、10.3、10.6。
- 取消的 10.1 不再是任何活动任务的依赖；其他任务编号保持不变。Phase 10 出口仍须完成健壮性、本地启动、最终 Benchmark 报告与完整产品演示验收。


## 14. 里程碑总览

| 里程碑 | 交付物                                    | 预计日期  |
| :----- | :---------------------------------------- | :-------- |
| M0     | Phase 0 完成：单 Agent LRU 缓存闭环       | Week 1    |
| M1     | Phase 1 完成：MCP 工具 + Docker 沙箱      | Week 2    |
| M2     | Phase 2 完成：6 角色完整回环              | Week 3.5  |
| M3     | Phase 3 完成：上下文工程（决策台账+交接） | Week 4.5  |
| M4     | Phase 4 完成：自适应编排（Tier 0/1/2）    | Week 5.5  |
| M5     | Phase 5 完成：群聊界面 MVP                | Week 7    |
| M6     | Phase 6 完成：多形态通信 + Eval 骨架      | Week 8.5  |
| M7     | Phase 7 完成：角色热插拔 + Eval 增量      | Week 9.5  |
| M8     | Phase 8 完成：人工卡点 + 裁决 Eval        | Week 10.5 |
| M9     | Phase 9 完成：真并行升级 + 对照基线       | Week 12   |
| M10    | Phase 10 完成：产品化 + 最终 Benchmark 报告 | Week 14   |


## 15. 风险与应对

| 风险                          | 影响 | 应对策略                                         |
| :---------------------------- | :--- | :----------------------------------------------- |
| Harness 频繁更新导致 API 变动 | 高   | 锁定具体版本，跟踪 changelog                     |
| MCP SDK v1 与 v2 迁移成本     | 中   | Phase 0-9 使用 v1，Phase 10 评估迁移             |
| Docker 环境配置耗时           | 中   | Phase 0 先用 LocalTemp，Phase 1 再切 Docker      |
| 真并行调试复杂度高            | 高   | Phase 9 前充分测试单 Worker 稳定性               |
| 公开 Benchmark 污染/刷榜      | 中   | 固定版本并单独报告公开集与 Agora holdout；保留失败分类，不为榜单反向定制生产 Agent |
| Eval 成本与方差失控           | 中   | Phase 6 小任务起步；固定预算/模型/环境，Phase 10 才重复运行并报告均值/方差 |
| 时间不够                      | 高   | 严格按阶段推进，每阶段有可演示产出才进入下一阶段 |


## 16. AI-Native 开发工作流

### 16.1 推荐工具链
- **IDE**：Cursor 或 Windsurf（内置 Composer/Agent 模式）
- **上下文注入**：将 `docs/详细设计方案.md` 和 `docs/系统架构设计文档.md` 作为 Cursor Rules 或 AGENTS.md
- **代码生成**：每次让 AI 生成代码前，明确指定“严格按照 Phase X 的规格实现，不超出范围”

### 16.2 建议的 Cursor Rules（`.cursorrules`）

```markdown
# Agora 项目开发规则

1. 技术栈：TypeScript 5.9+，宿主 Node.js 24+，pnpm workspaces（用户代码 Docker 沙箱仍为 Node 20）
2. 严格遵循 docs/详细设计方案.md 中的接口定义
3. Phase 0 约束：
   - 所有 Executor 统一为薄 Harness
   - Sandbox 使用 LocalTemp（非 Docker）
   - KnowledgeBase 仅读空对象，不写入
   - 全局调度器退化为单任务串行
4. 代码风格：Biome 格式化，Import 自动排序
5. 测试：Vitest 3.x，每个模块需有单元测试
```

### 16.3 每日开发流程
1. 晨会（5 分钟）：确认当前 Phase 的里程碑进度
2. AI 编码（2-3 小时）：将任务拆解为小单元，逐个让 AI 生成
3. 人工审查（1 小时）：检查 AI 生成的代码是否符合设计
4. 集成测试（30 分钟）：运行 Vitest 确保没有破坏已有功能
5. 提交（10 分钟）：commit message 遵循 Conventional Commits


## 17. 检查点：秋招 Demo 准备

**[2026-09-11 规格评审修订] 历史范围说明：** 本节以下场景保留 Phase 5 / Task5.5 的历史录屏契约，包括当时的完成方式和未解锁边界，不作为当前产品说明或10.6验收。终版演示按§13的10.6和详细设计§11.9执行，必须经过D16 Leader完成终审；当前阶段不能将已实现的Phase6–9列为“规划中”。

在 Phase 5（Week 7）完成后，应准备一份**可用于秋招展示、且与真实执行能力一致的 Demo 录屏**。录屏必须使用任务 5.5 接通后的 Web→编排→消息持久化→SSE 链路；静态种子消息只能作为空状态视觉占位，不得作为执行证据。

**Demo 场景**：
1. 启动单实例自托管系统，在 UI 创建并启动任务：“实现一个带 TTL 的 LRU 缓存类，包含 get/set/delete 方法，并编写单元测试”
2. 观察 Coordinator 从真实 State 评估为 Tier 0，跳过 PM/Architect 并派发 Coder
3. 观察 Coder 的进度消息经 State 持久化后实时出现在 main 群，并展示实际代码产物入口
4. 观察 Tester 执行真实测试；若首次失败，保留一次可解释的 Coder↔Tester 修复回环，否则直接进入下一步
5. 观察 Reviewer 基于真实 diff/测试结果给出通过结论，Coordinator 将任务收尾为 `done`
6. 刷新页面，确认消息、最终状态与归档产物由 JSON State + `.data/.../artifacts/` 恢复，且终态 Harness/Docker 已释放；展示角色头像、消息流、产物引用与任务状态
7. 可补充演示一条有效 `@ROLE` 指派和一条 deferred/rejected 指令，明确 Phase 6/8/9 尚未解锁的边界，不宣称完整抢占、sub 群或 Serverless 后端能力

**真实性门禁**：关键链路必须连续展示浏览器操作、服务端执行与最终产物；不得剪接静态假消息冒充 Agent 实跑，不得以独立编排测试 + 独立 UI 演示替代 Web 触发的同一任务闭环。

**如果时间紧张**：优先完成 Phase 0-5，确保有可演示的 UI + 完整闭环，Phase 6-10 可作为“规划中”在面试中介绍。

[2026-09-10 费用审计与Web投影回归] 供应商13:00–14:00 CDT账单覆盖此前未计量全回归：24请求，按原批准最高分类单价补计保守上界USD0.125900192。新增独立不可变audit adjustment参与原USD20/diagnostic USD3门禁，不伪造逐请求usage；补计后USD8.66252604，原费用阻塞解除。带计量全回归首个失败即停：20文件/144测试通过，Web脚本适配器仍从首条user解析旧投影导致1项失败，确认非真实模型接口故障。修正为读取唯一D1 system段，真实Docker/Harness/MCP链路2项通过，终审及产物断言未弱化。该次22真实官方请求全结算，新增USD0.021186272；当前官方保守占用USD8.683712312，0未结算。正在重新执行完整回归，尚不宣称G4通过；Go账本及v9历史不变。证据docs/evals/phase10-projection-input-repair-evidence.md，无commit/push。

[2026-09-10 D1全回归完成] 第二次全回归在Phase7首次失败处停止；确认phase7-exit与role-onboarding两个CapturingAdapter只采集messages，遗漏新的system投影。先复现后改为检查唯一D1段并采集完整system+消息，原入职事实及禁止原始群聊的断言保持。Phase7共4文件7项通过。按Vitest完整153文件清单保留43个未变文件的通过结果，补跑其余110文件801项，合计153文件1159项全部通过；没有跳过测试或移除真实凭据，文件集合逐一核对见.data/evals/phase10-regression-partition-20260910.json。typecheck/lint393文件通过。官方账本2396请求/保守USD8.701352732/0未结算；两次已停止回归均留档，原USD20不变。现开始两种Go模型合成压力验证，最多16请求，复用原USD0.50诊断余额；未启动新正式组。

[2026-09-10 Go修复实测与v10准备] 修复后两种指定Go模型各完成2轮合成文件读取，真实官方Harness持久事件各确认1次自动压缩成功，0失败/0未闭合，压缩后PAGE_MARKER与answer()=42约束保留。共10请求新增USD0.029477946，诊断累计59请求/USD0.129560616，原USD0.50池不变；不是Benchmark得分或重复原失败请求。证据.data/evals/phase10-go-projection-repair-df0e7a78-02e5-4f69-9aa1-6af2e5fa6f26。v10注册只更新协议/授权组标识并拒绝旧v9，12项针对性回归及typecheck/lint393文件通过；生产代码沿用已完成153文件1159项全清单验证。准备冻结opencode-go-system-projection-v10，任务/seed/模型/54次顺序与v9一致，共用原Go USD5账本（正式717请求/USD0.911844144），旧组不恢复或覆盖。

[2026-09-10 v10控制交付冲突修复] grade-school-mixed-2完成生产16项验证，但REVIEWER输出有效concern块后又追加verdict数组，严格解析器以非末尾控制块拒绝，未执行独立验题。23请求/USD0.015862212全部正常结算，cleanup/routing通过、最终lease0。审查确认普通最终JSON提示与通用异议末尾块提示缺少适用范围/优先级，而既有实现和规格已将控制turn与普通交付分开。明确三选一turn结果、控制turn优先于普通JSON格式、不混交requirements/architecture/verdict，普通交付待后续规范路由；PM/ARCHITECT/REVIEWER最终格式限定普通handoff。解析器与异议错误不重试规则未放宽，端口/State/D14/D16未变。详细设计§2同步提示范围校准，44项针对性回归通过，真实advisory链和后续门禁验证中。v10永久保留5final/3pass/2fail/49pending，Go累计794请求/USD0.976656432，旧失败不重写；修复后另冻组并继续原USD5。

[2026-09-10 控制交付修复验证] 普通handoff/异议/Channel action的提示优先级已澄清，严格解析和异常不重试规则保持。44项针对性回归、真实Docker/Harness/MCP的advisory→再次REVIEWER→Leader gate链通过。两种Go模型各在真实Harness中先交独立concern、后续turn交approved verdict，共4请求/19.384秒/新增USD0.001108308，全部stop正常结束并结算；不发送Benchmark/holdout，仅合成answer42事实。两轮修复诊断合计14/16请求，原USD0.50池累计63请求/USD0.130668924。证据.data/evals/phase10-go-control-handoff-5e3b0185-c57d-44d8-bb24-dda008d91130与/private/tmp/agora105-go-control-handoff.log。typecheck/lint394文件通过；v11注册已纳入当前全回归，完整结果待收尾，不启动正式请求。

[2026-09-10 v11完整门禁] 控制交付提示修复后的完整pnpm test一次运行153文件1159项全部通过（0 skip，382.51秒），包括真实官方模型、Docker/Harness/MCP和既有回归；typecheck/lint394文件通过。日志/private/tmp/agora105-control-scope-full.log。官方账本2415请求/保守USD8.720205628/0未结算，原USD20不变。新Go合成控制验证两模型共4请求正常交付独立concern及后续verdict；与压缩验证合计14请求，诊断累计USD0.130668924。准备冻结v11，沿用原Go USD5（正式794请求/USD0.976656432），不覆盖v9/v10失败或快照。

[2026-09-10 v13阻塞异议提示修复] v13首项库存串行通过，57请求/USD0.075952584/437.628秒，独立11测试全过、清理lease0；并行项29请求/USD0.052523832后因TESTER阻塞异议停止，保留2final(1pass1fail)/16pending。实际unitPrice对数组类别发生JS键转换，原隔离镜像重现4pass1fail，TESTER断言符合题面；runtime正确执行D14。审查确认提示只说proven conflict而未区分实现缺陷与质疑需求本身，且未说明accept撤回目标，导致普通返工误升裁决。按常驻授权先保持停组，再补明确语义边界、三角色真实请求提示回归（RED3项、既有24项绿），不改解析器/裁决权限、不替模型修答案。Go累计1585请求/USD1.924144974/0未结算，原USD5不变。

[2026-09-10 v14准备] 按D14既有accept撤回目标语义修正通用提示：普通实现缺陷使用测试失败/changes_requested返工，阻塞异议明确质疑需求或决策本身。三角色实际Harness请求提示回归修正测试roster后再RED3项（既有24绿），修复后6文件67项通过；解析器/控制块/Leader权限未变。库存题已用于定位提示问题，v13保留2final/1pass1fail/16pending及费用，后续不继续；v14仅未运行thermal-inspection与daily-availability各multi/parallel/sparse×3，继续引用v11公开36项，不跨版本混算。热量题为合成整数换算/剂量/阈值任务，五模块宽DAG与预授权中途阈值变更；模型不获取隐藏测试/参考解。原GoUSD5不变，目前1585请求/USD1.924144974；无新增模型诊断。


[2026-09-10 v14断流审计] daily-availability-multi-1及parallel-1完整通过，随后sparse-1首个PM请求在96.811秒发生官方STREAM_CLOSED: SSE stream ended without [DONE]，0工具、输入估计4693，usage未返回。已立即停止剩余6项，供应商/网络细分根因inconclusive，无执行器缺陷证据，不重放。显式审计原预留峰价USD0.0589824计入原USD5，原result及model-requests仍保留unknown，不伪造用量。当前12final/10pass/2fail/6pending，Go2137请求/保守预算占用USD2.774879802（包含历史v4和当前v14两个预留上界），0未结算/锁、369执行源码hash不变。报告区分用量费用与保守预算占用，未知格不显示完整费用均值；同组仅恢复未启动6项。证据phase10-final-v14/stop-reviews/daily-availability-sparse-1；无新增诊断、无commit/push。


[2026-09-11 Task10.5最终Go对照收尾] v11公开36/36归档、33pass3fail（single12/12、multi11/12、mixed10/12）；v14内部18/18归档、14pass4fail（multi6/6、parallel4/6、sparse4/6），两组冻结源码不同，分别报告，不计算合并成功率。内部14次独立隐藏判题全部通过；4失败为thermal并行1重复读取人工停止、daily稀疏1断流、daily稀疏2长评审/返工后20分钟准入耗尽、daily并行3重复推理后断流。最后样本已设置后续请求停止标记，未取消在途流，直接失败原因保留STREAM_CLOSED，不伪称人工中断流。两次缺usage原始结果仍unknown，显式审计各USD0.0589824原预留上界，不重放、不补跑、不改源码/模型/限额。Go正式总2462请求/保守预算占用USD3.089997504/5，含所有历史组及3次预留上界；v14本组877请求/保守USD1.165852530，v11公开USD0.748955406。独立Go诊断65请求/USD0.131913528，修复请求16/16用满；官方原USD20回归保守USD8.820343188单列。最终369执行源码hash一致，18次cleanup通过、lease0、0active/0pending/0锁/0未核算预算项；官方会话压缩审计0触发/0失败/0未闭合，已有两模型真实压力压缩成功另列。G3/G4/G5沿用冻结前153文件1175测试全绿0skip及typecheck/lint、真实Docker入口/返工/D14/D16，不新增付费回归。最终报告docs/evals/phase10-opencode-go-final-report.md及public/internal-metrics.json含逐次结果、方差、四比较/机制覆盖、完整费用历史与限制。源码不变，后处理修正未知费用口径；无commit/push/PR，Task10.5保持in_progress，10.7产品出口独立。

[2026-09-11 T10.6实测修复] 连续两次需求更新暴露CODER只能看见最新动作和旧架构的缺口；按蓝图D9补齐currentRequirements系统切片，保持正式消息/安全点/验证指纹，先修复回归后才恢复同一演示任务，不把暂停前后不同源码拼称单版本成功。

[2026-09-11 T10.6终态回收修正] 完成归档后发现D4重建未登记历史集成树，终态dispose只清当前进程登记项；按详细设计§11.9补充仅限当前task规范工作树的回收登记，容器停止后先完整校验、再沿既有staging策略回收，不影响候选代码或归档内容，增加重建/异任务/外部路径与真实并行终态验证。


**[2026-09-11 T10.6 正常容量恢复]** Leader要求完整产品展示使用正常上下文容量。已核实DeepSeek V4官方/Go1M，设置页支持已知容量填入、手动覆盖和明确恢复；当前Go六角色通过正式API保存1M新连接版本，输出8192及旧任务绑定保留。22项定向+1项官方适配器容量读取、静态检查、生产构建、桌面/手机浏览器通过；完整回归仍待此前USD0.37费用授权。证据见https://github.com/logan-suu/Agora/blob/83fe416c1015d86616acdead7af2ebbad82e8384/docs/demo/task106-english-rehearsal-evidence.md；不将容量配置或功能实测宣称为1M长上下文质量结论。


**[2026-09-11 T10.6发布准备同步]** Leader取消临时费用硬上限，此前待USD0.37的完整回归授权条件已被替代。当前Go六角色恢复1M上下文/384K输出，设置页新连接和显式恢复使用已核实模型容量，保留手动设置、旧任务冻结连接及自动历史压缩。真实回归继续执行并单独记录用量，不修改历史Eval预算或结果；新预演和录屏尚未启动。验证结果见演示预演证据文档最新节。


**[2026-09-12 T10.6预演同步]** quote-demo-en-3已到最终Leader gate：38项累计+8项独立验收通过，900分生效，REVIEWER与验证绑定4b8e2cce；仍待Leader本次终审/归档刷新。预演发现并修复混合Markdown JSON围栏及Leader需求命令展示遗漏，160文件1214项0skip全回归、静态检查、构建、真实桌面/手机恢复检查通过。无新录屏，未标10.6完成，详细证据见全英文预演记录。


[2026-09-12 英文预演3完成归档] Leader明确批准后，16:11:18.718Z正式浏览器approve_completion applied，actionId11975959-c8e0-442d-a198-270ab7127a15；相同4b8e2cce提交进入done，gate清除，Leader满足事实true。归档9文件逐字节匹配；额外Docker复测64进程上限导致线程创建/SIGABRT，保留失败证据，仅将辅助上限调至256后38项及独立8项全部通过、0skip，无产物/测试断言/产品代码变更。刷新COMPLETED/38passed，task容器及linked worktree均0，正式stop/浏览器关闭，三个历史State不变；0新增模型调用。全英文业务闭环完成，当前没有新视频；T10.6保持in_progress待正式录制与交付，未commit/push。证据https://github.com/logan-suu/Agora/blob/83fe416c1015d86616acdead7af2ebbad82e8384/docs/demo/task106-english-rehearsal-evidence.md与私有audit/english-rehearsal-3/completion-summary.json。


[2026-09-12 正式录制1冲突与修复] quote-demo-en-recording-1两条需求更新均applied，TESTER 11/12识别旧票价。A/B返工同基线f6eb4009，A修900为b42d7746，B越界也改ticket.mjs为3286623c，真实Git冲突ticket.mjs；abort成功、无MERGE_HEAD、当前集成树clean，持久integration gate保留。B官方request/header确认自身B和范围[A,B]并存，触发assignment范围歧义；最小回归先红，收紧CODER subtaskIds并明确累计失败不扩大写入责任后42项通过。Leader要求修复后删除旧录制、重新录制，故不resolve当前gate、不继续旧任务；正式服务与录像已停止，原视频待验证结束后按授权删除，保留审计。后续完整回归和新录制结果另记。


[2026-09-12 assignment修复验证与重录] 完整160文件1215项0skip全部通过，500.81s，日志SHA b6cd3dd4ed056ba7a79bf7b1136e4ae5989f5ccadcbf1dbfa421d46925c48813；typecheck/lint/生产构建/G7通过。官方新增37请求USD0.021248580，累计354/USD0.229460768全settled。按Leader要求删除录制1的3个WebM（正式原片及副本、预检片），保留hash/失败State/session/审计，不resolve其gate。新录制quote-en-take-2于16:42:49Z正式202/running，独立audit/english-recording-2；当前418文件来源e167fc9341632709692064a8d230c7df50dfe4ce8f238863faab012d1585d8e3。辅助浏览器初始化第一次误把Node变量放入page.evaluate，启动任务前已修正（0模型调用），不涉及产品源码。新任务六角色1M/384K，PM保留English要求，当前运行与媒体未宣称完成，后续须本次Leader终审。


2026-09-12 录制3暴露发送中UI竞态：按钮未禁用、旧响应清空新草稿。真实浏览器已复现并修复Sending…/aria-busy、防重复提交及新草稿保留；失败重试msgId相同，浏览器绿及隔离生产构建通过，typecheck/lint通过，完整回归仍在运行。正式stop等待旧Go请求有界收敛，录像停止，验证后按Leader要求删除重录。两条需求已逐字段canonical核对，早先辅助脚本回执误判已更正。

[2026-09-12 Go等待恢复执行] Leader回复“那就这样继续”，批准先补诊断、恢复正常有界等待、小范围Go验证及完整回归，再英文预演与重新录制。opencode.ai SDK与idle统一300秒，normal最多2次重试不变，正常1M/384K不变。RED阶段3个180秒合法等待/360秒持续流案例失败，修复后6个等待边界测试通过，连同会话/推理兼容共3文件8项通过。诊断只记录时间、用量、脱敏状态，合成SSE分片/心跳/取消本地2项通过。完整回归、真实Go与构建待完成；未切换服务、未录制。

[2026-09-12 Go等待恢复验证完成] 161文件1221项0skip完整通过（398.08秒，日志SHA4eac3cdc4a35c81f30b39d310ea401a90dd9c9d29bb1ebd29f1421010e79b064），typecheck/lint/正式生产构建/doctor通过。新增时序观察器本地分片/心跳与真实HTTP强制GC取消验证通过。Go合成3次成功、两次工具调用；第三次响应头96.902秒到达，总96.947秒。官方新增16请求USD0.010728864，累计401/USD0.262861676；Go累计325请求已知USD0.502408080，历史2次未知保留。现开始无录屏全英文预演quote-en-rehearsal-4，1M/384K、Go服务不变；消息回执按本次display精确关联，逐次canonical复核。新候选须Leader终审。

[2026-09-12 Git环境指引修复与验证] 预演4停于TESTER反复搜索容器Git，并非新Go超时：49请求全部200、用量完整，USD0.094315980；后续3次由本地停查guard拦截未出网。MCP git_applyPatch/git_diff实际已授予，固定镜像无Git CLI，产品遗漏了Eval既有环境说明。已将MCP Git/禁止CLI探测/运行时核验HEAD指引共用于文件角色及波次验证；不扩权限、不新增循环。合成真实Go TESTER在Docker中6请求/6工具执行完成写测试、运行、MCP提交及diff，0shell Git，累计1项测试通过、干净HEAD；首轮夹具baseDir错误在付费前被正确拒绝，保留日志后修正夹具。完整161文件1221项0skip再次通过（384.56秒，SHA04226928a6c1424ab6fd19d5b13dab9fa865c19ad01f2184978d0d5073a00f5e），静态及正式构建通过。官方累计415/USD0.270997166；Go累计380/已知USD0.598734984，历史2次usage未知保留。源清单SHAc252bc63371d1ef931cd242e17c928338da158bd9253037faa47f80e5d11562f。预演4服务/浏览器已关闭、8份会话及State扫描0泄漏，无录屏无完成候选。接下来无视频预演quote-en-rehearsal-5，A/B真实并发请求发出后及时发送两条有序需求变更，逐字段核对canonical回执。

[2026-09-12 架构交接恢复修复] 预演5的5次Go请求全部200、USD0.012789288，无新超时；ARCHITECT将conventions嵌入architecture，通用格式修复两次仍失败。保持严格schema与原生两次无工具上限，增加受信角色静态outputFormatHint并由生产组合根接入，执行器不理解/自动修改schema，不传原异常文本。新增嵌套错误回归RED/GREEN，格式/重试27项通过；一份合成错误注入后，真实Go仅一次纠正即恢复双顶层字段并保留conventions值，USD0.000582。Go累计386请求、已知USD0.612106272，历史2次未知保留。预演5已正式stop、浏览器关闭，2份会话及State扫描0泄漏，无视频/候选。静态与正式构建通过，完整回归待收尾；下一轮无视频全英文预演6仅已准备，尚未启动。

[2026-09-12 格式恢复完整验证] 161文件1222项0skip完整回归通过（409.70秒），静态/正式生产构建与真实Go合成格式纠正通过，source/hash/计量见audit/format-hint-validation/result.json。测试配置可选字段的纯类型修正后另跑27项格式/重试测试通过，生产逻辑未变化。现在开始无视频英文预演quote-en-rehearsal-6，保持Go 1M/384K与300s等待；新候选仍须Leader终审。


[2026-09-12 T10.6 返工投影修复] 预演6独立验收发现ticketCost非法输入TypeError；Leader已批准返工，但实际A/TESTER上下文缺少其裁决理由、旧HEAD未变化。停止后续请求并正式停止服务，按D16/R2补齐已验证completionFeedback系统切片。4项新增测试RED→GREEN、相关50项通过，真实历史State本地只读投影确认六角色均收到理由；完整回归、正式构建及合成真实Go修复正在验证。原预演保留失败证据，不宣称产物已修复或录屏可交付。


[2026-09-12 录制4停止与验证范围修复] 首轮A保留旧1000价，原生测试发现后返工仅ticket.mjs修正为900；另TESTER在A/B波次提前写quote.test.mjs，C依赖A/B尚未实现，首次18测试3失败、修价后18测试1失败（缺quote.mjs）。实际官方会话含既有future-only-on-final规则及finalWave=false，不能称旧规则完全缺失；问题还包括模型未遵从阶段规则。读代码确认TESTER只见A/B编号及全量验收，没有当前/后续任务名称与依赖对照。已停止后续准入、让在途请求收敛，47请求全部结算；录像/浏览器关闭、服务正式停止。补齐validationScope及明确的阶段边界说明，现有有效保留成员测试先红后绿；新增D/C/T夹具最初缺canonical coding_wave被正确拒绝，补齐夹具后相关47项通过。不跳过或弱化旧断言，不直接修改演示State/产物。全量回归和合成真实Go验证正在执行，通过后删除失败视频并全新重录；不宣称提示能消除所有模型错误。


[2026-09-12 T10.6] 录制5暴露TESTER说明尾部agora-result JSON直出；Markdown AST识别完整合法对象/数组标签并默认折叠，保留原文及普通代码/畸形标签。真实历史消息默认收起/展开/刷新通过，类型/Lint/正式构建通过；完整回归162文件1229项无跳过，日志SHA 8ce6fcbea5c986f562a2480c5318ca6b5813f69558f3e20e67a4cff278004b75。录制5视频删除、会话及候选保留，新录制6沿用冻结源码清单。


[2026-09-12 T10.6用户体验修复] Leader确认自然语言需求变更→可读草案→显式确认；先实现服务端解释/不可变草案/防陈旧确认/原子关联更新和群聊控件，再完成真实Harness/并行中变更验证及完整回归，之后重录。旧录制6只保留诊断，不作为最终用户体验验收。


**[2026-09-12 T10.6 英文复杂度识别修复]** 英文多模块目标与中文“模块”使用相同的既有Tier2启发式：TIER2_KEYWORDS补齐module，沿用大小写不敏感的包含匹配，覆盖module/modules。不改变Tier阈值、DAG校验、Worker执行额度或已有complexity的幂等恢复；真实并行仍须合法独立assignment和Trace重叠证据。录制前必须把实际goal传入生产evaluateComplexity检查Tier2，不能凭题面或Architecture已给DAG推断入口会并行。


**[2026-09-12 T10.6 累计测试职责修复]** D17依赖波次中，ARCHITECT角色指令要求executionPlan只安排CODER业务实现，不另建测试、验证、审阅或批准专用子任务；每波集成后的独立validation TESTER负责累计验收测试，测试约定放conventions。创建新测试时遵循与当前波次兼容的路径约定；既有累计测试保持原路径，CODER/TESTER不得删除、改名、迁移或合并为替代文件，REVIEWER反馈不构成例外。仅与Architect建议布局不同、但当前需求及测试覆盖已满足的差异作为审阅comment，不以此要求破坏累计测试；Leader显式要求额外入口时采用保留原测试的追加方案，不兼容的计划走architecture反馈。既有删除检测、精确HEAD验证、累计测试和D16权威保持不变。 这是角色指导而非已实现的节点语义拒绝器；模型偏离按事实记录，出口不得仅凭节点名称拒绝合法DAG，仍严格检查权限和累计验收。
