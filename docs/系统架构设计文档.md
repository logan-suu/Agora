# Agora 系统架构设计文档

**版本**：v2.2
**日期**：2026-09-07
**状态**：已定稿（同步至 Phase 9 / D17 精确 linked-worktree 成员身份、不可变 artifact receipt 与补偿式资源生命周期；当前进度以 `docs/task-status.json` 为准）

---

## 1. 架构概述与设计哲学

### 1.1 架构定位
Agora 是一套**以人为中心的多智能体编排系统**。它不是单一的程序，而是一个**“可插拔的虚拟软件公司内核”**。

### 1.2 核心设计哲学（4 大支柱）
1. **上下文即工程（Context as Code）**：不依赖模型的“长上下文”记忆，而是通过显式的**投影（Projection）**在运行时构建 Agent 的认知边界。
2. **最终一致性（Eventual Consistency）**：不追求分布式强一致性锁，通过**可交换的 Reducer 合并函数**容忍并行乱序写入。
3. **数据驱动编排（Data-driven Routing）**：拓扑结构不写死在代码里，而是由 `Coordinator` 根据 State 中的 `complexity` 信号动态决定。
4. **防腐层隔离（Anticorruption Layer）**：核心业务逻辑（编排/状态）与外部依赖（LLM/沙箱/工具）通过 Ports & Adapters 彻底解耦。

---

## 2. 分层逻辑架构（4+1 视图）

自内向外分为四层，严格遵循依赖倒置（Dependency Inversion）：

| 层级   | 名称                          | 核心职责                                                     | 禁止行为                                                     |
| :----- | :---------------------------- | :----------------------------------------------------------- | :----------------------------------------------------------- |
| **L1** | **领域模型层 (Domain)**       | 定义 State 实体、Reducer 语义、RoleSpec 接口。**纯 TypeScript 类型与纯函数**。 | 禁止引入任何 I/O 库（fs, http, child_process）。             |
| **L2** | **编排应用层 (Application)**  | 实现主循环（Orchestrator）、路由策略（Coordinator）、并发控制（Preemptor）与 ChannelLifecycleService。 | 禁止直接调用 LLM 或 Docker API，只能调用 L3 定义的端口接口。 |
| **L3** | **端口抽象层 (Ports)**        | 定义 Executor、SandboxManager、TaskStateStore、ProjectChannelStore、ProjectCollaborationStore、MessageBus 接口。定义“系统需要什么能力”。 | 不包含具体实现；MessageBus 不修改 State 或保存 inbox。                    |
| **L4** | **基础设施适配层 (Adapters)** | **具体实现**：Harness Executor、LocalTemp Sandbox、JSON TaskStateStore/ProjectCollaborationStore、ProjectChannelStore 兼容视图、MCP Client、SSE 投递适配器。 | 可以依赖第三方库（Harness, dockerode, express）。            |

---

## 3. 物理模块结构（Monorepo 映射）

代码仓库 `packages/` 目录严格对应上述逻辑层级：

```text
packages/
├── core/                     # [L1 + L2] 核心不可变内核
│   ├── domain/               # State, Reducer, Entity (无副作用)
│   ├── orchestration/        # 主循环, Coordinator 路由, ChannelLifecycleService
│   └── preemption/           # 配合式抢占信号控制
│
├── runtime/                  # [L3 + L4 混合] 执行与隔离
│   ├── executor/             # HarnessExecutor, ExternalExecutor (抽象+适配)
│   ├── sandbox/              # SandboxManager, LocalTempSandbox (适配)
│   └── state/                # TaskStateStore 端口 + JSON 任务快照适配器
│
├── comm/                     # [L3 + L4] 通信总线
│   ├── bus/                  # MessageBus 接口 (Port：只投递已提交事件)
│   └── channels/             # ProjectCollaborationStore + JSON 适配器 + ProjectChannelStore 视图 + Channel/Inbox 读模型
│
├── tools/                    # [L4] MCP 工具服务器 (独立进程/适配)
│   ├── fs-server/
│   ├── test-server/
│   └── ...
│
├── roles/                    # [L1 数据] 角色定义 (YAML/TS 配置)
│   └── definitions/
│
└── shared/                   # 类型与常量
```

---

## 4. 核心组件交互序列（Critical Paths）

### 4.1 标准任务执行流（无抢占）
1. **Entry**：`Orchestrator.run()` 初始化 `AppState`。
2. **路由决策**：调用 `Coordinator.decide(state)`，返回 `{ kind: 'worker', batch: [Coder] }`。
3. **执行**：`WorkerRuntime.runOne()` -> 调用 `SandboxManager.createWorktree()`（Phase 0 由 `LocalTempSandbox` 返回本地临时目录句柄）-> 实例化 `HarnessExecutor`。
4. **上下文注入（关键）**：在 Harness 的 `agent/pre-step` 钩子中，**覆写（Overwrite）** `messages` 数组为 `Projection.project(state, role)` 的返回值。
5. **写回**：Executor 返回 `StepResult.mutations` -> `applyMutations(state, mutations)`（Reducer 合并）。
6. **循环**：回到步骤 2，检查 `testResults`；失败则路由回 Coder，通过后进入 REVIEWER。REVIEWER `approved` 只生成 completion candidate，Phase 8/D16 起必须经绑定本轮 review 的 Leader completion gate 才能 finalize；此前阶段保留历史退化行为供回归，不代表最终产品语义。

> **[2026-09-05 架构决策更新] D17 Phase 9 并行执行流：** Coordinator 先持久化带稳定 workerId 的拓扑就绪波次；assignment 的 scheduler 身份为 `{projectId,taskId,workerId}`，每个 assignment 异步向唯一 `GlobalScheduler` 取得 lease，WorkerRuntime 处理完整 batch 而不截断。各薄 Harness Executor/session 并行执行模型与工具；每个 step/end 进入同 task 的 canonical commit queue，重读 State 并验证 roster、subtask 可执行状态、依赖、persisted assignment 与 mutation 所有权后提交。`Subtask.ownerRole` 表示实现责任归属，不是独占执行权限；TESTER/REVIEWER 可合法处理 CODER 所属 subtask，角色授权以 Coordinator 持久化的 WorkerState assignment + enabled roster 为准。并行 agent step 只能 append 稳定身份事实；WorkerState 注册与 status/safePoint 生命周期 merge 由 WorkerRuntime 内部负责，不接受模型输出。当前 Subtask 全字段均属串行控制面，并行覆盖型 set 与任何并行 subtask merge 留给 Coordinator/专用服务。批次失败使用 all-settled 收敛，不硬杀其他 worker；join 排空提交队列并从 TaskStateStore 重载最终 State。9.3 绑定 workerId worktree 并集成，集成后只由一个 TESTER 产生权威结果；9.4 使用独立验证分支与可信 receipt 后写 testResults，见下文。

> **[2026-09-05 架构决策更新] D17 Web 组合根接线：** D10 的全实例单活动 run 只适用于 Phase 5–8。9.1 起运行注册表可同时保存多个不同 task 的 active run，生产 `TaskCompositionFactory` 闭包持有唯一共享 GlobalScheduler 并注入所有 WorkerRuntime；TaskStateStore 仍按 task 串行提交，canonical join 通过 scope load 重读持久真相。相同 scope 重试仍幂等，不同 goal 仍冲突；跨 scope 不再仅因存在一个活动任务返回 409。为防止在 worker lease 外无限预分配 Docker/worktree/MCP/Git/Harness，运行注册表另设默认 3 个 active executable composition 的生命周期 admission cap，满载时在 factory 调用前返回可重试 429；该 cap 不授予 worker 执行额度，实际 worker 仍由唯一 GlobalScheduler lease 与项目轮转控制。D8 单实例部署边界不变。

> **[2026-09-07 架构决策更新] D17 worktree 与集成流：** L4 组合根以一个复合 workspace adapter 实现冻结的 `SandboxManager`/`RecoverableSandboxManager` 对外端口，内部协调 Git 与 Docker 两种 capability：将受信 seed 物化到任务 `.data` 内的 canonical repository，对逻辑 `{projectId,taskId,workerId}` 做确定性 Git-safe 防碰撞编码，创建真实 linked worktree，再把同一规范路径挂载为容器工作目录。编排层不得分别请求 Git worktree 与 Docker 目录，也不得以用户 checkout 为主仓或集成目录。指定 canonical 目录须证明本身为 Git root，向上发现父仓库不算初始化完成。State 持久化结构化 `WorktreeRef`；集成服务在专用 integration worktree 依稳定顺序合并，并以可恢复 `Integration` 快照记录 base、分支身份、进度、冲突和结果。冲突收集后必须 abort merge，再打开 `integration_conflict:<integrationId>` Leader gate；Phase 9 仅接受命中冲突 worker 的 `request_rework`，随后从记录 base 重建集成。

> **[2026-09-07 架构决策更新] D17 任务 9.4 波次闭合：** L1 新增可选 `ParallelExecution` 串行控制快照与严格 `ExecutionPlan`/`WaveValidationReceipt` 校验（详细设计 §3.1）；L2 Coordinator 锁定显式 DAG、当前 wave/attempt、共同 base 与稳定 dispatch，L4 通过 companion capability 从指定 base 创建 worker linked worktree，冻结六个 SandboxManager 方法不变。旧 modules 只作显式顺序链降级，invalid DAG 不静默降级。新 wave 的 base 来自上一条 accepted validation receipt，不能重新从 canonical 初始 HEAD 开工。
>
> **[2026-09-07 实测校准]** 累计复验 dispatch 以 sourceReceiptId 绑定前一可信验证 HEAD，保留 TESTER 的累计提交；L1 校验严格向前的同 wave/attempt/Integration 来源，L4 复核源证据及 Git HEAD。首次验证才直接从冻结 Integration.resultCommit 开始，复验不修改 Integration。

> Integration.done 只证明合并：合并 worktree/resultCommit 冻结，单一 TESTER 从 resultCommit 创建独立 validation worktree，提交测试并对精确 HEAD 运行真实沙箱测试。可信验证接缝将当前 dispatch/commit/需求决策指纹与执行证据原子写 receipt + testResults；Coordinator 据此一次闭合 wave 并推进 accepted receipt。最终 REVIEWER 只读累计验证产物、可引用全计划 subtask；D16 与最终 artifact 主目录绑定同一已测试 HEAD。TESTER/REVIEWER 不能覆盖 CODER 的 Subtask.worktree。可信验证固定使用现有 Node/TAP runner，当前 preparation 关闭；代码、测试和 receipt hash 在评审/归档前复核。当前 wave 分支引用与最终累计审阅索引是不同读模型，后者不扫描历史工作区。
>
> REVIEWER refs 返工重开目标及全部传递后继（含 done），缺 refs 才显式全量；坏 refs fail-closed。worker 失败保留成功分支且有界重试，merge conflict 固定原 base、由 Leader request_rework，旧 Integration 与替换身份绑定在单条 Leader resolution receipt；测试失败的新 attempt 可从失败验证提交修复但不得推进 accepted 基线。无就绪任务却有未完成任务时保留 needs_attention，不伪造完成。D4 恢复依当前 wave/dispatch 和 per-worker receipt，不能扫描历史 pending/done worker 重新派发；详细转换与旧快照兼容以详细设计 §3.1 为准。

> **[2026-09-07 审查修复]** root-cause 定向返工的规范 coding_wave 通过 reworkSourceMsgId 引用 review_rework；L1 从来源失败 receipt 推导未返工成员的累计验证范围，L2 仅派发所需 CODER，验证通过后闭合包括保留成员的任务。新波次不重复闭合已验收成员；单 CODER 的执行失败与多 CODER 使用同一有界重试，持久化/资源释放失败仍上报。L4 验证证据写入与 State receipt 提交之间的恢复窗口，以校验并复用首次证据闭合，不重跑或覆盖首次观察。architecture issueScope 显式走全计划重设计，规范 replanSourceReceiptId 保留累计 HEAD，L4 先校验来源证据再由 L2 采纳新计划。

### 4.2 配合式抢占流（Leader 中途改需求）
1. **入口预校验**：UI 发送带 `projectId/taskId/channelId` 作用域的 Leader 输入（浏览器只提交满足 `[A-Za-z0-9][A-Za-z0-9._:-]*` 的稳定 `msgId` + `display`，不自报可信意图） -> HTTP 边界先拒绝非法 msgId -> 服务端解析并构造“唯一 Leader 主消息 + 非 messages 控制 mutations”。Phase 6 起先从 `ProjectChannelStore` 校验 Channel 存在、sub 任务绑定、participant 资格与未关闭。普通 chat 仍直接按 D6/D9 提交；Phase 9 需抢占的需求/决策/优先级动作先只保留已校验计划，在安全点屏障完成前不写 State。
2. **task-scoped pause epoch（Phase 9）**：`Preemptor.requestPause()` 以稳定 `actionId` 建立任务串行的 pause epoch，固定接受请求时的 active worker cohort，并禁止该 task 再启动 Step。它不是全进程可覆盖的 `paused=true`。同参数重放共用结果，异参数冲突；另一 epoch 等待当前项闭合。尚排队的 acquire 取消后保持 worker `pending`，不记为失败。
3. **安全点屏障（Phase 9 多 worker）**：已进入模型/工具流的 Worker 经可选 SafePointExecutor 请求，在当前 Step 自然结束后的下一 `agent/pre-step` 拒绝 proposal，官方 loop 闭合 turn，再**提交 canonical mutations**，再 flush checkpoint，最后由 WorkerRuntime 更新自身 `WorkerState.safePoint/status`。每个 `safePointRef` 必须指向已 flush 的持久 source session 与已闭合 turn boundary，仅有进程内 sessionId 不算 checkpoint。自然完成的 worker 保留 done，失败保留 failed，不被 paused 覆盖。
4. **非阻塞 reproject**：屏障完成后，应用服务在 per-task canonical queue 重读 State，将第 1 步的 Leader 主消息与控制 mutations 交给一次 `TaskStateStore.commit()`，reducer 原子写入并持久化。提交成功且非幂等重放后才 `MessageBus.publish(MessageCommitted)`；SSE 只推不含 payload 的展示信封。各 paused worker 注入最新 D1 投影并由 WorkerRuntime 恢复自身状态，关闭 epoch；原 context/session/lease/composition admission 全部复用。
5. **D4 humanGate suspend**：gate 生产者只返回瞬态请求；第 3 步的全部需恢复 worker 引用与 canonical WorkerState 一对一验证后，才在同任务串行区一次持久完整 `{gateId,reason,options,phase,openedTs,safePointRefs}`。gate 落盘后先交还已静止 worker 的 GlobalScheduler lease，再销毁进程内 executable composition/Harness context 与可重建资源。冻结的 `SandboxManager.teardown()` 会移动 worktree，暂停时只可用 `RecoverableSandboxManager.suspend()`。TaskState、worktree 与 session 保留，不做终态归档。释放 lease 失败或 composition 清理失败均保持 `needs_attention`；后者继续占用 composition admission 直到清理成功，但不可重新占用已静止 worker lease。
6. **D4 resolve + 多 worker 真 Fork resume**：Leader 经 `POST /api/messages` 以稳定 `msgId=actionId` 提交绑定 gate 的 `/resolve-gate`；同一 Task State commit 追加 canonical resolution receipt、reason-specific mutations 并清 gate。Phase 8 单 worker `resumeSessionId` 保持兼容；Phase 9 多 worker receipt 增加按 workerId 稳定排序的 `{workerId,sourceSafePointRef,resumeSessionId}[]`，每个 child id 由 actionId+workerId 派生并与 paused WorkerState 交叉验证。新 composition 在全新 Harness context 中通过 `ctx.agents.create({sessionId,seed,meta:{parentSession,seedLength},...})` 或严格复核后的 `ctx.agents.resume()` 创建/接管各 lineage child；`done/failed` 不重开，未启动 `pending` 不伪造 lineage。全部 child、D1 投影与稳定 `human-gate-resumed:<actionId>` 控制事实就绪后才恢复 running，每个 worker 执行前重新排队取得新 lease；旧 lease 不跨 Fork。直接 `ctx.sessions.fork()` 仍禁用。

> **[2026-09-06 架构决策更新] D9 Phase 9 指令边界：** `/requirement <requirementId> <JSON>` 以完整 `{story,acceptance,nonGoals}` upsert 未撤回 Requirement；`/decision <topic> <JSON>` 以完整 `{decision,rationale,supersedes?}` append `leader-decision:<actionId>`，覆盖现有 topic 时必须显式 supersede 其唯一 current Decision；`/priority <subtaskId> <0-100>` 只更新存在且未 done 的 subtask。三者只允许 main Channel、非 done、无 active humanGate，所有 id/topic 与 msgId 均遵守 D9 安全 token。它们先预校验单主消息与控制 mutations，空 cohort 可立即越过屏障；屏障后再在 canonical queue 重读并一次 commit。同 actionId 重放必须交叉验证消息与状态效果，异参数或事实漂移冲突。提交后的 D1 投影向所有角色加入从规范 applied action 派生的 `leaderDirective`，只含归一化数据与引用，不含 display/raw log。

> **[2026-09-05 架构决策更新] D16 完成裁决流：** 当前 REVIEWER dispatch 后唯一的 `approved` verdict 是 `completionCandidate` 的稳定触发事实；其 id 必须满足 `[A-Za-z0-9][A-Za-z0-9._:-]*` 且不得跨轮复用，Coordinator 以该 id 打开 `completion_confirmation` gate。Leader 的 approve/request-changes 继续走上述 D4/D9 链，并在 resolution commit 中写规范最高权威 completion Decision；resume 后由 Coordinator 交叉验证同一 review 的 Message/Decision/receipt。approve 才令 `isRequestSatisfied=true` 并 finalize；request changes 保持 false、重开返工并派 CODER。旧 review resolution、损坏 receipt 或缺 Decision 一律 fail-closed。
>
> **[2026-09-05 实装记录] 8.5：** D16 已落在领域 completion resolution、Coordinator、human-gate resolver、Leader intent/MessageRuntime 与 Web task lifecycle 的既有分层接缝中，没有新增旁路端口。恢复组合根将 resumed marker 提交后的最新 State 交给全新 run；真实出口链证明等待期不归档、request-changes 生成新 review/gate、approve 后才归档并释放资源。Decision reducer 同时拒绝非 current 或跨 topic 的 supersedes。
>
> **[2026-09-03 实装记录] 8.1：** 上述步骤 5–6 已在 Web composition root、HarnessExecutor 与 LocalTemp/Docker recoverable sandbox adapter 落地；测试覆盖进程内资源释放、全新 context lineage 恢复、重放/冲突 fail-closed、离职接任续办及不误归档 `needs_attention`。

> **[2026-09-03 架构决策更新] D9/D4 阶段边界：** 上述 D4 gate 序列在 Phase 8 只覆盖已到达安全边界的回合制单任务；task-scoped pause epoch、多 worker 屏障、非阻塞 reproject 与 GlobalScheduler lease 对接属 Phase 9。Phase 5 的 Leader 输入统一经 `POST /api/messages`，只执行经校验的开头单一 `@ROLE` 定向激活（消息 + `nextRole` 在一次提交中落盘）与普通 chat；Coordinator 只消费最新 applied assignment 一次，追加 `sourceMsgId` 确认事件并经 Coordination Ledger 把 instruction 投给目标角色。其余意图显式 `rejected/deferred`。独立 `POST /api/commands` 临时投递不得绕过 State。

> **[2026-09-03 架构决策更新] Phase 7 离职 drain 边界：** Phase 7 不调用上述 task-scoped pause epoch 的 `requestPause()/complete()/abort()` 生命周期。`/role remove <ROLE> [to <SUCCESSOR>]` 由同一 `POST /api/messages` 入口生成稳定 actionId，先以 collaboration CAS 把目标置为 `departing` 并阻断新工作；离职服务随后只等待该目标的活动 Worker 完成当前 Step、提交 mutations、保存 safePoint 并退出。没有活动 Worker 时立即 quiescent。系统从重新加载的任务 State 确定性固化交接，不额外启动 LLM Step；目标不 resume，其他顺序工作在交接与责任事实提交后继续。全局广播、inbox 注入、terminate/fork 与 resume 仍分别遵守 Phase 8/9 边界。
>
> **[2026-09-01 架构决策更新] D10 Web→编排桥接：** 新任务创建/启动是组合根的生命周期入口，不是 D9 的任务内消息旁路。单实例运行注册表按 `projectId/taskId` 保存任务摘要，但 Phase 5 全实例最多一个活动 run；另一 scope 已运行时返回容量冲突，避免 GlobalScheduler/Phase 9 并行能力抢跑。组合根复用既有 `runOrchestration()`、HarnessExecutor、沙箱与 L3 端口。编排安全边界产出的 State/消息必须先由 TaskStateStore 持久化，再发布 MessageCommitted→SSE；Web 层不得直接写 State，也不得用静态种子消息补画面。终态产物先归档到任务耐久目录并写回引用，再释放 Harness/MCP/Git/Docker 资源；最终 State/消息/产物支持刷新恢复而不依赖活容器。活动 run 的进程重启自动恢复不在 Phase 5 范围，但中断后不得误报 active。

### 4.3 Sub Channel 生命周期流（Phase 6）
1. **可信入口**：Leader 通过 `POST /api/messages` 提交稳定 `msgId` 与确定性 `/channel open|close` 文本；Worker 只可在最终回复末尾输出一个严格 channel-action 控制块，Harness 校验并剥离 display 后以同一步 assistant msgId 绑定 actionId，通过既有 `StepResult.output` 在 Step 安全边界交付。open 控制块可显式给安全稳定的 threadId，但浏览器/模型不得自报 actor/actionId；组合根绑定 actor，畸形控制块 fail-fast。纯控制回复保留审计 Message，并生成确定性非空 display。
2. **预校验与标准化**：Leader 入口在同任务串行区先检查持久 State 的重复 msgId，命中即在任何 Channel 副作用前返回。随后加载项目 Channel 快照，校验 actionId、task、enabled roster、topic、关闭权限；participants 作为集合标准化为 leader 在前、其余按 roster 顺序。同 `taskId+threadId` 的同语义 open 和重复 close 为 no-op，冲突语义明确失败；channelId 用 taskId 长度前缀编码消除分隔符碰撞。
3. **项目级事实提交**：`ChannelLifecycleService` 按 project 串行执行 load→校验→`ProjectChannelStore.commit(expectedRevision)`；Channel 不进入 AppState Mutation，不修改冻结端口签名。sub 持久化 `threadId/topic/createdBy`，main 永不关闭。
4. **任务消息补齐**：Channel 提交成功后，原 Leader 消息或稳定 lifecycle announce 才经 MessageService 写入 TaskStateStore 并按 D6 发布。此处是两个真相源之间的可重试 saga，不假装跨文件原子事务；消息失败不回滚 Channel，同 actionId 重试只补消息。
5. **角色上下文派生**：6.3 的 `ChannelContextBuilder` 从 `ProjectChannelSnapshot + AppState.messages + role` 读时派生上下文，按 project/task/participants default-deny；只输出不含 display 的类型白名单结构化条目。每群的 `{entries,omittedRefs,omittedRefCount}` 共用 4000 字符动态消息预算：保留较新完整 entries 后，只在剩余空间保存最近的旧 refs，并以计数显式记录全部遗漏。Channel 不持久第二份 `localContext` 消息索引。
6. **关闭后处理**：6.2 close 置 `closed=true` 并阻止后续 sub 发言；6.3 对尚无 `bubbledSummaryRef` 的 closed sub，经 L3 `ChannelSummaryGenerator` 端口复用无工具薄 Harness，从该 sub 的全部白名单结构化条目生成 `ChannelSummary`。输入按完整 entry 装箱，超大单条用带 ref/序号的结构化 fragment；chunk 只可引用本块来源，中间输出有界，并在每次输入不超过 4000 字符的前提下多轮批次归并。以稳定 `channel-bubble:<channelId>` 先经 D6 写入 main，再以 revision CAS 只保存 MessageRef。main 消息是唯一摘要内容真相；恢复时先复用该消息再补引用，reconcile 返回前重新加载规范 State。Phase 8 才实现 thread 的 humanGate 裁决与 resume。
7. **发现与切换**：`GET /api/channels?projectId&taskId` 从持久 Channel 快照按字段白名单派生唯一 main + 当前 task sub 的展示元数据，不返回 `bubbledSummaryRef` 指向的内容或消息 payload。UI 初始化与动作完成时刷新一次，仅在 active run 持续短轮询；Channel 刷新错误独立呈现并在恢复后清除。选中群后复用既有 channel-scoped SSE/POST；不新增 Channel 事件总线或进程内列表真相。

### 4.4 Trace 只读派生流（Phase 8 / D15）
1. Web `GET /api/traces` 先经 `TaskStateStore.load(scope)` 验证任务存在与安全 scope，再调用 L3 `TraceReader`；Trace 不参与任务 mutation、MessageBus 或 Channel SSE。
2. L4 `HarnessTraceReader` 通过锁定版本官方 JSONL persistence 的 `list()/inspect()` 读取已完成格式/envelope/surface 校验及开放尾部恢复的 session header/events，不直接解析后端物理文件，也不持久第二份 trace；Trace 读侧另行验证跨事件 turn/step/tool 生命周期，合法开放尾部可见，闭合父节点、序号漂移或不成对子项 fail-closed。官方 crash-repair 的 `TOOL_NOT_STARTED` synthetic result 表示工具未实际启动，是无 `tool/call` 的唯一合法例外且不投影为工具调用。
3. 读侧按 `agentPreset` 绑定角色，以 `parentSession/seedLength` 校验无环 lineage；自环或多节点环拒绝。child 仅投影 seed 边界之后的 native 事件，父历史不重复。
4. DTO 只白名单投影 turn/step/tool 的身份、时间、状态、工具名与错误码；prompt、projection、assistant/reasoning 内容及工具 arguments/results 全部丢弃。响应有界并显式报告 `omittedEventCount`。
5. UI 在现有右侧栏按 role/session→turn→step→tool 展示；运行中短轮询，终态最终刷新。日志损坏、lineage 不一致或查询越界独立报错并 fail-closed，不以静态示例替代。

**[2026-09-07 架构决策更新] D15 9.4 并行泳道：** 继续只消费同一有界 GET 的官方 JSONL 白名单 DTO，以 session lineage 的根 sessionId 作为稳定 laneId；同 role 的不同根 session 分属不同泳道，D4 child 留在原 lineage 泳道并标记 Fork。横轴使用实际 turn/step 起止时间，开放尾部明确 running，省略事件显示 omittedEventCount；没有 session/事件只能显示等待或暂无记录，不把它画成执行。role/session/parent 足以渲染此视图，不复制 State worker 状态为 Trace 真相、不从 prompt/cwd 推断 workerId、不新增 trace SSE 或第二日志。真实 Web E2E 必须证明至少两个 CODER 泳道时间重叠与刷新后恢复，窄屏可横向滚动。

---

## 5. 数据一致性模型（并发控制）

### 5.1 Reducer 语义（State 写入）
由于多个 Worker 并行执行，**获准由并行 worker 提交的 mutation** 必须满足交换律（Commutative）和幂等性（Idempotent）；这不等于允许所有字段并行多写者更新。

- **Append-only 字段**（`messages`, `objections`）：按身份键（`msgId`/`id`）去重追加，无身份键值深相等兜底（集合并语义）；时间戳排序留给读侧投影，乱序不影响最终事实追加。`Objection` 不原地修改 resolution status，未决视图从异议事实与规范 Leader 裁决消息派生；同一个异议的结构化事实与同 id 可见 Message 必须在一次 Task State commit 中原子追加。
- **D14 快照升级**：L4 `JsonTaskStateStore` 读取缺少 `objections` 的旧任务快照时补为 `[]`，使 D4 重启恢复可安全进入 Coordinator；已存在的非数组值视为损坏并 fail-fast。已持久的同 id 异议重放按不可变事实先判幂等，不因其 target 后续被 supersede 而失败。
- **[2026-09-04/05 架构决策更新] D14 裁决一致性**：objection resolution 是同一 Task State commit 内的规范 Leader 消息 + 确定性 `authority='leader'` resolution Decision + 可选 target effect。blocking accept decision-target 以 `supersedes` 撤回当前 Decision；blocking accept requirement-target 以 `withdrawnByDecisionId` 撤回 Requirement；reject 与 advisory 裁决不改变 target。`deriveObjectionResolutions()` 读时交叉验证三类事实，重复/缺失/漂移 fail-closed；`activeRequirements()` 排除已由规范 resolution Decision 撤回的 Requirement，旧快照缺字段视为 active。Decision 的 supersedes 只允许指向同 topic 的 current 项，阻止历史分叉与跨事项覆盖；D14 resolution Decision 继承目标 topic。blocking 使用绑定 gate 的 `/resolve-gate` 并在清 gate 后走 D4 resume；advisory 使用 `/resolve-objection` 直接裁决。两者的非空自由文本 rationale 只作持久内容，不参与 ID/路径/命令；重放必须复核 resolution Decision 与 target effect。`project()` 向所有角色注入已验证的结构化 `objectionResolutions` 系统切片，不投 Leader display/raw log，保证 resume 后的实际执行者获得裁决事实。
- **[2026-09-05 架构决策更新] D16 完成裁决一致性**：completion resolution 与 objection resolution 一样使用“规范 Leader 消息 + 确定性 Leader Decision + D4 receipt”三事实闭合，但绑定的是当前 REVIEWER verdict。`deriveCompletionResolution()` 是 Coordinator 与 Progress Ledger 的唯一读侧；approve 才输出 `isRequestSatisfied=true`，request changes 与未裁决状态均为 false。重复、跨轮或事实漂移 fail-closed，不能只看 display、phase 或单个 boolean 猜 Leader 意图。
- **Merge-by-ID 字段**（`workers`, `subtasks`）：按 `id` 进行字段合并；D17 并行路径下 WorkerState 仅由 WorkerRuntime 内部按当前 worker 身份分区注册或更新生命周期字段，不接受并行 agent step mutation；不同 worker 分区可交换，同一 worker 内仍按程序序提交。当前 Subtask 全字段（含可选的 0–100 整数 `priority`）均属拓扑/所有权/集成控制面，只能由 Coordinator 或专用服务在任务串行区更新。旧快照缺少 priority 时按 `0`，依赖满足优先于排序，同一就绪集合按 priority 降序、稳定 id 破平局。
- **Set 字段**（`phase`, `testResults` 等）：覆盖更新本身不具交换律，禁止并行 worker 输出；仅 Coordinator/专用服务可在任务串行控制面提交。集成完成后只允许一个 TESTER 产生权威 `testResults`。
- **D17 canonical commit join**：模型/工具调用可并行，但 step 输出处理、权限校验、Channel 副作用与 TaskStateStore commit 按 task 串行。每次进入提交区重读最新 State；批次 join 等所有已启动 worker settled 和提交队列排空后再 load canonical State。禁止合并多个完整 AppState 快照、从 Promise 结果数组选择“最后一个”或依赖 Promise.all fail-fast。

**[2026-09-07 架构决策更新] 9.4 状态原子性：** parallelExecution 仅由 Coordinator/验证服务经 applyMutations + canonical commit 更新；activeWave 与 assignment/激活事实同 commit，validation receipt 与全局 testResults 同 commit，wave 闭合与 acceptedReceiptId 同 commit。receipt 为不可变规范 Message，纯 TS 类型断言、旧结果文件或未经实际命令验证的 passed 不可作证。需求/技术决策变化使旧验证与评审失效，D16 completion Decision 自身排除出控制指纹以避免自我失效。所有模型 set 仍禁止，单一 TESTER 的结果由可信服务验证后提交。
- **真相源分离**：State 只存 `fileRefs`（路径+行号）、结构化 `WorktreeRef{path,branch,baseCommit,headCommit?}` 与可恢复 `Integration` 引用；不保存代码全文、patch 副本或第二份工作目录。文件实际内容存在于任务所属真实 Git linked worktree 中，Docker 执行绑定同一路径。
- **读时拉取**：Agent 需要代码内容时，通过 `fs.read()` MCP 工具**实时拉取**，绝不依赖 State 中的缓存副本，防止版本漂移。
- **兼容迁移/恢复**：Phase 0–8 的字符串 worktree 快照只允许兼容读取；进入 Phase 9 文件并行前，必须从任务 canonical repository 的真实 Git 元数据查询 branch/base/head 并原子升级。除规范 task root 与 `git rev-parse --git-common-dir` 外，还必须从 canonical repo 的 `git worktree list --porcelain -z` 取得唯一记录，逐项精确匹配规范 path、branch ref 与 HEAD；canonical 主工作区、独立仓库、错误 linked worktree、路径或元数据漂移均 fail-closed，禁止根据字符串猜测。
- **集成幂等**：`integrationId` 与输入集合稳定，持久进度只能是有序 pending 的完整身份前缀。Git 已成功而 State 回执遗失时，仅接受 next branch 的一个可证明步骤：fast-forward 到 branch HEAD，或父提交精确为 previous/branch HEAD 的双亲 merge commit；目标已含后续 pending 或额外提交时拒绝补记。冲突必须确认 `merge --abort` 成功且真实 Git metadata 不含 `MERGE_HEAD` 后才持久化，不能吞掉 abort 错误或继续复用污染的 integration worktree。
- **创建/回收原子性**：worktree add、Docker bind 与 adapter remember 视为一个带补偿的生命周期；中途失败只清理本次创建物并同步 prune metadata/删除临时 branch。终态移动文件到可恢复 staging 后也必须清除 canonical repo 的 worktree admin entry 与临时 branch，保证相同稳定 identity 可重试。
- **终态归档幂等**：首次 artifact 物化持久化不可变 source→archived path receipt；重试先严格校验并复用该 receipt，不得从已改写 State 重算目标映射。目标存在但 receipt 缺失/损坏、路径越界或引用不存在时保持源 worktree 与 `needs_attention`，禁止继续回收。

### 5.2 文件系统一致性

**[2026-09-07 架构决策更新] 9.4 文件角色：** CODER 分支、冻结 integration 分支、可写 validation 分支各有明确生命周期，全部是同一任务 canonical repo 的真实 linked worktree，Git 与 Docker 始终指向相同路径。它们是不同提交角色，不是 Git/Docker 各存一份目录。验证 HEAD 成为累计基线；REVIEWER 只读，终态主产物归档 accepted validation worktree。历史 validation receipt 的 source 引用保持不可变，通过 artifact receipt 解析归档路径；新状态中的可变引用仍一次串行重写，任何证据或映射缺失都保留源资源并 fail-closed。
- **不持久派生索引**：Channel 注册表只保存生命周期、participants、任务绑定与可选 `bubbledSummaryRef`；inbox/ChannelContext 均从注册表 + Task State 读时重建，避免每条 sub 消息跨两个 JSON 文件双写。
- **摘要唯一真相**：结构化 `channel_summary` main 消息是摘要内容的唯一事实；Channel 只保存 `{taskId,msgId}` 引用，不复制字符串。
- **message-first/ref-second saga**：稳定 msgId 先经 TaskStateStore first-write-stays 提交，再经 ProjectChannelStore revision CAS 补引用。崩溃、投递失败或 revision 冲突后必须重新读取规范消息并有限重试，不得重新覆盖摘要或伪造跨文件事务。
- **恢复触发**：close 成功后与任务运行时恢复入口都运行 closed-without-ref reconciliation；不得依赖进程内 Map、常驻扫描器或活 Harness 才能发现未完成冒泡。

### 5.3 Channel 跨存储一致性

### 5.4 项目 roster 与 Channel 一致性（D12）

- **单一项目协作快照**：`ProjectCollaborationSnapshot={projectId,revision,roster,channels}` 是 roster 与 Channel 的共同持久真相；`AppState`、进程内 `DEFAULT_ROSTER` 与独立 `channels.json` 都不得成为第二份运行事实。
- **定义/成员分离**：`RoleSpec` 描述能力，`RosterEntry.status` 描述 `enabled | disabled | departing | departed` 生命周期；可选 `departure` 保存稳定 action/task、接任者、阶段与 handoffRef。所有普通路由、执行、agent 发言和新建 sub 都只接受 enabled；departing 只允许离职服务完成受限 drain，其中已登记活动 handle 的当前在途 Step 可经 role-bound 接缝提交 mutations 后保存安全点，但不得启动新 Step、执行额外 Channel 副作用或借此恢复普通发言；持久 roster 是必需权威源，缺失时 fail-closed。
- **原子成员变更**：add/enable/disable/depart 的每一步都在同一个项目串行区重算 main participants，并以一次 revision CAS 提交 roster+channels。离职跨任务 State 时使用 `departing CAS → step/end drain → TaskStateStore 幂等交接/责任 commit → departed CAS` 的可恢复 saga，不声称跨文件原子；重放入口先以持久 departure 阶段判断是否续办，再处理已有 Leader Message，内部 handoff id 冲突或交接/责任事实不完整时 fail-closed。既有 `ProjectChannelStore` 仅作为兼容视图，公开签名不变。
- **孤儿职责阻断**：存在未完成 subtask 且没有 enabled 接任者时，Task State 写入 handoff/humanGate 并把原 subtask 标 blocked，roster 保持 `departing/awaiting_replacement`；升级人工不等于责任已经转移，禁止进入 departed。只有职责全部完成或已转移后才能 `departed/completed`。
- **历史可读**：sub participants/createdBy 校验“项目已知身份”而不是“当前 enabled”；非 enabled 历史成员不能发送新消息或启动 worker，但其旧消息、关闭群和冒泡摘要仍可恢复。
- **迁移 fail-fast**：`channels.json → collaboration.json` 只做一次确定性迁移；正式快照写入后必须从正式路径重新读取并核对，再删除 legacy。两文件并存仅在 Channel 内容等价时由 initialize/load 幂等完成恢复清理，否则拒绝启动，禁止默认覆盖或长期双写。
- **任务接手不跨存储（D13）**：项目 `addRole` 只提交 collaboration；任务 `/role onboard` 只读取最新 enabled roster，并在单个 Task State commit 中提交符合 D9 安全 token 语法的稳定 Leader 主消息、`nextRole` 与规范离职 handoff Message 引用，不包装成跨 collaboration/Task State 事务。`onboardingContext` 从持久 Task State 的 applied onboarding 事实读时派生，不依赖进程内 Map、Channel 裁剪窗口或活 Harness；引用事实不完整、跨任务或重复认领时 fail-closed。同 ID applied 重放返回前重新校验既存主消息、完整 receipt 与当前 State 引用事实，不能把损坏数据当作幂等成功。onboard 提交与 Worker 消费之间若 roster 失效，使用既有 `required_role_unavailable:<role>` humanGate 收敛。

---

## 6. 错误处理与韧性（Resilience）

| 异常场景                           | 处理策略                                                     |
| :--------------------------------- | :----------------------------------------------------------- |
| **Harness LLM 调用超时/限流**      | 复用 Harness 的 `agent/request-error` 扩展点，执行 `{ kind: 'retry' }`，采用指数退避（Exponential Backoff）。 |
| **沙箱测试无限循环（死循环）**     | SandboxManager 强制设置 `timeout`（默认 30s），超时则杀死子进程，返回失败测试结果。 |
| **编排死循环（Agent 反复改不对）** | `iterationCount` 硬上限（默认 8），超限后 Coordinator 强制置 `humanGate`，升级给人。 |
| **humanGate checkpoint/fork 失败** | 任一 session 未 flush 时不得提交完整 gate；boundary 未闭合、作用域不匹配、source 缺失或 fork 失败均 fail-closed，run 保持 `needs_attention`。裁决前由 active gate、裁决后由 canonical resolution receipt 保存 checkpoint refs，同 actionId 可从事实断点重试。 |
| **humanGate suspend/resolve 竞态** | gate 可见后 resolve 可以耐久提交，但 resume 必须等待旧 run 的 suspend 收尾完成并重读 State；不得因旧注册项仍为 `running` 丢弃恢复。gate 已提交后的释放失败保持 `needs_attention` 且禁止终态归档。 |
| **终态重试与后续 humanGate 交错** | 归档/回收失败保留 pending finalization；原 composition 仍在时允许显式重试。若其后完整 humanGate suspend 已释放 composition，普通 start 只返回 `needs_attention` 并保留 pending/error，不创建依赖缺失 composition 的 finalize promise；仅 canonical gate resolution 可进入真 Fork 重建。 |
| **恢复资源或身份校验失败** | composition 重建失败释放本次已取得的全部资源；既有 child 同时校验 header 与 source seed 事件前缀；sandbox worktree 必须属于当前 task 的规范 root，跨 task 引用 fail-closed。 |
| **worktree 集成冲突/中断** | 在专用 integration worktree 收集冲突路径后 `merge --abort`，持久化可恢复 Integration；事实漂移 fail-closed。冲突升级 `integration_conflict:<integrationId>` gate，Phase 9 只允许 Leader 指定冲突贡献者 `request_rework` 后从原 base 重建。 |
| **知识库写入污染（决策 3）**       | 阶段 0 至阶段 N（直至另行启用）全量禁用写入（Write-Block）。未来启用时，仅在 `phase === 'done'` + `testResults.passed === true` 且收到 `/approve-kb` 命令时才执行蒸馏。 |
| **进程崩溃（Worker 意外退出）**    | `WorkerRuntime` 捕获 `unhandledRejection`，将对应 `WorkerState.status` 置为 `'failed'`，Coordinator 在下一轮路由检测到失败后，自动重试或升级人。 |

---

## 7. 扩展点设计（未来演进）

架构天然支持以下扩展，无需重构核心层：

1. **新角色（Role）**：在 `packages/roles/definitions/` 增加 `RoleSpec` 模板或由 Leader 提交自定义 spec，再作为 `RosterEntry` 写入项目协作快照。Phase 7 的通用 worker 可由 Leader 显式指派任意 enabled 角色；Coordinator 的默认自动拓扑仍只覆盖六个已定角色，任意自定义角色自主调度按 DEF-016 延期，禁止把“可装载”误写成“已自动路由”。
2. **新执行器（Executor）**：实现 `Executor` 接口（`step`, `saveSafePoint`, `injectInbox`）。例如 Phase 10 的 `OpenHandsExecutor` 只需放在 L4 适配层。
3. **新沙箱环境（Sandbox）**：实现 `SandboxManager` 接口（`createWorktree`, `run`, `read`, `write`）。例如将 `LocalTempSandbox` 替换为 `ECSSandbox`（AWS）或 `Firecracker`，编排层零感知。
4. **新工具（Tool）**：基于 MCP SDK 编写新的 Server，在 `RoleSpec.tools` 白名单中注册即可。

### 7.1 工程 Eval 扩展点（D11）

Eval 位于生产系统边界之外，通过 adapter 驱动公开任务或 Agora fixture，调用既有组合根并从评测专用持久 State、Harness 事件、工具结果和产物派生 `EvalResult`。依赖方向固定为 `eval adapter → L3 端口/生产组合根`，L1/L2 禁止 import 任何 SWE-bench、Terminal-Bench/Harbor 或 Eval runner 类型。每次运行拥有 `.data/evals/<runId>/` 独立 data root/workspace；生产组合根只在该边界内写评测 State，不能接触用户项目 `.data/projects/` 或 KB。

```text
版本化公开 Benchmark / Agora fixtures
              ↓ adapter
 AgoraEvalTask + canonical fingerprint
              ↓
隔离 run root → 生产组合根 → Harness/MCP/Docker/State/MessageBus
              ↓
 State + Trace + Patch + 资源指标
              ↓
Outcome / Process / Efficiency / Safety Graders
              ↓
结构化 checks + EvalResult + artifact hashes
```

结果正确性与过程正确性分开：公开 Benchmark 的测试/补丁 Grader 只能证明最终任务结果，Agora 自定义 Process Grader 负责验证角色投影、Channel/Leader 权威、安全点、交接和并行一致性。每个 Grader check 必须给出 `pass | fail | unknown`、说明和证据引用；缺失指标不可伪造，`pass` 只能从本次 run 的实际 State/trace/工具结果或明确分类错误派生。Runner 先保存 provisional 结果，再执行注册清理并原子定稿 final 结果；只有 final、无 failure、全部 Outcome/Process/Safety 通过且无已知 Efficiency 超限的结果可计为通过。`maxIterations` 对应实际编排轮次，不等同返工次数。实际执行与审计元数据共用冻结模型/环境配置；driver/schema/eval root 不合法均 fail-fast。Eval 运行失败可以留下隔离的评测 State/结果用于诊断，但不得修改用户产品 State 或触发知识库写入。deterministic 与真实 model profile 分开运行、分开汇总；结果与测试门禁分离，Benchmark 不能替代阶段集成测试/G5。

---

## 8. 架构权衡与取舍（Trade-offs）

> *这是面试官最看重的内容，体现工程务实精神。*

| 追求目标             | 做出的取舍                                                   |
| :------------------- | :----------------------------------------------------------- |
| **可控性 vs 性能**   | 为了 Leader 能随时裁决（可控），我们接受 **“秒级”** 的抢占延迟（等待安全点），而不做硬中断（会导致状态撕裂）。 |
| **简单性 vs 健壮性** | Phase 0 使用 **本地临时目录 Mock 沙箱**，舍弃 Docker 隔离。我们优先验证“逻辑正确性”，牺牲“环境安全性”，用快速迭代换取可行性。 |
| **成本 vs 效果**     | 严格限制并行度为 3，且规划/评审用强推理模型（贵）、编码用代码模型（便宜），通过**模型路由**做成本优化，而非无脑堆叠 Agent。 |
| **隔离 vs 复用**     | 项目间采用**默认 deny 导入（快照副本）**，舍弃了实时共享的便利性，换来了绝对的上下文隔离，防止“祖传代码污染新项目”。 |
| **公共可比性 vs 产品针对性** | 复用成熟 Coding Agent Benchmark 的任务/环境/Outcome Grader以获得外部可比性，同时自建小规模 Agora 协作语义 Grader；不为公共榜单反向定制生产 Agent。 |

---

## 9. 部署视图（Deployment View）

- **Phase 0**：单 Node.js 进程内运行 Harness context/Worker，State 存储在内存中；进程内 session 隔离不等于子进程隔离。
- **Phase 5–9**：增加 Next.js 前端；单实例自托管后端将任务 State 以 `.data/projects/{projectId}/tasks/{taskId}/state.json` 原子快照持久化。Phase 8/D4 起 Harness session 由锁定版本的 JSONL persistence plugin 写入 `.data/projects/{projectId}/tasks/{taskId}/harness-sessions/`，持久 header + 完整事件序列；`safePointRef` 引用其 source session 与已闭合 boundary，不再使用含义含混的单一 `events.jsonl`占位路径。D15 Trace 从同一规范 session root 读时派生白名单 DTO，不复制日志、不进入 MessageBus，运行中只在单实例边界内有界轮询。Phase 6 先以 `channels.json` 落 `Project.channels`；Phase 7/D12 确认 roster 与 main participants 不可跨文件短暂失配，迁移为 `.data/projects/{projectId}/collaboration.json` 的统一 `ProjectCollaborationSnapshot`。Phase 9/D17 在同一后端进程内加入唯一 GlobalScheduler lease 队列（全局 cap 默认 3）、per-task canonical commit queue 与 task-scoped pause epoch；它们是运行控制面而非第二份 State，普通进程重启后不从残留 lease/epoch 猜测运行状态。非阻塞 reproject 保留原 lease/composition admission；humanGate 在完整 gate 落盘后释放已静止 worker lease，composition admission 则只在可执行资源 suspend 成功后释放。Phase 9 resolution receipt 用按 workerId 稳定排序的 per-worker 恢复计划扩展 Phase 8 单 worker 格式，旧格式仍可兼容读。任务 State 与项目协作快照分别按 task/project 串行并原子替换；损坏、不变量失败或新旧快照冲突均 fail-fast。SQLite 只在需要复杂查询时作为可选迁移。Vercel 只承载前端，后端仍自托管。

> **[2026-09-07 架构决策更新] D17 部署目录补充：** Phase 9 在每个任务目录增加 `repository/`（task-owned canonical repo）与 `worktrees/`（worker + integration + validation linked worktrees）。Git 与 Docker 只共享这些规范路径，不 checkout/merge 用户源仓，也不自动 push。恢复入口把 Task State 中的结构化 `WorktreeRef`/`Integration` 与 Git 现场交叉验证；legacy 字符串路径未升级、task root 越界、base/head/ancestry 漂移均拒绝恢复。
>
> **[2026-09-07 实装记录] task 9.3：** 生产组合根已在上述 task root 内装配 `WorktreeGitService + WorkspaceAdapter + DockerSandbox`，容器工作目录由 host realpath 相对 task root 映射，兼容 macOS `/var`→`/private/var`。终态只释放容器与 linked worktree，不移动 canonical repository、State、Harness sessions 或 artifact；失败发生在首 worker 前时以 canonical repository 作为最小可归档来源，禁止把 task root 递归复制到自身 `artifacts/`。
- **Phase 10+**：若需将后端部署到 Serverless 或水平扩展，必须先将 `TaskStateStore` 与 `ProjectCollaborationStore`（包括 `ProjectChannelStore` 视图）都切换为外部耐久实现，并用跨实例事件传输取代进程内订阅；是否引入 Redis/托管数据库由部署任务根据实际需求裁决，不预置为必选依赖。
- **生产建议**：将 Sandbox 适配器切换为独立 Docker 容器，通过网络（REST/MCP）与主进程通信，彻底隔离文件系统。

> [2026-08-31 实装记录] 任务 5.1 建立 Next.js App Router 与进程内 Channel SSE 传输接缝，供单实例 MVP 实测；该接缝不宣称跨实例可靠性。State 持久化未纳入 5.1，登记于 DEF-014。
>
> [2026-09-01 架构决策更新] D6/D7 的 Phase 6 恢复边界：`ProjectChannelStore` 与 `TaskStateStore` 各自维护项目级 Channel 快照和任务级消息快照；提交消息前必须用前者校验寻址/成员/关闭状态，提交后 MessageBus 仍只做投递。DEF-015 在 Phase 6 的解决条件限于服务端固定 leader 的 Channel 完整性校验与 worker participant 隔离；账号认证仍是非目标，不得将其标记 resolved 解读为允许公开无认证部署。
>
> [2026-08-31 架构决策更新] D8 将“协议可被 Vercel 前端消费”与“后端可无状态部署”分开：Phase 5–9 只承诺 `.data` + 进程内 MessageBus/SSE 的单实例自托管后端。SSE 建连必须在快照与实时尾流间无缝衔接；同一逻辑发送重试复用 `msgId`。外部耐久存储与跨实例总线未落地前，不得宣称完整后端 Vercel Serverless 兼容。
>
> [2026-09-01 架构决策更新] D10 在该单实例边界内增加进程内任务运行注册表与 Web→既有编排器的组合根接缝。注册表不是耐久调度器：Phase 5 全实例串行执行，最多一个活动 run；终态产物归档到任务耐久目录后释放执行资源。后端重启后只从 JSON State 与 artifact 快照恢复最终事实，并将遗失的活动 run 呈现为中断，不在 Phase 5 自动重建 executable composition/Harness context。Demo 与出口 G5 必须由浏览器触发同一任务闭环，静态 UI 数据不构成运行证据。
>
> [2026-09-03 架构决策更新] D4 自 Phase 8 收紧上述 Phase 5 边界：普通遗失的活动 run 仍只标记 `interrupted`，但已成功持久 gate + flushed checkpoint refs 的 `needs_attention` 任务必须可在后端重启后接受同一 D9 Leader 裁决并重建 composition。gate 等待不是终态：不得归档不可变 artifact，不得删除 worktree/session，进程内 run Map 也不是恢复真相源。
>
> [2026-09-05 架构决策更新] D16 将 REVIEWER 通过后的完成候选纳入同一持久 gate 恢复边界。等待 Leader 时 run 为 `needs_attention` 且不得归档；approve resolution 经真 Fork 恢复并由 Coordinator finalize 后才执行终态归档，request-changes resolution 则恢复返工。进程重启后仍以 gate/receipt/Decision 为真相，不以 run Map 或 UI 状态猜测完成。

> **[2026-09-07 架构决策更新] PR #66 生命周期补齐：** D17 非 gate 并行基础设施失败先等待执行收敛，再 recoverable suspend 可执行 composition；State、源 worktree、Harness JSONL 与 validation 证据保留且不归档终态。成功释放 admission，失败保留 pendingSuspension，同任务 start 可重试清理。该路径不自动重开 done/failed worker；既有 gate 仍走 D4。暂停请求原生消费即清除，abort 取消尚未消费请求；未提交指令不得注入。Git 读侧区分 CODER 提交状态与 TESTER 严格目录状态，两条完成路径都验证继承测试路径未删除，详见详细设计 §3.1/§4。
