# 11.2 验证 Electron 打包与受管工具链方案

## 2026-09-14 开工与范围

- Leader 在初始化明确候选任务及 design 范围后授权“开始”。依赖和前阶段出口 10.7 均为 done。
- 计划：核对既有 Next custom server、helper 和凭据/状态；研究平台/版本/打包/签名与升级；执行隔离最小 Spike；提交设计和真实证据供 Leader 确认。
- 本次不自动启动 11.3/11.4 产品实现，不运行模型评测或发布，不更改旧数据；实际 Spike 与未完成发布验收分别记录。
- 初始工作区干净，基线 dev-1.0.0；设计工作分支 docs/task-112-desktop-packaging。

## 2026-09-14 设计与隔离实测完成，待接受

- 可审阅设计：`docs/reviews/task112-desktop-packaging.md`；可提交的无秘密证据：`docs/reviews/task112-packaging-evidence/`。现状与候选版本已同步技术选型§12、开发计划§18.2；未把未接受方案写成已定架构。
- 建议Electron44.3.0应用壳+独立Node24.20.0运行既有Next15.5.24 custom server；pnpm9.15.9/npm11.19.0、便携Git2.53.0、预编译helper。建议macOS15+ arm64/x64分别发布，最低系统与Intel尚未实测。正式构建候选Packager20.3.0仅查元数据，未安装；现有产品依赖和锁文件保持。
- 明确应用安装/持久state/用户workspace身份分离、独立服务的本机认证与生命周期、签名公证矩阵、手动覆盖安装及新桌面格式迁移/备份/journal/回退/异常拒绝协议。升级不移动worktree根，不丢身份/配置/证据，不迁移或删除旧Docker数据。
- 基线`58ef611e91ed27e7d05aebbcd9024190f50d99f3`；`git archive HEAD`生成干净临时输入，复制锁定依赖，以下载Node运行既有两个helper构建脚本及`pnpm --filter @agora/web build`，成功。未复制.env或.data；未改产品源码。直接读取生产开发目录trace发现.data路径，改为干净输入后15份trace共3889候选文件且无禁入路径。
- 官方Electron/Node/Git下载及pnpm包均核验上游hash/integrity；证据downloads.json。构建机Appleclang21.0.0/SDK26.5；两个helper均编译arm64/x64，min target13.5；只有arm64真实执行。
- 实测临时.app内运行Node/Next，无源码外部链接；真实Git linked worktree、文件helper读写/越界拒绝、真实隔离Keychain与生产instrumentation、动态设置API、认证/来源/写入口限制、既有React渲染及renderer无Node、停止回执/退出/Keychain保留。最终16项通过，进程退出码0，测试钥匙串删除。codesign ad-hoc验证通过；最终检查spike进程0、测试Keychain0。
- 失败和修复完整记录见设计§8：手工包Electron命名导致isPackaged=false；仅trace复制缺custom server的next依赖和链接；便携Git模板路径需要显式配置；收尾重复stop触发EPIPE。第三轮检查期间额外启动覆盖共享结果，不计完整通过；修复后使用独立目录/检查点完成新一轮，未弱化断言。原attempt1/2 JSON、最终结果、截图与manifest已保留。
- 本地复现材料`.data/spikes/task112-20260914/`含临时验证脚本/构建日志/栈采样，hash登记在提交证据；原实验目录`/private/tmp/agora-112-spike/`保留。本次.app约2.2GiB为未裁剪Spike，不是正式预览候选或最终体积承诺。截图的Start入口由临时服务返回405，不表示开放本机编码。
- 限制：没有Developer ID身份（查询为0）；没有公证、DMG/ZIP安装、Intel/最低系统/干净Mac、正式Packager/fuses、SSE尾流、真实新旧版升级或项目依赖安装验收；这些由11.3/11.4/11.5等对应任务完成。没有模型请求/新增计费、没有访问既有Agora凭据、没有启用或移除Docker产品路径。
- Check：task-status查询校验与git diff --check通过；证据hash/JSON和引用在最终检查复核。仅文档及证据变更，不虚构TDD或运行完整pnpm test；11.2的真实最小Spike已执行，但不宣称产品G1–G7全绿。请求提交时仍执行既有完整提交门禁。
- 下一步：Leader接受可审阅设计后，按agora-sync-docs同步正式来源，记录接受并标done/级联11.3；接受前保持in_progress，不自动启动11.3/11.4。未commit/push/建PR/tag/Release。

### 最终文档 Check 补记

- `node scripts/task-status.mjs check`：valid=true，171任务/31阶段/24决策/63历史。
- `git diff --check`：通过；15份已登记证据/本地复现材料hash匹配，设计稿6个本地文件链接有效；最终result为passed/16项/keychainRemoved=true。
- 尝试对变更文档运行`pnpm exec biome check docs/task-status.json docs/reviews/task112-packaging-evidence`，仓库配置排除docs，返回“0 files / no files processed”（exit1）。这是检查器不适用，不记为lint通过；以直接JSON解析、索引/来源链接和证据hash校验完成文档Check，未更改Biome范围或跳过任何应执行的产品测试。


## 2026-09-14 Leader接受与正式规格同步

- Leader在询问“Electron + 独立 Node 服务”是否意味着下载app后双击即可使用、并获得应用自动托管服务/无需手工安装Node与运行终端/首次自配模型的解释后，明确回复“好的确认”。按同一范围接受11.2设计，非代码类产出依AGENTS.md §8.1.2收尾为done；不将确认扩大为11.3开工、提交或发布授权。
- 按agora-sync-docs依次同步蓝图§21 D18/§22.3.2、详细设计§12.3.1–§12.3.7、架构§10、选型§12/§14、开发计划§18.2/§18.3/§18.11和D18摘要。正式规则涵盖macOS15+两架构、Electron与独立Node、受管工具链与限定预览、安全连接/单实例、签名与手动更新、稳定状态根/钥匙串及持久迁移故障协议。AGENTS目录映射补充获批apps/desktop交互层，不改变红线或启用范围。
- 审阅报告保留接受与实测快照，正式实现契约以详细设计§12.3及选型§12为准。原失败记录、未验收平台/签名/升级边界和15份证据hash保持；本轮再次逐一核验15份hash匹配。未修改产品源码、测试、依赖或锁文件，未重复模型或打包实验。
- 完成记录后幂等级联依赖：11.3由pending转ready；11.4/11.5保持pending，Phase11仍in_progress。不自动启动下一任务。文档检查以索引/长度/依赖、来源链接、证据hash及diff为准；完整提交门禁仍在请求提交时执行，不宣称产品全量G1–G7通过。

### 接受收尾 Check

- 索引校验valid=true：171任务、31阶段、24决策、63历史；D18摘要415码点；仅11.3级联ready，无剩余级联候选。
- 15份证据hash复核通过；新增/审阅稿本地文件链接25处及新正式来源锚点有效；git diff --check通过。变更仅AGENTS目录映射、docs规格/状态/历史及既有Spike证据，未改产品文件。
- 首次临时文档检查未使用Git的NUL路径输出，中文路径被引号转义导致范围断言失败；改为--name-only -z读取真实路径后全部通过，无需修改产品或放宽范围。
- 全量产品测试本轮未运行（纯设计接受与文档同步）；提交前完整门禁未执行，不宣称可直接提交或发布。


## 2026-09-14 提交门禁与交付授权

- Leader显式调用agora-commit，授权本功能分支提交、push及创建base=dev-1.0.0的英文PR，不授权合并。已接受的非代码任务11.2保持done，11.3保持ready。
- 分支docs/task-112-desktop-packaging；联网fetch后origin/dev-1.0.0仍为58ef611e91ed27e7d05aebbcd9024190f50d99f3，与本分支起点一致；同head暂无PR。
- pnpm typecheck通过；pnpm lint通过，检查430文件且未修改。pnpm build:sandbox-native通过。
- 首轮pnpm test在工具沙箱中遇到Docker socket、本机listen、Keychain及模型网络权限失败，停止该轮（exit130），不记为通过；保持DEEPSEEK_API_KEY并在正常权限下运行同一完整命令，不删除凭据、不排除或skip测试。沙箱内gh认证检查同样失败，正常联网权限复核账号及fetch成功，无需变更凭据。
- G5提交时真实复验：先纠正临时启动命令的可执行文件名（AgoraSpike）；工具沙箱启动exit134，正常桌面权限下运行未修改的同一.app后exit0。新增delivery-result.json记录16项全部通过，真实独立Node/Next、Git worktree、helper、Keychain、动态API/renderer及停服均再验；keychainRemoved=true，codesign --verify --deep --strict通过。原最小验证证据保持，manifest新增该复验结果hash，当前16份登记材料均匹配。
- G7：20份交付文件的敏感格式及已配置凭据精确匹配检查无命中；截图人工查看无凭据正文。仅发布筛选后的结果JSON和截图；含临时Keychain测试口令的原始失败测试日志不进入提交。运行时.data、安装包及临时执行脚本不入库。


### 提交门禁最终结果

- G1：已接受规则按蓝图→详细设计→架构/选型→计划/摘要→目录映射同步，原R1–R13和Phase11限定预览不变；公开端口/产品代码/依赖未变。
- G3：pnpm typecheck exit0；pnpm lint exit0，430文件。
- G4：正常权限下完整pnpm test exit0；Node任务追踪7/7，Vitest 170/170文件、1282/1282测试通过，0跳过，耗时140.18s。真实模型、Docker、Git、Keychain等既有回归保持启用；原沙箱失败不抹去。
- G5：同一已验证.app再跑16/16，exit0；delivery-result.json及manifest可核验。只证明本任务最小打包链路，§12.3的正式平台/签名/安装/升级实测仍由后续任务完成。
- G6/G7：历史与索引更新；结果JSON/截图及16份登记材料hash校验通过，20份精确交付文件无敏感模式/已配置凭据匹配。原始执行日志仅本机临时目录保存，不提交。
- node scripts/task-status.mjs check和git diff --check通过。后续若仅追加PR/hash交付记录，执行索引/敏感数据/diff检查；不重复未变产品的全量运行测试。

### PR交付记录

- 设计与证据提交：`d8b80b7520a9feaa6ec9a29d3a940100384a09d1`（Define desktop packaging and managed toolchain contracts），20份精确文件；已推送`docs/task-112-desktop-packaging`。
- 已创建英文PR：https://github.com/logan-suu/Agora/pull/79 ，base=`dev-1.0.0`。任务11.2保持已接受的done，11.3保持ready；未合并、未tag、未发布。
- 此次后续提交只追加PR/hash交付记录和短摘要，产品内容不变，沿用上段完整门禁，并重新校验索引、敏感数据和diff。


## 2026-09-14 PR审查P2修复

- 用户授权“进行修复”。红阶段：从PR Git tree校验manifest，只有10/16份材料可取得，4个必需脚本及2份日志仅存在gitignored .data，无法在新checkout复现报告步骤。
- 直接交付原始6份文件，逐字保留hash；敏感模式、现有环境凭据精确匹配及用户主目录路径检查无命中。补独立临时目录复现入口，固定源码提交和上游二进制hash，不依赖开发者node_modules或旧.app；继续保留16项真实断言。
- 本轮实际新增可执行构建/验证脚本，按追踪规范改走代码交付：11.2由done转in_progress等待本PR人工合并，11.3由ready恢复pending，11.1/既有接受事实和Phase11启用范围不变。不是因单纯提交文档重置设计接受，而是修复扩大了实际交付类型。


### P2修复验证

- 原4脚本/2日志逐字移入docs/reviews/task112-packaging-evidence/reproduction/，原hash不变；manifest全部使用仓库内file路径，不再有localFile。新增README、reproduce.mjs及reproduction-result.json，当前19项hash全部匹配。
- 真实重建：新mkdtemp目录，从固定58ef611提交git archive；只复用按既定hash复核的四份上游下载压缩包，未读取旧.app、旧node_modules或开发源码。使用受管Node/pnpm重新pnpm install --frozen-lockfile、构建两helper及Next，执行历史依赖闭包和打包脚本。执行副本仅改两处base及归档提交元数据；两份运行验证脚本字节不变，16项断言不变。
- 独立重建的.app运行16/16通过、exit0、keychainRemoved=true；资源审计36865文件、externalLinks=0、forbidden=0，ad-hoc签名完整性通过。无模型请求；新临时日志不提交，筛选结果见reproduction-result.json。
- 新checkout等价验证：仅复制仓库证据目录到全新目录，--verify-only通过；依次删除main.cjs、破坏result.json hash、让manifest引用越界路径，全部返回非零；恢复后通过。缺失材料的原P2已闭合，未弱化断言。
- 五份JS脚本node --check全部通过；pnpm typecheck/lint通过（430个既有适用文件）。Biome按仓库配置排除docs，未假报归档脚本经过Biome；新增入口以语法、真实重建与失败路径检查验证。
- 23份文档/材料的敏感格式及现有环境凭据精确匹配无命中；6份历史新增材料也无用户主目录路径。日志与截图属于历史诊断，不伪造成真实产品验收。完整回归结果在下段记录。
- PR尚无实质人工/bot review或行评论，CodeRabbit仅额度提示；没有可resolve的会话。修复后在PR留下英文结果，不自动请求合并或发布。


### P2修复完整门禁结论

- pnpm typecheck、pnpm lint通过；完整pnpm test exit0：7/7任务追踪测试、170/170文件和1282/1282 Vitest测试通过，0跳过，128.50s。保留真实模型/Docker/Git/Keychain测试与既有凭据。
- G1/G3/G4/G5/G6/G7已按本轮实际脚本变更复核：原接受契约不变，19份仓库证据可校验，干净源码重建及原16断言通过，失败路径明确拒绝，语法/全量回归/索引/diff和敏感数据检查通过。未新增依赖、未修改产品执行路径、未启动11.3。
- 修复交付到既有PR #79；因新增构建与验证脚本，11.2保持in_progress待人工合并，11.3保持pending。此前设计接受和曾标done的时点证据保留，不作为当前交付完成状态。

- 首次暂存后的diff检查发现原build.log含终端进度回车/尾随空格和末尾空行；尚未创建修复提交。为保留已记录的原始字节/hash，在reproduction/.gitattributes仅对build.log禁用文本转换及这两项空白规则，保留可读文本diff，不修改日志、不关闭代码检查。其余文件继续完整diff检查；19份材料hash再次通过。

- P2修复提交79ca4d6：19份manifest材料已直接从提交Git blob读取并全部匹配，无本机独有引用。修复没有改写原始6份材料，最新运行、复现入口及失败路径证据已交付。下一步推送到PR #79并更新英文说明，保持人工合并门禁。


## 2026-09-14 新增CodeRabbit评审逐条处理

- 用户指出PR仍有未审查review；重新读取GraphQL reviewThreads确认两个未解决会话：PRRT_kwDOUBGzms6iOqub（结果16与清单19的范围）及PRRT_kwDOUBGzms6iOquf（启动超时可能遗留子进程）。两项均对当前交付有效，不将额度提示或旧查询当作已完成全部审查。
- 数量问题：旧结果记录的是构建前实际16份输入；不能改成未在当时验证的19。旧reproduction-result原始字节归档为historical-reproduction-result.json，scope伴随文件列出16份内容及后来补登记的3项和原因。当前复现入口在新结果中记录构建前manifest hash、全部输入file/hash和明确的时点范围；生成结果本身属于之后的产物，不循环计入自己的输入验证。
- 生命周期问题红阶段：新增真实fork子进程的确定性单元回归，在Next prepare或credentials等待时发送stop，再放行；原脚本两例都未正常退出，1.5秒看门狗SIGKILL导致断言失败。fixture只控制这些启动等待点，文件头明确mock原因；不替代实际Next/Keychain G5。
- 修复后start/stop共享生命周期，stopping在接收请求时立即设置，等待已开始的初始化收敛后只清理已初始化资源；每个异步启动边界重查stopping，不再迟到创建HTTP服务或发送ready；重复stop复用同一shutdown。
- 父进程通过stop-child.cjs等待停止/退出期限，超时只终止自己fork({detached:true})创建的非模型Spike进程组，并等待真实exit，再处理临时Keychain；强制终止明确记录failed，不冒充正常停服或产品安全点。保留原始main/desktop-spike字节于historical/，原16项业务验证断言保持，当前复现明确使用修正版。
- 绿阶段：prepare中停止、credentials中停止、无响应子进程超时终止三个单元测试通过；每例校验退出/无迟到ready/无未初始化关闭错误，超时例确认PID已不存在。正式产品R4安全点及阶段启用范围不变。


### 新评论修复门禁中的实网超时调查

- typecheck/lint及3项新增子进程回归通过；新目录真实Electron重建16/16通过，forcedTermination=false、Keychain已删除。结果冻结当次24项输入manifest及各文件hash；reproduction-input-manifest.json保留对应字节，后来追加两份产物后当前manifest为26项，不混算历史时点。
- 本轮完整pnpm test中两项未改动的DeepSeek实网测试（g5-real-chain、channel-summary-g5-real-chain）均120秒超时，停止该轮exit130，不记通过。未改变凭据、请求配置、断言或超时。
- 假设按现有证据排序：①供应商/网络响应暂时停滞（两个不同测试均等待同一实网模型且超时）；②同时重建应用造成资源竞争（该轮与编译并行，需无构建负载复验）；③本次修复影响产品执行器（仅docs中的独立复现脚本变更、产品及测试未改，可能性较低）。当前没有足够证据断言具体网络根因。先单独运行原两项测试，成功后再串行执行同一完整pnpm test；不得把局部通过替代全量门禁。


### 新评论修复暂阻于G4，未交付

- 两项原始实网测试单独复验仍各120秒超时，exit1（2文件/2测试失败，120.68s），已排除同时编译负载这一必要条件；不修改原测试或默认模型。未执行第三轮相同批量请求。
- 公共/models接口401且约0.33秒返回，认证/models为200；实际列出deepseek-flash、deepseek-v4-pro。已核对Harness官方provider默认https://api.deepseek.com，无DEEPSEEK_BASE_URL覆盖。官方2026-09-10公告明确deepseek-v4-flash仍临时兼容路由V4.1-Flash，所以列表缺旧名不是失败根因证明：https://www.deepseek.com/en/news/deepseek-v4-1-flash/ 。
- 不经Harness的最小同别名/chat/completions诊断（max_tokens=16、thinking disabled）20秒超时，未获得生成回复。生成链路阻塞在当前环境可重现，但具体上游/网络原因仍inconclusive；不把官方整体状态页的正常状态当作本请求成功，也不静默迁移模型/价格/测试绑定。
- 已通过：typecheck、lint、3项新增真实子进程单元回归、固定提交全新构建的真实Electron/Next/Keychain 16项G5（无强制结束、临时钥匙串已清理）、输入快照hash和原字节保留、语法/索引/diff/敏感数据检查。失败证据见docs/reviews/task112-packaging-evidence/review-gate-blocker.json；当前manifest另包含后来产生的门禁结果，输入快照仍只计实际构建前24项。
- 按agora-commit“Stop on any failure”，本轮修改保留本地，未commit/push；PR已发布head仍280f24e。任务11.2标blocked，解除条件为生成链路恢复后原样通过两项实网测试及完整pnpm test；11.3保持pending。两条review逐条英文报告本地修复及门禁失败，暂不resolve，不能把本地代码当成已交付。

- 已逐条英文回复，明确本地修复/验证及G4阻塞，未解决会话：证据范围 https://github.com/logan-suu/Agora/pull/79#discussion_r4008567065 ；启动清理 https://github.com/logan-suu/Agora/pull/79#discussion_r4008567249 。最终敏感数据、索引及diff检查通过；工作区保留本轮未提交变更。


## 2026-09-14 Leader指定Go优先测试及V4.1模型名更新

- Leader指出Go的V4.1模型ID已变为deepseek-v4.1-flash，并要求以后测试尽量走OpenCode Go。官方Go模型列表当日核对一致；旧deepseek-v4-flash仍代表V4 Flash，不能全局替换。此前官方端点超时仍是独立历史事实，不声称Go更名证明了官方超时根因。
- 通用真实回归通过tests/helpers/live-model.ts优先使用已有Go凭据；未配置Go才选既有官方凭据，显式AGORA_TEST_PROVIDER保留官方专项。选中Go后的配置/请求失败不自动回退；不删除凭据、不减断言、不提高原120秒/600秒测试期限。三项G5继续使用真实官方Harness/DeepSeekAdapter，LRU仍执行真实工具与LocalTempSandbox。旧官方回归预算环境变量不能混记Go请求。
- 新Go模型名在现有PiAi兼容路由未命中reasoning回放：新增离线真实Harness工具/多turn测试先红（空reasoning_content缺失），仅扩充精确模型名后转绿，保留其他提供方、思考模式和用户容量。当前Eval源码模型/定价键更名，默认正式评测仍关闭，冻结组与历史模型记录不改写。
- 已同步蓝图§21、详细设计、技术选型及AGENTS.md测试偏好。14项离线回归通过，typecheck/lint通过。首次Go联网命令被自动审批以外传范围不明确拒绝；核对两项仅固定合成输入、未配置文件/shell工具后，附明确信息重审获准，真实复验进行中。

- Go两项实网测试仍各120秒超时（120.56s，2失败）；停止后续LRU/全量复验。只读Go模型列表HTTP200/108ms，当前及旧V4.1 ID均仍列出，不能据此归因旧名失效。一次带脱敏网络时序的同用例诊断确认：实际POST到Go、新ID、官方Harness会话头及User-Agent；约572ms返回HTTP200、约12.58s收到首个14字节网络块，但仍120秒超时（120.52s，1失败）。首块不等于有效模型完成；具体provider/stream/Harness原因保持inconclusive，未继续重复请求或提高期限。未取得完整用量，费用unknown不记零。
- 本轮最终typecheck/lint通过；9文件45项离线回归通过。首次受沙箱限制的3项本地HTTP测试listen EPERM，明确申请仅本机监听权限后全数通过；未mock绕开本地HTTP。桌面3项生命周期单测、27份manifest验证、索引/diff检查通过。新增Go路由的LRU实网验证尚未执行，不将局部离线结果当作完整G4或新路由G5。
- 新时点证据docs/reviews/task112-go-test-routing.json；旧review-gate-blocker.json及24项构建输入manifest保持原字节，当前失败/更名授权不改写历史。11.2仍blocked，解除条件改为按Leader批准的新测试路由通过实网和完整pnpm test后交付；11.3仍pending，PR本轮未提交推送、两条会话未resolve。


## 2026-09-14 按Leader要求进一步定位超时

- 初始假设按证据排序：上游只保活/未开始输出；SSE或Harness解析/结束问题；参数/会话头/代理兼容。宿主Node24.20.0未设置HTTP(S)/ALL代理、NODE_USE_ENV_PROXY、NODE_OPTIONS或DEEPSEEK_BASE_URL环境覆盖。
- 裸Node fetch对照Go V4.1/V4 Flash，使用同一合成JavaScript 2+2输入、认证配置、enabled/high、max_tokens64。V4.1在65秒内仅5条保活、0 data且无usage；V4 Flash在1.454秒完成，usage输入101/输出16。只有模型选择及每请求新会话身份不同，非统计性能基准。
- 独立真实Harness对照保持原Coder投影及默认256000输出，只显式选Go V4 Flash；45.43秒通过done/安全点/非空回复/单messages append。没有修改原测试或默认V4.1绑定。证明现有客户端可以完整收敛，外部V4.1停滞在无Harness时也成立。
- SDK源码确认SSE注释调用watchdog.pulse并重置300秒transport idle；没有模型data时turn仍等待，外层120秒测试期限先触发。直接超时原因定位到V4.1服务响应停滞；Go内部路由/上游排队/账号调度原因尚无服务端证据，不猜测具体宕机或额度耗尽。
- 新证据docs/reviews/task112-timeout-diagnosis.md及同名JSON。额外裸HTTP256000上限对照被自动审批以可能费用/资源扩张拒绝、未执行；此前已批准的同上限Harness对照成功后无需该实验，也未再做64上限Harness追加请求。V4.1及Harness调用用量unknown不计零，停止进一步重复调用。
- 本轮仅补充诊断报告/任务记录，未改产品或正式测试；11.2仍blocked待所选真实模型路径及全量G4通过，11.3 pending；未commit/push/resolve。


## 2026-09-14 Leader明确先只使用Go V4 Flash测试

- Leader明确授权“先只用go的V4 Flash进行测试”，据此将当前通用真实回归固定为opencode-go/deepseek-v4-flash。只读Go凭据；缺少、无效或请求失败均显式失败，官方凭据保留，不自动fallback或skip。历史V4.1失败、诊断、冻结评测及产品连接均保留。
- TDD：路由回归先改为缺Go不准使用官方凭据、Go模型必须V4 Flash、官方override拒绝，旧实现红；简化helper后转绿。三个G5移除缺凭据skip分支，原功能断言、原生思考/256000输出及120秒/600秒期限不变。蓝图、详细设计、选型及AGENTS.md按来源顺序同步当前临时策略。
- typecheck/lint及3项桌面生命周期单测、27份manifest复核通过。两项真实小回归及完整pnpm test作为本轮交付门禁运行；不把之前单例V4 Flash诊断当作全量验收。任务恢复in_progress，下一步完成G4及PR交付，11.3 pending。

### V4 Flash首次全量回归发现并修复测试接入缺口

- 小回归2/2通过（频道摘要8.148s、单turn71.891s）。首次完整pnpm test：7项追踪通过；171文件中169通过、2失败，1283项中1281通过、2失败，108.21s，无跳过。未提交。
- LRU在26.795秒失败，cause为INVALID_REQUEST/HTTP400，服务端提示空tool_call_id重复；这与此前V4.1保活超时不同。三个假设按证据排序：Go后续tool delta清空身份（已有诊断可复现）；序列化错误；偶发供应商故障。核对发现新的测试helper只接原生DeepSeekAdapter，漏用既有Go Eval normalizeGoToolStream。离线真实Harness/SSE工具回归先红（probe未执行），复用同一归一化在adapter公开stream及prepareCall两入口后转绿；保留原arguments，缺失/冲突拒绝且先自然读完流，不新增循环或mock真实工具。
- 第二个失败为离线phase10-benchmark混合角色fixture仍断言旧deepseek-flash，当前Eval已更名导致其抛错；更新精确模型期望为deepseek-v4.1-flash，仍核对同一角色映射，不触发V4.1实网。
- 4项针对性离线回归及typecheck通过；单独重跑真实LRU和12项benchmark集成回归，成功后再执行完整pnpm test。

### V4 Flash工具修复验证与文件并发对照

- 修复后真实LRU单独运行通过（186.742s），12项benchmark集成通过（57.70s）；typecheck/lint通过。确认空tool_call_id问题已在真实工具链路收敛。
- 随后完整默认并行pnpm test仍失败：7项追踪通过，171文件中169通过、2失败，1284项中1282通过、2失败，无跳过，总601.00s。单turn G5通过（7.679s），频道摘要120.020s及LRU600.017s触发原测试期限，未再次报告空ID HTTP400。没有修改期限或手动终止本轮。
- 频道摘要随后独立复验通过（10.147s）。因此V4 Flash也存在运行时延波动；现有证据不足以将它归因于测试文件并发或供应商内部调度，不能承诺换模型后不再超时。
- 执行完整文件串行对照：pnpm run test --no-file-parallelism。保留全部测试、原断言/期限及用例内部多worker并行，只限制测试文件同时运行。首次误用pnpm test --no-file-parallelism被pnpm参数解析立即拒绝，未执行任何测试；改用run后确认实际执行node追踪测试与vitest run --no-file-parallelism。最终结果待下文记录。

### Go V4 Flash最终串行对照：G4仍未通过

- pnpm run test --no-file-parallelism自然结束exit1，总1004.77s；7项追踪通过，171文件中170通过/1失败，1284项测试中1283通过/1失败，无跳过。唯一失败为真实LRU在600.017秒超时。频道摘要4.471秒、单turn21.903秒通过，日志均明确opencode-go/deepseek-v4-flash。
- 关闭文件并发仍复现LRU超时，不能将所有停滞只归因于测试文件并发；V4 Flash独立LRU曾通过也不构成稳定性证明。供应商内部排队/调度/容量尚无服务端证据，保持inconclusive。停止追加实网重试，不改原期限、断言或选择其他模型；正式Benchmark未启用，完整用量未知不计零。
- 脱敏汇总、源文件与本机日志hash见docs/reviews/task112-v4-regression.json；只记录时序、结果及hash，不复制凭据或原始模型上下文/输出。既有桌面G5及历史失败证据保持。
- 按agora-commit的“Stop on any failure”，未commit/push，PR #79已发布head仍280f24e，两条评审会话仍未resolve。11.2恢复blocked，解除条件为已选择Go V4 Flash的真实LRU及完整G4通过后交付本地修复；11.3保持pending。当前测试路由切换已完成，阻塞的是完整回归门禁。

## 2026-09-14 Leader要求继续排查LRU超时

- 按上游保活无输出、工具流结束/解析等待、工具/角色交接三项假设排序，使用透明观测运行原LRU一次；未复现后补一次会话头/返回模型观测，然后停止。两次均原Go V4 Flash、原生思考/256000容量、600秒期限及真实工具，不修改生产代码或验收断言。
- 两轮分别201.191秒/191.657秒通过，19/14次模型请求，模型请求累计197.946秒/188.142秒，约98%耗时在模型；11/9次sandbox.run累计2.737秒/3.039秒，Coder与Tester均正常交接并结束。第二轮最慢请求117.867秒，持续生成23200字符推理及10165字符工具参数，非纯保活停滞；只保存计数、不保存正文。33个请求均HTTP200及DONE，0解析错误/保活注释。第二轮所有原生会话头存在且角色内稳定、返回模型均deepseek-v4-flash。
- 失败串行目录的Git HEAD可解析、无index.lock、只有.git；历史失败没有请求观测或sessionPersistence，无法回溯当时在等待哪一步。两次成功不排除偶发上游等待或更长生成，不把V4.1已证实保活问题套用于V4，也不声称已修复/明确定位历史600秒失败。结论inconclusive；最有证据的后续方向为模型生成/响应时延及多请求累计耗时。
- 报告docs/reviews/task112-lru-timeout-diagnosis.md及JSON；observer源码、两轮脱敏事件/计数/日志及复现说明保存在docs/reviews/task112-lru-timeout-evidence/。两轮usage合计prompt248395/completion29584，缓存拆分不全，不估算精确费用，不改写旧unknown；未启用正式Benchmark。当前9份相关源文件hash与上轮完全一致。
- 未再次运行完整G4，历史全量失败仍有效，11.2保持blocked、11.3 pending，未commit/push/resolve。后续获准复验应带观测捕获失败现场，不能靠扩大期限/降低容量/关思考/改断言宣称通过。

## 2026-09-14 PR已合并后，Leader授权先提交推送后续修改

- 已核实PR #79于2026-09-14T21:58:06Z合并到dev-1.0.0，merge commit为2a8b3ea4116a29f3d7707890ef859dc8d4956784，只包含此前发布的280f24e内容。后续两项review修复、Go测试路由与诊断均未包含，不能把人工合并旧内容当作这些修复已交付或G4通过。收尾时只快进本地dev引用，49份未提交文件逐项hash保持不变，任务未误标done。
- Leader随后明确要求“先提交推送”。据此本次先保存并推送后续修改，覆盖本次agora-commit的失败即暂停提交要求；授权仅针对当前提交/推送，不改变G4失败事实、原测试断言/期限/容量、人工PR合并或任务完成条件，也不自动启动正式Benchmark。
- 从最新dev-1.0.0的2a8b3ea创建codex/fix-desktop-review-go-tests，保留全部已核对修改；不向已合并PR分支或dev直接提交。当前真实回归仍只用Go deepseek-v4-flash，原始历史证据字节及各时点的失败记录保持。
- 本次typecheck/lint通过，3项真实子进程生命周期回归及27份打包证据校验通过。沿用已保存的16项真实桌面重建G5和两次带观测LRU通过证据；完整G4仍以上次串行1283/1284通过、LRU600秒超时为当前未通过状态，本次不重复启动模型测试。索引/敏感数据/diff及精确暂存检查后提交，11.2保持blocked、11.3 pending。
- 暂存检查发现两份原始Vitest日志各自保留末尾空行。为维持已记录的原字节/hash，仅在诊断证据目录.gitattributes对这两个确切文件禁用文本转换及blank-at-eof检查；不改日志、不扩大到代码或其他检查。新增属性文件一并登记hash，精确提交范围变为50份文件。

- 本轮后续修复内容提交068480d5d38ca33f18b2e3ab1c532b1b2d2dfd53（Fix desktop review findings and route live regressions through Go V4 Flash），交付分支codex/fix-desktop-review-go-tests。精确50文件暂存内容与工作区字节一致，18份诊断来源/材料hash、索引及diff检查通过；现有凭据扫描无命中。随后仅补记本条交付身份和当前索引摘要，完整G4的阻塞结论保持，不自动创建/合并新PR。


## 2026-09-14 全面审查并通过完整 G4

- Leader要求“全面审查一下，目标是通过G4”。对cd9e99a相对dev-1.0.0的50文件差异、两条PR #79评论、适用规格及累计覆盖进行审查，未发现新的必须修复问题；本轮不改产品/测试源码。历史清单范围与子进程启动关闭修复已在当前分支核对，27份打包manifest哈希一致，3项真实子进程回归通过。
- 使用原Vitest配置的全部include/setup和默认文件并发，只向三个实网文件加入透明脱敏观测。pnpm run test --config .data/diagnostics/agora112-full-g4-review/vitest.config.mjs退出0：Node 7/7，Vitest 171文件、1284/1284，0失败/skip，201.86秒。typecheck、lint同时通过，两个真实已配置凭据在50个变更文件中0匹配。
- 三项实网均为Go deepseek-v4-flash；LRU200.198秒通过，17请求全部HTTP200/DONE，模型累计196.190秒，11次沙箱命令累计3.365秒；单轮Harness14.387秒、频道摘要9.427秒通过。原生thinking enabled/maxTokens256000及原期限/断言/重试均未改变，未恢复正式Benchmark或调用V4.1/官方后备。
- 完整G4当前已通过；历史600秒/120秒超时未复现，根因仍inconclusive，保留原始失败与此前诊断，不宣称间歇性延迟已修复。本轮只进行一次全量验证，无失败后的选跑或重复碰运气。报告docs/reviews/task112-g4-review.md及JSON；逐文件结果/hash、观测源码和三个请求时序见docs/reviews/task112-g4-evidence/。
- G4阻塞已解除，11.2恢复in_progress，11.3仍pending。PR #79仅合并旧280f24e，当前修复分支仍需后续PR人工合并；本轮仅保存证据及追踪更新，未commit/push/回复/resolve/合并PR。Phase11产品出口仍未实现，不能以累计G4冒充11.5发布验收。


## 2026-09-14 Leader授权提交推送完整G4证据

- Leader要求“先提交推送”。本次交付限于完整G4审查报告、脱敏观测材料及追踪更新，沿用codex/fix-desktop-review-go-tests分支，不自动创建或合并PR。
- 复核171份实际测试文件源码、全部新增证据及27份打包manifest哈希一致；产品、测试断言及默认配置与刚通过的cd9e99a一致，故沿用同轮typecheck/lint、完整G4和适用G5，不重复触发实网请求。新增材料中两个已配置真实凭据均0匹配。
- 原始typecheck日志保留尾部空行及原字节/hash，仅对此确切日志设置-text及blank-at-eof属性；不修改日志或放宽代码检查。任务保持in_progress，等待后续修复PR人工合并。
