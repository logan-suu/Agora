# 任务 1.1：MCP SDK v1 接入 + fs-server（read/write/list）

本文件保存历史证据；当前状态、依赖和时间戳只以 [任务索引](../task-status.json) 为准。原文中的 ready/in_progress/done、待合并和延期项状态描述记录时点，不覆盖当前索引或延期台账。后续事件追加到文末，不改写原文区。

## 2026-09-13 原 notes 无损迁移

Source commit: `fa3efafed328bdfeb5b0f575b4e57bbd248a45fd`
Original notes SHA-256: `0339786c0c3b0400a42b89cae45593815dc5ba23998f288b02f12b9544f623b0`

~~~~text
<!-- BEGIN ARCHIVED NOTES -->
MCP SDK v1（@modelcontextprotocol/sdk 1.30.0）接入完成，fs-server 落 packages/tools/fs/。实现：worktree-registry.ts（WorktreeRegistry 白名单，工具 worktree 参数必须已注册根，防传 /etc 越界）+ fs-service.ts（WorktreeFsService：read/write/list，assertInside 越界防护 R7，自研轻 glob：*/?/** 支持，跳过 .git/node_modules/.data）+ fs-server.ts（createFsServer 注册 read/write/list 三工具；serveFsServer(transport) 为 transport 注入点，默认 stdio、可切 Streamable HTTP，R9 换体不换接口）。transport 选型按用户确认走方案 C：stdio 默认 + 注入点。zod 对齐 v4（SDK peer 解析 zod@4.4.3，fs 包 zod ^4.0.0 消除双版本）。G5 真实执行：fs-server.test.ts 用 SDK InMemoryTransport 做 client-server 端到端 round-trip（write/read/list 真文件系统 + 越界返回 isError），非 mock。门禁：tsc 0 错误 / biome 0 违规 / vitest 85 通过 2 跳过（2 跳为既有 G5 live-LLM 需 DEEPSEEK_API_KEY）。
[2026-08-27 提交] PR #12 https://github.com/logan-suu/Agora/pull/12（feat/mcp-fs-server，提交 9063749，9 files / +491）。status 保持 in_progress，待人类审阅合并后 /pr-merge-agora 收尾标 done。
[2026-08-27 PR#12 评审] /pr-review-agora 预审 + CodeRabbit 三条评论逐条验证均合理，已当场修复并 push（vitest 85→88）：① task-status.json 1.1/1.2 间缺 `},` 分隔致 JSON 非法——补回分隔符恢复合法；② serveFsServer 补 transport 可选、默认 new StdioServerTransport()，落实「stdio 默认 + 注入点」（R9）；③ fs-service assertInside 补 realpath 规范化（根与目标 realpath，新写目标上溯最近存在祖先）封堵 symlink 逃逸（R7，CodeRabbit 🟠 Major）——新增 3 用例：read 文件 symlink 逃逸 / write symlink 父目录逃逸 / 根内 symlink 正常放行。
[2026-08-27 PR#12 二轮评审] CodeRabbit 复审通过后又提 2 条安全评论（均验证有效）：① CWE-59 路径穿越：WorktreeRegistry 改注册时绑定 realpathSync 规范化根，操作时 realpathSync 当前根并与绑定值比对，不一致即抛 retargeted 拒绝（fs-service assertRegistered 改用 canonicalOf，删除 isRegistered）——新增 retargeted symlink 用例（read/write 均拒），vitest 88→89；② CWE-367 TOCTOU：校验后到 readFileSync/writeFileSync 间并发 symlink 替换竞态——[DEF-004]。
[DEF-004] TOCTOU symlink 竞态（CWE-367），台账指针：docs/deferred-items.json#DEF-004；解除条件: DockerSandbox（1.4）worktree 隔离缓解；真并行（Phase 4/9.2）重评 openat 重构。target_phase: 1
[2026-08-27 PR#12 合并收尾] /pr-merge-agora：merge commit 369889d5（PR #12 merged），dev-1.0.0 已同步，本地 feat/mcp-fs-server 已删除。G5 实测结论：InMemoryTransport client-server round-trip（write→read→list 真文件系统）+ 越界/symlink 逃逸/retargeted 三类防护用例全部真实通过（vitest 89 pass 2 skip），非 test double。遗留/延期项：DEF-004（TOCTOU symlink 竞态）已入台账，解除条件=1.4 DockerSandbox worktree 隔离。门禁 G1-G7 全过，PR #12 合并 hash 369889d5b7d2c62ff1e03e9ba429bc01a3ee5c78。
<!-- END ARCHIVED NOTES -->
~~~~
