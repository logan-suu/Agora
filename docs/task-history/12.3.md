# 12.3 实现受控本机执行与普通目录工作区

## 2026-09-15 开工授权与 Explore

- Leader在初始化报告、下一任务12.3及“进入代码探索并形成具体实施计划”的说明后回复“确认”。本轮据此开工并完成Explore；尚未确认下面新形成的具体代码计划及验收目录，未执行宿主项目代码、G5、下载或模型调用。
- 当前Phase12；依赖12.2、11.4、11.5及前阶段出口11.5均done。复核无pending→ready级联候选，索引校验通过。任务类型code，完成仍需G1–G7及PR人工合并。
- 起始工作区干净，从dev-1.0.0创建功能分支`feat/task-12-3-controlled-local-execution`；未commit/push。初次分支创建因Git引用写权限失败，工具升级审批后创建成功。
- 已读AGENTS.md、task-tracking、蓝图§21 D18/§22.3.1、详细设计§12.2、计划§18.2/§18.3/§18.11及选型§12相关边界。未发现当前任务范围内的来源冲突；12.2的候选机制仍须实测，不把设计接受当可行性证明。
- 源码接缝：`packages/runtime/sandbox/src/secure-files.ts`固定根dev/inode并向native helper传递fd；`native/secure-files.c`已有openat及硬链接/特殊文件拒绝，但write采用ftruncate+transfer，没有预期版本、同卷交换和恢复journal。现有竞态测试覆盖根内目录/外部symlink切换，不能据此声称覆盖已打开目录被移到授权根外后的副作用。
- `local-temp-sandbox.ts`仍为Phase0真实实现，run使用shell、继承环境及单子进程kill，不能作为新本机launcher或supervisor。该差异是既有路径适用范围，不因本任务提前改写或移除旧回归。
- `apps/desktop/src/toolchains.ts`已有锁定版本选择与私有HOME环境构建；文件版本、OS策略与子孙进程收敛仍需新增。现有MCP/bridge及Web验证链以WorktreeRef/Git HEAD为中心，direct模式必须接新增类型，不能填假branch/commit。
- `tests/integration/phase12/`尚不存在；task.test_file仅是规划入口，未实施或运行。

## 待确认实施计划

### 单元A：先验证launcher与文件事务可行性

1. 在`packages/runtime/sandbox/native/`增加本机验证所需的受信launcher/事务helper，TypeScript接缝位于同包`src/`，构建接入既有`scripts/build-native.mjs`。不增加第三方依赖，不修改冻结SandboxManager/Executor/TaskStateStore方法。
2. 先在`packages/runtime/sandbox/test/`及`tests/integration/phase12/phase12-3.test.ts`写真实失败用例，再小步实现。检查Seatbelt策略缺失/错误时无执行；源码只读、专用输出可写、模拟凭据/环境/fd/IPC/网络拒绝及子进程继承；supervisor覆盖超时、setsid/double-fork与进程出生身份，未证明收敛不释放写根。
3. 文件事务先覆盖本机APFS、严格写路径、预期版本、no-replace创建、同卷交换保全、崩溃恢复，以及受控同步屏障下根/父目录被移出授权范围的真实竞争。任何已确认关键保护缺口立即停止依赖实现；不以路径前后检查或概率性压力测试声称证明安全。
4. 对应L02/L03/L05/L06/L14/L15的关键前提先形成证据。此单元通过仅允许继续实现，不宣称L01–L18或完整12.3已验收，也不开放普通项目入口。

### 单元B–D：仅在A可行后按同一范围继续

- B：`packages/core/domain`增加workspace/file版本与localExecution串行控制字段和校验；`runtime/sandbox`实现版本化registry、grant/claim、prepared/committed回执与恢复、文件读写及固定输入快照。测试跨任务/陈旧revision/重复action/半事务拒绝，覆盖L06/L16。接管和Git并行的完整功能仍归12.5/12.4。
- C：既有`packages/tools/fs`、`tools/sandbox`、`tools/bridge`接`workspace_read/workspace_apply/workspace_run`，同步角色工具白名单、WorkerRuntime/投影与必要Web组合根绑定；本机worker只注册版本化入口。direct采用files manifest，不走旧Git判别器；验证、终审和归档接同一版本边界，完整波次并行留12.4。
- D：受控安装/生成副本、下载代理白名单及受管依赖挂接；实测普通目录编辑/依赖安装/构建/测试、拒绝越界和凭据隔离，覆盖本任务L01–L06/L14–L16责任。保持既有仍适用回归，Docker移除仍归13.3。
- 每单元TDD及定向验证；产品变更完成后运行`pnpm build:sandbox-native`、`pnpm typecheck`、`pnpm lint`、完整`pnpm test`与适用G5。模型链使用已定Go `deepseek-v4-flash`，不启动Benchmark、不换模型。最低系统等缺失证据明确记录，不沿用11.5例外作自动豁免。
- 实装范围、实测结果及失败更新详细设计§12.2对应验证入口和本历史；索引仅保存短摘要。若可行性失败涉及实质契约取舍，停止依赖编码并按agora-sync-docs提交可审阅修订。

### 待确认的专用验收目录与权限

- 建议仅使用新建的`/private/tmp/agora-task123-validation-<随机后缀>/`作为12.3真实验证父目录；所有模拟用户项目、授权根内外哨兵、模拟凭据、私有HOME/tmp/cache、进程输出与安装副本均在该父目录内。外部哨兵只是相对被测授权根位于“外部”，不使用真实用户秘密。
- 工具链使用11.4锁定清单和受信绝对路径；缺少所需版本时先记录，不静默使用其他版本。受管工具只读执行、已锁定来源下载和依赖代理成功路径按实际测试需用范围留证；不授予任意网络/宿主目录访问。
- 测试只启动和停止本轮有出生身份记录的进程，目录重命名/故障注入仅限上述新建fixture。不触碰真实项目、正常依赖、全局缓存、产品状态、钥匙串或用户服务。
- 每轮按TEST-CLEANUP先保存结果、版本/来源/hash和必要日志，再核验进程/挂载停用和路径归属，清理专用下载/构建/依赖副本并记录空间变化。尚未创建上述目录或运行测试。

本轮验证：仅只读探索及追踪维护；不计产品G3/G4/G5。无测试产物和进程，TEST-CLEANUP不适用。下一步等待具体计划及专用验收范围确认。

## 2026-09-15 计划与验收范围确认

- Leader对上述具体计划及`/private/tmp/agora-task123-validation-<随机后缀>/`测试范围回复“确认”；授权按A→B–D顺序推进、真实fixture验证及先留证后清理，无需重复确认同一范围。
- 单元A优先验证L02/L06的目录reparent边界：以同步屏障安排“打开/校验目录→外部进程移出授权根→同卷交换”，另有正常交换对照。候选增加Seatbelt约束，避免只证明无OS边界的旧helper不足就否定候选。关键边界若失败即停止依赖实现，形成精确机制证据和可审阅修订。
- 当前宿主macOS26.5/build25F71、arm64；开发Node24.20.0仅运行研发测试控制器，不冒充11.4受管工具链G5。无最低系统结果，不推断macOS15覆盖。

## 2026-09-15 单元A实测失败与停止点

- TDD：新增`tests/integration/phase12/phase12-3.test.ts`，首次实际执行因缺少fixture模块失败（Vitest收集失败，0个用例执行）；随后增加真实控制器和仅用于可行性验证的C探针。`pnpm build:sandbox-native`成功；新探针不进入生产导出或打包。
- 最初在外层研发沙箱中sandbox_apply返回EPERM，helper没有执行；权限工具审批后同一控制器在外层限制之外执行，被测helper仍走Seatbelt。初始策略缺少dyld/libignition读取OS Cryptex及映射所需的例外，main前SIGABRT；依据本机Apple dyld-support.sb和对应崩溃栈校准最小只读规则，normal对照通过。没有裸跑/LocalTemp/Docker回退，也没有扩大fixture写范围。
- 首轮根相对交换三用例3/3通过：正常交换保留两版；父目录移出返回ENOENT；根移出返回EPERM。使用RENAME_SWAP/NOFOLLOW_ANY/RESOLVE_BENEATH，不将当前SDK标志视为macOS15实测。
- 增加已打开事务文件句柄场景后，完整5用例4通过/1失败：root移出后pwrite+fsync成功，把已处于outside的候选文件从candidate改为prepared!。源target保持original；只使用已批准的新建fixture。
- 当即停止registry/MCP/产品direct接入及其他依赖实验。按三假设排查：旧fd仍有权限（主要）、策略误放outside、移动时序错误。追加原地最小复核证实F_GETPATH为outside路径、同一路径重新openat返回EPERM，但旧fd写入成功；原安全断言保留。根因是当前根内事务区与已打开可写fd生命周期不能满足严格根移动边界，不能通过再加一次路径检查修复。
- 在添加固定NODE_ENV=test修复控制器类型检查后，最终完整5用例仍4通过/1失败、0skip，日志`docs/reviews/task123-boundary-evidence/final-five-cases.log`。早期定向-t输出的未选择用例仅作调试，不当验收豁免；默认测试匹配没有更改。
- `pnpm typecheck`初次因Next全局ProcessEnv需要NODE_ENV而失败，修复后退出0；`pnpm lint`退出0。完整pnpm test未执行、产品G4/G5未完成：关键前提失败后按§12.2.8停止依赖工作；不运行无关付费回归或Benchmark，不把局部对照和静态检查冒充完成。
- 原始逐轮JSON保存OS/架构、源码与二进制hash、策略、进程、结果和清理；最终源码/fixture/断言hash、静态日志及逐文件证据hash见`docs/reviews/task123-boundary-evidence/summary.json`。末轮测试与当前源码一致；环境失败和保护失败均独立保留。
- TEST-CLEANUP：所有已记录fixture在证据落盘后确认原生单进程close、lsof无句柄、无对应挂载及uid/dev/inode/realpath归属，删除测试目录/编译副本；逐轮路径、删除结果和磁盘可用空间变化见summary及原始JSON。可用空间受APFS并发活动影响，不将其变化总和冒充精确回收量。无下载、专用npm依赖或安装副本；正常项目native构建产物属于项目构建依赖，保留。两份临时静态日志已归档并核对句柄后删除；未触碰用户项目、共享缓存、应用、产品状态或钥匙串。OS自行生成的诊断报告未擅自删除。
- 使用agora-sync-docs只同步实测事实至蓝图§21 D18、详细设计§12.2、选型§12、计划§18.2；没有自行修改已接受的技术路线或扩大/放松保护。具体方案、精确失败和待确认条款见`docs/reviews/task123-local-boundary-feasibility.md`。
- 建议将事务区移到同卷的Agora受保护状态域，完成候选写入/flush并关闭全部写fd/映射后才做项目文件交换；首版跨卷拒绝。这改变§12.2已接受的根内暂存位置和卷范围，待Leader确认后先同步正式契约并继续最小验证，不保证候选已可行。
- 任务转blocked，解除条件：Leader确认上述修订或另一明确方案，正式契约同步后恢复单元A；保护复验成功才继续B–D。12.3不标done，未提交/推送/PR；Phase12及其他任务不变。

## 2026-09-15 Leader要求先评审规格合理性

- Leader明确：“不一定严格依照现有的规格文档，需要先评审一下当前的规格文档合不合理，不合理的话先对文档进行修改”。本轮按DOC-CONFLICT评审并直接同步不合理条款，不把上一轮迁移建议视为已接受，不再重复索取相同范围的规格维护授权。
- 评审范围为§12.2本机授权/根身份/文件事务/撤回收敛及相关D18文档，不声称全仓库规格已审计。来源原文、POSIX背景、原始回执、三类边界和逐项结论见`docs/reviews/task123-spec-reasonableness.md`。
- 关键结论：原文本把从未授权的独立外部对象与随根移动的原已准入技术文件混为一类，隐含OS瞬时撤销旧fd，与已有先关闭新准入、再收敛在途操作的撤回协议矛盾。探针在F_GETPATH/重新打开拒绝后仍故意pwrite，只观察原语；没有验证正式服务发现漂移后停写，不能据此认定产品已越权，也不能宣称产品已安全。
- 修正为：新访问严格检查当前身份/授权，独立外部哨兵/凭据仍不可读写；原已准入对象的在途效果不保证瞬时撤销。发现漂移后停止后续副作用并关闭能力/收敛；操作失去当前绑定的成功/验证/交付资格，保全实际副作用及版本，后代和fd未收敛不报告完成或清理。不跟随移动路径、不扩大旧目录fd权限、不把后置检查当已消除竞态。
- 保留本机APFS、同卷根内事务区候选、原冻结接口和L01–L18编号；不采纳上一轮“强制迁往应用状态卷”的过早建议，不增加跨卷限制。关闭自身候选写fd/映射后再交换到源码；涉及任意项目脚本越权、源文件保全及进程收敛的保护保持强制。
- 按agora-sync-docs顺序同步蓝图§21 D18/§22.3.1、详细设计§12.2、架构§10、选型§12、计划§18.2、D18摘要/12.3索引和AGENTS.md；上一轮报告加历史阅读说明，原始JSON/日志及测试源码不修改。没有变更12.2的已完成事实或删除历史。
- 解除“等待迁移方案确认”的阻塞：依赖仍全部done，12.3恢复in_progress，既有具体计划和测试目录授权保持；下一步为按新准入/在途收敛契约补真实端口/回执测试。产品G4/G5仍未完成，旧测试的4通过/1失败是历史事实，不通过只反转expect恢复所谓全绿。
- 本轮先完成文档复评/同步，不修改产品、native探针或测试断言，不启动模型/测试/下载；不虚构TDD或重新执行无关付费回归。代码提交前完整门禁仍适用；未commit/push。文档/索引/链接/hash校验另记下方。
- 文档验证：`node scripts/task-status.mjs check`通过（171任务、31阶段、25决策、69历史）；`git diff --check`通过。两份评审报告的4个本地链接均存在；详细设计L01–L18共18项且无重号。12.3依赖及前阶段出口均done，没有遗漏的pending→ready级联。D18摘要494码点、任务notes362码点，均满足索引长度限制。
- 证据保真：按原summary重新计算3份探针/fixture/测试源码及25份归档文件SHA-256，全部一致。本轮未重跑代码测试，原4通过/1失败与G4/G5未完成结论保持；文档检查不计产品验收。

## 2026-09-15 继续单元A：内部文件事务

- Leader回复“好的继续”，沿用既有计划和专用fixture授权。新增native单文件替换原语、内部host journal/检查点协调和真实测试；尚未公开导出或接入产品registry/MCP。详细实现、TDD失败/修复、适用范围及证据见`docs/reviews/task123-file-transaction-unit.md`。
- 首轮新测试缺fixture收集失败；随后修复macOS无closefrom、嵌套sandbox_apply被拒、精确父链目录读取权限、系统扩展属性兼容和最终异步授权后根检查竞态。各轮JSON保留实际源码/编译器/OS/binary hash与结果，不改失败断言来通过。
- 最终17事务场景+5原语诊断共22项通过。原瞬撤fd错误断言按已接受规格复评改为OS诊断；原测试源码按既存hash归档，旧25份JSON/日志hash保持。新增测试明确验证授权关闭、真实交换前后失效、用户修改/元数据保全、独立外部哨兵和坏回执拒绝；不是仅翻转旧expect。
- `pnpm build:sandbox-native`、`pnpm typecheck`、`pnpm lint`通过。完整`pnpm test`被自动审批拒绝，未启动；原因是外部真实模型的具体payload未明确授权。已核对三个真实模型用例并询问固定测试任务、Agora角色提示/工具描述、临时LRU代码及结果发送至Go的具体范围；不绕过审批，不改变provider/模型、不skip。G4及正式端口G5仍未完成。
- 事务控制器82个临时fixture全部先留证、核实无活进程/句柄/挂载及路径归属后清理，逐条见新证据summary；没有新下载/依赖安装，未删用户项目/共享缓存/应用或产品数据。新增原语测试编译副本随fixture删除，既有项目native构建产物保留。
- 后续继续单元A的受控项目命令与后代收敛证明；SDK不支持NOTE_TRACK自动递归追踪，不能据父进程退出宣称全部后代停用。L03/L04/L05/L14/L15未闭合前不进入依赖它的产品执行接入。任务保持in_progress；没有commit/push/PR，不标done。

## 2026-09-15 完整回归外发授权与执行

- Leader回复“授权”，明确接受上一轮列明的完整回归外发范围：固定算术/虚构群聊/LRU测试任务、Agora角色提示与工具描述、临时LRU示例代码和测试结果，目标OpenCode Go `deepseek-v4-flash`，使用现有模型额度。授权不扩展至任意用户项目内容、秘密或Benchmark。
- 自动审批在明确授权后允许原始`pnpm test`；本次已启动，不修改测试匹配、不skip、不切换提供方/模型。最终结果及清理证据将在完成后追加；运行日志`docs/reviews/task123-transaction-evidence/full-regression.log`。

- 授权后原始`pnpm test`退出0：任务追踪脚本7/7；Vitest 191个文件、1362项测试全部通过、0失败，耗时544.27秒。真实LRU闭环/Harness单回合/群聊摘要三个用例均在日志明确标记`opencode-go/deepseek-v4-flash`；未单独计量请求数或费用，不编造usage。没有skip、换提供方或正式Benchmark执行。当前代码G4通过，剩余本机能力G5仍待实现/验证。
- 完整回归TEST-CLEANUP：通过生成时段、源码固定/动态taskId及哈希、uid/dev/inode、文件清单hash、lsof/系统与Docker挂载核验，先留证再删除147+5=152个测试目录。31个测试目录仍有虚拟化进程只读目录句柄，保留并注明重验条件；没有为清理而停止用户Docker/虚拟化服务。每项路径/文件hash/清理结果及APFS可用空间变化见full-regression-cleanup.json与followup；空间变化受并发系统活动影响，不当作精确回收量。
- 结果同步至详细设计§12.2.8、首单元记录与当前索引；任务保持in_progress，不把全部任务或正式端口G5标完成，未commit/push。

## 2026-09-15 命令后代机制 Spike 与候选暂停

- 承接Leader“继续”，在原获批专用fixture执行固定C/受管Node探针；payload始终受Seatbelt限制，控制器经工具权限审核在外层沙箱之外运行，不执行用户项目。最初进程组过滤证明setsid/setpgid直接调用可拒绝，但posix_spawn属性仍可脱组；同时拒绝posix_spawn破坏受管Node子进程，ENOSYS触发libuv断言。失败及修正对照原样保留。
- 临时用户级launchd job的SessionCreate建立独立审计会话；double-fork/setsid/setpgid后孙进程保留ASID，job主进程退出不代表孙进程退出。按audit token出生身份停止成功，伪造pidVersion被拒绝。PID枚举只是发现机制，不用扫描空集合签发收敛。
- coalition私有计数候选在本机观察到6/5→6/6，但未证明完整继承、防迁移、可信登记/恢复、最低系统等。该候选此前只提交给Leader评审，未得到生产选型授权，没有接registry/MCP/产品入口。完整原报告：docs/reviews/task123-command-supervision-review.md。

## 2026-09-15 Leader要求参照成熟Agent继续复评

- Leader先询问市面Agent方案，随后对“保留原生沙箱和未知不复用保护、暂缓私有SPI，先核对成熟机制”的建议回复“按照你的建议继续”。沿用相同研究/fixture范围，不把此回复当降低收敛保证的许可。
- 使用agora-retry-task及agora-sync-docs恢复、核对来源和同步。官方源码显示所查Codex进程组路径标注best-effort；Gemini所查Unix实现组合进程组与pgrep父链扫描。Claude Code当前文档明确覆盖setsid等脱离shell进程的清理，但未取得对应完整生产实现；不臆测其使用coalition或某种标记。未运行竞争产品、未新增第三方依赖。
- 新候选为SDK公开但已弃用的auditon(A_GETSINFO_ADDR)。先按三假设（能以会话消失判定、引用滞留、参数错误混淆）设计最小复验。AVa3pC、u9AUO2两轮均证实自身会话正对照成功、错误长度EINVAL；已登记后代存活/停止/job卸载后三阶段均仍可查到会话，第二轮再有界观察9秒仍相同。未得到可用收敛判定，不更改断言或把等待当证据。弃用警告完整保存，仅探针编译允许该类warning，项目编译门禁不变。
- 形成docs/reviews/task123-agent-supervision-comparison.md，区分访问隔离、发现式清理和证明收敛；保留强保证、暂停coalition接入。另列“接受有界清理、明确漏检风险”的具体草案供Leader选择，未生效，不擅自改写正式保护。依赖launcher可行性的生产接入仍暂停，12.3保持in_progress，不标done。
- 同步蓝图§21 D18→详细设计§12.2.6→架构§10/选型§12→计划§18.2及任务索引。红线未变化，不再修改AGENTS。来源文档记录候选未验与暂缓，不把竞争产品功能说明替代G5。
- TEST-CLEANUP：两套新增fixture编译副本/临时job均已留证、卸载并核验路径/句柄/挂载后清理；原UiqBqr保留目录经原uid/dev/inode、binary hash、10场景全部waitpid结果及无句柄/挂载复核后删除，独立cleanup-retained-fixture.json保留旧未清理记录。此次空间可用量增加20480字节，仅APFS观察。前轮用户虚拟化进程持有的31个其他回归目录保持不动。
- 命令诊断汇总docs/reviews/task123-command-evidence/summary.json覆盖14份原始记录和38份文件，全部控制器/外置源码hash对应历史副本，14个fixture当前均不存在，各临时job原记录均卸载成功。内联Node脚本由对应控制器留证，不虚构额外sourceHash。未重复付费回归、未运行Benchmark；既有191文件/1362测试结论仍仅归属前轮源码版本。本轮是诊断/文档检查点，不宣称产品G4/G5完成。

- 本轮收尾检查：38份证据hash、7个诊断控制器语法、14个报告本地链接及git diff --check均通过。索引检查曾因D18摘要超过500码点失败；仅压缩摘要措辞并保留完整来源，最终498码点，task-status check通过（171任务/31阶段/25决策/69历史）。无commit/push/PR。Leader取舍问题已提交；答案前不执行降低保证的草案，私有SPI仍暂缓。


## 2026-09-15 扩大现成方案研究与launchd卸载补测

- Leader要求“多参考市面上的现成方案继续研究”。承接现有强保证，取消当前摘要里“等待Leader取舍”的过时下一步；历史降低保证草案未采用，coalition生产接入仍暂停，不重复二选一提问。
- 对照扩展到pi、Goose、GitHub Actions、Jenkins、Bazel、Nix，以及Linux cgroup v2、Windows Job Objects、Apple container和托管runner VM。6个源码仓库固定commit，成功文件记录SHA-256/字节数/定位行；Goose首个路径404保留，之后按固定commit树定位实际shell实现。公开响应仅内存读取，未下载执行或安装竞品；来源清单见docs/reviews/task123-command-evidence/market-source-manifest.json。
- 新发现Apple es_new_descendants_client：官方DocC标注macOS27引入，免root/TCC但要求申请Endpoint Security entitlement。修正“所有ES都需要root”的潜在泛化，不把新API说成现成可用；本机26.5 SDK无声明，动态库符号查询为false，未调用ES、未申请权限或安装SDK。网页Beta提示与DocC beta=false展示差异原样记录，不能代替发布可用性证明。官方来源hash、元数据与本机只读证据见apple-descendants-api-review.json。
- 假设复验：launchd主进程退出后仍有后代，不必然说明bootout也无效；故补测存活后代期间卸载job。随机用户域job显式AbandonProcessGroup=false、固定Seatbelt double-fork/setsid/setpgid payload；bootout退出0、job查询113不存在后，同一PID/ASID/pidVersion孙进程仍活。随后仅按登记audit token停止，fixture自退出期限结束后确认无句柄/挂载及路径归属再清理。结果launchd-bootout-9XEfTa.json，cleanup.removed=true，未停止用户服务。
- 结论：当前尚无满足既有全部部署约束且完成收敛验证的现成替代，不宣称公开机制均不可能。继续比较独立身份、内核进程容器、VM及新ES的部署成本/兼容性，避免再叠加扫描当证明；不扩大特权、不改最低系统/签名要求或保护强度。报告新增§六–九，按agora-sync-docs顺序同步蓝图、详细设计、架构、选型、计划、索引；D18摘要和AGENTS红线保持。
- 无产品代码修改，不重复付费回归/Benchmark。此前全量回归只归属其当时源码；本轮单独诊断不算任务G5。12.3保持in_progress，registry/MCP/产品执行仍等待launcher可行性；没有commit/push/PR。前轮31个虚拟化活句柄目录未触碰，本轮无专用下载/安装依赖。

- 收尾检查：原38份证据hash全部保持；新汇总覆盖15份诊断记录、43份文件hash，8个控制器node --check通过，报告7个本地链接存在，15套fixture路径均不存在。task-status check通过（171任务/31阶段/25决策/69历史），git diff --check通过。上述属于研究/记录检查，不扩展前轮产品回归结论。


## 2026-09-15 部署路线细化与具体契约草案

- Leader回复“请继续”，沿用已批准研究范围，使用agora-retry-task恢复后续部署比较及agora-sync-docs记录来源。保持12.3和前轮文件事务/15套命令诊断，未重跑已失败同类探针。
- 深入固定Nix提交的UID独占锁、root启用条件、组/UID降权、停止与降权竞态、残留进程及文件所有权处理；它是完整管理员构建域，不是可直接移植到登录UID的killUser工具。Apple SMAppService注册daemon需要管理员批准，不能以注册机制代替后代收敛。
- 固定Tart提交9bb2af243480ca4a2c210082e630ae425bd48b31，旧cirruslabs地址重定向openai；Stop.swift强制SIGKILL分支未追加停止等待，不把CLI返回0当回收凭据。内部VM.swift等待VZ.stop完成及网络停止；Apple官方API说明成功回调在VM停止后、强停不保证guest正常收尾。当前许可证FSL-1.1-ALv2，旧Fair Source博客不作当前依据；未作打包/分发许可判断或选用新依赖。
- 公开响应仅内存读取，固定来源/hash见deployment-source-manifest.json，官方DocC来源见apple-deployment-api-review.json。初轮补充抓取因无Makefile返回404而提前结束，随后保留404并逐文件留证；不把网络读取失败当功能失败。
- 形成docs/reviews/task123-native-execution-options.md：完整部署矩阵、首版不宜仅为清理引入root/VM的推荐理由、原生有界清理与不确定资源隔离的具体契约草案、残留CPU/内存/写根/端口及容量风险、获批后5步实施与真实验收。明确该草案降低任意后代完整退出承诺，不是对旧规格的等价修正；生效前要Leader作架构选择，当前强保证与L14不变。
- 按蓝图→详设→架构/选型→计划/索引同步研究事实和草案链接，D18摘要与AGENTS红线不变。未改产品代码、测试断言或正式接口；未安装软件、建用户/daemon/VM或下载镜像，无新增执行fixture；TEST-CLEANUP无新运行产物，前轮31个活句柄目录未动。没有付费模型/Benchmark/commit/push/PR，任务仍in_progress、launcher G5未过。

- 收尾校验：45份证据hash通过，原43份保持；两份研究报告11个本地链接存在，15套历史fixture仍不存在。task-status check通过（171任务/31阶段/25决策/69历史），git diff --check通过。本轮纯研究/文档，没有虚构新的产品测试或G5结果。


## 2026-09-15 Leader确认原生有界清理方案

- Leader对具体草案和风险说明回复“确认”，批准正式规格修订及分步实现，包含接受漏检后代占CPU/内存或写原授权私有输出风险，不放松源码/凭据隔离、不新增root/VM/私有SPI。
- 同步D18、详设§12.2.6/错误语义/L14、架构、选型、计划、索引及AGENTS阶段边界。计划默认命令30s、额外收尾5s，TERM1s/KILL复核2s，输出尾流1s/单流1MiB；checked与资源隔离分开，固定接口/安全点/D16不变。
- 当前小单元增加内部Seatbelt策略及真实子孙进程测试，覆盖源码、链接/别名、旧fd/跨命令输出、模拟凭据和允许写入；沿用专用fixture授权。首次文档同步脚本因stdin编码在解析期失败，未执行，拆分为UTF-8明确的小步后重试。

- 首个命令写域单元完成：内部Seatbelt策略、真实C子孙进程与受管Node24.20.0测试；28项C操作/8项Node操作含正反对照，4类根准入拒绝。没有接产品端口或把策略构造时检查冒充持续身份绑定，完整边界见docs/reviews/task123-command-isolation-unit.md。
- TDD首轮因fixture缺失收集失败；首次typecheck发现map(JSON.parse)签名不匹配，修为单参数解析。复核修正路径包含判断对..output的误判并实测嵌套拒绝；最终native构建/typecheck/lint通过。Phase12本地回归3文件24测试通过，用时9.72秒。
- 本轮完整回归两次被自动审批拒绝，均未启动。第一次要求OpenCode Go具体目的地及完整载荷授权；核对真实历史问答（问题01a0a77d-e641-78d2-99b3-9a6e11a6d583，授权01a0a796-42fc-78a3-ae49-c6ca42c1b2d1）后重试，审批仍认为工具输出不能证明可信用户授权。未绕过；向Leader说明原因后，Leader直接回复“授权本次固定测试外发并运行完整回归”，范围为固定算术/虚构群聊/LRU任务、Agora角色提示/工具描述、临时LRU代码和结果，OpenCode Go deepseek-v4-flash及现有额度，不含用户项目/秘密/Benchmark。随后原始pnpm test获准启动，日志docs/reviews/task123-isolation-evidence/full-regression.log。

- 完整回归首次失败为真实LRU链路，197.145秒后ExecutorRequestError（Model request failed after bounded recovery）。按证据排序保留三个待证假设：①Go传输/服务端瞬时故障耗尽内建恢复；②认证/额度/请求限制拒绝；③Harness请求或响应处理异常。此时缺cause明细，不能把首单元代码或提供方认定为根因。先读取本轮最终错误分类和既有适配器路径，不切提供方、不增开付费复测、不修改断言。

- 完整回归退出1：7项脚本测试通过，Vitest 191文件通过/1失败，1363测试通过/1失败，607.72秒。唯一失败LRU为STREAM_CLOSED/SSE stream ended without [DONE]；相同Go模型的真实单回合与群聊摘要均通过。核对官方适配器0.1.1-rc.2源码，缺DONE明确拒绝；Go兼容层未改写响应流。仅定位到适配器观察截断，具体上游根因inconclusive；未降低断言或改变超时/重试策略，原失败保留。按相同已授权固定载荷新增一次LRU单例最小复验，日志lru-reproduction.log，非批量实验。
- TEST-CLEANUP：完整回归清单183个目录中，152个来源/身份/内容hash/句柄/挂载均核验后删除；31个未满足清理证明的目录保留，路径见full-regression-cleanup.json；不停止用户服务。空间观察+4071424字节非精确回收量。前轮31个保留目录未触碰；本轮原生fixture由控制器逐次留证清理。

- LRU单例最小复验通过：同一源码/Go模型/载荷/600秒期限/断言，1文件1测试，180.83秒；没有重放不完整响应或更改DONE校验。上游截断根因仍inconclusive，首轮完整回归失败原样保留；当前各测试均有通过记录，不冒充一次完整全绿，后续交付仍须补足G4全绿调用。未追加批量实验。
- 复验清理1个LRU目录，来源/hash/无句柄/无挂载及路径身份复核后删除，APFS观察-1658880字节不当作精确回收。本轮累计153个回归目录删除，31个未停用证明目录保留；另7次原生/Node写域fixture均删除。脚本留档后删除临时控制器副本，无用户项目/产品数据/共享缓存/已安装应用清理。
- 12.3保持in_progress；正式本机入口、持续身份绑定、有界停止/资源记录、恢复/容量和完整G5仍待实现。没有commit/push/PR。首轮审批拒绝已由本次直接授权解决，不再把授权列为当前阻塞。

- 收尾核验通过：23份本轮证据hash、7个隔离fixture不存在、153个已删除回归路径及报告8个本地链接；历史命令研究45份hash保持。task-status check通过（171任务/31阶段/25决策/69历史），git diff --check通过。最终源码快照与本轮验证版本一致；证据汇总docs/reviews/task123-isolation-evidence/summary.json。

## 2026-09-15 继续：出生身份停止与资源日志

- Leader回复“继续”，使用agora-retry-task/agora-do-task恢复12.3，引用详设§12.2.6身份不匹配不发信号及有界停止约束，沿用方案/fixture/固定Go测试外发授权，不重复索取许可。依赖12.2/11.4/11.5均done，Phase11出口已过，保留既有未提交工作。
- 新增local-process-control.c及内部停止/输出原语，固定audit token及cohort快照，TERM1s/KILL复核2s/总5s；单流1MiB/主进程结束后尾流1s。registered-only只证明登记成员，不能签发完整checked。真实Seatbelt测试5项通过，包含错出生身份不发信号、忽略TERM升级KILL、正常TERM、双流超限和继承管道。
- 新增LocalCommandJournal：受信控制根及父链绑定、独占锁/原子文件/同步落盘、稳定命令输入/根与出生身份、revision CAS；中断/隔离持久阻断，同路径/嵌套/改名inode复用拒绝，无资源释放入口。固定进程真实身份接入该日志；另5项真实文件系统场景通过。容量限定与剩余生产链路明确记录，不把模拟遗留锁称为全部真实崩溃点验收。
- 两组TDD入口先因fixture缺失而红。首次typecheck因项目ProcessEnv要求NODE_ENV失败，最小重建环境补显式值后通过；首次Biome提示optional chain后修正。代码复核补锁内初始化二次检查、发布失败保留锁、固定cohort与返回输出快照；无降低断言。
- 本地Phase12检查点5文件34测试通过，21.84秒；最终源码typecheck/lint（506文件）/native构建通过。最终版本原始pnpm test已获准启动，仍为固定Go deepseek-v4-flash测试范围；前轮STREAM_CLOSED与单例通过记录不覆盖，完整结果/清理收尾后追加。证据docs/reviews/task123-stop-evidence/，实现说明docs/reviews/task123-command-stop-unit.md。

- 最终版本原始pnpm test退出0：7项脚本测试及194个Vitest文件/1374项测试全绿、0skip，675.99秒。真实LRU（242.874秒）、Harness单回合（39.071秒）、群聊摘要（4.007秒）全部为OpenCode Go deepseek-v4-flash；未运行正式Benchmark、未改自动重试/测试期限/断言。前轮STREAM_CLOSED及单例复验均保留，新全绿不回写旧失败。G4本轮通过，不代表完整本机G5或12.3完成。
- 本轮25个停止/资源日志固定fixture全部留证清理；10份实际内核audit token的持久记录、重开后的quarantined/discovery_incomplete及blocked状态逐份核对一致。用户项目及服务未触碰；原语尚未公开导出/进入desktop或web产品路径，最低系统、发现、持续绑定、正式回执与D17接入未完成。

- 完整回归TEST-CLEANUP：183个候选目录中152个在来源/身份/内容hash/无句柄/无挂载复核后删除，31个证据不足保留，详细路径见本轮full-regression-cleanup.json。APFS空间观察-3715072字节，不当精确回收量；临时控制器已留副本/hash后删除。既有保留目录、用户服务/项目/依赖/应用数据未动。

- 收尾核验：44份本轮证据hash、10份最终源码快照、报告7个本地链接通过；25个单元fixture与152个已清理回归路径不存在，临时控制器副本已删除。task-status check（171任务/31阶段/25决策/69历史）及git diff --check通过；前两轮45+23份历史证据hash保持。仍在原功能分支，未commit/push/PR，12.3不标done。


## 2026-09-15 继续：可信父子关系有界发现

- Leader回复“继续”，沿用原生有界清理、固定fixture及OpenCode Go固定测试外发授权；恢复依赖与检查点，使用agora-retry-task/agora-do-task，agora-sync-docs同步。未重复请求范围内批准，未创建PR或提交。
- 新增sysctl候选发现，当前父子关系由子audit token前后夹验，整轮由父身份前后夹验；不同出生版本、查询失败/竞态与上限明确不完整，不从项目输出PID授予停止权限。SDK libproc头有兼容警告，本轮不新增其私有枚举接口/结构，未接coalition/root/VM/ES。
- 内部发现总上限256身份/1秒，返回bounded-observation；主退出/空扫描不签发checked。固定Seatbelt树含setsid子进程与孙进程，同级无关进程保持存活；先发现再中间父退出后，孙进程仍按原登记身份停止。真实身份落日志并重开blocked；完整关系与生命周期回执尚未接入，资源仍隔离。
- TDD首轮fixture缺失而红，首次typecheck发现fixture错误字段births并改为既有birthIdentities；首次4项真实过程测试通过，补输入/退出边界后最终本地Phase12为6文件39项通过、25.80秒；typecheck/lint（508文件）/native构建通过。无mock/skip/提供方变更/断言弱化。
- 原始pnpm test已启动，日志docs/reviews/task123-discovery-evidence/full-regression.log，完整结果和TEST-CLEANUP收尾后追加；当前不宣称G4最终完成。实现与剩余项见docs/reviews/task123-command-discovery-unit.md。

- 首轮原始pnpm test退出1：194文件通过/1失败，1378测试通过/1失败，641.05秒。唯一既有ignore-term停止测试在TERM约1001.69ms返回unknown、仅发TERM，固定进程最终SIGALRM自退出；模型三用例均通过，非提供方问题。原失败记录及JSON保留。
- 按三个假设排查（查询超时/退出复核/出生身份变化）。真实延迟包装器前三次分别卡初次capture或TERM，未证实目标；最终固定130ms、已知身份由原helper取得且只读预运行包装器后，复现TERM约1003.15ms处unknown且无KILL。确认为阶段末端仍用极短剩余期限启动helper导致自造查询失败，抑制后续KILL。没有重写失败或扩大期限。
- 修复为发现/停止查询均要求剩余阶段至少完整250ms，TERM预算不足则按原阶段切换，真实错误仍needsAttention。新增真实延迟回归，未替代实际内核查询/信号。全部Phase12 6文件40测试通过、25.68秒，静态检查通过；最终源码另存10份快照，修复后原始完整回归已启动（full-regression-after-fix.log）。定向诊断-t未执行其他用例，不当作全量通过；后续无筛选回归完整执行。
- 首轮TEST-CLEANUP删除152个确认停用目录，31个不具停用证明保留，空间观察-3878912字节。首轮测试文件完成后新增测试使Vitest末尾磁盘错误片段行号变化，原用例全名/JSON和旧源码快照仍完整，报告已注明不按新行号解释旧失败。

- 修复后原始pnpm test退出0：7项脚本测试，195文件/1380测试全部通过、0skip，559.43秒。真实LRU137.636秒、Harness单回合22.503秒、群聊摘要4.971秒均Go deepseek-v4-flash；原ignore-term和新增延迟测试均在完整环境通过。原始失败、前三次未到目标的诊断与第四次目标复现均独立保留，不把局部复测替代全量。源码从最终冻结至全量结束未改。
- 本轮162个原生/文件事务/日志专用fixture先留证后清理，其中后代发现20个；清理索引保存每份记录hash及路径，全部确认不存在。旧命令研究45份、私有写域23份、停止单元44份历史hash保持。第二轮完整回归目录清理待收尾，不据此标12.3完成。

- 修复后完整回归TEST-CLEANUP删除152个确认停用目录、31个保留，空间观察7389184字节，清单full-regression-after-fix-cleanup.json。两轮累计304个回归目录删除、62个待停用证据；另162个单元fixture已清理。未触碰用户项目、共享缓存/依赖、安装应用、产品数据或服务。

- 最终收尾核验：67份本轮证据hash、10份最终源码快照、162份单元清理回执及报告链接通过；历史45+23+44份hash保持，304个已删回归路径不存在，两个临时清理控制器留档后删除。task-status check（171任务/31阶段/25决策/69历史）与git diff --check通过。索引保持in_progress，完整本机G5/生产入口仍未完成；未commit/push/PR。最终汇总docs/reviews/task123-discovery-evidence/summary.json。


## 2026-09-15 继续：持久父子关系与内部命令监督

- Leader回复“继续”，使用agora-retry-task/agora-do-task恢复最新195文件/1380测试检查点，沿用已确认方案、固定Seatbelt夹具和Go固定载荷外发授权；依赖12.2/11.4/11.5与前阶段出口均done，无级联候选。agora-sync-docs同步详设和计划。
- 内部日志v2新增父子关系和观察摘要，原子身份/关系、revision CAS、顺序无环、跨记录出生身份唯一；v1/坏记录拒绝，不提供迁移/清空或释放。内部监督接当前可信ChildProcess，默认30秒、运行中发现并持久登记；主退出/取消/超时/观察错误统一停止已登记成员并有界结束输出，返回主进程结果与durable状态，资源始终隔离。没有接生产入口或改变冻结方法。
- TDD入口缺fixture而红；初次格式检查发现关系校验函数缺结束括号，修正后静态通过。首5项真实Seatbelt监督测试通过；新增最终日志锁导致落盘失败场景后，Phase12 7文件46项通过、61.73秒。默认30秒实测，错误身份不发信号/保持进程活，磁盘失败保持reserved且durable=false。
- 复核补每轮控制面revision/隔离复查，并新增外部隔离终止观察用例，最终7项单元与完整回归待结果。具体实现范围/剩余项与证据见docs/reviews/task123-command-supervision-unit.md；任务保持in_progress，未commit/push/PR。

- 最终7项监督单元全通过（35.73秒），typecheck/lint（511文件）/native构建通过；18个目前已结束的监督fixture均已留证清理。已固定9份源码快照并启动原始pnpm test，三个真实模型用例仍限定已授权Go deepseek-v4-flash载荷；旧45+23+44+67份证据hash保持。完整结果及回归清理收尾后追加。

## 2026-09-16 内部监督最终验证与清理

- 原始pnpm test退出0：7脚本测试、196文件/1387测试全绿、0skip，566.90秒；真实LRU97.627秒、Harness单回合21.070秒、群聊摘要3.815秒均Go deepseek-v4-flash。新增7项监督场景在完整环境全通过；未换提供方/模型、未恢复Benchmark、未改变断言或期限。
- 最终9份源码快照与实测代码一致；本轮103个固定原生/事务/日志fixture均已逐份核验结果/hash及删除状态，其中监督25个。完整回归临时目录清理正在按来源、身份、内容、句柄与挂载核验，完成后追加。

- TEST-CLEANUP最终：完整回归152个确认停用目录删除、31个证据不足保留，空间观察+8642560字节；103个单元fixture（监督25个）已核验删除。临时控制器保存源码/hash后删除，无用户项目/服务/正常依赖/缓存/产品数据清理。
- 收尾核验52份证据hash、9份最终源码、10个报告本地链接及全部清理回执；旧45+23+44+67份证据hash保持。task-status check及git diff --check通过，12.3保持in_progress，未commit/push/PR。最终证据docs/reviews/task123-supervision-evidence/summary.json。

## 2026-09-16 继续：受信暂停启动闸门

- Leader回复“继续”，使用agora-retry-task恢复最新监督检查点，沿用已授权Seatbelt固定验收目录及OpenCode Go固定测试外发范围，不重复索取同范围许可。agora-sync-docs同步详设、计划与索引；既有未提交工作保留。
- 复核Apple XNU及本机SDK：exec会改变audit身份，Darwin START_SUSPENDED可在目标运行前暂停。新增内部受信bootstrap，在Seatbelt内暂停创建目标；内核父子关系/身份持久登记，输出观察附着后，复查授权接缝及revision才按audit token放行。控制fd关闭、宿主环境重建，目标退出结果和bootstrap结果分开；资源仍隔离，正式授权/根/工具链与生产companion未完成。
- TDD先缺fixture而红，首6项真实测试通过；初次静态检查因项目NODE_ENV要求及可空记录失败，修正后通过。补目标信号测试的预期红保留，启用固定探针信号分支后，8项启动场景在Phase12 8文件/55项累计回归全部通过（66.04秒）。随后保留前置启动失败停止结果并加断言，最终源码固定11份快照，完整回归已经启动。
- 详细边界/源码依据/原始日志见docs/reviews/task123-command-start-unit.md、docs/reviews/task123-start-evidence/。当前仍为macOS26.5 arm64实测，不把15.0编译目标当最低系统验收；未改冻结接口、未接产品入口、未commit/push/PR。完整回归/清理收尾后追加。

- 最终原始pnpm test退出0：7项脚本测试、197个Vitest文件/1395项测试全绿、0skip，583.49秒。真实LRU130.269秒、Harness10.985秒、群聊摘要6.335秒均Go deepseek-v4-flash；新增8项启动场景均通过。typecheck/lint（514文件）/native构建通过，11份源码hash与完整回归输入一致，无提供方切换、期限/断言弱化或Benchmark。
- 本轮122个固定原生/文件事务/日志fixture（含30个启动fixture）均留证并清理；逐份路径/hash索引见task123-start-evidence/unit-fixture-cleanup-index.json。完整回归另152个目录具备来源/内容/身份/无句柄/无挂载证明，31个证据不足保留；清理结果随后追加。

- TEST-CLEANUP完成：152个回归目录已删除、31个待停用证明目录保留，APFS空间观察+6541312字节；临时控制器源码/hash留档后删除。122个单元清理回执、11份最终源码及60份本轮证据逐份核验，旧45+23+44+67+52份证据hash保持。无用户项目、服务、共享缓存/正常依赖、安装应用或产品数据清理。
- 最终task-status check与git diff --check通过；12.3保持in_progress，持续grant/根/工具链绑定、正式持久目标回执/companion、D17与恢复仍未完成，完整G5未过。未commit/push/PR。报告docs/reviews/task123-command-start-unit.md，汇总docs/reviews/task123-start-evidence/summary.json。

## 2026-09-16 继续：持续绑定与失效收尾

- Leader回复“继续”，使用agora-retry-task恢复启动闸门检查点，引用详设§12.2.2/§12.2.6，沿用固定Seatbelt目录及Go固定载荷外发授权；agora-sync-docs同步实现。依赖和前阶段出口保持done，未跨任务/阶段、未提交。
- 新增内部不可刷新LocalCommandBinding，冻结授权身份/版本/epoch、调用摘要、根和父链、工具身份/hash；启动检查点前后、运行循环及收尾复核，失效保持。停止helper每次调用另核验，工具漂移时拒绝执行新文件并保留needsAttention；实际目标退出与绑定资格失败分开。内部日志v3/观察摘要v2持久绑定和失败原因，旧格式/坏父链拒绝，不新增另一份授权真相源或生产入口。
- TDD先因fixture缺失而红；初次类型通过，lint的控制字符正则/表达式赋值改为显式检查，无忽略规则。首轮绑定10项+启动8项真实测试通过（12.27秒）。随后补helper替换和持久记录边界。
- 首次Phase12累计66通过/1失败（73.60秒）：重复根父链被接受，旧格式已拒绝。运行中曾补唯一性校验及新用例，该次不是单一冻结源码验收。按旧模块缓存/校验遗漏/故障写入三假设检查，保留原始失败及旧校验缺少唯一性判断的事实；不能用当时磁盘source hash代表所有已加载模块。冻结14份源码后12项原断言全部通过（8.06秒），含helper陷阱无执行、收尾撤权保留真实exit0；随后不再修改源码。
- typecheck/lint（517文件）通过，原始完整回归已启动，仍仅Go deepseek-v4-flash固定测试范围。详见docs/reviews/task123-command-binding-unit.md及docs/reviews/task123-binding-evidence/；正式授权registry、固定输入/工具链及OS门禁、companion/D17与恢复仍未完成，完整G5未过。结果/清理后续追加。

- 冻结后原始pnpm test退出0：7项脚本测试、198个Vitest文件/1407项测试全绿、0skip，764.29秒；新增12项绑定测试在完整环境通过。真实LRU236.684秒、Harness70.077秒、群聊摘要15.294秒均Go deepseek-v4-flash，未换提供方/模型、未恢复Benchmark或放宽期限/断言。native构建通过，14份源码hash与受测版本一致。
- 本轮162个固定单元fixture（绑定46个）均已留证并删除，记录hash/路径逐份核验；6组历史45+23+44+67+52+60份证据hash保持。完整回归目录清理进行中，结果随后追加。

- TEST-CLEANUP完成：152个确认停用回归目录删除、31个证据不足保留；APFS可用空间观察+12455936字节，不作为独占回收量保证。162个单元fixture（绑定46个）已核验删除，临时控制器源码/hash留档后删除；无用户服务、项目、依赖/共享缓存、应用或产品数据清理。
- 收尾核验77份本轮证据hash、14份最终源码与清理回执；证据汇总docs/reviews/task123-binding-evidence/summary.json。任务仍in_progress，正式registry/TaskState接合、固定输入/工具链与OS门禁、companion/D17及恢复仍未完成，完整G5未过；未commit/push/PR。
- 最终task-status check（171任务/31阶段/25决策/69历史）、git diff --check、77份汇总hash及11个报告本地链接核验通过。


## 2026-09-16 继续：目标退出事实持久化

- Leader回复“继续”，用agora-retry-task恢复持续绑定检查点，agora-sync-docs同步本轮实现；依赖/前阶段出口done，无级联候选。沿用固定Seatbelt验收和Go固定载荷授权，不新增外传、部署或产品入口。
- 发现实际目标exit/signal只在内存，bootstrap观察才落盘；依详设§12.2.3.3/§12.2.6补内部日志v4的launchReceipt v1。目标出生关系、放行与退出事实交叉验证，quarantine后CAS追加一次，拒绝覆盖/旧revision/旧格式，revision不回绕。launchDurable明确最终事实是否落盘，未闭合继续隔离。
- TDD 8过3红；实现后23项真实测试通过（15.38秒），补观察提交后的真实lock故障后24项通过（16.08秒）。typecheck、lint（517文件）、native构建通过；初次finally throw的Biome检查已改为显式分支，无忽略。冻结16份源码后启动原始完整pnpm test，Go deepseek-v4-flash授权范围不变。
- 报告docs/reviews/task123-command-result-unit.md；本轮尚非正式CommandReceipt，不保存可恢复的输出正文、不接正式registry/TaskState或companion/D17。完整回归与TEST-CLEANUP结果随后追加；未commit/push/PR。

- 冻结后原始pnpm test退出0：7项脚本测试、198文件/1411测试全绿、0skip，662.01秒；LRU178.173秒、Harness20.207秒、群聊摘要9.449秒均Go deepseek-v4-flash。16份源码与快照一致，无提供方/模型切换、断言/期限变更或正式Benchmark。
- 本轮128个固定fixture留证并核验删除（启动46、绑定36，其余46）；索引同时覆盖没有startedAt但有completedAt的旧原语fixture，完整路径/hash见task123-result-evidence/unit-fixture-cleanup-index.json。7组历史45+23+44+67+52+60+77份证据hash保持。回归目录另152项准备清理、31项证据不足保留，最终结果随后追加。

- TEST-CLEANUP最终：152个回归目录已删除、31个证据不足保留，APFS可用空间观察+11829248字节；128个单元fixture清理核验，临时控制器源码/hash归档后删除。35份本轮汇总证据hash、16份源码快照及fixture路径/hash核对通过。无用户服务、用户项目、正常依赖/缓存、应用或产品数据清理。最终汇总docs/reviews/task123-result-evidence/summary.json。
- 最终task-status check（171任务/31阶段/25决策/69历史）、git diff --check、35份汇总hash及12个报告本地链接通过；12.3保持in_progress，完整产品G5未完成，未commit/push/PR。


## 2026-09-16 继续：Registry文件持久化底座

- Leader回复“继续”，agora-retry-task恢复目标退出回执检查点，依赖与前阶段出口done，无级联候选；沿用本机固定目录、Go固定外发和TEST-CLEANUP授权。agora-sync-docs同步详设§12.2.3和计划。未跨阶段、未提交。
- 复评现有普通JSON store不能直接满足registry唯一写者/fsync/中断阻断；新增内部LocalRegistryFile，复用桌面owner能力、固定local-workspaces/registry.json、严格外层版本/字段、必填不转换codec、CAS+排他锁+文件/目录fsync。输入/返回复制，拒绝坏身份/链接/权限/溢出/残留，不自动恢复或重置。
- 当前是存储底座，生产roots/grants/workspaces/claims/operations schema及授权转换仍未实现；fixture codec只接空实体列表与fixed-*操作串，不冒充正式授权。文件发布闭合不等于TaskState业务闭合，无执行入口或成功/释放能力。
- TDD缺模块而红；22项真实文件/owner测试首轮通过（3.30秒），补有界读取及锁模式/硬链接核验后最终通过（3.88秒）。真实注入owner丢失、目录移动、发布后锁替换均保留阻断。typecheck/lint（519文件）/native构建通过；冻结5份源码后启动原始完整回归。报告docs/reviews/task123-local-registry-file-unit.md，结果与清理随后追加。

- 冻结后原始pnpm test退出0：7脚本测试、199文件/1433测试全绿、0skip，684.78秒；新增22项在全量环境通过。真实LRU199.686秒、Harness14.951秒、群聊摘要9.068秒均Go deepseek-v4-flash，未换模型/提供方、未改期限/断言或恢复正式Benchmark。5份受测源码快照一致。
- 136个固定fixture（其中registry66个）逐份核验hash与删除状态，索引见task123-registry-evidence/unit-fixture-cleanup-index.json；8组历史45+23+44+67+52+60+77+35份证据hash保持。完整回归目录清理随后补记。

- TEST-CLEANUP最终：152个确认停用回归目录删除、31个证据不足保留；APFS可用空间观察+5091328字节，不作独占回收量保证。136个固定fixture（registry66个）留证删除，临时控制器源码/hash归档后删除，无用户服务/项目、正常依赖/缓存、应用或产品数据清理。89份汇总证据hash及5份源码、fixture索引核验，汇总docs/reviews/task123-registry-evidence/summary.json。
- 最终task-status check（171任务/31阶段/25决策/69历史）、git diff --check、89份汇总hash和12个报告链接通过。12.3保持in_progress，完整产品G5未完成，未commit/push/PR。


## 2026-09-16 继续：工作区领域类型与不可变身份

- Leader回复“继续”，agora-retry-task恢复registry文件底座，沿用同范围本机fixture/Go固定载荷外发授权，agora-sync-docs同步详设§12.2.3.2和计划；保留全部既有未提交内容。
- 先细化严格类型/字符/长度与引用不变规则，再实现L1 WorkspaceRef/Version、FileVersion/Change、WorkspaceCall及纯校验；registry输入/读取强制workspace校验，文件锁内重读后拒绝已有ID改绑/删除。数据shape不等于授权，根/grant/claim/operation生产codec及TaskState闭合尚未实现。
- TDD模块缺失而红；3项真实文件红测复现codec放行后的混搭/删除/跨项目改绑，修复后65项通过。补集合accessor负测先红后修，最终42领域+25真实文件测试全部通过（4.35秒、0skip），typecheck/lint（521文件）/native构建通过。冻结9份源码后运行原始完整pnpm test，结果和清理后续追加。
- 9组历史77+45+67+23+89+35+60+44+52份证据hash保持；报告docs/reviews/task123-workspace-identity-unit.md。任务保持in_progress，完整产品G5未完成，未commit/push/PR。

- 冻结后完整pnpm test退出1：7脚本通过、200文件中199过1失败、1478测试中1477过1失败、0skip，710.57秒。新增42领域+25registry全过；真实LRU164.251秒、Harness20.282秒、群聊摘要8.144秒均Go deepseek-v4-flash。9份冻结源码保持。
- 失败为既有启动撤权场景首次control helper捕获bootstrap身份时250ms ETIMEDOUT，尚未到release回调；released=false、无目标输出、quarantined且launchDurable=true。11份故障路径源码与此前通过检查点一致；不改实现/断言/期限重跑原文件12/12过（8.98秒）。按调度/首次加载迟缓、过早身份查询、workspace改动间接影响三假设排查，只有超时位置得到证实，根因为inconclusive，G4不标通过、不继续批量重跑凑绿。详见报告和task123-workspace-evidence/startup-failure-analysis.json；下一步补首次helper捕获耗时分段证据。
- TEST-CLEANUP：160个固定fixture（registry78个，含原失败及重跑）保存结果/hash并核验删除；另152个回归目录删除、31个证据不足保留，APFS空间观察+5627904字节。临时清理控制器归档/hash/无句柄挂载核验后删除；无用户服务/项目、依赖/缓存、应用或产品数据清理。12.3仍in_progress，G4未闭合、完整产品G5未过，未commit/push/PR。


## 2026-09-16 审查并修复helper启动超时

- Leader明确要求“审查原因并修复”，沿用原固定目录和Go载荷授权。确认旧失败在bootstrap ready之后、项目创建之前，helper单次250ms包含程序冷启动；三假设、原失败与12项单跑通过证据保持。
- 新增真实延迟/错误/退出故障，旧代码7项红/原12项过。有限只读重试后退出测试暴露Node退出事件滞后，新增原私有控制端点p/ready存活握手。仅重试及临时故障fixture预热仍遇原descriptor用例3次连续超时；停止加次数，独立12二进制/36调用对照实测首次153.4–229.2ms、后续1.5–3.6ms，无查询命令同样呈冷启动开销。本次诊断本身无超时，不声称已定位原时刻的OS子系统原因。
- 最终改为原5秒启动窗口先完成真实helper ready校验，再创建bootstrap和250ms身份查询；仅ETIMEDOUT最多3次、共用deadline，绑定/权限/取消及私有端点在途复核。全部测试专用预热移除，错误/次数严格断言恢复；原12项测试正文哈希保持。新增12项与原12项启动、12项绑定共36/36通过（43.86秒），typecheck/lint（521文件）/native构建通过。
- 冻结17份源码后开始原始完整回归；10组532份历史证据hash保持。报告docs/reviews/task123-capture-timeout-fix.md。详细设计§12.2.7同步，整体12.3仍in_progress，完整G5未过；未commit/push/PR。

- 最终原始完整pnpm test退出0：7项脚本、200文件/1490项测试全绿、0skip，900.75秒；本次24项启动在全量环境再次通过（30.13秒）。LRU406.313秒、Harness4.223秒、群聊摘要9.741秒均Go deepseek-v4-flash，600秒LRU/250ms查询/5秒启动及原断言保持。17份冻结源码与原12项测试正文hash复核一致；本轮G4闭合，不改写此前失败。
- 本轮279个固定fixture（启动147个，含失败/修复/诊断）保存结果、版本/hash后核验删除。完整回归另152个已确认停用目录执行清理、31个证据不足保留，最终空间/汇总随后补记。

- TEST-CLEANUP最终：279个固定fixture（启动147个）核验删除；152个回归目录删除、31个证据不足保留，APFS空间观察+6754304字节。诊断wrapper及清理控制器归档/hash/身份/句柄/挂载核验后删除，无用户服务、项目、依赖/缓存、应用或产品数据清理。新完整回归证据不覆盖旧失败，本修复G3/G4与真实启动验证闭合，整体12.3仍in_progress，未提交。


## 2026-09-16 持续推进：串行本机状态与双存储闭合

- Leader要求“继续进行Task12.3直到完成”；按既有计划及本机/Go固定测试授权推进，无新费用或外发范围、未跨任务开工。使用agora-retry-task/agora-sync-docs，不重复索要相同授权。
- 细化并实现localExecution-v1、加载/reducer/投影校验、模型写入拒绝与未接companion时的执行阻断；生产registry codec及锁内转换检查，内部双存储prepared→canonical→committed协调与恢复。记录不等于活授权，未开放HTTP/MCP或用户目录。
- 17领域/9记录红测转绿；真实双存储负测发现额外未登记workspace未被assertClosed拒绝，补完整最新结果核验后通过。初次投影新测试错误导入已修正；原断言不改。最终定向7文件150测试全绿，typecheck/lint（527文件）/native构建通过；18份源码冻结后原始pnpm test启动，完整G4待结果。
- 详见docs/reviews/task123-state-binding-unit.md；日志/源码/fixture清理回执在docs/reviews/task123-state-evidence/。完成当前检查后继续正式控制/文件创建与端口接合，12.3整体及完整G5仍未完成。


## 2026-09-16 继续：根准入检查、排他初始化与原子文件创建

- 状态接合完整回归退出1：7脚本通过、203文件中202通过/1失败、1529测试中1528通过/1失败，741.78秒。唯一失败为reducer精确字段白名单遗漏localExecution，补字段且保留精确断言，3文件100项复验通过。Go deepseek-v4-flash LRU229.830秒通过，无提供方/模型/期限切换；原失败保留，G4待下一完整回归。
- 控制语法22项通过，接入既有Leader解析后与Web意图共68项通过；未绑定controller明确拒绝，零mutation。授权/执行入口仍未开放。
- APFS根只读检查卷UUID因mountroot metadata权限缺失而失败，按Apple XNU/本机SDK校准仅检查器策略后通过，负向探针确认未放开内容读取、写入或fork。
- 新建文件独立absent基线+RENAME_EXCL事务11项及原替换17项通过；初始化prepared→排他mkdir→真实回执11项通过，含撤权、漂移、同名目录、重放/缺回执与外部读写/fork拒绝。新增测试场景表及C终止函数声明错误已修正，原始失败留存。typecheck环境NODE_ENV遗漏已修正；静态/native最终通过。
- 详细边界和证据见docs/reviews/task123-root-and-creation-unit.md。仍需正式控制、root/grant/claim活准入、版本端口/输入快照、下载依赖、companion/D16与完整产品G5，12.3保持in_progress，无commit/push/PR。随后冻结源码运行完整回归并收尾TEST-CLEANUP。

- 上一状态单元失败全量的TEST-CLEANUP已闭合：152个已证明停用目录删除、31个证据不足保留，可用空间观察+5668864字节；临时清理脚本和根检查红测日志归档/hash/无句柄核验后删除。13组586份既有汇总证据hash保持。
- 当前535份源码冻结后启动原始完整pnpm test；受管Node诊断在另外固定目录执行，不改冻结源码：官方24.20.0归档与catalog hash一致，实际暂停启动/监督器完成源只读、私有输出与虚构凭据拒绝，exit0/launchDurable，仍非正式授权端口G5。5903条目manifest与journal/hash留证后清理Node下载/解压及专用副本，空间观察+263716864字节。详情docs/reviews/task123-managed-node-evidence/。

- 当前原始完整pnpm test退出0：7脚本测试、207文件/1576测试全绿、0skip，749.70秒。535份冻结源码hash全部保持；LRU232.369秒、Harness19.171秒及群聊用指定Go deepseek-v4-flash，无模型/提供方/期限切换。本检查点G4闭合，首次1528过1失败记录保留；整体12.3产品G5仍未完成。

## 2026-09-16 根注册闭合与正式Leader授权事务接合

- 前一1576测试检查点清理已闭合：152个已证明停用目录删除、31个证据不足保留，可用空间观察-991232字节（并发APFS活动，不表述为独占回收量）；报告在task123-root-init-evidence/full-regression-cleanup.json。临时清理控制器仍待独立归档核验收尾。
- 评审出根注册顺序冲突：原记录要求初始化身份，但初始化必须在授权后。先同步§12.2，再允许root.staging=null登记授权，拒绝workspace/claim准入；仅真实prepared/终态初始化操作可一次补全staging。通用binding不能伪造已初始化根，加载也验证证明来源。真实初始化中断/坏回执/未批准/错选择/只读授权/同名目录及记录与状态共33项通过（task123-root-registration-evidence/expanded.log）。
- D9正式入口原要求Leader消息预先存在，无法实现消息+授权引用一次提交。增加不可变sourceMessage输入并在prepared记录持久保存，同一真实TaskState mutation批次append规范消息+set localExecution。原内部生命周期来源引用保持；正式控制不能走分拆提交兼容路径。完整消息、scope、revision、display/payload/action及重放交叉校验；中断两侧、拆分消息及假Leader负测通过。首次异步错误被同步抛出导致5项测试失败，移入串行Promise后42项通过，原断言保持。
- 增加owner私有内容寻址提案对象：排他写入/fsync/0400，读取按固定根链、单链接身份、权限、大小和内容hash验证；损坏对象不覆盖。19项真实状态/对象测试通过。
- LocalGrantController把受信选择+当前策略+APFS身份与提案hash绑定，真实POST /api/messages经MessageRuntime/MessageService一次持久消息+引用再SSE。配置变化、根漂移、错hash/选择/scope、私有状态根及同ID异内容拒绝；重试复用首次prepared服务端ts。当前只提供明确network disabled的内部策略，不把登记授权当命令/安装能力。实际普通项目UI与生产组合根仍未启用。
- POST初测因测试编制误用空roster失败，改用真实DEFAULT_ROSTER；随后9过1失败定位为新POST时间戳误参与重放身份，保存首次ts恢复并返回canonical消息。一次补丁未应用完整，后续明确复核再修正，所有原始失败日志保留。最终10项真实POST测试通过（task123-state-evidence/grant-fourth.log），typecheck/lint539文件通过（task123-grant-evidence）。所有固定fixture均留hash/结果并清理，未读取用户项目或调用模型。
- 最新修改尚未重新执行完整pnpm test，1576全绿仅为前一检查点；G4不冒用。仍需claim/版本文件/固定输入、命令端口/下载依赖/MCP/companion及产品G5。12.3保持in_progress，继续实现，无commit/push/PR。

## 2026-09-16 固定字节版本、活工作区准入与版本化读取

- 新增二进制读取/替换原语：同一次native fd读取字节和元数据，不经UTF-8改写；首次await前复制expected/candidate Buffer。二进制正常/幂等/调用者后续修改Buffer及原17替换、11创建共31项通过，原字节备份/交换与元数据断言保持。
- native新增只读固定fd目录枚举，不跟随symlink，显式列出.git/.env系列/技术目录排除项，未知类型拒绝。链接/秘密路径负测及全部创建/替换32项通过。版本库保存字节内容对象、FileVersionV1及完整目录/排除清单，采集与发布后复核，变动/撤权不返回有效version；存储的固定字节与当前活目录资格分开。3项真实版本测试通过，追加不完整manifest负测。初次新路径验证误用absent.name=leaf导致正常名被拒绝，按契约改为实际末段，未削弱完整性断言。
- 不可变对象增加原始字节及稳定action引用；已有坏/异输入引用不覆盖，读端验证并flush文件和目录后才交付事实。新引用红测保留；原状态/对象及POST共29项通过。
- GlobalScheduler增加只读assertActive，原对象/当前worker分区才接受，克隆及已释放/旧lease拒绝，不新增额度。LocalWorkspaceAuthority以规范grant提案、真实初始化回执、固定version、TaskState worker/subtask、registry claim/epoch和活lease共同准入；源grant及最新worker事实均复核。注册保持旧Leader授权的生命周期引用，不伪造新批准消息。
- POST normal场景进一步串接真实根初始化→版本捕获→注册workspace/claim→活lease。测试先错误使用subtask set和裸workers merge，按既有mergeByIdMutation构造器修正；跨层参数展开带入多余字段由严格root schema拒绝，改为显式project/task映射。原保护不变，全部失败日志保留。两文件20项接合/lease测试通过（claim-fourth/fifth.log）。
- 新LocalWorkspaceFiles.readFile串接同一权限链，持久化规范scope/revision/epoch/canonicalSourceRef的不可变读回执，支持file/absent；同动作异路径冲突、秘密/越界读取拒绝、lease释放后拒绝。回执返回前再次复核活准入；尚未实现applyFiles/命令/MCP/WorkerRuntime companion，不把测试中受信注册的worker当完整Harness闭环。最新39项接合通过后补齐共同receipt字段，继续验证。
- 前一root-init测试清理控制器已归档/hash/身份/无句柄/无挂载核验并删除，证据task123-root-init-evidence/cleanup-controller-{source.txt,result.json}。本轮所有固定fixture均按各测试记录留证和清理，不删除用户数据。
- 当前仍无新完整pnpm test结果，之前1576全绿不覆盖上述新实现；不宣称12.3完成。未commit/push/PR。继续版本化写入、固定输入命令与下载/依赖、MCP和companion及完整产品G5。


### 2026-09-16 版本化写入与有界多文件批次（继续实现，未完成任务）

- 单文件正式接合：`LocalWorkspaceApply`以真实grant/claim/lease、读取回执和私有字节对象调用Seatbelt创建/替换；prepared/native-start/result及后续失效事实持久留证。原生result丢失后，同动作重放及新文件准入均拒绝。`file-apply-second.log`为10项通过；不是完整产品G5。
- 新增`LocalWorkspaceBatch`与最多64项的私有readset；整批前检、逐项子回执、整批后检，partial或不确定状态封锁工作区。候选/基线仍按字节保全，禁止自动回滚覆盖用户当前文件。
- 红灯证据：`file-batch-red.log`为尚无readset实现；`file-batch-first.log`为新批次堆入旧综合用例超过5秒，拆为独立场景后`file-batch-second.log`11项通过。后续竞争fixture新增dependency遗漏精确目录断言，`file-batch-race-red.log`保存原失败，修正fixture清单而不弱化断言。
- 真实竞争发现业务缺口：`file-batch-race-red-2.log`显示只读依赖在首个真实交换后变化，旧实现误报applied。根因为仅前检读集、未后检未写依赖；新增完整读集后检，保留已完成文件并返回partial。`file-batch-race-third.log`另记录fixture成功分支漏收batch-stale及综合用例超时；已修正分类，将已有授权重试覆盖保留在原正常/中断场景，新增批次只承担自身断言，未提高5秒期限、未skip或删除既有覆盖。最新结果另续记。
- 所有场景继续使用`/private/tmp/agora-task123-validation-*`固定虚构项目；结果/完整文件hash/涉及源码hash/owner释放/lsof/mount/路径identity/空间变化按场景JSON保存后清理。来源清单扩展为全部local原语与GlobalScheduler，版本fixture去除源码缺失时静默过滤。
- 证据目录：`docs/reviews/task123-grant-evidence/`。remove、命令端口/受控依赖、MCP/WorkerRuntime和完整产品G5仍未完成；1576项全绿是旧代码检查点，不能覆盖当前变更。没有提交、push或PR。

- 批次修复最终结果：`file-batch-race-fifth.log`14项全绿，normal 4.405s、batch 3.693s、partial 3.510s、read-drift 3.246s、stale 2.459s，原5秒期限保持。依赖漂移使用一个真实写目标+一个未写依赖的最小读集，多目标部分失败单独覆盖；第四轮超时记录保留。之后增加回执封闭校验、子回执结果与父级effect/stage交叉核验、native文件后置身份核验，纳入后续相关回归。
- `LocalFixedInputs`真实物化旧版本原始字节，用户当前文件变化不改变快照；重用核验完整树和不可变回执，撤权及私有输入篡改拒绝。`task123-version-evidence/fixed-input-red.log`为接口未实现红灯，`fixed-input-first.log`3项全绿，fixture删除留证在对应JSON。
- 启动器新增异步当前授权接缝，真实释放前拒绝/读取异常场景的定向红灯见`task123-start-evidence/async-authority-red.log`（该次为TDD定向运行，24项未选择，不作回归通过）；随后启动、监督、绑定、grant、version五个完整相关文件回归无过滤。该轮发现receipt-lock旧故障注入绑定microtask时机，新增await后锁出现在观察提交之前；先保留失败证据，再改为真实recordObservation提交后注入，不改变生产持久化实现和原断言。


### 2026-09-16 固定输入命令接合与正常退出竞争诊断（进行中）

- `LocalWorkspaceCommands`接真实规范grant/claim/lease、固定字节物化、已安装受管Node24.20.0、Seatbelt/held launcher/supervisor、native journal及私有stdout/stderr对象。当前仅Node+禁网，不开放普通入口；不把受信compiled helper诊断当打包已验收。固定脚本真实读取快照、写独立构建产物、测试源码只读/活目录不可读写/模拟凭据不可读，所有哨兵均属获批fixture。
- red入口不存在及首次fixture精确目录漏列verify.cjs失败分别保留`workspace-command-red.log`、`workspace-command-first.log`；修正固定输入清单后`workspace-command-second.log`出现命令needsAttention（实际输出文件hash证明脚本已生成结果，但该轮完整native journal未在断言前保存，因此不单独判根因）。补留证后定向`workspace-command-focused.log`1项真实通过；其14项未选择仅属最小诊断，不算完整回归。
- `workspace-command-third.log`及`workspace-command-related.log`保留完整相关文件中的超时/needsAttention；原5秒文件场景和20秒新命令场景未延长。不能以单例通过覆盖这些失败。新原因码区分授权、固定输入、源版本、启动、输出和清理失败，详细native事实仍在私有journal，测试先复制必要诊断再清理。
- 已确认命令失败根因：`command-agora-task123-validation-1M3FT9.json`为exit0、stdout完整、bindingFailure=null，却discoveryFailed；加入逐身份记录后`command-agora-task123-validation-oE9HZH.json`明确唯一失败state=exited，随后同出生身份stop复核均exited。故障是live→children窗口中正常退出被误记失败，不是已知越权或模型/脚本错误。`workspace-command-discovery-diagnostic.log`保留原判定红灯。
- 修复保持primitive发现状态和原测试断言；新增`checkDiscoveryClosure`在原deadline内逐出生身份再次确认exited，supervisor单独记录closedDuringDiscovery；unknown/身份不匹配/查询失败/超限仍失败，不凭全体known后来退出清除未知错误。原生发现测试补真实退出与错误身份/helper缺失负例；新结果在`discovery-closure-first.log`续记。
- 异步授权故障注入修复后`async-authority-start-green.log`完整26项启动回归通过（30.67s）；实际recordObservation写入后创建真实锁，全部原断言保留。该结果及之前G3不覆盖之后的发现诊断/修复源码，仍需新静态与完整G4。


### 2026-09-16 MCP接合与发现竞争复验

- 第一轮发现闭合修复仍遇bootstrap唯一子进程被回收期间children=unknown（0uDzwo证据），项目target已退出。对受信bootstrap源码与两身份/唯一关系复核后，持续发现从真实target开始；bootstrap保留停止和身份核验，项目后代unknown不豁免。discovery-closure-regression.log启动26/监督7/发现5通过，文件组4失败：新分类遗漏2、综合批次超时2；未冒称全绿。
- 修复fixture分类，discovery-closure-grant-fixed.log为16通过/normal综合超时1。把原正常授权、文件更新、创建、缺回执按独立职责拆开，所有原断言保留，旧场景5秒/命令20秒不变；新MCP整体往返场景20秒。不存在skip或排除回归文件。
- 新L3 workspace-port类型、受信bound工具adapter、独立MCP server/catalog接真实官方ToolRuntime；模型不能给scope/action，稳定session+exec.callId派生动作。工具返回保留字节编码与版本/读回执，apply完整读集，命令显式固定输入，旧写工具完全不注册到该catalog。
- workspace-mcp-red.log保存缺入口红灯；初次类型断言和不存在ctx.dispose的测试收尾错误在typecheck/first日志留存，改为真实已有catalog transport关闭。两项协议/权限单元通过，真实数据路径另验，不以port spy当G5。
- 最新workspace-mcp-native-first.log：3文件27项全部通过，含20项真实POST/lease/文件/命令/MCP（MCP写入4.247s、MCP命令4.720s）、5项真实进程发现、2项协议拒绝测试。所有fixture结果/源码hash/owner释放/句柄挂载身份与清理已留证。最新typecheck通过；新完整G4尚未执行。
- 下一步WorkerRuntime companion/角色/验证与D16/归档、受控依赖/安装生成、生产组合根和产品Harness G5。12.3仍in_progress，无commit/push/PR。


### 2026-09-16 WorkerRuntime本机生命周期接缝（继续）

- 新L3 WorkspaceWorkerPort/Session与真实LocalWorkspaceSessions能力生命周期：Runtime供应实际lease验证闭包；CODER/direct注册或重开已有规范绑定；安全点校验活授权和文件/命令闭合，技术boundary回执入私有内容库，close先关新工具、排空队列、持久bounded收敛后归还控制。关闭能力不释放task写claim，不宣称完整接管/恢复已完成。
- WorkerRuntime本机状态只走专用companion/factory，不调用旧worktree/executor；完成先checkpoint+close再canonical done，finally重复close幂等；模型不得直接提交local testResults。projectForAssignment仅投自身workspace/子任务，不投根路径或其他能力。增加内部workspace包类型依赖并同步lock，不引入第三方库。
- 两个新生命周期单元红测保留workspace-worker-red/first.log；缺localWorkspace投影修正后原单元+投影78项通过（second.log）。单元executor/port double只验证顺序，不充当Harness G5。
- 真实fixture增WorkerRuntime→canonical MessageRuntime→活lease→真实sessions→MCP读取→边界持久化→关闭后拒绝，执行器仅脚本化生命周期驱动，明确非模型/Harness G5。首轮6文件134项中133通过/1失败（workspace-sessions-native-first.log）：夹具未切到registering阶段，错误要求生命周期set同时append新的Leader授权消息。保留精确断言，仅在真实grant闭合后切换fixture阶段，复验中。
- 初次fixture transition误返回MutationCommitResult而非state由typecheck发现，改用真实返回state；typecheck-3通过。原授权/注册幂等断言移回file-update场景继续执行，不因场景拆分丢覆盖。新完整G4及全部任务G5仍未完成；下一步只读/验证角色与受控安装生成、验证终审归档和产品组合根。


### 2026-09-16 单CODER真实Harness→本机MCP→固定输入LRU G5

- companion夹具阶段修复后workspace-sessions-native-second.log两文件66项全绿（真实companion 2.953s），保留原失败。角色变体及强制sessionId同源后，角色/Runtime两文件46项通过；最新typecheck和lint560文件全绿。当前仍非完整G4。
- 本机CODER角色从既有白名单映射workspace.read/apply/run，缩减权限不扩大；独立Web组合接缝复用真实HarnessExecutor，不改变loop，普通生产任务入口仍关闭。L3 Session显式提供sessionId，Runtime和MCP动作域同源；其他角色不冒充CODER。
- 新固定LRU真实测试只外发已授权固定要求、角色/工具、临时代码与测试结果，提供方OpenCode Go、模型deepseek-v4-flash；未访问用户项目/秘密、未运行Benchmark。受管Node24.20.0及manifest/binary hash核对；5个固定测试文件、虚构.env始终不变，命令执行于Seatbelt固定输入副本。
- 首次workspace-live-first.log失败：native exit0/quiescent、真实stdout五项通过，但grader预期TAP，Node24默认spec输出为“ℹ tests 5 / pass 5 / fail 0”。排查候选为输出格式、无命令回执、实际测试失败；持久run/输出明确排除后两者，确定为新fixture reporter假设错误。原失败及全部回执保留，不改写为通过。
- 显式--test-reporter=tap，不降低5测试/5通过/0失败要求，补0跳过/0取消检查；workspace-live-second.log 38.427s通过并验证最终活文件与实测固定版本相同。随后补强实际prepared request.inputHash、toolId、精确argv和inputVersion与规范回执关联，避免仅凭输出文本签发通过；workspace-live-third.log 35.679s通过。新测试期限360秒保持，既有测试期限不变，未换模型或skip。
- 三次固定fixture均保存结果/版本/hash/命令输出及必要原语回执，owner/句柄/挂载/路径身份核验后删除；证据docs/reviews/task123-live-workspace-evidence/。成功样本保存生成源码；首次失败已保存完整run输出，未因清理丢根因证据。Node来自已安装Agora.app，只读使用，不删除应用或普通依赖。
- 当前G5仅覆盖单CODER普通目录真实模型读写/命令，未将它表述为完整六角色终审或Phase12出口。未完成项：非编码角色/验证与D16/归档的同版本接合、受控下载/安装/生成/依赖挂接、生产组合根及启动探测/打包、最低macOS15、完整G4。12.3继续in_progress，无commit/push/PR。

## 2026-09-16 本机CODER检查点累计回归与后续验证证据

- 冻结546份源文件运行原始`pnpm test`：脚本7/7；Vitest212文件通过/1失败、1637项通过/1失败、0skip，963.66秒。结果与完整日志：`docs/reviews/task123-command-evidence/full-regression.log`；所有冻结hash一致，见`full-source-verification.json`。真实Go/deepseek-v4-flash旧LRU273.600秒、新本机LRU49.112秒均通过；不将总体结果记为G4通过。运行中只新增未接入冻结路径的4份验证领域/命令文件，另测7项通过，不混入本次全量计数。
- 单个失败为discovery/reparent的第一个sibling身份捕获`ETIMEDOUT`，尚未进入后代断言。按证据顺序审查：①诊断fixture未走已有生产helper ready，冷加载与250ms查询混用；②payload过早退出/存活竞态；③PID/内核身份错误。源码确认首调用没有ready；错误为spawn超时而非非法token/身份拒绝，固定payload已ready且其10秒自然期限未到。旧冷启动独立诊断和生产修复见task123-capture-timeout-fix.md；不声称追溯到具体macOS服务。
- 根因修复仅让discovery/supervision/stop的受信fixture在创建payload前调用现有生产`prepareLocalProcessControl`，绑定实际helper hash并记录就绪耗时；保留5秒初始化、250ms查询、原测试期限和全部断言，不改变产品查询行为，不以重跑碰巧成功作为修复。验证待下文补记。
- 全量TEST-CLEANUP先保存log/hash/源码版本、生成时间与启动前临时目录基线、文件清单，再核对uid/dev/inode/realpath、lsof及系统/Docker挂载；删除152个确认停用测试目录，保留31个证明不足条目。逻辑字节4190453；APFS可用空间观察-13787136字节，受其他活动影响，不冒充独占回收量。详见full-regression-cleanup.json；控制器待最终归档清理。失败原生fixturehotpiO已按自己的收尾回执删除。
- 新增local-validation领域回执与固定Node命令/报告解析，严格版本/任务/Tester绑定，保留真实失败而拒绝超时/未收敛/skip/cancel/错误退出。Node集合出现未支持的TypeScript测试时明确拒绝，红测1失败/2通过后修正，最终7项通过。初次类型检查3项fixture类型错误已纠正，最终typecheck及新文件lint通过。详设§12.2.4.4已记契约；尚未接完整Tester/Reviewer/D16/归档，不作G5或12.3完成声明。
- 继续审查发现工具适配层部分批次重放疑点，三假设：①prepareReadSet的新操作门禁先于不可变旧输入重放；②native/子回执损坏；③MCP动作身份派生不一致。先新增真实POST/MCP最小复现：第一文件成功后外部创建第二文件，保留partial；同callId应只返回首次partial，新callId及新read仍拒绝。未复现前不修改权限门禁。

## 2026-09-16 重放修复验证与文件版本终审数据接合

- MCP partial红测精确失败在同callId再次workspace_apply（首次partial已产生），错误workspace_file_recovery_required；不是native子回执损坏或actionId变化。修复prepareReadSet对已存在输入先核对完整scope/epoch/revision/条目及原始read回执，只返回不可变引用；writer仍查首次批次/原生子回执。新动作与读取继续阻断。4个受影响文件40/40、0skip、100.90秒：readiness-replay-regression.log。debug的-t选择未运行用例只属最小复现，验收为这次四个完整文件。
- 40份真实fixture报告（含红测）按各自owner/identity/lsof/mount收尾全部删除，汇总readiness-replay-cleanup-index.json。全量临时控制器已归档校验并删除，见full-cleanup-controller-removal.json。
- D16增加CompletionEvidence判别数据联合，local必须携带与独立TESTER规范回执完全一致的文件版本、当前结果与review binding；HumanGate receipt及POST重放保留该数据，worker模型禁止伪造workspace_validation，reducer拒绝同ID覆盖。历史Leader返工意见使用原验证结果与规范resume marker，之后新Tester dispatch/结果不能抹掉历史意见，也不能让旧版本重新成为当前完成证据。先红测3失败后修复，历史结果变动再红测后修复；相关75/75（6文件）通过，typecheck及全仓lint564文件通过。尚未接入全部实际目录映射重验/独立Tester命令及归档，不将领域校验称为完整D16 G5。
- 上述75项中的3个真实Git残留已留证并删除（82603逻辑字节、APFS观察+36864）；见local-completion-test-cleanup.json。专用清理控制器已核验归档并删，unit-cleanup-controller-removal.json。
- 复评发现旧local binding强制subtaskId与既有任务级Tester/Reviewer调度不匹配。详设§12.2.3.2改为非编码只读绑定可省略、与WorkerState一致；CODER/编码workspace仍必须真subtask；不能后续给旧绑定补字段来重新解释身份。新增红测后，领域/加载/registry相关56/56通过、typecheck通过。没有重开done子任务或伪造CODER分配。
- 下一单元开始独立Reader真实快照：新增POST reader场景，先红测registerReadOnly缺失，再实现无源码claim的固定manifest注册与快照read。writerEpoch=0仅“无写权”标记，仍需活lease/当前grant/worker与闭合binding；Reviewer不允许edit/run，外部源修改后新read仍返回首次快照。当前23项完整grant文件正在验证；尚未把Reader接入所有WorkerRuntime/角色factory。

### 2026-09-16 只读角色、固定验证与Leader批准接缝

- 继续既有A–D计划，不扩大外发/费用/目录范围，不提交或合并。沿用agora-retry-task/agora-do-task及agora-sync-docs；依赖12.2/11.4/11.5完成。
- 修正新增registry测试的无效夹具：原coding工作区缺已提交root-initialization回执；补齐真实codec要求后原正/反断言通过，不放宽生产codec。只读validation工作区禁止write claim，epoch0仅表示无源码写能力；ARCHITECT/REVIEWER只读，TESTER对绑定快照执行。`localWorkspaceRole`不扩张原角色工具白名单，原CODER适配入口保留。
- LocalWorkspaceSessions现可为无subtask的TESTER/REVIEWER建立独立固定快照，读/inspect保持旧版本，即使外部编辑已发生；不冒用CODER claim。PM/COORDINATOR走无工具control companion，无workspace/binding/claim，仍校验活lease、规范worker和当前授权，保存边界回执。真实Harness工厂新增对应控制角色与文件角色入口；正式任务组合尚未全部接完。
- `reader-session-native-first.log`原23项过/新增2项失败，是夹具误用assignedSubtasks字段；按实际assignment投影检查无subtask身份，`reader-session-native-final.log`完整25/25（51.14s）通过。`reader-unit-final.log`7文件128/128，`reader-typecheck.log`通过。较早WorkerRuntime missing import、registry初始化夹具失败均为测试编写错误，不写成生产漏洞红灯。
- 新`LocalValidationService`运行完整固定Node测试集，重读私有prepared/result/原生journal/输出/固定输入，绑定实际工具链与逐命令空依赖事实；核对当前源目录映射、需求/决策/架构/约定/任务拓扑指纹。原生fixture实测两项Node测试通过、同dispatch复用规范回执、lease释放后验证、控制变化/伪造command hash/外部编辑失效且保全外部字节。初次完整调用25过/1失败仅为新增sentinel.test.cjs未加入夹具初始目录预期；补齐预期后定向实测通过，后续全文件覆盖。
- 修复WorkerRuntime收尾顺序：模型Step安全点后，保持原活lease和会话运行受信completeLocalAssignment，再关闭能力、提交done并释放lease。红灯`local-validation-lifetime-red.log`缺受信回调，绿色`local-validation-lifetime-unit.log`2文件82/82；typecheck通过。现有TESTER模型set testResults与模型伪造workspace_validation仍拒绝。
- `local-validation-control-native-full.log`完整28/28（62.39s）通过，包含真实PM/Coordinator无工具生命周期、只读角色、验证回执及既有文件/MCP/命令故障回归。`validation-service-unit.log`4文件13/13；该检查点lint仅两项optional-chain警告，随后显式修正。上述原生生命周期driver不等于真实模型/真Fork完整G5，原Go LRU证据仍独立保留。
- 实际发现Leader完成批准可在未绑定本机验证服务时先提交裁决，随后才因缺resume端口报错。`local-leader-validation-red.log`保留该失败；新增本机批准验证端口，MessageService串行队列内await复核再提交，缺端口/版本失效不提交批准；已批准重放先复核再resume，request_changes不被此限制阻断。定向`local-leader-validation-second.log`通过；中间首次绿色尝试的失败为测试误把Ledger.isRequestSatisfied当AppState字段，改检查实际phase仍review，未改变完成规则。异步规划队列另测拒绝时不提交消息且后续消息保持顺序。
- 把夹具手工审阅消息换为实际Coordinator后，`local-review-route-red.log`复现缺workspaceReviewBinding；Coordinator现从规范当前TESTER回执派生绑定，无REVIEWER的本机路径拒绝直接finalize。正在运行完整28项`local-review-native-full.log`与关联单元/静态检查；尚不能把此轮记为通过。
- TEST-CLEANUP：`reader-validation-native-cleanup-index.json`汇总131份此前原生fixture报告，均保存源码/结果/身份和无句柄/挂载证明后删除测试根；18个WorkerRuntime测试Git遗留目录经来源、时间窗、hash、UID/dev/inode、无进程/挂载复核删除，逻辑495607字节、观测APFS可用空间+716800（非独占归因）。详见`reader-validation-unit-cleanup.json`；清理controller归档并删除，见`reader-cleanup-controller-removal.json`。后续新增批准/路由回归报告仍需纳入下一清理汇总。
- G4仍未完成：最近全量pnpm test是1637过/1失败；其就绪夹具修复已40/40受影响回归，但未获得新全量全绿。任务剩余包含受控安装/生成/下载、完整生产组合/真正D4 Fork/终审归档与同版本交付、claim终态释放、最低OS和最终G1–G7；普通项目入口保持关闭。

### 2026-09-16 审阅绑定、固定产物归档与写者收尾

- `local-review-native-full.log`完整28/28（63.95s），`local-review-unit.log`4文件112/112，typecheck和567文件lint通过；真实Coordinator已携带workspaceReviewBinding，实际Leader消息提交前和批准重放均复核，异步拒绝保持消息队列后续顺序。此Fixture的resume端口是明确标注的调度记录器，不作官方Harness真Fork证据。
- 新文件产物归档只允许同候选的规范approve_completion+resumed事实及done状态，从私有manifest独立物化；精确重放既存归档和文件版本，返回文件引用而非伪Worktree。`local-archive-red.log`缺方法，`local-archive-first.log`实际固定字节归档及重放通过。实际TaskComposition/官方Fork接合仍待后续单元。
- 真实顺序CODER接续复现旧claim阻断（`local-claim-successor-red.log`）。改为关闭证据精确匹配旧worker/session/workspace/epoch/来源后，同次registry事务旧claim→released、新claim→active且epoch增长；跨任务/活动/暂停/隔离/无关闭证明仍拒绝。下一失败发现预登记worker没有sessionId却已使用默认会话，WorkerRuntime现在在running提交持久实际默认ID；随后夹具硬编码旧MCP session导致动作冲突，改用session.sessionId后通过。两项定向`local-claim-targeted-final.log`验证真实接续及删除私有close引用后的拒绝，原claim保持active，无新写者；不同根因分别保留，不称三次同阻塞。
- `local-archive-claim-native-full.log`完整30/30（67.34s）通过，覆盖所有既有授权/文件/命令/MCP/生命周期，以及归档与顺序claim保护。`local-lifecycle-unit-final.log`11文件236/236，typecheck通过。
- 终态释放红测`local-terminal-release-red.log`证明完成归档后原claim仍active。新releaseCompleted仅在已批准恢复的done归档及真实关闭证据齐备后执行可恢复registry事务；引用原Leader批准与关闭证明，不删源文件/历史身份。`local-terminal-release-first.log`定向通过（12.81s）。复评补recoverCompletion：先恢复已准备的原释放事务，再重新核验归档/当前版本，避免prepared自锁；恢复释放不重新赋予旧快照当前资格。增加提交前/提交后的真实中断用例，正在运行`local-terminal-native-full.log`32项全文件，尚未记通过；`local-terminal-recovery-typecheck.log`与lint通过。
- `local-terminal-source-checkpoint.json`补充本次本机集成调用的源码hash和启动后修改检查（仅本次集成验证，不冒称全量pnpm test冻结）。Task12.3仍在执行，安装/生成/下载、正式组合与实际Fork、最低OS、新完整回归尚未完成，无commit/push/PR。

- 终态恢复最终验证：`local-terminal-native-full.log`完整32/32通过（95.07s，0skip）；提交前/后中断均恢复原release operation，原归档/文件版本不替换、source claim最终released。`local-terminal-source-checkpoint.json`与verification核对508份源码启动后未改且hash一致；typecheck和567文件lint通过。仍仅当前本机集成检查点，不替代最终完整pnpm test或真实Harness恢复G5。
- 本轮清理：`local-terminal-native-cleanup-index.json`再汇总103份原生夹具（含失败/诊断/全文件），均保全证据后删除测试根；`local-terminal-unit-cleanup.json`核验后删除3个单测Git遗留目录，逻辑82605字节，APFS观测-3411968字节（同期空间变化，不声称独占回收增量）；清理controller归档并核验删除。已有131份汇总保持。下一步为正式本机任务组合/实际Harness恢复与受控安装生成下载，不把当前内部边界单元宣称Task12.3完成。

### 2026-09-16：显式本机任务组合、官方 Fork 与 D16 归档

- 增加`local-task-composition.ts`：已授权task显式prepare、本机只读/写入/无工具角色Harness、共享GlobalScheduler、可信Tester callback、Reviewer固定版本、Leader批准私有证据复核、同版本归档及claim释放；Web factory在旧资源创建前检查localExecution并显式分流，缺工厂拒绝。普通项目选择/开始UI仍归12.6，验收夹具通过现有持久gate进入，不伪装已开放普通项目入口。
- 首次draft三处类型错（opaque安全点公开身份没有sourceSessionId、工厂无须依赖checkpoint/close）已修正。新夹具入口、字段和gate reason断言错误均保留原始日志。完成回合产生blocking异议时worker已done、cohort为空；不将此称为真Fork。最终夹具在CODER实际模型Step内通过正式生命周期端口请求暂停，保留原工具执行及安全点边界。
- 真实组合发现并修复三项接缝：①paused会话close复用running工具准入导致failed：拆分只读生命周期闭合审计，原lease/授权/绑定/操作journal核验保持；②外部gate持久化后旧顺序循环仍派发下一角色：已存在canonical humanGate即停止编排，红测失败后16/16通过；③顺序Tester路由没有规范announce导致可信验证找不到dispatch：local路由在同次提交追加规范调度，红测失败后Coordinator63/63通过。原失败全部留在`local-composition-chain-native-1..7.log`及`local-paused-dispatch-red.log`、`local-tester-dispatch-red.log`。
- `local-composition-chain-native-8.log`首次18.635s通过，包含PM/Architect、实际CODER MCP修改、活Step暂停、官方Agent factory新context/lineage、同worker新session/lease恢复、真实Node原五项LRU测试、Reviewer读同版本、Leader POST终审、D4 resumed标记、归档与全部claim释放。模拟秘密和原测试字节不变。外部LLM回答固定且消费实际工具结果；既有Go真实模型LRU证据另存，不混称本轮使用真实模型。
- 增加官方HarnessTraceReader父子session断言后，`local-composition-native-full.log` **2文件33/33通过，0skip，113.61s**；32项授权/文件/验证/终态释放恢复未删，组合17.249s。`local-composition-unit-full.log` **7文件140/140通过**；`local-composition-final-typecheck.log`通过。完整G4仍待最终重新运行，不以这些替代旧1637过/1失败的全量结论。
- 所有上述组合尝试的专用目录按测试内原路径dev/inode、owner释放、lsof空和mount检查后删除，逐文件hash及cleanup在`docs/reviews/task123-composition-evidence/*.json`；原生累计32例按其既有受信清理链留证。下一步受控HTTPS下载、隔离安装/生成及实际依赖挂接；macOS15环境仍未提供，已询问入口；无commit/push/PR。

### 2026-09-16：最低系统验收例外与下载原语检查点

- Leader对最低macOS15环境询问明确回复“跳过macOS 15”。已按来源层级同步蓝图、详设、选型、计划与DEF-018；仅本次12.3获批例外，最低15.0构建目标不变，实际已验平台为Apple Silicon/macOS26.5，不记macOS15通过，不自动豁免阶段出口或其他任务。
- 下载策略与DNS/授权拒绝单测47/47通过（local-download-unit-fixed.log）；真实固定picocolors1.1.1包HTTPS下载1/1通过（local-download-live.log），按锁文件SHA-512核验，字节仅在内存，不执行包代码。报告见docs/reviews/task123-download-evidence/。此为内部下载原语，尚未接grant/安装/依赖闭环，不冒称安装G5完成。

### 2026-09-16：下载授权闭合与禁网安装机制实测

- 提取封闭LocalGrantPolicy，增加明确install动作及brokered-https配置；8项纯策略红绿测试保持旧disabled路径。LocalWorkspaceDownloads复用实际Leader提案的policyHash/networkHash、活lease/claim/版本校验及私有对象；prepared先持久，首次完整下载receipt不可覆盖，同输入重放重验完整性与当前授权，同action异输入/未批准origin/策略变化拒绝。没有增加模型URL代理或项目进程联网权限。
- 新真实POST下载用例首次误沿用5秒框架期限（local-authorized-download-native.log），新用例单独采用40秒上限容纳原30秒下载上限及编译/留证，旧用例期限不变；第二次通过。随后完整授权与官方Fork两个文件34/34通过（119.29秒，0skip），local-download-composition-native-full.log。定向-t只用于诊断，不作全量验收。
- 固定安装机制验证使用已安装受管Node24.20.0/pnpm9.15.9及其完整manifest校验，固定picocolors1.1.1经HTTPS/SHA-512进入只读输入；真实pnpm --offline、私有store/cache及shell-emulator配置，在Seatbelt副本实际执行postinstall、build、test。输入源码、只读归档、模拟秘密保持，输入目录没有node_modules。该机制fixture使用明确标注的固定authority seam，不能替代正式安装端口、版本事务或依赖挂接的G5。
- 安装首次失败是pnpm9不接受顶层--shell-emulator选项；读取实际受管9.15.9实现后改为其配置变量，不跳过生命周期脚本。第二次发现真实策略缺已批准Node父目录精确metadata，pnpm stat失败；进程提前退出时监督保持失败关闭，未改弱未知进程规则。策略仅补可执行文件祖先literal metadata，不开放读取目录内容/文件或网络。第三次2.34秒通过，全部失败日志及源码hash保留local-installation-native-1..3.log和docs/reviews/task123-installation-evidence/。
- 修改后的四个完整文件（授权33、受控启动26、官方Fork组合1、安装1）**61/61通过，0skip，157.39秒**，local-installation-boundary-native-full.log；策略/DNS/授权三文件**55/55通过**，local-download-unit-final.log；typecheck及579文件全仓lint通过。最终全量pnpm test仍未重跑，旧1637过/1失败结论不被局部结果覆盖。
- TEST-CLEANUP：新安装每次保存来源/版本/工具链manifest/hash、执行输出与全部普通文件hash，并证明路径uid/dev/inode、lsof无句柄和mount无挂载后删除下载/安装/store/cache/编译副本；逐次JSON记录空间变化。首次下载超时残留RE6wOy目录经原owner PID86486退出、日志时间窗、registry install授权及固定字节匹配、逐文件hash/无句柄/无挂载证明后单独回收，逻辑168068字节，APFS观察+196608；见local-download-timeout-cleanup.json。清理控制器留档后核验删除，未清理其他旧未证实目录。新增夹具提前落盘初始身份，以便超时后追溯。
- 研究来源：pnpm9.x在线文档现重定向12.x（https://pnpm.io/cli/install），不把新版本选项视为9.15.9事实；配置行为以受管pnpm9.15.9实际源码及上述真实执行为准，未添加依赖。下载地址分类参考IANA IPv4/IPv6特殊用途注册表，HTTPS/DNS实现使用Node24原生API。
- 当前剩余：正式隔离安装/生成端口、持久依赖挂接和Tester依赖hash/同版本交付接合、生产helper交付/启动探测、最终累计G1–G7。macOS15仅本次12.3获批例外，普通入口仍关闭，无commit/push/PR。

## 2026-09-16：helper、启动探测、生成与安装闭环收尾

- `build:sandbox-native`新增五项macOS helper并显式最低15.0，真实构建通过（local-helper-build.log）；package.mjs纳入来源、签名和最终工具清单。`verifyLocalExecutionToolchain`在完整旧清单核验及前后清单字节一致后要求五helper存在/hash/执行位，旧预览安装ready不自动获得新执行能力。三个交付单测红→绿；本轮未构建/发布新DMG，安装介质验收仍归12.7。误用无参数`pnpm build:desktop`被既有CLI用法拒绝且未创建临时构建，随后正确运行`pnpm --filter @agora/desktop build`通过；不把编译当安装包验证。
- 本机task factory在模型配置/角色准入前运行owner私有固定启动探测，真实允许输入读/输出写、拒绝输入写/模拟凭据读/loopback、环境白名单及Node子进程继承。最初binding列表重复包含父根和子根，重叠保护拒绝；删除多余父根，保留每个子根完整父链绑定后组合实测通过（local-startup-probe-native-2.log，23.402秒）。失败统一sandbox_unavailable，不降级、不发放任务授权。
- `node-generate`从固定源复制至新输出，代码生成在Seatbelt内运行；新增workspace_read的commandReceiptId分支由当前grant、同worker、成功命令、版本映射和原生路径读取保护。真实MCP生成→字节读取→版本事务创建文件通过（local-generation-native-3.log，7.399秒）；私有副本对sentinel的修改未进入原目录。两个初始红测为夹具错用无效生成入口和多传MCP op字段，按接口修正夹具，不改现有保护。纯入口/引号单测7项通过。
- `pnpm-install`首版接精确32包以内计划、真实Leader install grant、版本绑定HTTPS下载、只读受管pnpm核验、offline/copy/hoisted安装和生命周期脚本。实际node_modules和生成lock经原生安全读取/完整清单后进入不可变对象；后续Node只读执行输入由原源字节和固定依赖组合，不覆盖用户node_modules或其他输出。当前只支持完整精确直接/dev包计划；已有lock/config/workspace/override/optional/peer及未缓存传递解析明确拒绝/失败，不能对外宣称任意生态安装。未添加第三方依赖。
- 实测修复了NODE_ENV=production下pnpm漏装devDependencies：增加--prod=false，并以真实devDependency执行证明。保留pnpm/Node24 DEP0169原始警告，只对测试夹具允许该精确已知警告，不屏蔽stderr或修改产品退出结果。安装适配单测6项；五文件相关单测21/21（local-adapters-unit.log）。
- 安装→依赖支持的Node两项测试首先通过（local-installation-port-native-3.log，19.286秒）。扩展独立TESTER后复现workspace_command_scope_mismatch：验证器误用Tester workspace校验Coder安装回执；私有安装事实现在保存原workspace，读验证在同task/root/源版本/工具链内交叉核对该原始命令，不复用Coder lease/claim。command prepared/inputHash绑定dependencyInputHash，挂接引用缺失不能被解释为空依赖。缺安装先拒绝，不持久坏command；删除挂接引用后验证拒绝。
- 最终专用安装验收通过（local-installation-validation-native-3.log，39.501秒）：正式MCP安装、真实生命周期产物、独立Tester及原两项测试、版本绑定review/Leader校验/归档/claim释放；外部修改后资格失效及用户新版本保留。该确定性组合不伪装真实模型/Fork；完整官方Harness/Fork以及Go原五项LRU由各自独立G5和累计回归覆盖。
- 当前typecheck、585文件lint、桌面TypeScript编译通过。冻结609份源码/配置启动原始`pnpm test`，包含旧Docker与固定Go deepseek-v4-flash真实测试，不增加Benchmark、费用范围或skip。结果待下文，最终full-source-checkpoint.json/final-full-regression.log；运行期间不修改冻结源码，文档整理单独留证。macOS15仅本次例外沿用，未commit/push/PR，任务保持in_progress。

### L03补充IPC证据

- 逐项审查发现原有模拟凭据、环境/fd及子孙继承测试不能单独充当Mach/Unix的直接证据。新增独立验证源`docs/reviews/task123-ipc-evidence/`，使用原生产buildLocalCommandPolicy，同一原生父/子在未限制基线能连接专用Unix监听器并查到securityd服务端口；受限后两个进程均Mach=1100、Unix=EPERM/EACCES，1/1通过（local-ipc-native-2.log）。不发送Mach消息、不调用或读取任何Keychain项，不读取真实秘密。
- 初次探针把launchd的Label com.apple.securityd误作Mach service，未受限基线返回1102；从系统com.apple.securityd.plist核对实际MachServices为com.apple.SecurityServer后修正探针，保留原失败，未修改策略/保护断言。两份夹具均在关闭监听器、真实进程退出、lsof/mount/identity核验及hash保存后删除；临时Vitest配置也归档核验后删除。该额外G5不改变正在运行的609份冻结源码/default测试匹配，不混入默认全量计数。

- G1审查同步：详设中的“尚未接MCP/companion/归档”等中间状态已改为实际契约及责任边界；开发计划删除已经过期的三段临时执行状态，既有失败/检查点报告和本历史保留。蓝图明确当前适配范围，架构记录跨workspace不可变依赖证据而非活能力共享，选型记录无新依赖及真实pnpm选项。安装限制、macOS15例外、普通入口/后续任务及发布边界保持显式。审查表见docs/reviews/task123-acceptance.md。
- G7启发式扫描6133份Git可见文本：无候选真实凭据模式或敏感文件名；不读取ignored .env、不打印匹配内容。报告final-sensitive-data-scan.json。该扫描为辅助证据，实际凭据保护另由原生G5及配置/Trace契约验证。

## 2026-09-16：最终累计门禁与待交付

- 原始`pnpm test`完整通过：7脚本；225个Vitest文件/1748测试，0失败、0skip，912.90秒。包括Phase12的18文件168测试、既有Docker/累计跨阶段链路和真实Go deepseek-v4-flash。旧LRU148.535秒、新本机LRU36.784秒均通过；完整provider单回合和群聊摘要真实测试也通过。正式Benchmark未启动，eval-support的139项属于默认适配器回归，不改称正式评测。分层统计/完整日志：final-test-summary.json、final-full-regression.log。
- G1对照详设§12.2 L01–L18的责任分配完成审查；G3 typecheck、585文件lint、桌面编译通过；G4如上；G5真实原生/模型/官方Fork/固定版本完成链及额外Mach/Unix 1/1通过；G6报告与短索引同步；G7数据扫描及隔离/清理证据完整。仅本任务获批跳过macOS15，当前实测macOS26.5 Build25F71 arm64，最低15.0目标和DEF-018未验证状态不变。
- 609份冻结源码/配置全部hash一致（final-source-verification.json）。期间仅补文档、额外独立IPC验证源及报告，没有改默认匹配或冻结产品/测试源码。追加补验不混入1748计数。
- TEST-CLEANUP完成安全范围：152个已确认测试目录保存完整清单/hash后，逐项再次核验owner/dev/inode/realpath、内容、lsof、系统/Docker挂载并删除；逻辑1523674字节，APFS观察+25567232字节（背景活动下不声称独占回收量）。31个本轮目录因macOS Virtualization服务PID76991仍持只读DIR句柄保守保留；已知源码来源不代表当前可回收，不杀系统/用户服务。旧证明不足目录保持不动。详见final-full-regression-cleanup.json、final-cleanup-prepare.log、final-cleanup-apply.log。控制器保存源码/hash并核验删除，见final-cleanup-controller-removal.json。
- 本轮新增生成/安装10份定向fixture清理均有deleted=true；全量产生的原生fixture下载、依赖、可执行副本另随各自报告清理；本机正常开发build、正常依赖/共享缓存、安装应用及产品状态未删除。
- 交付审查稿：docs/reviews/task123-acceptance.md；英文PR草稿：docs/reviews/task123-pr-draft.md。当前分支feat/task-12-3-controlled-local-execution，基线ea32c4c，未commit/push/PR。实现与当前范围验收就绪；首版安装限制已显式记录，普通项目入口、Worktree/接管与退出恢复/发布仍归对应后续任务。AGENTS.md §10要求明确commit/push授权，§8.1.2要求人工PR合并后done，因此12.3保持in_progress，等待本次交付授权；未自动级联12.4或推进阶段。

## 2026-09-16：交付授权与提交检查

- Leader明确回复“授权提交、推送并创建 PR”，授权本次功能分支feat/task-12-3-controlled-local-execution提交/推送及目标dev-1.0.0的PR；不包含自动合并。
- GitHub登录在受限环境检查失败，使用正常网络权限复核后有效，未更换账号或凭据。复用刚完成的累计门禁，609份冻结源码/配置hash仍一致；交付扫描6450份Git可见文件，与已配置秘密作内存比较为0匹配，不输出任何秘密值（delivery-sensitive-data-verification.json）。
- 当前提交覆盖本任务实现、测试、规格同步及原始证据；task状态保持in_progress，PR人工合并后再收尾。
