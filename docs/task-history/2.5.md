# 任务 2.5：Phase 2 出口集成测试

本文件保存历史证据；当前状态、依赖和时间戳只以 [任务索引](../task-status.json) 为准。原文中的 ready/in_progress/done、待合并和延期项状态描述记录时点，不覆盖当前索引或延期台账。后续事件追加到文末，不改写原文区。

## 2026-09-13 原 notes 无损迁移

Source commit: `fa3efafed328bdfeb5b0f575b4e57bbd248a45fd`
Original notes SHA-256: `45b1bbc1109092418eda54ada9ac0dc6e24ec46d2c14234bb159a05ba58b7f8d`

~~~~text
<!-- BEGIN ARCHIVED NOTES -->
六角色完整回环端到端：需求→设计→编码→测试→评审→finalize；回环与超限升级路径验证。
[2026-08-28 级联翻转] 依赖 2.2/2.3/2.4 全部 done，幂等翻转 pending→ready。
[2026-08-28 开工·用户确认] 状态置 in_progress。延期项处置经用户认可：DEF-006 于 2.5 拆分 git 组（git.worktree=applyPatch+diff→CODER/TESTER；git.readonly=diff→ARCH/REVIEWER；createWorktree/merge 不授模型角色）；DEF-008 组合根启动期 roster 绑定校验（方案 b，不动 2.2 已批接口）；DEF-010 不解锁 pendingPatch（e2e 断言 worktree 引用链路 + pendingPatch 全程 null 佐证，滑 Phase 3）；DEF-005/007 按既定处置断言。
[2026-08-28 计划批准·用户确认] 五改动面：① executor 增 readTurnMutations 接缝（PM 无工具/ARCH·REVIEWER 无 fs.write，三角色以终局结构化文本为输出通道，组合根解释器校验后产 mutations）；② WorkerRuntime 增只读 roster getter（DEF-008 可见性前提）；③ LOGICAL_GROUPS 拆组 + ARCH/REVIEWER tools git→git.readonly（PHASE0_ROSTER 不动，R9）；④ phase2-runtime.ts 六角色组合根（DEFAULT_ROSTER 双源共用 + assertRosterBinding + WorktreeGitService 预建真 git worktree——1.6 预留 getWorktree 指针落地，交接文件/in-loop git 全部落位其上）；⑤ phase2-exit.test.ts 九链（happy/test 回环/review 回环/超限/DEF-006 矩阵/DEF-008 绑定）。
[2026-08-28 Code+Check+Verify] TDD 红→绿：先写测试（6 failed 指向实现缺失与期望校准），实现后 9/9 绿。关键实测：iterationCount 语义=PM 派发计一轮（happy 1 / 回环 2 / 种子 7→humanGate 8）；REVIEWER in-loop git_diff 见 CODER applyPatch 提交（40hex commitId）；node --test 真实 exitCode 序列 [1,0]（回环证据）；R2 负向断言五角色投影无 channelId/fromRole/msgId；D1 首请求=纯投影+末请求保留 tool 交换。
[门禁实测] pnpm typecheck exit=0；pnpm lint exit=0（biome --write 修格式 + --unsafe 修 useLiteralKeys 后 0 违规）；pnpm test 255 passed / 2 skipped（31 files，2 skip 为既有 G5 live-LLM 需 DEEPSEEK_API_KEY；Docker 真实容器 15 例全绿）。期望值同步：roster.test（ARCH/REVIEWER git.readonly）、bridge.test + phase1-exit（CODER resolve 不含 createWorktree/merge，直链 git 全链不受限）。
[R1/R9/R12 复核] State 写入全走 applyMutations（解释器产 mutation 对象经 readTurnMutations 接缝进 StepResult）；无接口签名破坏（两处均为增量可选字段/getter）；详细设计 §2/§9 实装记录 + deferred-items 五项处置已同步。G1-G7 全过（G5：真实 git 二进制/真实 node --test/真实 MCP fs/git 链路，唯一 mock=脚本化 LLM R11 注明）。
[2026-08-28 /commit-agora] 门禁复跑通过（typecheck 0 错 / lint 0 违规 / test 255 passed 2 skipped）；分支 test/phase2-exit-integration，提交 ceff905，PR #27：https://github.com/logan-suu/Agora/pull/27（base=dev-1.0.0）。状态保持 in_progress，待人类合并后 /pr-merge-agora 收尾标 done。
[2026-08-28 DEF-005 解决·用户指示在 PR #27 内落地] packages/tools/lint 实现 §6 lint-server（check(worktree, paths) -> issues[]）：WorktreeLintService 以 `node <biome-bin> lint <paths> --reporter=json` 包裹真实 Biome CLI（cwd=注册 worktree 根、无 shell 派生、worktree 相对路径校验、registry allowlist/retarget 加固镜像 fs/test service）；JSON diagnostics 解析为 {file,line,message,rule,severity}（1-based 行号），biome exit>=2 显式 throw（§12）。bridge 接线：LOGICAL_GROUPS 增 'lint'→lint_check（wire 名）+ MCP_TOOLS 增 lint.check 条目+ BridgedServer/connectServer 联合类型 + createToolCatalog 装配 lint-server；bridge package.json 增 @agora/tools-lint。期望值同步：bridge.test（10 wire 工具+CODER lint_check+unavailable=[]）、phase1-exit（CODER resolve 同步+describe 更名）、phase2-exit（DEF-006 链 reviewer/coder lint_check + REVIEWER_TURN 增 in-loop lint_check + chain 3 断言真实 biome 空诊断）。phase0/1 工具面注释校准（lint 仍不进 P0/P1 循环面，catalog 已解析）。
[DEF-005 门禁实测] lint 包 12 例全绿（真实 biome 子进程：clean []/noDoubleEquals 结构化诊断 file/line/rule/断言/整树默认/未注册拒绝/5 类路径逃逸拒绝/坏 binary 显式错/InMemory round-trip x2）；全量 pnpm test 267 passed 2 skipped（+12），typecheck 0 错，lint 0 违规（119 files）。REVIEWER 在六角色回环内真实调用 lint_check（G5）。R12：详细设计 §2 实装记录 + deferred-items DEF-005→resolved。§2 矩阵 lint 行 CODER ✓/REVIEWER ✓ 自此全格落地。
[2026-08-28 /pr-review-agora 复审] Coderabbit 两条 actionable + 1 nitpick 全部验证并当轮修复：① WorktreeGitService 增 dispose()（服务自有 temp base / 创建的 worktree MOVE 至 GIT_TEARDOWN_STAGING 唯一槽位、caller main repo 不动、幂等；phase2-runtime 的 catalog-init catch 与 dispose 两处接线；+2 单元测试；修复过程发现确定性命名跨 run 撞 staging 目录导致 renameSync ENOTEMPTY，改 mkdtemp 唯一槽位）；② DEF-008 resolved_at 纠正为 ceff905 落地时刻（原 13:20Z 早于 created_at 14:55Z）；③ REVIEWER_ROLE 陈旧 DEF-005 注释同步（lint 已实装）；④ phase2-exit 改 import PHASE2_TOOL_SURFACE 不再镜像。门禁：typecheck 0 错 / lint 0 违规 / test 269 passed 2 skipped（全量连跑两轮绿）。
[2026-08-28 /pr-merge-agora] PR #27 已合并（merge commit aca9238d7a3b4cf45f4170dc76ffb15b7cc46cb8），任务标 done；dev-1.0.0 已同步至 aca9238（fast-forward），本地分支 test/phase2-exit-integration 已删除。G5 实测结论：phase2-exit 九链全部真实链路无 mock（真实 git 二进制 / 真实 node --test / 真实 MCP fs/git/lint server InMemoryTransport round-trip，唯一 mock=脚本化 LLM R11 注明）；六角色完整回环（需求→设计→编码→测试→评审→finalize）、测试回环（node --test exitCode 序列 [1,0]）、评审回环（changes_requested→CODER feedback）、超限 humanGate 升级（iterationCount 语义=PM 派发计一轮，种子 7→8 触发）全部验证；REVIEWER in-loop lint_check 真实 biome 空诊断（DEF-005 resolved）；DEF-006 git 组拆分矩阵断言成立；DEF-008 组合根 assertRosterBinding 启动校验成立（resolved）。门禁最终态：typecheck 0 错 / lint 0 违规（119 files）/ test 269 passed 2 skipped（2 跳=既有 G5 live-LLM 需 DEEPSEEK_API_KEY，全量连跑两轮绿）。exit_criteria「PM/ARCHITECT/REVIEWER 补齐，完整回环 + 迭代上限可控」逐条满足，Phase 2 → done，current_phase → 3。遗留延期项：DEF-009（重复升级消息幂等，target_phase=8）、DEF-010（pendingPatch writer，已滑 Phase 3）均 open 于 docs/deferred-items.json；级联翻转：3.1 pending→ready（依赖 2.5 done）。
<!-- END ARCHIVED NOTES -->
~~~~
