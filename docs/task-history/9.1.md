# 任务 9.1：GlobalScheduler lease + WorkerRuntime.runParallel 有界并行池

本文件保存历史证据；当前状态、依赖和时间戳只以 [任务索引](../task-status.json) 为准。原文中的 ready/in_progress/done、待合并和延期项状态描述记录时点，不覆盖当前索引或延期台账。后续事件追加到文末，不改写原文区。

## 2026-09-13 原 notes 无损迁移

Source commit: `fa3efafed328bdfeb5b0f575b4e57bbd248a45fd`
Original notes SHA-256: `05d9e78a19e985c0023a52b3d7b632eda233cd79b43f4a9d6263a4270b2b52de`

~~~~text
<!-- BEGIN ARCHIVED NOTES -->
[2026-09-05 规格评审·Leader 授权] 旧规格不合理：batch.slice 会静默丢任务，mergeStates 无契约，Promise.all fail-fast 会在其他 LLM 仍运行时提前返回，GlobalScheduler 与本地 maxParallel 双重控额，且未限制并行 set/同实体多写者。D17 改为：GlobalScheduler 稳定 lease 是跨项目唯一额度、按项目轮转；runParallel 处理完整 batch；稳定 workerId + 独立薄 Harness Executor/session；模型/工具并行、step 输出与 State commit 经 per-task canonical queue 串行；只允许稳定 append/自身 worker/subtask merge；all-settled 后 reload canonical State。9.1 不做文件写真并行，workerId worktree 留 9.3。
[2026-09-05 /agora-do-task] Leader 确认 D17 并授权启动 9.1；依赖 8.5=done、Phase 8 出口完成、current_phase=9，跨阶段门禁满足。任务置 in_progress，进入 Explore/Plan；实现计划再次确认前不写测试或生产代码。
[2026-09-05 Explore] 代码接缝确认：现有 runParallel 是顺序 fold；Assignment 无 workerId、Route 固定单元素；AppState 尚无 workers，reducer 的 workers 写入仍禁用；Web TaskStateStore 已按 task 串行 commit，但 WorkerRuntime 尚无 canonical load/join；TaskOrchestrationRuntime 仍有 Phase 5 全实例单 run 容量闸。规格范围校准：Route 形状和稳定身份归 9.1，9.4 只消费该接口生成拓扑波次。未发现需新增依赖或修改冻结 L3 签名的阻塞项。
[2026-09-05 Code+Check+Verify] 按 Leader 确认的 D17 完成 TDD 落码：新增跨项目按项目轮转的 GlobalScheduler 稳定 lease（默认全局 cap=3）；WorkerRuntime 以完整有界队列执行 batch，每个 worker 独立薄 Harness Executor/session，并经 per-task canonical commit queue reload/join State。AppState/reducer/JSON store 落地 WorkerState 及旧快照迁移；Coordinator 持久化稳定 workerId 并恢复同一 dispatch；Web 组合根共享唯一 scheduler，D17 自 Phase 9 取代 D10 的全实例单 run 闸。所有并行 set、越区 merge、同 ID 异内容和依赖漂移均 fail-closed；fail-fast 仍等已启动 siblings 收敛，未启动尾部显式标记。D17/D10 已按蓝图→详细设计→系统架构/技术选型→开发计划/task-status→AGENTS 同步，无新增依赖或延期项。G5 tests/integration/phase9/phase9-worker-pool.test.ts 使用真实 JsonTaskStateStore 与真实 HarnessExecutor/Context/Agent loop，5 个 assignment 在本地 cap=2/全局 cap=3 下全部执行，实测峰值=2，5 个独立 executor/session 与最终 canonical State 均持久化；唯一 test double 是文件头声明的外部计费 LLM 文本适配器。门禁：typecheck 0 错；Biome 258 files 0 违规；原样 pnpm test 97 files/813 tests 全绿，包含 DockerSandbox 16/16、Web Docker/Harness/MCP 真实链及 live DeepSeek LRU 端到端；tracking JSON/git diff 检查通过。G7 gitleaks 对当前 diff 与新增文件扫描无泄漏；全仓扫描的 18 条既有候选均位于已忽略的 apps/web/.next 生成缓存。任务保持 in_progress，待 $agora-commit 交付。
[2026-09-05 /agora-commit·门禁] 分支 feat/phase9-worker-pool（base=dev-1.0.0，开工时 0 ahead/0 behind）；typecheck 0 错；Biome 258 files 0 违规；9.1 专属真实 worker-pool 链 1/1；原样 pnpm test 97 files/813 tests 全绿，包含 live DeepSeek LRU、Web Docker/Harness/MCP 和 DockerSandbox 16/16；tracking JSON、git diff 及当前 diff/新增文件 gitleaks 检查通过。任务保持 in_progress，禁止 Agent 自动合并。
[2026-09-05 /agora-commit·交付] 功能提交 e657d00 已推送至 origin/feat/phase9-worker-pool；PR #63：https://github.com/logan-suu/Agora/pull/63（feat/phase9-worker-pool → dev-1.0.0）。PR 标题与正文为英文，任务保持 in_progress，等待人类审阅与合并；Agent 不自动合并。
[2026-09-05 /agora-pr-review·规格复评计划确认] Leader 确认先修规格再修 PR。复评认定四处旧口径不合理：当前 Subtask 无非控制面字段却授权并行 merge；lease 缺 taskId 导致同项目跨 task 身份碰撞；无限强引用 released tombstone 与进程内幂等需求不相称；允许多 task 登记却未限制 lease 外的 Docker/worktree/MCP/Git/Harness 预分配。已按 D17 将并行写收紧为稳定 append/自身 WorkerState，lease 身份改为 {projectId,taskId,workerId} 且同进程同 capability 幂等，新增默认 3 个 active executable composition 生命周期 admission（满载在 factory 前返回可重试 429，不授予 worker 额度）。后续按已确认计划 TDD 修复并重跑 PR 门禁。
[2026-09-05 /agora-pr-review·修复复检] 二次规格校准确认 ownerRole 只是实现责任归属，不能阻止 TESTER/REVIEWER 执行同 subtask；同时将 agent 输出权限进一步收紧为仅稳定 append，WorkerState 注册/status/safePoint merge 只由 WorkerRuntime 内部生成，当前 Subtask 全字段归串行控制面。实现已修复 scheduler cap=3 公平轮转、{projectId,taskId,workerId} 身份、同 capability release 幂等/伪造拒绝、canonical assignment/subtask/roster 复核、Phase 9 harness-only executor、持久 workers 元素校验、factory 前 composition admission 429 与 D4 清理失败保留容量/可重试。TDD 新增对应回归；任务专属 Phase 9 G5 1/1；typecheck 0 错；Biome 258 files 0 违规；原样 pnpm test 97 files/823 tests 全绿，包含真实 DockerSandbox、Web Docker/Harness/MCP 与 live DeepSeek LRU；task-status JSON 与 git diff 检查通过。任务保持 in_progress，待本轮修复提交、PR 评论处理及人工合并。
[2026-09-05 /agora-pr-review·交付] 修复提交 b2d471f 已推送至 PR #63；5 个既有 CodeRabbit 会话均已英文回复并 resolved：composition admission/API 协议/WorkerState 持久校验已修复，Subtask 建议按修订后的控制面规格拒绝，MD028 建议因独立决策 blockquote 的语义分隔且非项目门禁未采纳。GitHub 复核 head=b2d471f、PR open/mergeable、未解决 review thread=0；CodeRabbit 对新 head 的自动复审在约 4 分钟等待后仍为 pending 且未产生新 thread，不虚报通过。任务保持 in_progress，等待远端复审完成与人工合并；Agent 不自动合并。
[2026-09-06 /agora-pr-review·增量复审修复] CodeRabbit 对 b2d471f 完成增量复审后新增两项有效发现：裸 NUL 分隔的 scheduler worker key 对含分隔符的合法 identity 非单射；worker 在 drain 期间自然完成时会被 pauseAtSafePoint 从 done 错写为 paused。已先补红测，再将 key 改为 JSON tuple 单射编码，并在完成态 drain 中保留 canonical status=done、继续 flush safePoint 和完成 drain receipt、禁止写 paused；同步详细设计 D17 口径。Phase 7 出口旧断言曾把错误 paused 写入固化为事件序列，已删除该错误事件期望并新增持久 done 强断言。定向回归 4 files/42 tests 全绿；原样 pnpm test 97 files/825 tests 全绿，包含 live DeepSeek LRU、DockerSandbox、Web Docker/Harness/MCP 与 Phase 9 G5。任务保持 in_progress，待提交推送、英文回复并等待人工合并。
[2026-09-06 /agora-pr-merge] PR #63 已由人类合并至 dev-1.0.0，merge commit=`414fa2343356dce9557edd5f423e80d5107ae6e2`，功能 head=`dfbbe2438dc909ea9c2982957792443ff7fd82e1`，CodeRabbit 检查 PASS 且所有 review threads 已英文回复并 resolved。G1–G7 证据完整：D17 规格经 Leader 复评后按真相源同步，实现与最终规格一致；typecheck 0 错；Biome 258 files 0 违规；完整 Vitest 97 files / 825 tests 全绿；Phase 9 worker-pool G5 使用真实 JsonTaskStateStore 与真实 HarnessExecutor/Context/Agent loop，5 个稳定 worker 在本地 cap=2/全局 cap=3 下完整执行，峰值=2、独立 executor/session 与 canonical State 持久化均有证据；真实 DockerSandbox、Web Docker/Harness/MCP 与 live DeepSeek LRU 链均通过；task-status JSON、git diff 与 gitleaks 检查无问题。无新增延期项。任务 9.1 标 done；9.2、9.3 依赖满足并级联 pending→ready；Phase 9 保持 in_progress，等待后续任务及出口 9.5。
<!-- END ARCHIVED NOTES -->
~~~~
