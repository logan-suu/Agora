# 任务 5.3：TaskStateStore + 消息提交/MessageBus/SSE 接入

本文件保存历史证据；当前状态、依赖和时间戳只以 [任务索引](../task-status.json) 为准。原文中的 ready/in_progress/done、待合并和延期项状态描述记录时点，不覆盖当前索引或延期台账。后续事件追加到文末，不改写原文区。

## 2026-09-13 原 notes 无损迁移

Source commit: `fa3efafed328bdfeb5b0f575b4e57bbd248a45fd`
Original notes SHA-256: `6b57385ec4ce65979790448716e4f80a945329d1e02741cb4906417e888304a9`

~~~~text
<!-- BEGIN ARCHIVED NOTES -->
D6/D7 Phase 5 退化落地：TaskStateStore 端口 + .data JSON 原子快照适配器；消息先经 applyMutations 提交/持久化，changed=true 后才发布 MessageCommitted；MessageBus 只投递，SSE/UI 只收不含 payload 的展示信封；HTTP/SSE 显式携带 projectId/taskId/channelId，浏览器消息由服务端标记 leader；每活动任务只支持固定 main，动态 Channel/participants/角色 inbox 留 6.1。任务完成时应关闭 DEF-014；DEF-015 的完整 participants 授权仍留 Phase 6。
[2026-08-31 /agora-pr-merge] 依赖 5.2 已由 PR #42 合并并标 done（merge commit 881eda9），级联翻转 pending→ready。
[2026-08-31 规格评审] Leader 确认 D6/D7，已按真相源顺序同步通信职责、Channel 所有权、Phase 5/6 边界与 JSON 持久化要求；任务仍为 ready，待重新锁定实现计划后开工。
[2026-08-31 /agora-do-task] Leader 已确认 5.3 实现计划；依赖 5.2=done、Phase 4 出口 4.6=done，跨阶段门禁满足，任务置为 in_progress。按 TDD 分三单元实施：TaskStateStore、消息提交服务、SSE/UI 实链。
[2026-08-31 Code·TDD 红→绿] 三单元均先确认红灯再实现：① AppState 增加 projectId，移除误列的 AppState.channels mutation 字段；TaskStateStore + JsonTaskStateStore 以 .data/projects/{projectId}/tasks/{taskId}/state.json 原子临时文件替换并按任务串行提交，覆盖初始化、幂等、12 路并发、路径防护与损坏 JSON；② MessageService 先 commit 后 publish，重复 msgId 不广播，投递失败不回滚持久事实；③ HTTP/SSE 显式三元作用域，服务端固定 leader，SSE/快照只含 display 信封，EventSource + POST 替代 UI 本地伪追加。DEF-014 已按实证关闭；DEF-015 保持 open，动态 participants 授权归 6.1。
[2026-08-31 Check+Verify] pnpm typecheck exit=0；Biome 161 files 0 违规；next build exit=0（首页 3.79 kB、First Load JS 106 kB）；全量回归 46 files passed / 2 skipped、437 passed / 2 skipped，真实 DockerSandbox 15/15。任务专属测试：runtime state 5/5、MessageBus 1/1、message service 4/4、Web 18/18。Codex in-app browser 对生产构建 localhost:3100 实测：POST→SSE 回显、刷新恢复、停止并重启 Next 后由仓库根 .data 快照恢复；SSE 无 payload，console 0 error/warn；默认桌面与 390×844 均无横向溢出，移动端左右抽屉可开合。G5 生成数据已移至 /private/tmp/agora-5.3-g5.8uCiuW，未遗留工作树数据。任务保持 in_progress，待 $agora-commit 建 PR，人工合并后由 $agora-pr-merge 标 done。
[2026-08-31 /agora-commit·交付门禁] 分支 feat/persisted-message-flow（base=dev-1.0.0）。typecheck exit=0；Biome 161 files 0 违规；next production build exit=0（首页 3.79 kB、First Load JS 106 kB）；全量回归 46 files passed / 2 skipped、437 passed / 2 skipped，真实 DockerSandbox 15/15。任务专属 G5 使用隔离数据根 /private/tmp/agora-5.3-commit-g5.HiVDFg：生产页面 POST→持久化→SSE 回显，刷新恢复与 Next 进程重启恢复均成功，页面 Agora/localhost:3100、Live、console 0 error/warn。git diff --check 通过；敏感数据模式扫描无命中。任务继续保持 in_progress，禁止 Agent 自动合并。
[2026-08-31 /agora-commit] 功能提交 3a83c88；PR #43 已创建：https://github.com/logan-suu/Agora/pull/43（feat/persisted-message-flow → dev-1.0.0）。PR 保持 open，任务保持 in_progress，待人工审阅合并后由 $agora-pr-merge 收尾。
[2026-08-31 /agora-sync-docs·规格复评] 保留 D6/D7 方向，补齐快照→实时尾流无缝衔接与逻辑重试复用 msgId 契约；新增 D8，Phase 5–9 只承诺单实例自托管后端，Vercel 仅前端，完整 Serverless/水平扩展需外部耐久 TaskStateStore + 跨实例事件传输。蓝图旧 AppState.channels 表述已删除；以修订后规格重新审查 PR #43。
[2026-08-31 /agora-pr-review·阻断修复] TDD 新增 SSE 建连竞态与逻辑发送幂等回归，红灯分别命中快照期订阅数为 0 与重试契约缺失。修复后 SSE 先订阅、缓存建连期事件，快照发出后再冲刷尾流；客户端 pending submission 在失败后保留 msgId、成功后清除。定向 9/9；typecheck 0 错；Biome 161 文件 0 违规；Next production build 成功（首页 3.86 kB，First Load JS 106 kB）；全量 46 files passed / 2 skipped、439 passed / 2 skipped，真实 DockerSandbox 15/15；git diff --check 通过。PR 保持 in_progress，待修复提交推送与人工合并。
[2026-08-31 /agora-pr-merge 收尾] PR #43 已由人工合并至 dev-1.0.0（merge commit feebaec931ee2dc283394d851b3d0f5602bbc6a9）；任务标 done。G1–G7 证据复核：修订后 D6/D7/D8 规格与实现一致；typecheck 0 错；Biome 161 文件 0 违规；Next production build 成功；全量 439 passed / 2 skipped（既有 live-LLM 条件跳过），真实 DockerSandbox 15/15。G5 真实生产链完成 POST→JSON 持久化→无 payload SSE、刷新恢复、Next 进程重启恢复，并有确定性建连竞态与 msgId 重试回归；敏感数据与 git diff --check 无问题。DEF-014 已 resolved；DEF-015 按既定边界保持 open，解除节点为公开暴露前/最迟 Phase 6 出口。级联翻转：5.4 pending→ready；Phase 5 仍 in_progress，出口任务 5.6 尚未完成。
<!-- END ARCHIVED NOTES -->
~~~~
