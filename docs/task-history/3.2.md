# 任务 3.2：handoffPackets 结构化交接包

本文件保存历史证据；当前状态、依赖和时间戳只以 [任务索引](../task-status.json) 为准。原文中的 ready/in_progress/done、待合并和延期项状态描述记录时点，不覆盖当前索引或延期台账。后续事件追加到文末，不改写原文区。

## 2026-09-13 原 notes 无损迁移

Source commit: `fa3efafed328bdfeb5b0f575b4e57bbd248a45fd`
Original notes SHA-256: `6fa60ac131fc8f92304ba9474a58bf6e1a49a00781ea71fb5ead31817486e226`

~~~~text
<!-- BEGIN ARCHIVED NOTES -->
core/domain/handoff.ts。HandoffPacket{fromRole,toRole,done,keyDecisions,openIssues,fileRefs}；角色切换时由 Coordinator 触发生成。借鉴 AutoGen Handoff-as-tool 协议（模式④）：载荷写入共享状态切片供投影消费，不 splice 进对话流。[2026-08-29 开工·用户确认] 状态置 in_progress。[2026-08-29 计划批准·用户确认] 两项裁决：① Coordinator 角色切换生成接线延后至 3.3/3.5（本任务仅域层，接线点已定位：coordinator.ts 五处 nextRole 翻转的 dispatch/evaluate 函数）；② keyDecisions 引用完整性纳入 reducer 边界校验（悬空 decision id throw，镜像 3.1 supersedes 先例）。[2026-08-29 Code+Check·TDD 红→绿] RED 实测 17 failed/58 passed（handoff.test.ts 模块未导出 9 例 + reducer 门控 disabledFieldError/初始 undefined 8 例）；实现后域层 75/75 绿。改动面：① 新建 core/domain/handoff.ts（HandoffPacket 接口 §1 逐字无 id 字段 + assertValidHandoff 形状校验 + assertAppendableHandoff(ledger, packet) 形状+keyDecisions 引用完整性，零依赖零 I/O R8/L1）；② state.ts AppState+=handoffPackets: HandoffPacket[]（必填，reviewComments 与 decisionLedger 之间对齐 §1 协作层相对顺序）+ createInitialAppState 初始 []；③ reducer.ts ENABLED_APPEND_FIELDS+=handoffPackets（0.2 预铺「表内加一项+补一个 case」路径，签名未变）+ applyAppend 补 case（边界 assert→deduplicatedAppend，无 id 字段走深相等去重幂等）；④ index.ts 导出。测试：handoff.test.ts 9 例（形状校验/悬空引用/纯函数性）+ reducer.test.ts 新 describe 7 例（交换律按 ts 归一化排序/幂等重放/不同 packet 不被深相等误合并/畸形边界拒绝/悬空引用拒绝/ledger 既有 id 通过/初始空）+ 门控断言同步 + state.test.ts fixture 补 handoffPackets:[]。无 mock（R11，纯域层纯函数，真实断言无弱化）。[门禁实测] pnpm typecheck exit=0；pnpm lint exit=0（biome --write 修 2 测试文件格式后 123 files 0 违规）；pnpm test 303 passed / 2 skipped（34 files，较 3.1 基线 284 +19 例，2 skip 为既有 G5 live-LLM 需 DEEPSEEK_API_KEY）。R1 写入全走 applyMutations（append 家族，交换律+幂等测试锁定）；R9 零签名变更（Mutation 联合 0.2 已定稿、Executor/SandboxManager 未触、AppState 纯增量字段）；R10 camelCase + handoff.ts 对齐 §0 目录树。G5：纯域层任务无沙箱/Harness 执行链路对象，注明暂缺（3.1 先例）。R12 详细设计 §1 实装记录已补。待 /commit-agora 建 PR，合并后 /pr-merge-agora 收尾标 done。 PR 已建：https://github.com/logan-suu/Agora/pull/29（feat/handoff-packets，commit 6015483，base dev-1.0.0），待人类审阅合并后 /pr-merge-agora 收尾标 done。 [2026-08-29 /pr-review-agora·PR #29 架构预审] 结论：无红线违规（R1 append 家族+交换律/幂等测试锁定、R2 未触投影且不 splice 进对话流对齐模式④、R9 零签名变更、R10 对齐 §0 目录树、R11 无 mock、R13 英文祈使句）；2 条警告当场修复：① handoff.ts 两处 JSDoc 注释中译英（AGENTS.md §3.1.4 代码注释一律英文）；② 顶层 last_updated 时间倒流（08:00→07:35）已校正为真实提交时间。CodeRabbit 仅限流通知 0 条 actionable。门禁复跑：typecheck 0 / lint 0（123 files）/ 305 passed + 2 skipped（含 PR #29 评审修复 ce98826 新增 2 例）。 PR #29 预审（CodeRabbit 3 线程）：test_file 已补；fileRefs 边界语法门禁（path:line / path:start-end，粘贴代码 throw）落地；数组序=到达序语义定稿（详见详细设计 §1 实装记录）。 [2026-08-29 /pr-merge-agora 收尾] PR #29 已合并至 dev-1.0.0（merge commit 374ec886fac0d241cf6a2a71c18adc6a0b53b0ec），标 done。G5：纯域层任务无沙箱/Harness 执行链路对象，维持暂缺（3.1 先例）；合并前门禁实测 typecheck 0 错 / lint 0 违规 / 303 passed + 2 skipped。遗留：① Coordinator 角色切换生成接线延后至 3.3/3.5（解除条件：3.3/3.5 实装接线点 coordinator.ts 五处 nextRole 翻转）——3.3/3.5 均未实装，已由 /test-phase-agora 出口覆盖审查登记 DEF-012（docs/deferred-items.json，target_phase=4，解除条件挂靠 4.4）；② 2 skipped 为既有 G5 live-LLM 需 DEEPSEEK_API_KEY。本地分支 feat/handoff-packets 已删除。级联翻转：无新增（3.4 依赖 3.3 未 done、3.5 依赖 3.2+3.4 未全 done）。
<!-- END ARCHIVED NOTES -->
~~~~
