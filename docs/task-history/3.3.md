# 任务 3.3：投影完善：三条铁律 + project() 精确切片

本文件保存历史证据；当前状态、依赖和时间戳只以 [任务索引](../task-status.json) 为准。原文中的 ready/in_progress/done、待合并和延期项状态描述记录时点，不覆盖当前索引或延期台账。后续事件追加到文末，不改写原文区。

## 2026-09-13 原 notes 无损迁移

Source commit: `fa3efafed328bdfeb5b0f575b4e57bbd248a45fd`
Original notes SHA-256: `9a2c0aa14a0cfb205b7c7a5c6c886b0ec97c43cb2cb0803b4a30fc743f281cd5`

~~~~text
<!-- BEGIN ARCHIVED NOTES -->
三铁律：永不投原始群聊 log（只投结构化切片+所属 channel localContext）；代码传引用不传全文（fileRefs 路径+行号）；理由随决策走（rationale 防下游推翻上游）。
[2026-08-29 开工·用户确认] 状态置 in_progress。
[2026-08-29 计划批准·用户确认] 切片语义裁决=A：leaderDecisions 仅投影 authority='leader' 条目（切片名+详细设计 §2 PM 行「相关 leader 决策」字面语义），按角色相关性细化延至 Phase 6 channels（届时才有成员资格信息基础，R9 升级点）。改动面三处：① project.ts 切片实现+文件头注释（铁律 3 已实现；channels localContext/blockingObjections 为 Phase 6/8 R9 升级点）；② project.test.ts 占位测试改写为铁律 3 套件+drift-guard fixture 增补 decisionLedger；③ 详细设计 §7 实装记录（R12）。project() 签名零变更（R9）。
[2026-08-29 Code+Check·TDD 红→绿] RED 实测 2 failed/14 passed（占位 return [] 命中新断言）；实现后定向 16/16 绿。实现：leaderDecisions = state.decisionLedger.filter(authority==='leader').map(defensive copy)（WO 只读投影）。测试覆盖：leader 级带 rationale 进投影（哨兵）/supersedes 链可见/agent 级排除（哨兵负向）/空账本 []/视图条目非 State 条目引用（拷贝守卫）；铁律 1/2 既有守卫测试不动。
[门禁实测] typecheck 0 错（tsc --noEmit）；lint 0 违规（biome check .，123 files）；test 306 passed / 2 skipped（34 files，2 skip 为既有 G5 live-LLM 需 DEEPSEEK_API_KEY，无回归）。注：当前 shell 无 pnpm，三门禁以 node_modules/.bin 等价命令执行。G5：纯投影纯函数任务，无沙箱/Harness 执行链路对象，注明暂缺（3.1/3.2 先例）；投影消费链路（worker-runtime.loop→project()）由既有 e2e 全量回归覆盖。R1 未触 State 写入；R2 强化（铁律 3 落地）；R9 零签名变更；R10 不变。R12 详细设计 §7 实装记录已补。[2026-08-29 PR 已建] PR #30 https://github.com/logan-suu/Agora/pull/30（feat/projection-leader-decisions → dev-1.0.0，commit af52ba7）：铁律 3 落地 + 铁律 3 测试套件 + §7 实装记录 + 本 notes 更新。合并后 /pr-merge-agora 收尾标 done（级联翻转 3.4→ready）。
[2026-08-29 /pr-merge-agora 收尾] PR #30 已合并（merge commit 59211f1，base dev-1.0.0），本地分支已清理，dev-1.0.0 已同步。标 done，级联翻转 3.4→ready。G5：纯投影纯函数任务，无沙箱/Harness 执行链路对象，暂缺（合并后复跑全量 306 passed / 2 skipped 无回归，2 skip 为既有 G5 live-LLM 需 DEEPSEEK_API_KEY）。遗留项：channels localContext/blockingObjections 投影为 Phase 6/8 R9 升级点（见 notes 计划批准段）；leaderDecisions 按角色相关性细化延至 Phase 6。
<!-- END ARCHIVED NOTES -->
~~~~
