# 任务 1.4：DockerSandbox（dockerode）接入，替换 LocalTempSandbox

本文件保存历史证据；当前状态、依赖和时间戳只以 [任务索引](../task-status.json) 为准。原文中的 ready/in_progress/done、待合并和延期项状态描述记录时点，不覆盖当前索引或延期台账。后续事件追加到文末，不改写原文区。

## 2026-09-13 原 notes 无损迁移

Source commit: `fa3efafed328bdfeb5b0f575b4e57bbd248a45fd`
Original notes SHA-256: `9f99f097c22c39b58061bd69b79bb51ebca588a7c0b9a80da7a7e3a5258f5cdc`

~~~~text
<!-- BEGIN ARCHIVED NOTES -->
保留 SandboxManager 接口不变，新增 DockerSandbox 实现（一任务一容器、限网络与资源）；Phase 1 支持通过配置切换 LocalTemp/Docker（决策 D5 的阶段推进）。[2026-08-26 /pr-merge-agora] 级联翻转 pending→ready（依赖 0.4 已 done）；注意 §8.1.4 跨阶段阻断：Phase 0 出口集成测试 0.7 完成前不得开工。[2026-08-27 Code+Check] 实现完成，门禁部分通过。四源文件 + 三测试文件：① src/path-guard.ts 共享 assertInside（root+target 双 realpath 规范化，target 走最近存在祖先回退，封堵 symlink 逃逸 DEF-001；LocalTemp 与 Docker 共用，防语义漂移）；② src/docker-sandbox.ts DockerSandbox implements SandboxManager（一任务一容器：首次 createWorktree 惰性 createContainer，host 根 mkdtemp bind mount 至 /workspace，role 子目录隔离；NetworkMode 'none' 默认禁网可配 bridge + Memory 512MiB + CpuShares 512 + SecurityOpt no-new-privileges + Cmd sleep infinity 保活；read/write 走 host 侧共享文件，run 经 container.exec /bin/sh -c + WorkingDir 容器路径映射 + demuxStream 分流 + exec.inspect 取 ExitCode；超时默认 30s R7，dockerode 4.x Exec 无内置 kill → modem.dial 调 Docker API POST /exec/{id}/kill 强杀，兜底整容器 kill；integrate 显式 not-implemented（Git 归 tools/git 1.3，Phase 9 集成检查点实现）；teardown stop+remove(force) 容器 + host 根移入 trash 移动非删除；镜像缺失自动 pull（modem.followProgress）；默认镜像 node:20-slim）；③ src/factory.ts createSandbox(config) 配置切换工厂（默认 {kind:'local'} 保 Phase 0 行为 R9 向后兼容；{kind:'docker', image?/networkMode?/memoryBytes?/cpuShares?/docker?/baseDir?} 转发 DockerSandboxOptions；1.6 出口测试用 docker 模式验证双切换）；④ local-temp-sandbox.ts 重构为共享 path-guard + node:path dirname/basename（DEF-002 解决）。测试：docker-sandbox.test.ts 13 例 G5 真实 Docker（daemon 探测，unix://DOCKER_HOST//var/run/docker.sock/~/.docker/run/docker.sock 三候选，不可达自动 skip 注明原因对齐 DEEPSEEK_API_KEY skip 模式）；factory.test.ts 4 例纯构造切换；path-guard.test.ts 6 例真实 symlink 逃逸加固。[G5 实测·真实 Docker] Docker Desktop v29.2.1 就绪后复跑 docker-sandbox.test.ts 13/13 全绿（真实 node:20-slim 镜像、真实 createContainer/exec/stop/remove，无 mock）：容器创建/同 task 复用+角色隔离/读写往返/覆盖写/越界拒绝/真实 exec stdout/容器内 node --version 输出 v20.x/非零退出码/stderr/bind mount 共享可见（write 后 run 读到文件）/超时强杀 1s 超时 sleep 5（timedOut=true exitCode=null elapsed<5s）/integrate 抛错/teardown 幂等。[修复记录] 首轮 G5 实测 6 个 run 测试失败『no container record for worktree』——根因 recordFor 用 Object.values(record.roles) 遍历 Map，Object.values 对 Map 返回空数组致匹配恒失败；修复为 [...record.roles.values()]，复跑 13/13 全绿（修根因未弱化断言）。[2026-08-27 PR#17 /pr-review-agora 修复] 复跑门禁后 152 passed / 2 skipped（13 例 docker G5 真跑 + path-guard 8 例含 2 新例）。评审修复四处：① 超时机制废弃不存在的 POST /exec/{id}/kill（实测 404）——改为容器内 coreutils `timeout -s KILL` 包装命令（argv 数组免二次 shell 解析），超时仅杀 exec 进程组、容器存活可复用；新增回归测试『run enforces a timeout and kills only the exec, container stays usable』；② path-guard 补 dangling symlink 拒绝（walk-up 时 lstat 每个组件，存在的 symlink 若 realpath 失败即拒绝），堵死『目标不存在的 symlink 指向根外』写逃逸，新增 2 例回归（dangling final + dangling parent），DEF-001 notes 同步；③ ensureContainer 按 taskId 串行化创建（in-flight promise 缓存），消除并发 createWorktree 双容器竞态，新增并发回归测试；④ path-guard.test.ts afterEach 改 rmSync 清理（原 void root 泄漏临时目录）。文档同步：技术选型 §7.2 / 详细设计 §6 超时描述改 coreutils timeout 包装；deferred-items DEF-001 notes 补 dangling 覆盖。[门禁最终态] pnpm typecheck exit=0；pnpm lint exit=0（biome Checked 98 files）；pnpm test 152 passed 2 skipped（2 跳=既有 G5 live-LLM 无 DEEPSEEK_API_KEY；13 例 docker G5 已实跑通过不再跳过）。R12 已同步：技术选型 §7.2（DockerSandbox 落地参数+createSandbox 切换+path-guard 统一，打 [2026-08-27 架构决策更新]）；详细设计 §6（同标记）；deferred-items.json DEF-001/DEF-002 标 resolved（resolved_by=Sisyphus (task 1.4)，含测试覆盖说明），DEF-004 notes 追加 [2026-08-27] DockerSandbox 缓解已落地。[2026-08-27 /commit-agora] PR #17 已创建：https://github.com/logan-suu/Agora/pull/17（feat/docker-sandbox → dev-1.0.0，提交 aa62086，15 files）。门禁复跑实测：tsc --noEmit 0 错 / biome 98 files 0 违规 / vitest 152 passed 2 skipped（13 例 docker G5 真跑通过）。状态保持 in_progress，待人工合并后由 /pr-merge-agora 标 done。[2026-08-28 /pr-merge-agora] PR #17 已合并（merge commit 8f14a3038141ffb40f2ec54aa10d8419e34d06d0，2026-08-28T02:46:26Z），任务标 done。dev-1.0.0 已同步 fd59d68→8f14a30（fast-forward），本地 feat/docker-sandbox 已删除。G5 实测结论：docker-sandbox.test.ts 13/13 真实 Docker（node:20-slim 镜像 + Docker Desktop v29.2.1）实跑全绿，覆盖容器创建/角色隔离/读写往返/真实 exec stdout/超时强杀（coreutils timeout 包装）/integrate 显式抛错/teardown 幂等；factory.test.ts 4/4 构造切换；path-guard.test.ts 8/8 含 2 新例（dangling symlink 拒绝）；全量 vitest 152 passed 2 skipped（2 跳=既有 G5 live-LLM 无 DEEPSEEK_API_KEY）。R9 接口签名保持不变（SandboxManager 不变，新 DockerSandbox + createSandbox 工厂）。R12 已在 PR 内同步：技术选型 §7.2 + 详细设计 §6（[2026-08-27 架构决策更新]）；deferred-items DEF-001/DEF-002 resolved（resolved_by=Sisyphus task 1.4），DEF-004 notes 增 [2026-08-27] DockerSandbox 缓解说明。无新增延期项。级联翻转：1.6 仍 pending（依赖 1.5 ready 未 done）。
<!-- END ARCHIVED NOTES -->
~~~~
