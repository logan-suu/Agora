# 任务 6.1：项目级 Channel 注册表 + participants/角色 inbox 路由

本文件保存历史证据；当前状态、依赖和时间戳只以 [任务索引](../task-status.json) 为准。原文中的 ready/in_progress/done、待合并和延期项状态描述记录时点，不覆盖当前索引或延期台账。后续事件追加到文末，不改写原文区。

## 2026-09-13 原 notes 无损迁移

Source commit: `fa3efafed328bdfeb5b0f575b4e57bbd248a45fd`
Original notes SHA-256: `18fb24c419c1ec81edb5bfff098cd2c08a9e455345ac988433813cdfb766a2ee`

~~~~text
<!-- BEGIN ARCHIVED NOTES -->
D7：Project.channels 是 Channel 注册表唯一真相源；项目只有一个 main，sub 必须绑定 taskId，localContext 用 {taskId,msgId} 引用，'leader' ∈ participants 恒成立。6.1 实现 comm/channels 的 Channel schema/不变量、ProjectChannelStore（.data/projects/{projectId}/channels.json；幂等 initialize + revision 校验 + 按项目串行 + 原子替换）、提交前 Channel/task/participant/closed 策略与 inboxFor(projectSnapshot,task,role) 纯读模型。inbox 从持久 Channel 快照 + AppState.messages 重建，保留消息事实顺序；to 只标 direct 优先级。MessageCommitted 可唤醒 worker，但 MessageBus 不保存 inbox。不得在 AppState 复制 channels，不在 6.1 修改 project(state,role,roster) 调用面；localContext 投影属 6.3。
[2026-09-01 /agora-pr-merge] Phase 5 出口任务 5.6 已由 PR #46 合并并标 done（merge commit 096307c）；跨阶段门禁解除，级联翻转 pending→ready。
[2026-09-01 /agora-init-session·规格评审] Leader 确认先评审而非盲从旧规格；已按蓝图→详细设计→架构/选型→开发计划→task-status/DEF-015 同步上述 D7 恢复与路由边界。任务保持 ready，未开工。
[2026-09-01 /agora-do-task] Leader 确认开始执行 6.1；依赖 5.6=done、Phase 5=done、current_phase=6，跨阶段门禁满足。任务置 in_progress，按已批准的 TDD 计划实施。
[2026-09-01 Code+Check+Verify] 按四个 TDD 单元完成：① core/domain Channel/MessageRef schema 与 main/sub/leader/roster/task/localContext 不变量；② ProjectChannelStore 端口与 JsonProjectChannelStore（项目串行、revision CAS、相同内容不增版、临时文件原子替换、损坏 JSON/路径逃逸拒绝、重启恢复和防御性拷贝）；③ DerivedChannelInbox 按当前任务+participants 派生，保持 AppState.messages 事实顺序，to 只标 direct；④ MessageService 提交前加载注册表校验 unknown/cross-task/non-participant/closed，Web 组合根幂等 main 初始化并支持已注册 sub 的 HTTP/SSE 动态寻址。负面测试证明失败不写 State、不 publish/stream；未修改 MessageBus、AppState 或 project(state,role,roster)，6.2/6.3 边界保持。DEF-015 的 Channel 完整性与 worker participant 隔离条件已满足并标 resolved，完整账号认证仍非目标。最终门禁：typecheck 0 错；Biome 184 files 0 违规；Next production build 成功；全量测试 57 files passed / 2 skipped、516 passed / 2 skipped；真实 DockerSandbox 15/15、Phase 5 累计 G5 4/4。任务保持 in_progress，待交付提交/PR；Agent 不自动合并。
[2026-09-01 /agora-commit·交付门禁] 分支 feat/project-channel-registry（base=dev-1.0.0）。typecheck exit=0；Biome 184 files 0 违规；任务专属真实 JSON/HTTP/SSE 链 7 files / 42 tests 全绿；Next production build 成功；全量 57 files passed / 2 skipped、516 passed / 2 skipped，真实 DockerSandbox 15/15、Phase 5 累计 G5 4/4。git diff --check、tracking JSON 与高置信敏感数据扫描均通过；无 secrets/.env/token。任务保持 in_progress，禁止 Agent 自动合并。
[2026-09-01 /agora-commit] 功能提交 b3a3d11 已推送；PR #47：https://github.com/logan-suu/Agora/pull/47（feat/project-channel-registry → dev-1.0.0）。PR 保持 open，任务保持 in_progress，待人工审阅合并后由 $agora-pr-merge 收尾。
[2026-09-01 /agora-pr-review·规格复评与修复] Leader 批准先校准规格再修 PR：D7 明确旧 task State 缺失 channels.json 时惰性补建 main、损坏快照 fail-fast、enabled roster 去重并排除 leader、对象键序不触发 revision；D9 收紧为单主消息且 planned mutations 禁止写 messages。同步蓝图→详细设计→架构/选型→开发计划→standing_decisions；DEF-004 与 DEF-007 保持 open，DEF-015 仅按 Channel 完整性范围由 6.1 resolved，完整账号认证仍为受信任单用户部署边界下的明确非目标。TDD 修复覆盖 HTTP/SSE 与生命周期恢复、损坏快照保留、附加消息拒绝无 State/bus 副作用、roster 不变量、canonical no-op 及端口完整签名。复核门禁：typecheck 0 错；Biome 184 files 0 违规；定向 6 files/44 tests；Next production build 成功；全量 57 files passed/2 skipped、522 passed/2 skipped；真实 DockerSandbox 15/15、Phase 5 G5 链通过。PR 保持 open，待修复提交推送与二次审查；Agent 不自动合并。
[2026-09-01 /agora-pr-review·交付] 规格复评与修复提交 69d369c 已推送至 PR #47。7 个 CodeRabbit inline 会话均已英文逐条回复并 resolved：5 项代码/台账/Markdown问题修复，DEF-015 按批准后的 Channel 完整性范围解决，账号认证建议因与 D8 受信任单用户边界及明确非目标冲突而说明后不实施。canonical equality、完整端口签名和文档元数据等非 inline 建议已在 PR 总结评论留证。PR 保持 open，任务保持 in_progress，禁止 Agent 自动合并。
[2026-09-01 /agora-pr-merge 收尾] PR #47 已由人工合并至 dev-1.0.0（merge commit 76573277546daf0779737ec3925d5f906f7218d6）；任务标 done。G1–G7 证据复核：修订后 D7/D9 与蓝图、详细设计、架构、选型、开发计划和 standing_decisions 一致；typecheck 0 错；Biome 184 files 0 违规；定向 44/44；Next production build 成功；全量 522 passed / 2 skipped（既有条件用例）；真实 DockerSandbox 15/15，Phase 5 Harness/MCP 累计 G5 链通过；git diff、tracking JSON 与敏感数据检查通过。DEF-004/DEF-007 保持 open，DEF-015 按 Channel 完整性范围 resolved，完整账号认证仍非目标。Git 确认功能分支完整合入后，本地 feat/project-channel-registry 已删除。级联翻转：6.2 pending→ready；Phase 6 保持 in_progress，current_phase 保持 6。
<!-- END ARCHIVED NOTES -->
~~~~
