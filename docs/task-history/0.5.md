# 任务 0.5：HarnessExecutor 薄执行器 + agent/pre-step 投影覆写

本文件保存历史证据；当前状态、依赖和时间戳只以 [任务索引](../task-status.json) 为准。原文中的 ready/in_progress/done、待合并和延期项状态描述记录时点，不覆盖当前索引或延期台账。后续事件追加到文末，不改写原文区。

## 2026-09-13 原 notes 无损迁移

Source commit: `fa3efafed328bdfeb5b0f575b4e57bbd248a45fd`
Original notes SHA-256: `6878ab4827662c100b39452aae6251f18f15e2eadc8d513183603c16cf0bdaa7`

~~~~text
<!-- BEGIN ARCHIVED NOTES -->
runtime/executor/base.ts：Executor 接口 = step/saveSafePoint/loadSafePoint/injectInbox 四方法（loadSafePoint 不可省，决策 D4 的 Fork 恢复依赖它）。[已废弃·2026-08-26 实测校准] runtime/executor/harness-executor.ts：复用 ctx.subagents.start（inheritsParentContext=false、toolFilter/persona 映射 CODER/TESTER RoleSpec、outputSchema→mutations、depthLimit）；自行驱动 runStep（非 one-shot）。（真实 dsh API 无 runStep；执行器改经 ctx.agents.create + followup→whenIdle，见下方校准记录）agent/pre-step 钩子中【直接覆写】messages 数组为 project(state,role) 返回值（决策 D1；deriveMessages 仅事件流记录；compaction 压缩伪历史属预期行为）。agent/request 固定模型路由。先不接 turn-stopping 抢占。风险预案：pre-step 覆写与 compaction 定序冲突时约定『先 compaction 后 injectProjection』。
[2026-08-26] 开工。已完成 Harness API 实测（dsh@0.1.1-rc.2）：D1 pre-step 覆写地基成立（detail drift 非 capability gap）；真实模型为 event-driven（无 runStep），执行器以事件驱动插件方式实现。
[2026-08-26] 实现完成。HarnessExecutor + 最小占位 project() 已落地，单元测试 5 条全绿（FakeLlmAdapter，R11 注明原因）；真实链路 G5 留待 0.6（需 DEEPSEEK_API_KEY）。
关键实测校准（detail drift 非 capability gap）：dsh@0.1.1-rc.2 真实 API 为事件驱动自驱 loop，无 runStep；D1 的 agent/pre-step 覆写地基成立。执行器以 ctx.agents.create() + followup→whenIdle 实现，一次 worker 调用=一个 turn（turn 边界近似阶段 0 安全点）。dsh-base 不可代码加载，需程序内组合 8 插件集。模型路由：RoleSpec.model ?? env.AGORA_MODEL ?? deepseek-chat；provider 可注入 fake adapter。
R12 已同步：详细设计方案 §0/§3/§6/§9 + 技术选型 §4.1 均打 [2026-08-26 实测校准] 标记。
门禁：typecheck 0 错 / lint 0 违规 / test 62 全绿（含 5 新增）。

[2026-08-26] G5 执行链路实测通过（真实 DeepSeek provider，deepseek-v4-flash，约 3.9s 一次真实 turn）：
  ① State 传递 ✓ 真实 AppState 构造投影视图送达；② Harness 接入 ✓ 真实 agent loop 跑通完整 turn（ctx.agents.create + followup→whenIdle）；③ 投影覆写 ✓ 模型回复内容印证其看到的是结构化投影视图（'an empty structure'=Phase 0 空切片），非原始群聊 log——D1 生效。
测试 g5-real-chain.test.ts 在无 DEEPSEEK_API_KEY 时自动 skip（CI 保绿）；key 仅在 env 传递，未落盘（R13/G7）。
门禁最终态：typecheck 0 错 / lint 0 违规 / test 63 全绿（含 5 单元 + 1 真实链路）。
[2026-08-26 /commit-agora] PR #9 已创建：https://github.com/logan-suu/Agora/pull/9（feat/harness-executor-projection → dev-1.0.0，提交 c9e24b2，10 files）。门禁复跑实测：tsc --noEmit 0 错、biome 72 文件 0 违规、vitest 10 files / 62 passed + 1 G5 skipped（本 shell 无 DEEPSEEK_API_KEY，G5 已于 Code 阶段实测）。状态保持 in_progress，待人工合并后由 /pr-merge-agora 标 done。
[2026-08-26 /pr-merge-agora] PR #9 已合并（merge commit 6e12587c78c1e252d676c14d7dd5990511417dda），任务标 done；G5 实测结论：真实 DeepSeek provider 跑通完整 turn，State 传递 ✓ / Harness 接入 ✓ / 投影覆写 ✓（'an empty structure'=Phase 0 空切片印证 D1 生效），g5-real-chain.test.ts 无 key 时自动 skip（CI 保绿）；遗留/延期项：无（turn 边界近似阶段 0 安全点，turn-stopping 抢占留 Phase 9）。本地分支 feat/harness-executor-projection 已删除。级联翻转：0.6 → ready（0.3/0.4/0.5 全 done）。

<!-- END ARCHIVED NOTES -->
~~~~
