# 11.4 交付受管 Node Git 包管理器与原生辅助程序

## 2026-09-14 开工与 Explore

- 用户在初始化报告明确下一任务为11.4后回复“开始”，授权启动该任务；尚未确认具体实现计划。
- 任务类型：code。11.2、11.3及前阶段出口10.7均为done，当前阶段11；无pending任务符合级联条件。任务索引校验通过。
- 从干净dev-1.0.0创建功能分支`feat/task-11-4-managed-toolchains`；不包含commit/push/发布授权。
- 规格已读取：蓝图§21 D18及§22.3.2、详细设计§12.3、选型§12/§14、开发计划§18.2/§18.3/§18.11、任务追踪维护规范。
- 当前实现：`build.mjs`及`package.mjs`固定arm64；包内有Node/npm和Keychain helper，缺少Git、pnpm及secure-files helper的完整交付。`storage.ts`支持格式1初始化，对非空upgrade目录统一拒绝，尚无持久升级事务。预览服务已有能力门禁，继续复用。
- 当前宿主只读检查：arm64、macOS26.5、Node v24.20.0、pnpm9.15.9；`security find-identity -v -p codesigning`返回`0 valid identities found`。没有据此验证Intel或macOS15，也未运行签名、公证、项目安装或模型测试。
- 未发现需重新裁决的规格冲突。正式Developer ID签名/公证及签名升级钥匙串连续性需要有效构建身份；Intel/最低系统安装验收需要相应环境，不能以交叉构建或ad-hoc签名代替。

## 待确认实施计划

1. **工具链清单与打包**：在`apps/desktop/src/`新增受管工具链清单/校验模块，扩展`apps/desktop/scripts/build.mjs`、`package.mjs`。按选型锁定Electron44.3.0、Node24.20.0、Git2.53.0/dugite-native v2.53.0-4、pnpm9.15.9、npm11.19.0；纳入两个原生helper及第三方声明。arm64/x64使用独立受信输入，校验来源、版本、架构及hash，不以arm64构建宣称x64运行通过。
2. **准备与环境检查**：新增工具链准备模块，接入`service.ts`和预览状态输出。检查受管组件、项目版本声明/lockfile冲突、允许的固定版本和安全PATH；Git设置自身exec/template路径。下载先入cache staging，校验后原子发布，取消/失败仅回收本次未发布内容。普通项目install/命令执行入口保持关闭；真实依赖获取只在批准的临时测试项目中验证，不向项目进程传递模型凭据。
3. **升级事务与配置保留**：扩展`storage.ts`并新增升级事务模块，在唯一写者锁内实现prepared备份、逐文件flush/rename、持久journal、hash校验、恢复/回滚及最后格式提交；未闭合前服务数据入口关闭。保留稳定state/workspace/凭据身份和不可变证据，未知转换拒绝。生产初始格式仍为1，转换机制用专用测试格式验证，不提前发明Phase14/15领域迁移。
4. **签名与安装介质**：扩展`sign.mjs`，新增介质构建入口；逐层签名、按组件限制entitlement、校验bundle，生成每架构DMG/ZIP、checksum及物料/证据清单。正式签名与ad-hoc开发验证显式区分；公证与staple仅在有效身份/权限具备时执行，不改变Gatekeeper或quarantine。
5. **测试与证据**：新增`apps/desktop/test/`针对版本冲突、hash/架构/路径错误、取消、资源缺失与升级每个中断点的有意义测试；新增`tests/integration/phase11/phase11-4.test.ts`并纳入`test:desktop`。按TDD小步推进，以真实临时目录/子进程/受管二进制和隔离安装包验证依赖准备、环境隔离、配置保留、失败恢复；执行typecheck/lint/完整pnpm test及适用G5。通用真实模型回归沿用OpenCode Go的deepseek-v4-flash，不静默换模型或跳过。正式Benchmark不启动。
6. **文档同步与收尾**：同步详细设计§12.3实现/验证入口、选型实际交付、开发计划与任务短摘要；完整结果追加本历史并链接专项报告。缺少签名/目标机器的验证如实保留未完成，代码任务保持in_progress至人工合并。

## 当前检查点

已完成开工授权核对、依赖/规格与现有代码Explore及具体计划；下一步为Leader确认上述实现计划。尚未修改产品代码、运行测试、调用模型或构建安装包。

## 2026-09-14 计划确认与实现

Leader回复“确认”，批准前述具体计划及临时项目真实验证。实施范围沿用该授权，未commit/push/发布。

- 完成工具链版本选择/受管环境、完整性清单/组件故障提示、受信下载cache、arm64/x64本架构构建入口、Git/pnpm/npm与两个helper入包、签名及DMG/ZIP、升级事务基础、预览工具状态、单元与Phase11集成测试。现有公开领域端口未变，普通项目执行入口继续关闭。生产格式仍为1；测试转换不伪造未来领域迁移。
- 采用独立私有npm用户/全局配置路径。真实npm首次验证拒绝双重加载同一`/dev/null`，先补失败回归再修复；包内npm11.19.0最小复验通过。此前失败没有改记通过。
- 版本范围测试补足部分比较器（如`>24`不得接受24.20.0）；不支持语法明确报错，不静默忽略项目声明。
- 升级覆盖准备/备份完成/逐文件替换/格式提交断点、继续与回滚、坏备份、源文件异值和失效owner。新增`preparing`后，旧测试的无条件中断回调过早停在新阶段；修正为明确`prepared`，保留原`upgrade_state_changed`断言。首轮完整回归因此1失败，其余1326项通过；修正后第二轮185文件1327项全绿。
- 真实工具验证的第二次失败为验证器将`/var`与`/private/var`当不同路径。检查实际marker证明npm依赖和脚本成功，改以realpath后的包内Node绝对路径严格比较；最终15项工具验证通过，非放宽为任意Node。
- 原生Electron复验一次在成功stop后显示状态断言失败，同时出现导航拒绝；再次相同诊断复验通过。代码审查确认当前ready事件的异步loadURL失败会在正常drain后误写`window_failed`。新增真实子进程回归后，只允许对应当前且ready的lifecycle报告导航失败，并处理故障页Promise；后续包与完整回归独立重验。

### 环境限制与未执行项

- 受限工具沙箱的本机HTTP监听报EPERM、干净依赖安装DNS报ENOTFOUND；停止该次受限构建后，经工具权限审查运行相同回归/构建，未改断言、排除文件或删除凭据。
- 当前宿主arm64/macOS26.5，无有效codesigning身份。正式Developer ID签名/公证、签名升级钥匙串连续性、Intel真机和macOS15干净安装尚未完成；已向Leader询问可用环境。x64构建代码/固定输入不等于x64产物或运行验收。
- 开发包为`development-working-tree`，保留输入hash；没有提交授权，不称固定提交发布候选。ad-hoc完整性通过不等于Gatekeeper或公证通过；未关闭Gatekeeper、删除quarantine或上传Apple/发布GitHub。
- 既有Keychain验证用独立临时钥匙串，退出时删除本次测试项；不读取用户模型密钥正文。通用模型测试固定`opencode-go/deepseek-v4-flash`，正式Benchmark未运行。

最终门禁、安装产物、日志与hash见后续验收记录及专项报告。

## 2026-09-14 最终本机验证与未完成门禁

- G3：`pnpm typecheck`及`pnpm lint`通过，0错误/违规；`git diff --check`通过。
- G4：导航/停服修复后的最终`pnpm test`退出0，Node7/7，Vitest185文件1328/1328，0失败/跳过，264.19秒。原Harness、频道摘要、LRU三个真实Go `deepseek-v4-flash`用例分别9.159、15.184、262.930秒通过；默认并发、原断言/期限保留。未运行正式Benchmark。
- G5本机：最终工作树开发包工具检查15/15，正式组合根12/12；ZIP解压及DMG只读挂载复制后的签名/工具清单通过，测试卷已卸载；从DMG复制出的应用再次12/12，两个最终原生run均state=stopped、exitCode=0、临时Keychain已清理。工具含真实npm/pnpm依赖安装及生命周期/测试、Git linked worktree与回收、签名helper读写及越界拒绝。
- 下载器：真实锁定pnpm HTTPS下载成功并原子发布；错误hash实测拒绝且没有未验证产物/staging残留。取消与已有版本保留另由单元回归覆盖。
- 本地开发包：`/private/var/folders/_r/6dh_2jf542d4p97jl9nqwk480000gn/T/agora114-build-XlVNqk/output/app/Agora-darwin-arm64/Agora.app`。介质：`/private/tmp/agora114-media-verified/Agora-darwin-arm64.dmg`及同目录ZIP；完整checksum保存于仓库证据。安装复制验证位于`/private/tmp/agora114-install-d492Jp/installed/Agora.app`。
- G1/G6：同步蓝图、详细设计§12.3.9、选型§14、开发计划和任务索引；规格/实现/测试映射及可重现入口见[专项报告](../reviews/task114-managed-toolchains.md)，机器可读结果、构建输入、下载、介质、截图和日志hash在`docs/reviews/task114-evidence/`。索引校验通过。
- G7范围：开发构建隔离输入并审计禁入资源/外部链接；项目执行仅在授权临时fixture，凭据环境不传递；未输出用户密钥正文，未改用户项目/全局工具，不关闭Gatekeeper或删除quarantine。原生验证使用隔离真实Keychain并清理。
- 未完成：Developer ID签名/公证、正式签名升级Keychain连续性、Intel及macOS15干净安装没有当前可用环境；首次无前一已发布桌面版本的实际覆盖升级不适用，但事务/未知格式/配置保留测试已执行。上述限制不被ad-hoc、交叉编译或PATH隔离代替。G1–G7的完整目标平台验收不宣布全部完成。
- 交付状态：11.4保持in_progress，11.5及后续阶段未启动；无commit/push/PR/tag/Release。下一步补齐环境与正式验收证据，再按用户交付指令执行提交门禁及人工PR流程。

## 2026-09-14 Leader收敛Apple Silicon范围

- Leader原话：“只需要Apple Silicon就好了，其他不管，为什么需要Apple 签名资格”。当前仅支持Apple Silicon（arm64），撤销Intel/x64构建、发布与验收要求；历史未测事实保留，不改写为通过。
- 按agora-sync-docs同步蓝图、详细设计、系统架构、技术选型、开发计划、报告及任务索引。既有x64实现参数保留，不构成当前支持承诺。本轮仅变更文档，无运行时代码改动。
- macOS15最低系统与干净验收不由处理器范围决定自动取消。签名问题为必要性询问，未视为取消正式分发要求或选择受限预览；无购买会员、公证上传或系统保护调整。
- 当前保持in_progress，未commit/push；本轮运行索引校验和diff检查，既有运行测试证据保持原时点。

## 2026-09-14 Leader确认GitHub DMG未公证分发

- Leader原话：“确定这个方案：以在github中放dmg的形式发布”。承接此前已说明的ad-hoc签名、未公证并附安装说明方案；仅Apple Silicon，公开交付DMG及SHA-256校验和，英文Release说明。ZIP仍可用于内部验证。
- 按agora-sync-docs同步蓝图D18/§22、详细设计§12.3、架构、选型、开发计划§18.11及本任务报告；取消当前Developer ID/公证/staple前置，不列为必须未来补齐的延期承诺。
- 保留嵌套签名完整性、真实下载隔离/手动允许/首次安装、最低与当前干净系统、不同ad-hoc构建之间Keychain访问和拒绝授权恢复、配置/证据保留及适用G1–G7。现有开发工作树产物不自动升级为固定提交候选。
- 本轮是文档规则同步，不修改代码，不执行公证或GitHub发布；无commit/push/tag。任务仍in_progress，具体发布按阶段出口和人工合并流程执行。

## 2026-09-14 agora-commit门禁暂停

- Leader显式调用agora-commit，授权功能分支提交、推送和创建英文PR；未授权自动合并或发布版本。当前分支feat/task-11-4-managed-toolchains，基线8fb1f90；fetch后origin/dev-1.0.0与HEAD一致，无已有同分支PR。
- G3：本轮pnpm typecheck和pnpm lint通过，Biome检查485文件。G5：对既有DMG复制安装包复跑工具15/15及原生组合根12/12，临时Keychain已清理；347个应用/包/构建输入hash与当前源码一致。G7：50个待交付文件经gitleaks脱敏扫描无泄漏，未stage/commit/push。
- G4首轮默认pnpm test：185文件，1325通过/3失败，203.26秒。task-composition恢复清理与Phase8 durable-restart-fork超过5000ms，Phase9 review-rework超过60000ms；Phase8超时后另有ENOTEMPTY清理错误。停止交付，按资源争用、生命周期等待/清理竞争、本次回归三个假设排查。
- 原断言/时限单独复现受影响3文件：28/28通过，127.94秒；恢复清理576ms、Phase8全文件726ms、Phase9 review-rework18967ms。相关测试及直接实现与HEAD相同。第二轮不并行桌面G5的默认pnpm test仍1325通过/3失败，228.65秒；失败改为Phase9 architecture_failure 60000ms、Docker teardown 5000ms、Git恢复回收5000ms。实际宿主8核，观察到1分钟load average 110.97。时序/资源争用有证据支持，但根因仍inconclusive，不宣称代码缺陷已修复。
- 本轮只将测试文件调度改为pnpm run test --maxWorkers=2；保留全部用例、各测试内部产品并发、原断言/时限和Go deepseek-v4-flash，未改仓库测试配置。此前pnpm test --maxWorkers=2被pnpm拒绝参数，该次未启动测试。两文件并发仍出现Git专用集成工作树冲突中止用例超过5000ms，故停止新增复跑与交付；等待已启动全量运行自然结束，不硬停模型流。最终结果记录docs/reviews/task114-evidence/commit-gates.json。
- 按skill原文“Stop on any failure.”，当前没有commit/push/PR。准备的PR正文仅在本机临时文件，未对外发布；任务保持in_progress，不因平台/签名范围调整豁免G4，也不把既有通过证据冒充当前门禁通过。

### 提交门禁最终结果

两文件并发全量结束：Node7/7；Vitest185文件中184通过/1失败，1327/1328项通过、无跳过，406.57秒。唯一失败为packages/tools/git/test/git-service.test.ts:450的真实冲突合并/abort用例，原5000ms期限内未完成（6316ms）。三项真实Go deepseek-v4-flash均通过：Harness15293ms、频道摘要6709ms、LRU313721ms。G4仍失败，根因inconclusive；无断言/期限/提供方/测试配置变更。完整日志hash与实际结果已写入commit-gates.json；未stage/commit/push/创建PR，交付授权保留待门禁恢复后继续。

## 2026-09-14 审查与测试资源隔离修复

- Leader要求“进行审查并修复”，继续既有范围内修复及交付授权。按Git内部阻塞、重复子进程开销、共享宿主调度竞争三个假设检查；生产Git服务及失败测试与基线一致，保留规范根身份、专用linked worktree和冲突abort/MERGE_HEAD复核。
- 原失败冲突用例单独诊断811ms通过。完整Git文件29/29通过（13.50秒），冲突用例885ms、恢复回收1007ms；通过仅添加GIT_TRACE2_EVENT并exec真实/usr/bin/git的临时包装器收集完整追踪，没有替换Git实现或模拟结果。422次顶层Git调用累计1.477809秒、最慢0.015936秒，未复现Git命令阻塞。单用例筛选仅作诊断，不充当G4。
- 确认测试配置没有隔离同一宿主的多个真实Docker/Harness/Git文件。结合此前8核宿主load110.97和失败位置漂移，根配置增加fileParallelism:false，以串行文件调度消除本轮测试文件之间的资源竞争。保留全部185文件、所有原断言/期限、内部真实worker并发与Go deepseek-v4-flash；不修改生产Git逻辑、simple-git依赖、系统保护或用户其他进程。历史每次超时的精确阻塞位置仍未复现，不宣称已证明Git逻辑bug。
- 详细设计§0与技术选型§11.1同步测试运行方式。修复后pnpm typecheck与pnpm lint通过；默认pnpm test完整回归进行中，最终结果追加于后，不预先宣布恢复交付。

### 修复后完整门禁

- 默认pnpm test退出0：Node7/7，Vitest185文件1328/1328通过，0失败/跳过，783.85秒。Git文件29/29，原冲突合并/abort用例824ms；Docker16/16，teardown2144ms；Phase9并行流程11/11，原architecture_failure12607ms、审阅返工18733ms；task-composition全文件460ms、Phase8场景全文件167ms。三项真实Go deepseek-v4-flash均通过：LRU376537ms、Harness13762ms、频道摘要13528ms。保留全部原期限和断言，未运行正式Benchmark。
- G5再次从DMG复制安装包实测：工具15/15，原生Electron12/12，afterStop=stopped/exitCode0、独立临时Keychain已清理。原生日志包含故障注入及关闭时的导航错误，机器结果的故障反馈和正常退出断言均通过。完整构建清单350个输入hash全部匹配，测试配置与文档修改不影响已测应用包。
- G3 typecheck/lint通过；G7对52个待交付文件扫描无泄漏。日志hash、Git追踪摘要和最终实测结果追加commit-gates.json，先前失败记录保留。共享宿主文件调度修复获得本轮全量通过证据；不将历史单次阻塞位置的未复现改写为已证实的生产Git缺陷。
- 继续已授权的功能分支提交、推送和英文草稿PR，base=dev-1.0.0；任务保持in_progress。真实quarantine下载及安装、Apple Silicon干净支持系统、不同ad-hoc构建间Keychain访问连续性仍待补验；不宣称阶段出口通过、不自动合并或发布。

### 功能提交与草稿PR交付

- 已按agora-commit提交`db31a6e`（Bundle managed desktop tools and recoverable upgrades），精确包含52个已审查文件，并推送`feat/task-11-4-managed-toolchains`。
- 已创建英文草稿[PR #82](https://github.com/logan-suu/Agora/pull/82)，base为`dev-1.0.0`，正文列明完整测试结果、Git诊断边界、未公证DMG分发规则及未完成安装/升级验收。没有自动合并、tag或Release。
- 最终文档及索引校验、lint和52文件敏感扫描通过。此次交付元数据补记只修改历史/索引/证据，不改变已通过测试和G5的代码；复用本节对应全量验证。11.4保持in_progress，11.5及后续阶段未开工。
