# 任务 2.4：角色投影细化（按详细设计 §7 切片表）

本文件保存历史证据；当前状态、依赖和时间戳只以 [任务索引](../task-status.json) 为准。原文中的 ready/in_progress/done、待合并和延期项状态描述记录时点，不覆盖当前索引或延期台账。后续事件追加到文末，不改写原文区。

## 2026-09-13 原 notes 无损迁移

Source commit: `fa3efafed328bdfeb5b0f575b4e57bbd248a45fd`
Original notes SHA-256: `15967a318269fb416b0212f36e8238609d6655c2487a31ac875c66852c8f19c4`

~~~~text
<!-- BEGIN ARCHIVED NOTES -->
CODER=分配 subtask+architecture+conventions+当前失败测试+相关 fileRefs；TESTER=acceptance+当前分支/patch+接口约定；REVIEWER=pendingDiff+conventions+architecture；PM=goal+requirements+leader 决策；ARCHITECT=requirements+repo 结构+conventions。明确不含列同样重要。
[2026-08-28 开工·用户确认] 状态置 in_progress；同步修正 phases[2].status not_started→in_progress（2.1-2.3 已 done 的漂移）。
[2026-08-28 计划批准·用户确认] 三项裁决：① conventions 走独立 State 字段+set 解锁（§1 定稿位置，0.2 预铺路径签名不变）；② pendingPatch 只读不解锁 writer（显式 null 缺省，2.5 e2e 评估 CODER 提交补丁流时再解锁）；③ 未知切片名 throw（§12 禁止静默降级，drift-guard 测试锁定）。
[2026-08-28 Code+Check+Verify·TDD 红→绿] 先写测试全红（14 failed：conventions set 未启用+切片仍占位+未知切片未抛错），实现后全绿。① 域层解锁：state.ts AppState+=conventions?；reducer.ts SET_FIELDS/ENABLED_SET_FIELDS+=conventions + applySet case（守卫对齐 architecture：'conventions must be a non-array object'）。② project.ts phase0Stub→sliceOf 按 §7 切片表落地六角色 14 切片：global.summary（+complexity:null/workers:[]/testSummary，Phase 4/9 显式空）、goal、requirements（逐项浅拷贝）、leaderDecisions=[]（3.1 升级点）、repoStructure={}（Phase 1）、conventions、assignedSubtask（保持现状——ownerRole 过滤即「不含其他 worker 的 subtask」负向保证）、architecture、failingTests（failures 拷贝）、fileRefs（testResults.failures 按 file 去重 {file,lines[]}——铁律 2 只路径+行号不传全文）、acceptance（{requirements:[{id,acceptance}]}，移除旧 stub 混入的 goal/phase/testResults——TESTER 明确不含实现内幕）、branchOrPatch（{worktrees:非 done subtask 的 worktree 路径, patch:pendingPatch??null}）、interfaceContracts（architecture.interfaces 类型收窄透传，原始值拒绝）、pendingPatch；default 分支 present-but-empty {} 改为 throw。③ 负向测试（「明确不含列同样重要」）：铁律 1 六角色 sentinel——PM payload/display 与 channelId/fromRole/msgId 键全不出现在任何角色视图 JSON；铁律 2 fileRefs 不含 failure message；PM 无 testResults、CODER 无他人 subtask、TESTER 无 goal 回显。
[兼容性实测] phase0/phase1-exit chain4（channelId/fromRole 负向断言+CODER worktree 断言）、harness-executor（'write LRU'/role 键）、tool-governance/g5-real-chain、coordinator.test projection:[] 全部通过。
[门禁实测] pnpm typecheck exit=0；pnpm lint exit=0（biome --write --unsafe 修 project.test useLiteralKeys 后 0 诊断）；pnpm test 246 passed/2 skipped（基线 229+17 新增全绿；2 skip 为既有 G5 live-LLM 需 DEEPSEEK_API_KEY）。
[R12] 详细设计 §1 补 [2026-08-28 实装记录]（conventions set 缺口已修+pendingPatch 缺口待 writer）；GitHub Issue #25 记录。
[依赖] runtime-executor devDependencies+=@agora/roles-definitions workspace:*（project.test 用 DEFAULT_ROSTER 做六角色切片漂移守卫）。
[待办] pendingPatch writer 转 DEF-010 台账跟踪（target_phase=2.5 评估，解除条件见 docs/deferred-items.json）；Issue #25 已处置关闭——conventions 半边由本任务修复，pendingPatch 半边显式延期。
[2026-08-28 PR] PR #26 https://github.com/logan-suu/Agora/pull/26 已创建（base=dev-1.0.0），保持 in_progress 待人工合并；实现摘要：域层解锁 conventions set + sliceOf 落地六角色 14 切片 + 未知切片 throw + 三铁律负向测试。
[2026-08-28 PR 合并收尾] PR #26 已人工合并至 dev-1.0.0（merge commit: 0b579181bc30fefcae929a7b7155fb70fb9c2c2a），状态置 done。G5 实测结论：phase0/phase1-exit chain4 负向断言、harness-executor 覆写链、tool-governance/g5-real-chain 全部通过；门禁 typecheck/lint exit=0，246 passed/2 skipped（既有 G5 live-LLM 需 API key）。遗留：pendingPatch writer 转 DEF-010（target_phase=2.5 评估，解除条件见 docs/deferred-items.json），不阻断本任务出口。
<!-- END ARCHIVED NOTES -->
~~~~
