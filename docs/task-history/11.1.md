# 11.1 定稿后续基础身份与跨批次契约

## 2026-09-14 开工与范围

- 初始化后 Leader 回复“开始”，授权开展 11.1 的基础身份、所有权、生命周期、阶段适配及冻结接口影响设计。任务类型为 design；本次不实现产品代码，不提交或推送。
- 开工前复核：current_phase=11，11.1=ready；直接依赖及前阶段出口 10.7=done；没有可级联 pending 任务。工作区干净，起点为 dev-1.0.0。
- 工作分支：`codex/design-task-11-1-identity-contract`。首次创建因沙箱不允许写 Git refs 失败；获工具权限后创建成功，未修改提交历史。
- 规格锁定：项目蓝图 §21 D18、§22.4.1、§22.4.3、§22.5.1；详细设计 §12.1；系统架构 §10；开发计划 §18.2、§18.3、§18.11；任务追踪维护规范。
- 计划：核对现有 AppState/Channel/roster/worker/lease/workspace/Harness 身份；交付概念关系、唯一写入所有权、最小契约、生命周期/故障/恢复与分阶段适配；列明冻结接口影响和后续真实验收用例；检查来源一致性与索引；形成可审阅稿供 Leader 确认后定稿。
- 当前证据只属于规格读取和设计准备；不宣称产品实现、运行时 G5、阶段出口或发布通过。

## 2026-09-14 设计审阅稿与规格一致性检查

- 产出：[基础身份与跨批次契约审阅稿](../reviews/task111-identity-contract.md)。覆盖八类身份和基数、当前代码映射、唯一所有权、最小关系字段、跨聚合创建/责任认领回执、D4恢复、工作区/版本证据、冻结接口影响、Phase11–30适配与14项后续真实验证案例。
- 推荐方案：taskId继续作为工作项身份；conversationId/memberId表达产品会话与稳定成员；assignmentId保存长期责任；workerId保持一次派发执行身份，D4真Fork只新增官方session和重新获取lease。未来项目控制事实使用版本化协作原子域，TaskState与文件/JSONL仍各自唯一所有。新端口建议未批准，不修改现有代码或公开签名。
- 自检修订：明确Phase11–13仅启用task-only所需绑定，不伪造会话/成员；Phase14/15通过正式新桌面升级协议落地新身份；辅助模型调用保持真实独立来源与额度，不伪造WorkerState；worker结束不自动释放成员责任，认领释放必须撤销新增执行资格并证明旧执行已静止。
- 已核对的现行来源包括详细设计§1的State/Channel/D12所有权、§5的展示/控制边界、§6的Executor/SandboxManager/D4 checkpoint契约、§8的协作聚合所有权，以及§12.6的14.1/15.1/16.1/17.1/19.2后续设计职责。生产代码证据逐项链接在审阅稿§2；这里只进行静态核对，不把源码或既有测试当本轮实测。
- DOC-CONFLICT修正：D17短摘要残留“Phase11按D18移除Docker后端”，与蓝图§21 D18、详细设计§12.3、架构§10及开发计划§18.2已批准的13.3移除/13.4验收冲突。来源本身已一致，无新架构取舍；按agora-sync-docs将摘要校准为13.3移除、13.4验收，保留D17 ID/source及其他规则。
- 文档同步顺序：蓝图§22.5.1增加审阅入口 → 详细设计§12.1增加范围/证据入口 → 架构§10增加候选原子域/接口影响入口 → 开发计划§18.3改为按索引查当前状态并链接设计产出 → 任务索引记录短摘要。技术选型/AGENTS.md无新选型、依赖或红线变更，不修改。审阅入口均明确不批准稿内新接口；正式规则同步等待本次设计确认。
- 开发计划原“第一批新任务未开工”改成“任务登记本身不授权开工”的持续规则，避免将已有启动授权误判为禁止更新状态；保留正常Skill、确认和跨阶段门禁。
- 检查结果：`node scripts/task-status.mjs check`通过（171任务、31阶段、24决策、62历史）；`git diff --check`通过。临时只读Python检查17个本地链接存在、3条控制原文逐字匹配、3组冻结接口声明逐行匹配生产代码、I01–I14共14个唯一验证场景；将索引与Git HEAD结构化比较，只允许顶层日期、11.1状态/notes/日期、Phase11状态及D17摘要变化，其他任务/依赖/出口/决策source不变。
- 查询过程曾把R9误传为decision ID，脚本按预期报Unknown decision；R9是AGENTS红线而非standing_decisions条目，随后改用有效ID查询。两处探索路径不存在后按rg实际文件定位；均无写入或测试副作用。
- 本任务只有Markdown和任务索引变更，无产品代码、测试或构建脚本；未运行pnpm typecheck/lint/test和G5，也未运行Electron Spike。纯设计Check已执行，不宣称G1–G7全部通过；后续commit仍必须执行AGENTS.md §3.1.3完整门禁。本次未commit/push、未创建PR、未发布。
- 下一步：Leader审阅稿§1四项建议；确认后按来源层级正式定稿、记录接受证据，将非代码任务11.1标done并幂等级联。未获设计接受前保持in_progress，不启动11.3或其他依赖实现。

## 2026-09-14 Leader接受、正式定稿与非代码收尾

- Leader对“确认按这套方案定稿11.1吗？”回复“确认”，明确接受此前完整审阅稿及四项设计结论；此授权用于设计定稿和非代码任务收尾，不扩大到产品实现、commit/push或发布。
- 接受时原始审阅文件SHA-256：`92d74e8f6c064e11f8741258ebf8ecd6a42760d74b7cd0319f5e96f003da37b6`。原§1–§9保留为[已接受的审阅快照](../reviews/task111-identity-contract.md)，仅更新外层标题/接受说明；其中“建议/待确认”为当时措辞，不作为当前状态。正式来源完整保留其契约与I01–I14，只更新定稿措辞、章节编号和相对链接。
- 按agora-sync-docs依次完成：①蓝图§21 D18增加`[2026-09-14 架构决策更新]`的身份/所有权/恢复/接口/阶段定稿，§22.5.1区分已定基础契约与15.1待细化内容；②详细设计§12.1.1–§12.1.9纳入完整契约，并建立`identity-contract-111`稳定锚点；③架构§10同步L1–L4分工、项目CAS原子域和跨存储回执，技术选型§14同步现有JSON/本机单实例及新桌面升级边界，无依赖版本变更；④开发计划§18.2/§18.3同步设计产出、固定基线与后续任务职责，索引D18摘要随后更新。现有公开方法和红线没有被改变，AGENTS.md无需修改。
- 非代码DoD：八类身份、最小关系字段、所有权、生命周期/故障/幂等、阶段适配、冻结接口影响和14项后续用例均已覆盖并获Leader接受；未决细则已有12.2/14.1/15.1/16.1/17.1/19.2等任务归属，不是11.1未完成设计或新增延期项。无产品代码、测试、构建脚本、依赖或新费用实验；本任务无需Electron Spike，11.2仍须独立实测。
- 定稿检查：9个正式子节、4个生命周期子节、I01–I14逐行保留；Executor/TaskStateStore/SandboxManager三组签名逐行匹配生产代码；33处本地链接/锚点核验通过；正式来源已移除11.1仍待接受的陈旧措辞。索引check与git diff --check通过。
- 检查过程记录：首次蓝图patch因过长上下文不匹配被拒绝且未写入，缩小到唯一锚点后成功；临时Python检查首次使用子串计数误把`###`算入`##`而触发断言，改为行首完整层级匹配后9个章节准确通过。没有修改设计内容、弱化运行时测试或掩盖产品失败。
- 按AGENTS.md §8.1.2“非代码类任务（文档/调研/验证产出）：产出经人类确认后标done”收尾11.1；保持Phase11进行中，下一项为ready的11.2。后续提交仍执行§3.1.3完整门禁，不以本次设计接受或静态校验宣称产品G3/G4/G5、Phase11出口或发布通过。
- 收尾后实查：11.1=done；遍历全部pending任务，符合依赖全部done的级联项为0；11.3仍依赖未完成的11.2。D18摘要400个Unicode码点，ID/source保留；索引check再次通过（171任务、31阶段、24决策、62历史），git diff --check通过。未自动启动后续任务、提交或推送。

## 2026-09-14 提交门禁与交付授权

- Leader显式调用`agora-commit`，授权提交、推送当前功能分支并创建base=`dev-1.0.0`的英文PR；不授权自动合并。11.1已按非代码规则获接受，交付时保留done。
- 范围：蓝图、详细设计、系统架构、技术选型、开发计划、task-status及11.1审阅快照/任务历史，共8个文档/索引文件；没有产品代码、测试、构建脚本或依赖变更。分支`codex/design-task-11-1-identity-contract`基于`b950f00c4d5a418561f711c885acce8dbbc9f70c`；远端dev-1.0.0仍为该提交，同分支无既存PR。
- G1：复核已接受范围、正式来源§12.1、阶段适用边界、任务依赖和全文历史；原接口未变，14项未来验收不冒充当前实现。此前定稿的字段/来源/链接/快照检查及本轮索引复检通过。
- G3：`pnpm typecheck`退出0；`pnpm lint`退出0（430文件，无修复）；`pnpm build:sandbox-native`退出0。
- G4：保留已配置DEEPSEEK_API_KEY，完整执行`pnpm test`，退出0。Node任务追踪测试7 passed、0 failed、0 skipped；Vitest 170/170文件、1282/1282测试通过，无跳过，耗时152.71秒。默认套件未开启独立Eval预算配置，未额外运行Benchmark。
- G5：11.1为纯身份契约设计，没有新增运行时能力或任务专属可执行验收对象；不新增空测试。提交回归实际包含live DeepSeek Harness、live Channel summary、LRU Coordinator→CODER→TESTER端到端、Phase9/10出口及真实并行/暂停/Fork/验证/归档链路，均通过；不宣称未来I01–I14已实装验收。
- G7：核对8个精确交付文件，私钥、GitHub/model token、云访问key及高熵凭据赋值模式扫描0发现，并审阅文档内容；无.env、凭据文件或测试日志进入提交。测试密钥仅检查是否存在，没有输出其值。未修改既有凭据、测试断言或skip条件。
- 完整测试日志保存在本机临时文件`/private/tmp/agora-11-1-test.log`（不提交；可能随系统清理），SHA-256：`9a91f46a61fa99fa3d502dd031460832226ee1ce6603eee6c4347356bc0e7fdb`。可重跑命令与持久摘要以上述记录为准。
- 环境过程：默认沙箱内Git远端DNS和Docker socket访问失败；经工具权限批准后执行原始完整测试和远端只读查询成功，无测试排除。探索命令一次glob匹配不存在路径退出，未影响门禁。没有自动审批拒绝或未解决环境阻塞。
- G6：本节保存交付授权、精确范围与完整检查结果；提交/PR身份在创建后追加，索引只保留摘要和历史引用。
