# 任务 4.1：复杂度评估 complexity.tier（Tier 0/1/2）

本文件保存历史证据；当前状态、依赖和时间戳只以 [任务索引](../task-status.json) 为准。原文中的 ready/in_progress/done、待合并和延期项状态描述记录时点，不覆盖当前索引或延期台账。后续事件追加到文末，不改写原文区。

## 2026-09-13 原 notes 无损迁移

Source commit: `fa3efafed328bdfeb5b0f575b4e57bbd248a45fd`
Original notes SHA-256: `ab74492e0f20cb805d758ca753b3545a8e03b3cdf8a36f0e3868eed25c28b22d`

~~~~text
<!-- BEGIN ARCHIVED NOTES -->
core/orchestration/complexity.ts。输入 goal/文件数估算/依赖数；输出 {tier:0|1|2, signals}。Tier 0=单文件/单函数；Tier 1=小型多文件需简单设计；Tier 2=复杂多模块可并行。[2026-08-30 开工·用户确认] 状态置 in_progress，进入 EPCC-V Explore（精读 entry 恒等退化处/AppState schema/0.2 预铺 set 家族解锁路径），Plan 待批。[2026-08-30 计划批准·用户确认] 三项裁决：① Tier 启发式规则表与 TIER0/TIER2 关键词清单按提案（R1 多模块关键词→Tier 2 / R2 规模输入 fileCount≥5 或 depCount≥2→Tier 2 / R3 单实体关键词或 fileCount≤1 且 goal≤80 字符→Tier 0 / R4 默认 Tier 1，自上而下首条命中）；② entry 幂等语义=已置 complexity 不覆写（重放稳定）；③ project() 的 global.summary.complexity 占位接线延至 4.2（消费随路由一并评估）。[2026-08-30 Code+Check·TDD 红→绿] 先写测试红实测：3 files failed / 8 failed / 53 passed（complexity 模块未建 + set 门控 disabledFieldError + entry 未评估）；实现后定向 74/74 绿（complexity 13 + reducer 53 + orchestrator 8）。五改动面：① domain/state.ts 增 `export interface Complexity { tier: 0|1|2; signals: Record<string, unknown> }`（§1 逐字，HumanGate 先例）+ AppState.complexity?: Complexity（初始不置）；② domain/reducer.ts SET_FIELDS/ENABLED_SET_FIELDS+=complexity + isComplexity 守卫（非数组对象+tier∈{0,1,2} 整数+signals 非数组对象，违规 throw 'complexity must be { tier: 0 | 1 | 2; signals: Record<string, unknown> }'，镜像 humanGate/conventions 先例）——0.2 预铺「表内加一项+补一个 case」路径，Mutation 联合与 setMutation 签名未变（R1/R9）；③ orchestration/complexity.ts 新建 evaluateComplexity（R8 纯函数零 I/O；estimatedFileCount/dependencyCount 为可选输入位，Phase 4 无估算器，entry 仅传 goal；signals={rule,goalLengthChars,matchedTier2Keywords,matchedTier0Keywords,estimatedFileCount,dependencyCount} 理由随决策走）；④ orchestrator.ts entry 接线：complexity 未置时 applyMutations+[set('complexity',evaluateComplexity({goal}))]，已置原样返回（签名不变 R9）；⑤ orchestration barrel 加性导出 complexity。biome --write 修 2 处 import/export 排序后复检通过。[门禁实测] tsc --noEmit exit=0；biome check . exit=0（128 files 0 违规）；vitest 全量 345 passed / 2 skipped（37 files，基线 324+21 对账吻合=complexity 13+reducer 新 describe 5+orchestrator entry 3，零回归；2 skip 为既有 G5 live-LLM 需 DEEPSEEK_API_KEY）。state.test.ts 核查：无穷举夹具需同步（首个用例仅断言特定可选键缺省，complexity 初始未置已由 reducer 新 describe 覆盖）。R1 写入全走 applyMutations；R8 complexity.ts 零 import I/O；R10 对齐 §0 目录树（orchestration/complexity.ts）。G5：纯评估纯函数任务，无沙箱/Harness 执行链路对象，注明暂缺（3.1-3.4 先例）；entry 写入链路由既有 orchestrator/全量回归覆盖。[R12 已同步] 详细设计 §1（解锁面+=complexity）与 §3（entry+Tier 启发式实装记录）均打 [2026-08-30 实装记录] 标记。status 保持 in_progress，待 /commit-agora 建 PR，人工合并后 /pr-merge-agora 收尾标 done。 [2026-08-30 PR] PR #33 已创建（feat/complexity-tier-eval → dev-1.0.0）：https://github.com/logan-suu/Agora/pull/33——门禁复核全过（tsc exit=0；biome 128 files 0 违规；vitest 全量 345 passed/2 skipped，首轮 1 例 docker worktree existsSync 断言为容器启动竞态偶发，同代码复跑全绿，未改断言）。[2026-08-30 PR 预审] CodeRabbit 指出 §1 Mutation 联合 set 行漏列 complexity（AppState L189 原有字段，规格缺口对齐 2.2/2.4 先例）——已补列并修正 4.1 实装记录「原已定义 op」的不实表述，记 GitHub Issue #34；门禁复跑全绿（tsc exit=0；biome 128 files 0 违规；vitest 345 passed/2 skipped）。 status 保持 in_progress，待人工合并后 /pr-merge-agora 收尾标 done。
[2026-08-30 /pr-merge-agora 收尾] PR #33 已合并（merge commit c36d0944aa9eac1f1269a527bbaf0178305beb58），dev-1.0.0 已同步，本地分支 feat/complexity-tier-eval 已删除，任务标 done。G5 结论：纯评估纯函数任务，无沙箱/Harness 执行链路对象，注明暂缺（3.1-3.4 先例）；entry 写入链路由既有 orchestrator/全量回归覆盖（345 passed / 2 skipped，2 skip 为既有 G5 live-LLM 需 DEEPSEEK_API_KEY）。遗留项：project() 的 global.summary.complexity 占位接线延至 4.2（裁决③）。级联翻转：4.2 → ready。
<!-- END ARCHIVED NOTES -->
~~~~
