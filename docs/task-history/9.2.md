# 任务 9.2：配合式抢占完整实现

本文件保存历史证据；当前状态、依赖和时间戳只以 [任务索引](../task-status.json) 为准。原文中的 ready/in_progress/done、待合并和延期项状态描述记录时点，不覆盖当前索引或延期台账。后续事件追加到文末，不改写原文区。

## 2026-09-13 原 notes 无损迁移

Source commit: `fa3efafed328bdfeb5b0f575b4e57bbd248a45fd`
Original notes SHA-256: `41db08dfd99868c6c8626cc32827cdcd9bf53db08c8dcfab5acbc565bf8fd63a`

~~~~text
<!-- BEGIN ARCHIVED NOTES -->
D4+D17：配合式抢占以 task-scoped 稳定 actionId pause epoch 固定 active cohort；已启动 worker 到 step/end 后先经 canonical queue 提交当前 Step，再 flush checkpoint 并由 WorkerRuntime 写自身 safePoint/status，排队 acquire 取消但保持 pending，done/failed 不被 paused 覆盖。非阻塞 reproject 以一次 canonical commit 写 Leader 主消息+控制 mutations，注入最新 D1 投影并复用原 context/lease/admission。blocking/humanGate 在全部 checkpoint 与完整 gate 落盘后先释放已静止 worker lease，再收尾 composition；清理失败保留 admission/needs_attention 但不占 worker 执行额度。Phase 9 receipt 增 per-worker 恢复计划并兼容 Phase 8 单 worker receipt；每个 paused worker 真 Fork 后重新排队取得新 lease，旧 lease 不跨 Fork。
[2026-09-06 /agora-pr-merge] 依赖 9.1 已由 PR #63 合并并标 done（merge commit 414fa23），级联翻转 pending→ready。
[2026-09-06 /agora-sync-docs·规格评审] Leader 确认先评审并修正 9.2 规格。评审确认高层安全点/D4 方向合理，但旧 requestPause(reason)+applyAndResume((AppState)=>AppState) 缺 scope/稳定重放/cohort/canonical commit，单 resumeSessionId 未定义多 worker lineage，队列 worker 可在暂停后继续启动，且将 worker lease 与 composition 清理捆绑会在清理失败时阻塞全局额度。已按蓝图→详细设计→系统架构→开发计划→task-status/AGENTS 同步 D4/D17 两模式抢占、pause epoch/cohort、Step-commit-first、队列取消、额度分层与 per-worker Fork 计划。无新依赖、无冻结 L3 端口变更、无新延期项；任务保持 ready，待 /agora-do-task 实现计划确认。
[2026-09-06 /agora-do-task·启动] Leader 授权继续执行 9.2。依赖 9.1=done，Phase 8 出口 8.5=done，current_phase=9，跨阶段门禁满足；任务置 in_progress，进入 Explore/Plan。修订后的规格计划再次确认前不写测试或生产代码。
[2026-09-06 Explore·规格空洞] 现有 GlobalScheduler 已支持 AbortSignal，WorkerRuntime 仍使用全局 paused boolean 且正常 finally 会过早释放 lease，D4 Web composition 仍只保存 latestExecutor/单 safePoint。进一步发现 `/requirement`、`/decision`、`/priority` 只定义了 Phase 9 命令名与 deferred 分类，未定义参数语法、稳定业务身份、控制 mutation 及重放校验；其中 priority 在 AppState/Subtask 也没有对应字段。该缺口会直接改变领域 schema 与 D9 行为，按 §0.3/EPCC-V 暂停 Plan/Code，等待 Leader定稿。
[2026-09-06 /agora-sync-docs·Phase 9 指令契约] Leader 确认 `/requirement <id> <JSON>` 完整 upsert Requirement、`/decision <topic> <JSON>` append 显式 current-only supersedes 的 Leader Decision、`/priority <subtaskId> <0-100>` 更新未完成 Subtask；三者 main-only、msgId=actionId、先预校验再 cohort 安全点、canonical 单次提交与严格重放复核。新增可选 `Subtask.priority`（旧快照缺省 0；依赖满足优先，同一就绪集合高优先级先行）和所有角色共有的结构化 `leaderDirective` 投影；不投 display/raw log。已按蓝图→详细设计→系统架构→开发计划→task-status/AGENTS 同步；无新依赖、无冻结 L3 接口变更、无新增延期项。任务保持 in_progress，等待实现计划确认。
[2026-09-06 /agora-do-task·实现与验证] 按确认后的规格以 TDD 完成 task-scoped Preemptor、WorkerRuntime 安全点 barrier、排队 acquire 取消、reproject 原 context/session/lease 续跑、D4 完整 gate 后 worker lease 释放与 composition suspend，以及按 workerId 绑定的确定性真 Fork 恢复。D9 落地严格 Phase 9 指令语法、预校验、稳定 action 重放复核、canonical 单次提交；新增 `Subtask.priority` 旧快照缺省与全角色结构化 `leaderDirective`。真实 Phase 9 G5 使用 Harness Context/Agent、JSONL session persistence、LocalTempSandbox、JsonTaskStateStore、MessageRuntime、WorkerRuntime、GlobalScheduler 与生产 Web composition，验证双 worker 暂停、释放、Leader resolve、两条确定性 child lineage 恢复并完成；Web reproject 回归验证 barrier 前不提交、同 session 续跑且仅注入一次最新投影。最终门禁：typecheck 0 错；Biome 263 files 0 违规；完整 Vitest 100 files / 857 tests 全绿，含真实 Docker 与 live Harness 链；git diff --check、tracking JSON 与旧接口漂移扫描通过。无新增依赖、冻结 L3 签名变更或延期项。任务保持 in_progress，待 `$agora-commit` 交付。
[2026-09-06 /agora-commit] 交付门禁复跑：typecheck 0 错；Biome 263 files 0 违规；任务专属 Phase 9 cooperative preemption G5 1/1；完整 Vitest 100 files / 857 tests 全绿，包含真实 DockerSandbox、Web Docker/Harness/MCP、live DeepSeek Harness 与 199.62s live LRU E2E；staged diff gitleaks 扫描无泄漏，git diff/JSON 校验通过。功能提交 `19a5825c891745cc014513647987b995560a9fec` 已推送至 `feat/phase9-cooperative-preemption`；PR #64：https://github.com/logan-suu/Agora/pull/64（base=`dev-1.0.0`）。任务保持 in_progress，等待人工审阅合并；Agent 不自动合并。
[2026-09-06 /agora-pr-review·修复复核] 对 PR #64 的 6 条 CodeRabbit 评论逐条对照规格：5 条成立并以 TDD 修复——drain-paused worker 只有具 canonical D4 resume plan 才可恢复；pause scope 在安装 epoch/取消 acquire 前校验；barrier 中单 worker checkpoint 失败后容忍其 handle 已退出并释放剩余 cohort；failed complete/abort 清除终态 promise 以允许同 capability 重试；JsonTaskStateStore 在 initialize/write/load 共用 priority/worker/objection slice 校验。1 条“9.2 立即按 priority 排序 Coordinator”不采纳，因为详细设计明确由 9.4 消费，CodeRabbit 已复核并正式撤回。人工复审另修复 D9 已提交 action 在 reproject complete 失败后的重放恢复，并拒绝从非 main Channel 重放。红测 5 个失败 + D9 2 个失败均复现；修复后聚焦 5 files/139 tests 与跨 Phase 13 files/174 tests 全绿。最终门禁：typecheck 0 错；Biome 263 files 0 违规；任务专属 Phase 9 G5 1/1；完整 Vitest 100 files/863 tests 全绿，含 DockerSandbox、Web Docker/Harness/MCP、live DeepSeek Harness 与 95.65s live LRU E2E；git diff/JSON 与 gitleaks（no leaks）通过。修复提交 `c675e090ca9c744d52024391259ca26a65c0e481` 已推送到 PR #64；6 个 review threads 均已英文回复并 resolved，CodeRabbit 对最新 head 检查 SUCCESS，PR mergeable。未新增依赖、冻结 L3 签名或延期项；任务保持 in_progress，等待人工合并，Agent 不自动合并。
[2026-09-06 /agora-pr-merge·收尾] PR #64 已由人类合并至 dev-1.0.0，merge commit=`2b6f4da3474220026cd9c6c44921b7b1922d4069`，功能 head=`b94aa4daad5fd220608132545aeb9a7048417798`，CodeRabbit SUCCESS，6 个 review threads 已英文回复并 resolved。G1–G7 证据完整：合并树与最终功能 head 完全一致；合并后 typecheck 0 错、Biome 263 files 0 违规、Phase 9 cooperative preemption 真实 G5 1/1；完整 Vitest 100 files/863 tests、真实 DockerSandbox、Web Docker/Harness/MCP、live DeepSeek Harness 与 live LRU E2E 证据沿用最终 head 复核；task-status JSON、git diff 与 gitleaks 无问题。无新增依赖、冻结 L3 签名或延期项。任务 9.2 标 done；9.3 保持 ready；9.4 因仍依赖 9.3 而保持 pending；Phase 9 保持 in_progress，等待后续任务及出口 9.5。
<!-- END ARCHIVED NOTES -->
~~~~
