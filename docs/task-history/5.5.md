# 任务 5.5：Web 编排执行桥接 + 秋招 Demo 录屏

本文件保存历史证据；当前状态、依赖和时间戳只以 [任务索引](../task-status.json) 为准。原文中的 ready/in_progress/done、待合并和延期项状态描述记录时点，不覆盖当前索引或延期台账。后续事件追加到文末，不改写原文区。

## 2026-09-13 原 notes 无损迁移

Source commit: `fa3efafed328bdfeb5b0f575b4e57bbd248a45fd`
Original notes SHA-256: `aebdde2d9943a8f4bc03e5a04ad44ca9105df1ad3996d7f7264d8408a0f4eb38`

~~~~text
<!-- BEGIN ARCHIVED NOTES -->
[2026-09-01 /agora-sync-docs·规格评审] 原 §17 Demo 假设 Web 已接通 runOrchestration 与 Agent 实时消息，但现状 POST /api/messages 只提交 Leader 消息/intent，编排循环独立存在，UI 的 Agent 初始消息为静态种子；仅改弱录屏文案会掩盖 Phase 5 出口缺口。Leader 批准把本任务修订为 Web 编排执行桥接 + Demo：受信任单用户、单实例边界内由 UI 创建并启动新任务，服务端组合根复用既有 runOrchestration/Harness/沙箱；同任务最多一个活动 run；Coordinator/Worker 展示消息必须先写 State/TaskStateStore 再经 MessageBus→SSE；刷新恢复最终 State，活动 run 的进程重启自动恢复不在 Phase 5 范围但状态不得误报。Demo 使用带 TTL 的 LRU 真实链路，展示 Tier 0→Coder→Tester→Reviewer→done、产物与快照恢复；禁止静态种子消息或分离的测试/UI 片段冒充 Web 触发的同一任务闭环。任务保持 ready，须经 $agora-do-task 锁定接口与 TDD 计划后方可编码。
[2026-09-01 /agora-do-task] Leader 确认开始 5.5；依赖 5.4=done、Phase 4 出口 4.6=done，跨阶段门禁满足。任务置为 in_progress，进入 Explore/Plan；须先锁定 D10 生命周期入口、单 run 注册表、编排 checkpoint/持久化接缝与 G5 方案，计划获批前不写实现代码。
[2026-09-01 Code·TDD] 按获批计划新增可选 StateTransition：runOrchestration 的 entry/coordinator/finalize 与 WorkerRuntime 每个 Step 均经异步 transition；Web 绑定 MessageService.commitMutations，保证 TaskStateStore.commit 后才按新增 msgId 发布 SSE。新增 TaskOrchestrationRuntime、POST/GET /api/tasks、同 scope 单活动 run、不同 goal 冲突、完成态/中断态恢复；未知 scope 的 SSE/消息入口不再自动创建占位 State。生产组合根复用 DockerSandbox、HarnessExecutor、MCP 工具、宿主 Git 与 DEFAULT_ROSTER；角色结构化输出契约从 Phase 2 测试 helper 提升到 production roles 包。UI 删除静态 Agent 种子，增加任务启动、运行状态、当前角色、测试结果、artifact 与刷新轮询。
[2026-09-01 Check+Verify] typecheck exit=0；Biome 171 files 0 违规；Next production build 成功（首页 4.76 kB、First Load JS 107 kB，/api/tasks 动态路由）；全量 49 files passed / 2 skipped、473 passed / 2 skipped，真实 DockerSandbox 15/15。新增 Web G5 在真实 Docker 中通过同一生产组合根连续执行 Harness+MCP+宿主 Git+JSON State+SSE 的 TTL-LRU Coder→Tester→Reviewer→done，真实 node:test 3/3 且 artifact 可读；仅外部 LLM 使用 R11 允许的确定性 adapter，shell 无 DEEPSEEK_API_KEY，既有 2 个 live-LLM 测试按规则 skip。浏览器 QA：Agora/localhost 桌面与 390×844 移动布局、Goal→摘要交互、移动侧栏均通过，console 0 error/warn、无框架错误层；发现并修正未建任务时误显示 Connecting，现为 Idle。录屏真实性门禁要求浏览器连续触发 live-LLM 同一任务，当前因 DEEPSEEK_API_KEY 缺失未录制，任务保持 in_progress，不以脚本 adapter 冒充作品集录屏。
[2026-09-01 Live G5·真实 DeepSeek] Leader 在自有终端提供 DEEPSEEK_API_KEY 后，由浏览器同页 POST 启动稳定 ID `lru-demo`，连续完成 Tier 0→CODER→TESTER→REVIEWER→done；State 最终 phase=done、subtask=done、testResults=2/2、REVIEWER approved、messages=9，产物 `lru-cache.mjs` 的独立 node --test 实跑 16/16。运行中 SSE 文章数 2→4→7→9，证明 Coder/Tester/Reviewer 消息实时抵达；刷新后自动恢复同一 `lru-demo`、completed/done、Tests passed 与 9 条消息。实测先后暴露并 TDD 修复三项仅 live provider/Next 路由可见缺陷：① MCP Bridge 原把作者态 DSL 直接注册，DeepSeek 拒绝缺少根 type=object 的 fs_read schema，改为 Harness defineTool 编译；② Reviewer 返回单一 json Markdown 围栏，按 §0 终局结构化文本语义仅剥离无围栏外文字的确定性包装，形状校验保持严格，围栏外自然语言仍 throw，并同步详细设计实测校准；③ Next route/HMR 模块级 ChannelStream 分裂导致发布与订阅不同实例，生产 MessageRuntime 提升为进程级单例。最终作品集录制 `https://github.com/logan-suu/Agora/blob/83fe416c1015d86616acdead7af2ebbad82e8384/docs/demo/Agora-Phase5-TTL-LRU-Live-Demo-2026-09-01.gif`，19 帧/39.6s/960×477，覆盖启动、角色切换、实时消息、测试、Reviewer、done 与刷新恢复。任务保持 in_progress，待交付提交/PR。
[2026-09-01 /agora-commit·交付门禁] 分支 feat/web-orchestration-demo（base=dev-1.0.0）。typecheck exit=0；Biome 172 files 0 违规；全量 50 files passed / 2 skipped、477 passed / 2 skipped，真实 DockerSandbox 15/15；任务专属 G5 Docker/Harness/MCP 连续链单独复跑 1/1。live DeepSeek 浏览器链已完成 lru-demo Tier 0→CODER→TESTER→REVIEWER→done、SSE 2→4→7→9、刷新恢复 9 条消息、产物 node --test 16/16；录屏 GIF 已入 docs/demo。git diff --check、task-status JSON 校验与源码/GIF 敏感数据扫描均通过，无 secrets/.env/token。任务继续保持 in_progress，禁止 Agent 自动合并。
[2026-09-01 /agora-commit] 功能提交 25216e7；PR #45 已创建：https://github.com/logan-suu/Agora/pull/45（feat/web-orchestration-demo → dev-1.0.0）。PR 保持 open，任务保持 in_progress，待人工审阅合并后由 $agora-pr-merge 收尾。
[2026-09-01 /agora-sync-docs·PR 规格复评] 先评审规格合理性后校准 D10：原“同 scope 单 run”会允许不同 taskId 并行，和蓝图“项目内初期退化单任务”、Phase 9 才启用真并行冲突；改为 Phase 5 全实例最多一个活动 run，其他 scope 启动返回 409，终态任务可共存。原刷新恢复只要求保留 artifactPath，未定义执行资源退出；现要求终态产物先归档到 .data/projects/{projectId}/tasks/{taskId}/artifacts/ 并经 reducer/TaskStateStore 持久化引用，再释放 Harness/MCP/Git/Docker，刷新不依赖活容器。已按蓝图→详细设计→架构→技术选型→开发计划→task-status→AGENTS 同步；以修订后 D10 重新审查 PR #45。
[2026-09-01 /agora-pr-review·修复复检] 按批准计划以 TDD 修复评审项：全实例生命周期队列保证任一时刻最多一个 running run，其他 scope 的并发启动返回 409；completed/needs_attention/failed 均先把 worktree 原子归档到 `.data/projects/{projectId}/tasks/{taskId}/artifacts/worktree`，经 mergeById + TaskStateStore 持久化引用后释放 Harness/MCP/Git/Docker，初始化失败也清理已分配 sandbox。MessageService 在 commit 前拒绝非 main 的消息 mutation，ARCHITECT/REVIEWER 运行时契约与 prompt 对齐，UI 轮询错误可见，Docker G5 正确解析 tcp/http/https DOCKER_HOST。门禁：typecheck 0 错；Biome 173 files 0 违规；Next production build 成功（首页 4.85 kB、First Load JS 107 kB）；全量 51 files passed / 2 skipped、485 passed / 2 skipped，真实 DockerSandbox 15/15，Web 真实 Docker/Harness/MCP G5 2/2；git diff --check 与 task-status JSON 校验通过。PR 保持 open，任务保持 in_progress，待修复提交推送与人工合并。
[2026-09-01 /agora-pr-review·交付] 修复提交 `094e66a` 已推送至 PR #45；9 个 CodeRabbit 会话均已英文回复并解决。8 项有效问题已修复；task ID 归一化评论经核验为不成立，因为 tasks/stream/messages 服务端入口均由 `requiredString` 统一 trim 后查找同一 scope，已说明理由且未做无效改动。PR checks 显示 CodeRabbit pass（review rate limited），GraphQL 复核未留未解决 review thread；PR 保持 open，任务保持 in_progress，禁止 Agent 自动合并。
[2026-09-01 /agora-pr-merge 收尾] PR #45 已由人工合并至 dev-1.0.0（merge commit 17758a41e0a8044222d7e2b4f681b7ad75f5473d）；任务标 done。G1–G7 证据复核：修订后 D10 与蓝图/详细设计/架构/选型/开发计划/AGENTS 同步且实现一致；typecheck 0 错；Biome 173 files 0 违规；Next production build 成功；全量 485 passed / 2 skipped（既有 live-LLM 条件用例），真实 DockerSandbox 15/15、Web 真实 Docker/Harness/MCP G5 2/2；真实浏览器 DeepSeek 同页链完成 Tier 0→CODER→TESTER→REVIEWER→done、SSE 2→4→7→9、刷新恢复 9 条消息与归档产物，独立 node --test 16/16，录屏 GIF 已入库；task-status/规格文档、git diff 与敏感扫描通过。无新增延期项；DEF-015 按既定公开暴露前/最迟 Phase 6 出口边界保持 open。级联翻转：5.6 pending→ready；Phase 5 仍 in_progress，须完成出口集成测试。
<!-- END ARCHIVED NOTES -->
~~~~
