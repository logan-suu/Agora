# 任务 6.2：sub 群生命周期（发起/通信/关闭）

本文件保存历史证据；当前状态、依赖和时间戳只以 [任务索引](../task-status.json) 为准。原文中的 ready/in_progress/done、待合并和延期项状态描述记录时点，不覆盖当前索引或延期台账。后续事件追加到文末，不改写原文区。

## 2026-09-13 原 notes 无损迁移

Source commit: `fa3efafed328bdfeb5b0f575b4e57bbd248a45fd`
Original notes SHA-256: `f351957d250d2b4c862f48391574abb9245aed913d5c51d3d1f39bb570566980`

~~~~text
<!-- BEGIN ARCHIVED NOTES -->
agent 自发为主（新群 participants 自动并入 leader）+ Leader 可发起；threadId 会话链支撑问答/升级的持久链与路由，异议 humanGate/resume 明确留 Phase 8。Coordinator 轻量治理：同 taskId+threadId 去重、防小群无序膨胀。
[2026-09-01 /agora-pr-merge] 依赖 6.1 已由 PR #47 合并并标 done（merge commit 7657327），级联翻转 pending→ready。
[2026-09-01 /agora-sync-docs·规格评审] Leader 确认先评审再实现。修订后 6.2 定稿：sub 持久化 threadId/topic/createdBy；participants 为集合并按 leader+enabled roster 标准化；稳定 actionId（Leader=msgId）提供 open/close 幂等，同 task+thread 语义冲突明确失败；actor 由组合根绑定，只有 leader/participant 可关闭，main 不可关闭；生命周期服务按 project 串行，先提交 ProjectChannelStore，再经 D6 补原消息/稳定 announce，失败重试不回滚/不重复 Channel。agent 最终回复末尾至多一个严格 channel-action 控制块，Harness 剥离 display、以 assistant msgId 绑定 actionId 后复用 StepResult.output，不修改冻结端口。GET /api/channels 从持久快照派生 main+当前 task sub 展示元数据，UI 可刷新/切群且不扩 MessageBus。6.2 close 只置 closed+main 公告，bubbledSummary 归 6.3，humanGate/resume 归 Phase 8。任务保持 ready，待实现计划确认。
[2026-09-01 /agora-do-task] Leader 已批准修订后的五单元 TDD 计划；依赖 6.1=done、Phase 5 出口 5.6=done、current_phase=6，跨阶段门禁满足。任务置 in_progress，分支 feat/sub-channel-lifecycle；按 Channel schema/迁移→生命周期服务→Agent output→Leader/API→UI/G5 顺序实施。
[2026-09-01 Code+Check+Verify] 五个 TDD 单元完成：① SubChannel 必填 threadId/topic/createdBy、participants 标准化及 legacy 全缺字段一次性 revision 迁移，部分缺失 fail-fast；② L2 ChannelLifecycleService 按 project 串行，实现 open/close 权限、幂等、语义冲突与关闭后禁止重开，严格 Channel-first 后稳定 main announce；③ Harness 严格解析唯一末尾 channel-action，剥离 display，以持久 assistant event 派生稳定 msgId/actionId，经 StepResult.output 回传；WorkerRuntime 在 State transition 前调用可选 handleOutput；④ Leader `/channel open ROLE[,ROLE...] <topic>` 与 `/channel close <channelId>`、GET /api/channels 和 Web 组合根落地；⑤ UI 频道列表/选中切换 SSE+POST、初始化/动作后/2 秒短轮询刷新，closed 可读不可写。G5：真实 JsonProjectChannelStore + JsonTaskStateStore + HTTP/MessageBus 链验证 Leader 与 Agent action 均先落 Channel 再落 State/announce，失败重试补消息且不重复 Channel。门禁：typecheck 0 错；Biome 187 files 0 违规；全量 58 files passed / 2 skipped、550 passed / 2 skipped；真实 DockerSandbox 15/15、Phase 5 Docker/Harness/MCP 累积链通过；任务定向 3 files/27 tests 通过；git diff --check 通过。任务保持 in_progress，待 `$agora-commit` 交付，Agent 不自动合并。
[2026-09-01 /agora-commit·交付门禁] 分支 feat/sub-channel-lifecycle（base=dev-1.0.0，HEAD 与 origin/dev-1.0.0 同基线）。交付审计发现并以 TDD 修复生命周期业务拒绝：非法角色等预校验错误现在保留 Leader 原消息并返回 rejected，Channel revision 不变；仅捕获 ChannelLifecycleRejectedError，存储/投递故障继续 fail-fast。最终门禁：typecheck exit=0；Biome 187 files 0 违规；任务专属真实 JSON Channel/State + Harness output + HTTP/MessageBus 链 3 files/28 tests 全绿；Next production build 成功；全量 58 files passed / 2 skipped、551 passed / 2 skipped，真实 DockerSandbox 15/15、Phase 5 Docker/Harness/MCP 累积链通过；git diff --check、tracking JSON、敏感文件名与高置信凭据模式扫描均通过，无 secrets/.env/token。任务保持 in_progress，禁止 Agent 自动合并。
[2026-09-01 /agora-commit] 功能提交 1c151db 已推送；PR #49：https://github.com/logan-suu/Agora/pull/49（feat/sub-channel-lifecycle → dev-1.0.0）。PR 保持 open，任务保持 in_progress，待人工审阅合并后由 `$agora-pr-merge` 收尾；Agent 不自动合并。
[2026-09-01 /agora-sync-docs·PR 复评批准] Leader 批准先修规格再修 PR。复评将 channelId 改为 taskId 长度前缀编码以消除连字符拼接碰撞；agent open 线协议允许显式安全 threadId；Leader 生命周期入口在同任务串行区、任何 Channel 副作用前检查持久 State 重复 msgId；lifecycle announce 改为按 Channel 实例去重，actionId 留在原消息审计链；纯控制回复保留审计 Message 并生成非空 display；Channel DTO 使用字段白名单，UI 仅 active run 持续短轮询且刷新错误独立可恢复。D8 受信任单用户边界不变，不引入账号认证。已按蓝图→详细设计→系统架构→开发计划→standing_decisions 同步，进入 TDD 修复。
[2026-09-01 /agora-pr-review·规格修订与修复] TDD 新增并先复现 12 个失败，覆盖重复 Leader msgId 在去重前产生 Channel 副作用、连字符 channelId 碰撞、显式 threadId 被线协议拒绝、同 actionId 不同 thread 的普通存储异常、Channel DTO 泄漏 bubbledSummary、纯控制回复空白 display、非 JSON 错误丢 HTTP 状态及 UI 刷新错误粘滞。实现改为 taskId 长度前缀 channelId、Leader task 串行预去重、Channel 实例级 announce、显式 threadId 严格校验、碰撞业务拒绝、DTO 字段白名单、审计 Message 非空摘要、独立可恢复 Channel error 与仅 active run 持续轮询。D8 账号认证两条评论按受信任单用户明确非目标不实施。复核：typecheck 0 错；Biome 187 files 0 违规；任务相关 4 files/43 tests；全量 58 files passed / 2 skipped、556 passed / 2 skipped；真实 DockerSandbox 15/15；Next production build成功；tracking JSON、git diff --check 与敏感模式扫描通过。任务保持 in_progress，PR 禁止自动合并。 修复提交 ec90912。
[2026-09-01 /agora-pr-review·修复交付] 修复提交 ec90912、证据提交 f29eeb4 已推送 PR #49。8 个 CodeRabbit inline 会话均已英文回复并 resolved：非 JSON 错误、刷新错误、预去重、ID 规则/碰撞与控制块空白展示已处理；两项账号认证建议依据 D8/DEF-015 的受信任单用户明确非目标说明后不实施。PR OPEN/MERGEABLE，CodeRabbit check=SUCCESS（review rate limited）；任务保持 in_progress，禁止自动合并。
[2026-09-01 /agora-pr-merge 收尾] PR #49 已由人工合并至 dev-1.0.0（merge commit 80d91e6f1e938a09ca7a3464f328fb592a6ec7db）；任务标 done。G1–G7 证据复核：修订后 D7/D9 与蓝图、详细设计、系统架构、开发计划及 standing_decisions 一致；typecheck 0 错；Biome 187 files 0 违规；任务相关 43/43；Next production build 成功；全量 556 passed / 2 skipped（既有条件用例）；真实 JsonProjectChannelStore + JsonTaskStateStore + HTTP/MessageBus 的 Channel-first/retry 链与 DockerSandbox 15/15 通过；tracking JSON、git diff 与敏感数据检查通过。DEF-015 已按 Channel 完整性范围 resolved，完整账号认证仍为 D8 明确非目标；无新增延期项。Git 确认功能分支完整合入后，本地 feat/sub-channel-lifecycle 已删除。级联翻转：6.3 pending→ready；Phase 6 保持 in_progress，current_phase 保持 6。
<!-- END ARCHIVED NOTES -->
~~~~
