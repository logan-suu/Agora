# 任务 5.4：Leader 指令意图映射（intent.ts）

本文件保存历史证据；当前状态、依赖和时间戳只以 [任务索引](../task-status.json) 为准。原文中的 ready/in_progress/done、待合并和延期项状态描述记录时点，不覆盖当前索引或延期台账。后续事件追加到文末，不改写原文区。

## 2026-09-13 原 notes 无损迁移

Source commit: `fa3efafed328bdfeb5b0f575b4e57bbd248a45fd`
Original notes SHA-256: `fd542ef7d11ca0e5eb136ef46d8bdaa26d331c6fea72c70e6923a773cc07d2c5`

~~~~text
<!-- BEGIN ARCHIVED NOTES -->
D9 Phase 5 最小真实闭环：Leader 发言与指令统一经 POST /api/messages，浏览器只提交稳定 msgId + display，服务端生成结构化 intent/payload。仅开头单一 @ROLE 在角色已启用且任务非 done/非 humanGate 时执行：Leader 消息 + set(nextRole) 在同任务串行区内经一次 TaskStateStore.commit 原子、幂等落盘，changed=true 后才投递；普通文本仅 chat。未知/禁用角色或非法状态返回 rejected；新需求/新决策/优先级变更 deferred→Phase 9，humanGate/objection 裁决 deferred→Phase 8，sub 群 deferred→Phase 6。退役绕过 State 的 /api/commands 临时 SSE 旁路；不得声称 Phase 5 已支持完整抢占/resume。
[2026-08-31 /agora-pr-merge] 依赖 5.3 已由 PR #43 合并并标 done（merge commit feebaec9），级联翻转 pending→ready。
[2026-08-31 /agora-sync-docs·规格评审] Leader 批准先评审并修订规格。确认旧范围与现有 Phase 5 State/端口及 Phase 6/8/9 能力边界不匹配，新增 D9 并按蓝图→详细设计→架构/选型→开发计划→task-status→AGENTS 顺序同步。任务保持 ready，待修订后实现计划再次确认。
[2026-08-31 /agora-do-task] Leader 确认 D9 修订后的实现计划；依赖 5.3=done、Phase 4 出口 4.6=done，跨阶段门禁满足。任务置为 in_progress，分支 feat/leader-intent，按 intent parser→原子消息动作提交→HTTP/UI 单入口→G5 实链的 TDD 单元执行。
[2026-08-31 Code·TDD 红→绿] 新增服务端 intent parser/planner，覆盖 chat、开头单一 @ROLE、未知/禁用角色、done/humanGate 拒绝及 Phase 6/8/9 deferred；MessageService 增加按任务串行的 planned commit，Leader 消息与动作 mutations 一次提交，重复 msgId 不重算、不重放动作、不重复投递；浏览器 payload 被忽略并由服务端生成，/api/commands 返回 410。发现单写 nextRole 会被原 Coordinator phase 路由覆盖，补充 D9 规格与红灯测试后实现最新 applied assignment 的 sourceMsgId 单次确认、定向派发及 Ledger instruction 传递。
[2026-08-31 Check+Verify] pnpm typecheck exit=0；Biome 163 files 0 违规；Next production build exit=0（首页 3.84 kB、First Load JS 106 kB）；全量回归在宿主权限下通过，真实 Docker 双套件另行定向通过。任务专属 intent 14/14、routes 9/9、message service 7/7、message flow 4/4、Coordinator 52/52。G5 隔离数据根 /private/tmp/agora-5.4-g5.qtF9tk 实测生产 POST→原子 State→SSE：客户端伪造 payload 未落盘，@ROLE 更新 nextRole，两条不同指派按序生效，重复 msgId 不重复消息/动作/投递，deferred 返回目标阶段；确定性跨链测试进一步验证 @REVIEWER→持久 State→Coordinator 定向派发→Ledger instruction + sourceMsgId 确认。git diff --check 与 JSON 校验通过。任务保持 in_progress，待 $agora-commit 建 PR，人工合并后由 $agora-pr-merge 标 done。
[2026-09-01 /agora-commit·交付门禁] 分支 feat/leader-intent（base=dev-1.0.0）。typecheck exit=0；Biome 163 files 0 违规；Next production build exit=0；全量 47 files passed / 2 skipped、460 passed / 2 skipped，真实 DockerSandbox 15/15。任务专属 86/86。隔离数据根 /private/tmp/agora-5.4-commit-g5.qAf0WX 的生产链验证：POST→原子 State→SSE 实时信封（无 payload）；两次有效指派最终 nextRole=TESTER；重复 msgId 返回 published=false 且不执行伪造重放；/channel 明确 deferred→Phase 6；客户端伪造 secret 未落盘。高置信敏感数据模式扫描无命中，git diff --check 与 JSON 校验通过。任务继续保持 in_progress，禁止 Agent 自动合并。
[2026-09-01 /agora-commit] 功能提交 ed74eb0；PR #44 已创建：https://github.com/logan-suu/Agora/pull/44（feat/leader-intent → dev-1.0.0）。PR 保持 open，任务保持 in_progress，待人工审阅合并后由 $agora-pr-merge 收尾。
[2026-09-01 /agora-pr-review·阻断修复] 对照详细设计 §5 规则 1/7 复现两项 G1 阻断：空格分隔的 @CODER @TESTER 被误识别为单角色指派；客户端只检查 response.ok 并吞掉 rejected/deferred action。TDD 红灯 3 项后最小修复：拒绝 instruction 中任何额外 @ mention；严格解析 LeaderActionStatus，applied/rejected/deferred 以可访问 role=status 提示展示，畸形响应进入错误路径。定向 94/94；typecheck 0 错；Biome 163 files 0 违规；Next production build 成功（首页 4.07 kB、First Load JS 107 kB）；全量 47 files passed / 2 skipped、462 passed / 2 skipped，真实 DockerSandbox 15/15。Codex in-app browser 在 localhost:3106 验证页面 Agora/Live、无框架错误层、console 0 error/warn：@UNKNOWN 显示 Command rejected，/channel 显示 deferred to Phase 6，提示位于输入框下且可读。React 最佳实践复核无新增 waterfall、重型依赖或不必要 effect。任务保持 in_progress，待修复提交推送并由人工合并。
[2026-09-01 /agora-pr-review·修复交付] 修复提交 8a54b05（Fix leader intent review findings）已完成，覆盖多角色 mention 拒绝与 LeaderActionStatus UI 可见反馈；门禁及浏览器证据见前条。准备推送至 PR #44；任务保持 in_progress，禁止 Agent 自动合并。
[2026-09-01 /agora-pr-merge 收尾] PR #44 已由人工合并至 dev-1.0.0（merge commit 79acb834fc27706533172934c935883d8d082535）；任务标 done。G1–G7 证据复核：D9 与蓝图/详细设计/架构/选型/开发计划同步且实现一致；typecheck 0 错；Biome 163 files 0 违规；Next production build 成功；全量 47 files passed / 2 skipped、462 passed / 2 skipped，真实 DockerSandbox 15/15；任务专属生产链完成 POST→原子 State→无 payload SSE、msgId 幂等与 Phase 6/8/9 deferred，Coordinator 以 sourceMsgId 单次消费并写 Ledger instruction；审查修复后浏览器验证 rejected/deferred 可见、无框架错误层且 console 0 error/warn；敏感扫描、JSON 与 git diff --check 通过。DEF-015 按既定 Phase 6 授权边界保持 open，无新增延期项。级联翻转：5.5、5.6 pending→ready；Phase 5 仍 in_progress，须完成 Demo 录屏与出口集成测试。
<!-- END ARCHIVED NOTES -->
~~~~
