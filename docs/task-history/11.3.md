# 11.3 实现 Electron 应用壳与本地服务生命周期

## 2026-09-14 开工与 Explore

- Leader在初始化确认下一任务为11.3后回复“开始”，授权任务开工。11.1、11.2及前阶段出口10.7在规范索引均为done；11.2历史中的旧blocked/pending记录不覆盖当前状态。
- 初始工作区干净，从dev-1.0.0建立feat/task-113-electron-lifecycle分支。尚未修改产品代码、安装依赖或执行模型请求；未commit/push。
- 已读取AGENTS.md、任务追踪维护规则及本任务documents_required：蓝图§21 D18/§22.3.2、详细设计§12.3、开发计划§18.2/§18.3/§18.11、选型§14及§12锁定版本。
- 已检查apps/web/scripts/local.mjs、local-process.mjs、src/server/local-startup.ts、instrumentation.ts、Next配置与11.2打包证据。既有custom server和钥匙串初始化可复用，但CLI入口耦合源码布局/全局依赖检查；Spike不是正式产品入口。未发现需要重新裁决的规格冲突。
- 控制原文：“Phase11只开放安装、环境检查与故障反馈，11.3必须同时提供预览界面及服务端能力门禁”；“主进程只调用受信生命周期和既有服务入口，L1–L3 的依赖与 Executor/TaskStateStore/SandboxManager 公开方法不变。”

## 待确认的实现计划

1. **桌面组合根与生命周期**：新增apps/desktop/package.json和src下main、preload、service-lifecycle、protocol、paths模块，使用Electron44.3.0与独立Node24.20.0。实现稳定userData/state根、单实例先取锁、受信绝对路径及允许项环境、经校验IPC、starting/ready/draining/stopped/failed状态。区分停止回执和进程退出，覆盖启动中退出、崩溃和IPC断开；主进程不读取模型密钥。
2. **正式服务接缝**：在apps/web/scripts中抽取可复用的custom server生命周期，保留既有CLI行为；新增桌面服务入口，复用local-process的规范控制路径/钥匙串适配和生产instrumentation。桌面固定新凭据service ID与独立state根，初始化/校验格式1后才ready；未知格式或未闭合升级明确拒绝，完整升级事务故障交付仍归11.4。
3. **认证及预览能力**：新增精确origin/capability、Host/Origin与跨站校验；Electron网络层认证注入覆盖HTTP/SSE/资源，重启销毁旧session。配置CSP/nonce、禁止外部导航/窗口/权限，限制preload sender/frame与消息schema。apps/web新增桌面预览页及服务端能力门禁，只展示安装、环境检查及故障恢复状态；正式任务/消息/模型执行入口不得绕过预览限制。复用现有UI样式及SSE通信基础，不启动Docker或模型任务。
4. **正式打包**：新增apps/desktop/scripts/build/package及资源审计模块，更新workspace构建入口和精确锁文件；按选型使用@electron/packager20.3.0，配置ASAR integrity及生产fuses。从固定源码输入构建Next/custom server生产资源与依赖闭包，审核trace和pnpm链接，拒绝.env/.data/.git、秘密、外部绝对链接和未知资源，保留许可证。开发验证记录基线和变更指纹；最终固定提交候选在授权提交后复核，不擅自commit。11.4继续负责完整工具链介质和签名升级，11.5负责目标平台干净安装出口。
5. **TDD与验收**：新增apps/desktop/test的协议、目录/环境、生命周期、认证与打包边界测试；为抽取的CLI接缝补回归；实现tests/integration/phase11/phase11-3.test.ts。逐单元先红后绿，再在隔离目录运行正式应用包、真实Node/Next/临时Keychain，覆盖全部启用API/资源、SSE、CSP/fuses/IPC、重复启动/竞争、启动中退出、崩溃/停服失败和凭据保留。故障fixture仅用于确定性单元测试，不替代真实G5。
6. **门禁与文档**：执行原生helper构建、pnpm typecheck、pnpm lint、完整pnpm test及适用桌面G5；通用真实模型测试固定OpenCode Go/deepseek-v4-flash，不修改原断言/期限/思考/容量，不skip或切换模型，不启动正式Benchmark。按实际改动同步详细设计/架构/选型/计划与证据；任务保持in_progress，提交及PR另按Leader授权执行。

### 开工检查点

- 当前仅完成Explore及计划，等待计划确认后进入Code。
- 正式Developer ID/公证、Intel和最低系统/干净Mac证据是后续对应任务的独立验收项，不把11.2开发机Spike或本任务本机测试视为已完成这些验收。

## 2026-09-14 计划确认与实现中

- Leader回复“确认”，批准上述实现计划；沿用同一范围授权。正式提交/PR/发布仍待明确请求。
- 已新增桌面协议、真实子进程生命周期、状态所有权与格式1、独立Next服务、精确认证/预览HTTP-SSE网关、Electron窗口/preload与预览页面。首批TDD均先记录缺模块红阶段再实现；桌面6文件12项测试通过，包括真实HTTP/SSE、Unix socket与子进程。
- 本机listen在工具沙箱返回EPERM；同一网关测试以正常权限执行通过，不放宽断言。桌面build及typecheck已通过一轮；继续按变更复验。
- Packager20.3.0不直接配置fuses，明确说明后按已批准fuses目标引入官方构建工具@electron/fuses2.1.3；Electron44.3.0/Packager20.3.0均精确锁定，fuses仅构建依赖，版本同步将在本任务完成。
- 正式打包脚本使用隔离源码快照及输入hash；未经提交授权的当前构建明确标development-working-tree，不冒充最终固定提交发布候选。初次干净Next生产编译通过，生产资源闭包组装失败，正在停止后续操作并定位原因；尚未完成正式应用G5。

## 2026-09-14 实现与研发验证收尾

- 已完成应用壳、有限preload/窗口隔离、独立Node/Next服务、状态/控制锁、格式1、固定新Keychain、认证/HTTP/SSE与预览双层门禁、生产打包及fuses。CLI接缝不再抽取，直接复用既有local-process及instrumentation；旧CLI/Harness/公开端口无修改。
- stop重入先红后绿；生产资源分类/未知资源拒绝先红后绿；最终桌面专项7文件17项通过。完整失败根因、修复及测试替身边界见[执行报告](../reviews/task113-desktop-lifecycle.md)，保留首次打包、CSP错误假设、trace开发文件混入及验证器退出清理的失败记录。
- 最终静态门禁：pnpm typecheck通过，pnpm lint通过（466文件）。完整pnpm test两轮通过：第一轮178文件1300项+Node7项；资源规则新增测试后最终178文件1301项+Node7项，116.58秒。真实通用模型为OpenCode Go/deepseek-v4-flash，未skip、切换提供方/模型、弱化期限/断言或运行正式Benchmark。
- 最终正式Packager包通过23项真实Next/Keychain/HTTP/SSE/CSP/IPC检查与9项生产主进程/真实双启动/关闭/重启/崩溃检查；专用临时Keychain已删除。已查看实际窗口截图。单元故障注入不冒充真实G5；安装默认用户Keychain与签名升级连续性仍归后续任务。
- 构建输入基线6c8a7888f5d6d6cccdab11bf9403a17e7d693ad0，333个输入与工作树hash零差异，development-working-tree；记录input/resource清单hash及36项明确排除候选。最终Resources含10163文件/323583839字节，生产资源不含开发源码/测试、禁入路径或外部链接；fuses与逐层ad-hoc签名验证通过。
- 已按agora-sync-docs同步蓝图§22.3.2、详设§12.3.8、架构§10、选型§12和开发计划§18.2；新增fuses2.1.3为构建工具，不改变常驻决策或红线。完整证据与截图见[results.json](../reviews/task113-evidence/results.json)及[执行报告](../reviews/task113-desktop-lifecycle.md)。
- 任务保持in_progress：未commit/push/建PR，待Leader明确提交授权后按agora-commit完成固定提交候选与PR；合并仍由人类执行。11.4/11.5工具链、升级、Intel/最低系统/干净安装及发布验收未提前标完成。当前无pending任务满足级联条件。
- 最终收尾复核：47个变更文件未发现配置中真实secret的正文匹配或敏感文件名；git diff --check、最终lint和任务索引check通过。检查过程不输出凭据值，原开发凭据保留。

## 2026-09-14 agora-commit交付授权

- Leader显式调用agora-commit，授权本任务功能分支commit/push及向dev-1.0.0创建英文PR；不授权自动合并或发布。
- 远端dev-1.0.0仍为6c8a7888f5d6d6cccdab11bf9403a17e7d693ad0，当前分支无已有PR。47个变更文件均属本任务，已逐项列清单；敏感文件/已配置secret正文匹配为零，既有开发凭据保持。
- 提交前重新执行typecheck、lint、diff检查及任务索引check均通过；已有正式包333项输入与当前代码hash无差异，23项真实服务/安全及9项主进程检查重新通过，临时Keychain均已清理。完整回归运行中，待结果通过后提交，再从该固定提交构建验证。

- 提交前完整回归通过：Node7项、Vitest178文件1301项，106.13秒；通用真实提供方/模型仍为opencode-go/deepseek-v4-flash。门禁全部通过后进入功能提交。
