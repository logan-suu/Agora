# 11.5 Phase 11 集成测试与发布验收：Electron 安装与受管工具链

## 2026-09-15 开工与 Explore

- Leader在初始化报告明确下一任务11.5后回复“开始”，授权启动本任务。已从dev-1.0.0的891bc93创建test/task-11-5-phase-exit；尚未确认下述新增测试计划，未写产品代码/测试，未commit/push。
- 类型exit；依赖10.7、11.1、11.2、11.3、11.4全部done，前阶段出口10.7已通过；无待级联任务。规格已读取蓝图§21 D18/D8、§22.3、详细设计§12.3、开发计划§18.2/§18.11及追踪维护规范。
- 现有phase11-3.test.ts覆盖状态所有权和认证预览HTTP/SSE，phase11-4.test.ts覆盖准备/升级中断回滚；尚无phase11-exit.test.ts。原生validate.cjs、validate-host.cjs、validate-tools.mjs和validate-media.mjs为已有真实验证入口，正式应用入口需另行实装，不能只用ready后载入组合根证明双击成功。
- 已追读11.4近期修复/安装/钥匙串/交付历史并定位11.3正式验证记录。11.4最新已记录Node7/7、Vitest187文件1334项通过；这是既有证据，不冒充11.5本次门禁。最终固定提交候选仍待构建与验证。
- DEF-018仍open：macOS15干净环境仅对11.4本轮获批暂缓，11.5复核；当前已记录安装/升级实测范围为Apple Silicon/macOS26.5。未将暂缓当通过，也未扩大豁免。当前磁盘可用约45GiB；未删除任何旧构建或用户数据。

## 待确认执行计划

1. 新增tests/integration/phase11/phase11-exit.test.ts，组合真实状态目录/唯一写者、受管工具完整性与预览HTTP/SSE，覆盖正常启动/停止、损坏组件拒绝、未闭合升级阻止就绪和恢复后配置保留；复用正式模块，不以空测试或测试数量替代出口。
2. 复核并运行已有真实Electron/Node/Next/受管工具验证器。基于固定提交构建arm64应用和ad-hoc DMG，记录输入/资源/介质hash；正式.app入口、单实例、退出、隔离安装和适用钥匙串升级证据单独核验。若本任务修改打包或产品代码，须在获授权提交后重建最终候选。
3. 执行helper构建、typecheck、lint、完整pnpm test及适用G5；通用真实模型固定OpenCode Go deepseek-v4-flash，保持凭据、断言、期限与全部回归。正式Benchmark不启动。
4. 新增docs/reviews/task115-phase11-exit.md及必要证据，按出口条件逐项写通过/未验证/延期，准备英文发布说明草稿；同步详细设计验证入口及任务历史/索引。
5. DEF-018不得自动关闭或豁免；先完成其他可执行验收，再依据实际环境/Leader决定处理剩余缺口。发布版本、commit/push、人工PR合并、tag/Release均不从本次开工推定授权。

## 当前检查点

已开工并完成初步Explore，等待新增测试及上述执行计划确认。未运行本次门禁，不宣称阶段通过或已发布。

## 2026-09-15 计划确认

- Leader回复“确认”，批准上述出口测试、候选构建、完整回归和证据整理计划。开始小步执行；commit/push/发布仍待明确授权，DEF-018不自动豁免。

## 2026-09-15 实施与本机验收完成

- 新增phase11-exit.test.ts的3项跨模块测试：真实状态锁/格式、完整性验证、HTTP/SSE关闭、拒绝未启用API、旧capability失效、升级未闭合拒绝和回滚后配置字节/目录身份保留。Next readiness与合成工具完整性夹具已在文件头声明，不用其替代真实G5。未修改产品代码或构建脚本。
- 首轮工具沙箱1通过/2失败；最小Unix socket探针确认listen EPERM。相同测试获本机监听权限后3/3通过，断言/时限未变；未将环境错误当产品TDD红阶段或故障修复。helper构建、typecheck、lint通过。
- 从固定提交891bc93420b6e7df0e18cae8d930c4d15394c61f构建arm64正式Packager包；354个生产/构建输入与当前工作树hash一致。使用已有官方下载缓存并重验锁定hash，未改变依赖版本。此候选未包含本轮尚未提交测试/文档，但这些不进入产品构建输入；最终发布版本或提交若改变输入须复验。
- 真实包验证：服务23项、主进程12项、工具15项、安装介质3项通过；隔离真实Keychain均清理，正常停服及进程exit=0核对。CSP/untrusted IPC负向断言与故障注入连接重置为预期，不误报成产品失败。
- CUA按实际.app路径启动候选，界面显示Local service Ready、钥匙串Available、工具verified与Project development Coming later；通过Agora菜单Quit后主进程57057/服务57114均消失。快捷键未导致退出时改用菜单并再次核验，未把按键动作本身当退出成功。
- DMG为317283884字节，SHA256 ff770dd6523d77116bf13a25272099e7c177c5a5df51671c52848e026e6860e0。Safari从本机loopback真实下载，hash匹配，quarantine保留；只读挂载并复制至/private/tmp/agora115-quarantine-hjdflp_a/installed/Agora.app，strict嵌套签名通过。实际运行被系统转位至AppTranslocation，CUA截图确认三项就绪，菜单Quit后无本次应用/服务残留。当前已有用户环境未出现新的系统允许弹窗；不宣称干净安装、GitHub下载或免提示。挂载卷与临时下载服务均已关闭。
- 下载复制辅助脚本首次因旧Python不支持hashlib.file_digest在挂载前失败，改流式SHA-256后完成；CUA键入URL丢冒号后改粘贴准确URL，服务端GET和下载hash确认；读取原安装路径超时后按系统实际AppTranslocation路径确认成功。保留工具失败原因，不伪造产品缺陷。
- 复核11.4跨构建36项/独立锁定恢复19项Keychain证据；本候选keychain helper、service.js、local-process.mjs与原已验证新构建逐字节一致。复用原生denied/人类ACL授权/同密钥解密/锁定恢复事实；本轮原生候选和实际隔离应用另验证就绪。未更改用户钥匙串ACL、明文读取密钥或重置配置；原报告边界保持。
- 完整pnpm test退出0：Node7/7、Vitest188文件1337/1337、0失败/跳过、654.08秒。真实Go deepseek-v4-flash：LRU194270ms、Harness25008ms、频道摘要4885ms；保留原断言/期限/提供方，正式Benchmark未开启。
- G1/G6：详细设计§12.3.10同步验证入口，报告/英文发布说明及构建/安装/原生/日志hash保存在docs/reviews/task115-phase11-exit.md与task115-evidence/。G7：待交付文件gitleaks脱敏扫描0发现，生产打包禁入资源/外部链接审计通过。已有安装应用未覆盖，用户项目与全局工具未变；保留一套本次固定候选、DMG及临时安装证据，不删除其他项目/系统内容。
- DEF-018本轮复核：Leader明确“目前没有，先完成其余验收”。干净Apple Silicon macOS15仍未验证，未获得新的豁免，阶段出口不可宣称全部通过。DEF-016属Phase24、DEF-017属未重启Linux范围，保持原状态。11.5为末项且依赖覆盖同阶段全部任务，下一Phase12全部依赖11.5，未推进阶段。
- 只读GitHub Release列表为空；内部版本0.0.0，英文说明为草稿。没有commit/push、PR、tag或Release授权，尚未执行这些动作。下一步是处理干净目标系统验收及按授权交付新增测试/文档，再核验最终发布候选身份。11.5保持in_progress。

## 2026-09-15 当前验收例外与测试后下载清理授权

- Leader明确“当前无干净 macOS 15设备，先忽略这个验收；每次测试完要清理删除测试时下载的以节省磁盘空间”。当前11.5及限定预览候选不再以干净macOS15设备缺失阻塞；DEF-018保留未验证事实与后续补验条件，不伪记测试通过，不修改最低15.0构建目标，也不自动豁免后续全部阶段。此前无豁免/待设备记录为当时事实，保留。
- 按agora-sync-docs顺序同步蓝图§22.3.2/§21 TEST-CLEANUP、详细设计§12.3、架构、选型、计划§18.2/§18.11、延期台账/索引、追踪维护规范及AGENTS.md v2.22。新增测试仍按人工PR合并收尾，未commit/push或发布。
- 每轮测试后清理下载和派生临时构建/安装副本的常驻要求已记录；先保留结果/来源/版本/hash/必要日志，再确认进程/挂载/路径归属，只删除明确测试专用且停用的文件。正常项目依赖、全局共享缓存、已安装应用、用户数据/钥匙串不在范围。当前清理正在按精确清单归档核对，完成结果随后追加。

## 2026-09-15 清理完成

- 初次清理前权限自动审查超时，未执行删除；同范围重试获准。预检发现下载缓存另含校验和/公开包元数据后停止，检查并归档这些文件，再核对4个版本归档hash，未盲删未知文件。
- 删除前后核验14个精确目标：本轮构建、DMG/ZIP/安装副本、验证临时目录、Safari下载及3处已确认历史下载缓存。根目录身份和归档hash复核，进程/挂载/打开文件占用均0。14项全部删除，分配量7.2894 GiB，可用空间实测增加6.6575 GiB；差值受APFS共享块和同期系统活动影响，不把分配量冒充实际释放量。回执：docs/reviews/task115-evidence/cleanup/receipt.json。
- 固定候选/DMG已删除，来源/版本/输入清单/结果/hash保留；发布须重建复验。未删除正常项目依赖、共享全局缓存、已安装应用或产品状态/钥匙串。本轮后续仅文档与清理，不重跑已通过的付费完整回归；lint、索引及diff复核结果随最终检查记录。

- 清理后复查：pnpm lint通过（489文件），task-status check通过（171任务/31阶段/25决策/66历史），git diff --check通过；全部清理归档hash复核一致且14个目标路径不存在。gitleaks对task115-evidence全目录脱敏扫描4.28MB，0发现。没有新的代码/构建输入变化，复用先前完整回归结果；任务保持in_progress等待授权交付及人工合并。

## 2026-09-15 agora-commit 提交门禁

- Leader显式调用agora-commit，授权提交/推送/创建dev-1.0.0 PR，未授权自动合并或Release。远端基线891bc93与当前HEAD一致，无已有同分支PR。
- 提交前typecheck/lint通过。完整pnpm test退出1：Node7/7；Vitest187文件通过、1文件失败，1336通过/1失败/0跳过，568.03秒。LRU真实Go deepseek-v4-flash返回INVALID_REQUEST/HTTP400，要求思考模式回传reasoning_content；同提供方单轮Harness与频道摘要测试通过。暂停提交，不以此前通过记录覆盖本次失败。
- 提交G5重新下载锁定版本并从固定891bc93构建；当前354项输入与既有候选完全一致。真实工具15项及Electron/Next/隔离Keychain23项通过。测试结束后归档并删除本轮4项下载/构建/验证目录，可用空间增加2.50GiB。证据docs/reviews/task115-evidence/commit-gates/results.json与commit-cleanup/receipt.json。
- 诊断先核对三个方向：提供方流/请求异常、账户/限流、适配丢失字段。实际HTTP400排除本次超时/限流；发现官方序列化省略空reasoning_content，Go归一化只修改tool身份。正在进行一次原LRU测试复验，只记录请求/响应字段存在性与长度，不记录内容/密钥，不关闭思考或变更断言。

- 原LRU诊断复验再次失败（321.31秒测试/326.18秒总计，原600秒期限未改）。20次请求中前19次HTTP200，第20次HTTP400/INVALID_REQUEST，新增工具调用的reasoning_content缺失；此前部分缺字段请求被接受，因此字段省略与上游条件的精确因果仍inconclusive。非空思考字段保留已观察到。元数据仅含存在性/长度/计数/status，未记录提示、思考文本、工具参数或凭据。保留pass-through诊断脚本和元数据在commit-gates/，未修改产品/模型适配/测试断言，不再批量重试。
- 按agora-commit“Stop on any failure.”停止交付：没有commit、push或PR；不撤回用户已授予的交付授权，后续修复并通过门禁后可继续原范围。阶段不推进，干净macOS15验收例外保持。诊断运行已结束，原测试finally负责sandbox/runtime收尾；诊断脚本与临时日志在归档hash核对后清理。

## 2026-09-15 审查并修复Go字段回传

- Leader明确“审查并修复”，沿用已获交付授权。重新审查发现2026-09-12来源规则已要求非空思考原样回放、无思考补空reasoning_content；生产PiAi路径已配置原生compat，但9月14日通用测试切换的DeepSeekAdapter锁定版本在serializeAssistant里仅非空才输出字段。Go工具流归一化没有删除非空reasoning。
- 先新增确定性断言：真实Harness工具后续请求及官方stream/prepareCall空字段测试均红（3失败/1通过）。在tests/helpers增加异步作用域传输兼容，只补缺失assistant字段为空字符串，保持原生序列化其他字段、流/重试、headers和实际signal；不改依赖、产品模块、原测试断言/期限或模型。
- 初轮新增直接adapter测试还错误假定裸GenerateOptions必发送thinking=enabled；官方裸调用保留提供方默认且省略该字段。修正为与未包装官方请求完整比较（唯一差异为空reasoning_content），不会强行修改思考默认。确定性测试最终5/5通过，包含并发非本适配器隔离、实际取消signal连接及迭代器finally闭合；typecheck通过。原LRU实网验证进行中。
- 按agora-sync-docs同步蓝图§21、详细设计页首当前测试规则、选型兼容说明。既有2026-09-12 PiAi及历史HTTP400边界保留；此修复解决已证实的空字段协议缺口，不虚构已经解释所有上游间歇接受/拒绝条件。

- 修复后原LRU真实Go deepseek-v4-flash通过（280714ms测试、281.46秒总计），原断言及600秒期限未变。结果与红/绿记录保存于docs/reviews/task115-evidence/reasoning-fix/；本轮无安装包下载，原测试finally负责runtime/sandbox收尾，临时日志hash核对后删除。已恢复提交前完整pnpm test；完整结果待回收。

- 最终提交门禁通过：pnpm typecheck、pnpm lint（491文件）；完整pnpm test退出0，Node7/7、Vitest189文件1340/1340、0失败/跳过，591.80秒。修复后的全量LRU170814ms、真实Harness27492ms通过，频道摘要同样通过；正式Benchmark未启动。证据reasoning-fix/results.json保留代码hash及压缩日志双hash。
- G5生产输入354项仍与固定891bc93一致，复用本次交付已重建实跑的工具15项/原生23项及前面的主进程/隔离安装证据。大型清单和原始日志无损gzip归档，减少PR噪声且保留终端原始字节；原始尾部空行/回车导致的diff whitespace已通过压缩存储解决，不删除日志内容。归档索引compressed-artifacts.json，压缩/解压双hash复核。临时测试日志已核对后删除。
- 暂存区gitleaks扫描（含gzip内容）0发现，diff校验通过；当前执行已授权提交/推送/PR流程，任务仍in_progress，不自动合并或发布。
