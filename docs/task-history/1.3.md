# 任务 1.3：git-server（createWorktree/diff/merge 基础操作）

本文件保存历史证据；当前状态、依赖和时间戳只以 [任务索引](../task-status.json) 为准。原文中的 ready/in_progress/done、待合并和延期项状态描述记录时点，不覆盖当前索引或延期台账。后续事件追加到文末，不改写原文区。

## 2026-09-13 原 notes 无损迁移

Source commit: `fa3efafed328bdfeb5b0f575b4e57bbd248a45fd`
Original notes SHA-256: `bb762e8318c9198aac0fd333e569ce9a0b2d4ea35f288de17584976be8236ba2`

~~~~text
<!-- BEGIN ARCHIVED NOTES -->
simple-git 3.36.x（文档原写 8.x 为版本漂移，见 Issue #14 / docs PR #15 修正）；Phase 1 仅实现基础操作；merge 冲突检测留给 Phase 9 集成检查点完善。
[2026-08-27 /pr-merge-agora] 级联翻转 pending→ready（依赖 1.1 已 done，PR #12 合并 hash 369889d5）。
[2026-08-27 开工] 计划经用户确认（mainRepo+链接 worktree 模型；merge 遇 worktree 锁返回 ok:false+原因）。TDD 红→绿落地 packages/tools/git/。
[2026-08-27 Code+Check+Verify] 实现完成，门禁全绿。src/git-service.ts：GitService 接口 + WorktreeGitService（仅 simple-git 依赖，零 MCP import 可单测）——mainRepo 惰性 git init+初始空提交；createWorktree=git worktree add <path> -b <branch>（validateBranchName git-ref 安全校验）+自动注册 WorktreeRegistry；applyPatch=assertRegistered 复校→patch 暂存临时文件→git apply --whitespace=nowarn→add -A→commit→revparse HEAD；diff 缺省 diff HEAD 有 ref diff <ref>；merge=checkout base→merge，干净 {ok:true}，冲突/分支被 worktree checkout（锁）→{ok:false, conflicts} 不抛异常。src/git-server.ts：createGitServer 四工具 + serveGitServer(transport?) stdio 默认注入点（R9）。跨包复用 @agora/tools-fs WorktreeRegistry；simple-git 在 optionalDependencies（D5）。[G5 实测·无 mock] git-service.test.ts 11 例真实 temp+真实 git 二进制（真实 worktree/真实 commitId/15 非法分支名/未注册与 retargeted 拒绝/diff 两模式/干净 merge/被 checkout 返回 ok:false/真实冲突 {ok:false, conflicts:['conflict.txt']}）；git-server.test.ts 4 例 SDK InMemoryTransport round-trip（四工具真执行 + 未注册 isError）。门禁：typecheck exit=0；lint exit=0（92 files）；test 124 通过 2 跳过（2 跳为既有 G5 活链路）。
[交付] PR #16 (feat/git-server) + PR #15 (docs 修正 simple-git 版本，Issue #14)。任务保持 in_progress，PR 合并后 /pr-merge-agora 收尾。
[2026-08-27 /pr-merge-agora] PR #15 (docs 版本修正, merge commit 290fed0c) 与 PR #16 (feat/git-server, merge commit 29e22c76) 已按序合并，任务标 done。G5 实测结论：17 例真实 git 二进制执行全过（含 2 例新增守卫测试，126 passed/2 skipped 全量回归）；R7 加固两处（taskId 路径穿越、ref 选项注入）已在 PR 内修复，无遗留延期项。预审信息项：merge() 后主仓库停留 base 分支，Phase 1 基础操作无影响，留待 Phase 9 集成检查点确认。
<!-- END ARCHIVED NOTES -->
~~~~
