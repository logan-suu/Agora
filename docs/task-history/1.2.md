# 任务 1.2：test-server（运行测试、返回结构化结果）

本文件保存历史证据；当前状态、依赖和时间戳只以 [任务索引](../task-status.json) 为准。原文中的 ready/in_progress/done、待合并和延期项状态描述记录时点，不覆盖当前索引或延期台账。后续事件追加到文末，不改写原文区。

## 2026-09-13 原 notes 无损迁移

Source commit: `fa3efafed328bdfeb5b0f575b4e57bbd248a45fd`
Original notes SHA-256: `d80dde8aef74f10a217bba1934a3f202c3f36779088a74c61a7cc89661175666`

~~~~text
<!-- BEGIN ARCHIVED NOTES -->
run(cmd) → {passed,total,failed,failures[{test,message,file,line}],coverage}，对齐 State.testResults 结构。
[2026-08-27 /pr-merge-agora] 级联翻转 pending→ready（依赖 1.1 已 done，PR #12 合并 hash 369889d5）。
[2026-08-27 开工] 计划经用户确认（含 WorktreeRegistry 跨包复用点）。TDD 红→绿落地 packages/tools/test/。
[2026-08-27 Code+Check+Verify] 实现完成，门禁全绿。四源文件 + 三测试文件：① src/tap.ts 纯 TAP v13 解析器（无 I/O，R11 可单测）：解析 # tests/# pass/# fail 汇总（取最后一次=顶层聚合，兼容嵌套 subtest）与 # coverage N% 可选行；扫描任意缩进的 not ok 断言 + 其 YAML diagnostics 块（--- 开 ... 闭），提取 failures[{test,message,file,line}]——name 取 '- name' 后缀或回溯 # Subtest；message 取 error: |- 多行块（含 operator/expected/actual 兜底）；file/line 取 location:/at: 值（greedy 前缀保留路径内冒号，剥离 file:// 与前导引号）。② src/test-service.ts WorktreeTestService implements TestService：run(root,cmd,timeout?)——assertRegistered 复用 WorktreeRegistry.canonicalOf + realpathSync 复校（拒 unregistered/retargeted 根，对齐 fs-service 1.1 加固）；spawn cwd=canonicalRoot 默认超时 30s（R7）超时 SIGKILL；TAP 命中→passed=exitCode===0 && failed===0；非 TAP 回退 exitCode 判定且失败时给单条 (exit N) failure，绝不静默绿。③ src/test-server.ts createTestServer 注册 run 工具（worktree+cmd+timeout 可选，未注册根拒绝）+ serveTestServer(transport?) stdio 默认注入点（R9）。④ src/index.ts 导出。跨包依赖 @agora/tools-fs（workspace:*）复用 WorktreeRegistry（组合根共享实例，防任意目录跑命令）。TestRunResult 结构与 State.testResults 逐字段对齐（passed:boolean/total/failed/failures[]/coverage?）。[G5 实测·无 mock] test-service.test.ts 6 例真实临时 worktree + 真实 node --test 子进程：通过/失败(file/line/message 断言)/超时 SIGKILL/非 TAP exit 回退/未注册拒绝/cwd 正确；test-server.test.ts 3 例 SDK InMemoryTransport client-server round-trip（run 真执行 + 未注册 isError + timeout 参数生效）。tap.test.ts 8 例纯单测（真实 node --test 样本 fixture）。实测发现：macOS 上 node --test 的 location 报 realpath（/private/var/... 因 /var 为符号链接），测试期望用 realpathSync 对齐（非 parser bug）。[覆盖说明] coverage 仅在 TAP 出现 '# coverage N%' 行时填充，Phase 1 node --test 默认不产出故保持 undefined，属预期（字段 optional）。[门禁实测] pnpm typecheck exit=0；pnpm lint exit=0（Checked 88 files，biome --write 自动排序 import 后复检通过）；pnpm test 106 通过 2 跳过（2 跳为既有 G5 live-LLM 需 DEEPSEEK_API_KEY）。状态保持 in_progress，待 /commit-agora 建 PR 至 dev-1.0.0。
[2026-08-27 /commit-agora] PR #13 已创建：https://github.com/logan-suu/Agora/pull/13（feat/test-server-mcp → dev-1.0.0，提交 270258e，10 files / +807）。门禁复跑实测：tsc --noEmit exit=0、biome 88 文件 0 违规、vitest 16 files / 106 tests 全绿（2 skip=既有 live-LLM）。状态保持 in_progress，待人工合并后由 /pr-merge-agora 标 done。
[2026-08-27 PR#13 评审] CodeRabbit 三条评论逐条评估后均在当前 PR 修复（代码验证为真实缺陷，非误报）：① tap.ts 增 1..N plan 行作为 # tests 缺失时的 total 兜底（原实现 total=0 会落入非 TAP 回退丢失结构化 failures）——加 PLAN_RE + 回归测试；② collectDiagnosticsBlock 原跳过所有行直到 ---，会吞掉紧邻的无 diagnostics 的后续 not ok 记录——改为仅跳空行、非 --- 非空行原样保留供主循环处理——加回归测试；③ test-service runInDir 超时仅 SIGKILL shell 会孤儿化真实测试进程——改 detached 进程组 + POSIX kill(-pid, SIGKILL)（win32 回退 child.kill），加回归测试验证孙进程确实被击杀。另补 unquote/textResult/errorResult/timeoutResult 四个 JSDoc 满足 CodeRabbit docstring 覆盖率门禁。门禁复跑实测：tsc --noEmit exit=0、biome 88 文件 0 违规、vitest 18 files / 109 tests 全绿（2 skip=既有 live-LLM）。状态保持 in_progress，待人工合并后由 /pr-merge-agora 标 done。
[2026-08-27 /pr-merge-agora] PR #13 已合并（merge commit ed9601cfc934af5f5b3816730d9d92301b703a6b），任务标 done。G5 实测结论：test-service 6 例真实临时 worktree + 真实 node --test 子进程（通过/失败/超时 SIGKILL 进程组击杀/非 TAP 回退/未注册拒绝/cwd 正确）；test-server 3 例 InMemoryTransport client-server round-trip 真执行；tap 8 例纯单测——全程无 mock。门禁最终态：tsc 0 错 / biome 0 违规（88 files）/ vitest 18 files 109 通过 2 跳过（skip=既有 live-LLM）。延期项：无新增；DEF-004（TOCTOU）由 1.4 DockerSandbox 缓解。遗留：test-server 未接真实 Docker 沙箱，属 Phase 1 后续 1.4 范围。本地分支 feat/test-server-mcp 已删除。
<!-- END ARCHIVED NOTES -->
~~~~
