# 任务 0.7：Phase 0 出口集成测试

本文件保存历史证据；当前状态、依赖和时间戳只以 [任务索引](../task-status.json) 为准。原文中的 ready/in_progress/done、待合并和延期项状态描述记录时点，不覆盖当前索引或延期台账。后续事件追加到文末，不改写原文区。

## 2026-09-13 原 notes 无损迁移

Source commit: `fa3efafed328bdfeb5b0f575b4e57bbd248a45fd`
Original notes SHA-256: `e5c8fb438fbe30dd145b15fa6daf158567f568973b1f1a25e7b86457c9ac61fe`

~~~~text
<!-- BEGIN ARCHIVED NOTES -->
端到端回归：State 合并流转 / Harness loop 接入 / LocalTempSandbox 执行 / pre-step 投影覆写四条链路各有断言。出口通过 → 进入 Phase 1（MCP 工具 + Docker）。
[2026-08-27] 实现完成（EPCC-V Code+Check）。新增 tests/integration/phase0/phase0-exit.test.ts：唯一 mock 为外部 LLM（R11 注明），用脚本化 ScriptedLlmAdapter（按消息中 tool-result 计数决定下一个脚本动作）驱动真实 Harness agent loop + 真实 fs_write/sandbox_run 工具 + 真实 LocalTempSandbox（真实 child_process 跑 node --test，断言 exitCode=0）。四链路断言：① State 流转至 done（testResults.passed、subtask done、三角色 messages、iterationCount=0）；② 每角色 ≥2 次 LLM 调用且含工具执行；③ worktree 落盘 4 个真实文件 + node --test 真实子进程 exitCode=0（解析 sandbox_run tool-result 文本）；④ pre-step 覆写（首调只含投影 role/slices、无 channelId/fromRole、CODER 切片含 worktree 路径；后续调用保留 tool-call/tool-result，D1 校准）。组合根 createPhase0Runtime 增加可选 adapter/provider 注入点（测试侧装配，向后兼容，G5 真链路仍走 deepseek）。根 devDependencies 增加 @deepseek-ai/dsh-llm@0.1.1-rc.2（与 runtime-executor 锁版本一致，Harness 栈内）。验证：typecheck 0 错误 / lint 0 违规 / pnpm test 71 通过 2 跳过（G5 live-LLM 无 key 按设计 skip，已由 0.6 实测覆盖）。
[2026-08-27 G5 实测] 提供真实 DEEPSEEK_API_KEY 运行两条 live-LLM 链路全部通过：① packages/runtime/executor/test/g5-real-chain.test.ts（HarnessExecutor 真 DeepSeek 单回合，done StepResult + messages 突变，4.98s）；② packages/core/__tests__/e2e/lru-cache.test.ts（Coordinator→CODER→TESTER 全链路真 LLM 到 done，testResults.passed + sandbox 真实输出，156s）。Phase 0 出口验证基线「State 传递 + Harness 接入 + 投影覆写」三件事真链路成立，配合确定性四链路集成测试双保险。
[2026-08-27 提交] PR #11 https://github.com/logan-suu/Agora/pull/11（feat/phase0-exit-integration-test）。status 保持 in_progress，待人类审阅合并后 /pr-merge-agora 收尾标 done。
[2026-08-27 PR#11 评审] /pr-review-agora 预审 + CodeRabbit 两条评论逐条评估均合理，已当场修复：① createPhase0Runtime 增加 adapter/deepseek 互斥守卫（两者同传时假 adapter 会注册到 deepseek-official provider 名下静默遮蔽真 DeepSeek 路由，且 worktree 先建后失败会留悬挂资源）——守卫置于函数顶部任何资源创建之前；② 脚本化 LRU 测试套件补齐 TTL 过期用例（goal 原文含「带 TTL」，LRU_SOURCE 已实现 TTL 但 3 条测试均未覆盖），testResults.total 与 chain 1 断言 3→4；③ 按评论要求补三情形覆盖（adapter-only / deepseek-only / both-throws 各一测试）。门禁复跑：tsc 0 错误 / biome 0 违规 / vitest 74 通过 2 跳过（新增 3 例）。
[2026-08-27 /pr-merge-agora] PR #11 已合并（merge commit 7d56265554d2d86d1c1ad7df13d8058a99549f3e），本地 dev-1.0.0 已 fast-forward 同步，功能分支 feat/phase0-exit-integration-test 已删除。G5 实测结论（合并前已录）：真实 DEEPSEEK_API_KEY 下两条 live-LLM 链路全部通过（g5-real-chain 4.98s + lru-cache 全链路 156s），「State 传递 + Harness 接入 + 投影覆写」三件事真链路成立，确定性四链路集成测试（tests/integration/phase0/phase0-exit.test.ts）74 通过 2 跳过（skip=需 key 的 G5 用例，已实测覆盖）。无遗留延期项。
<!-- END ARCHIVED NOTES -->
~~~~
