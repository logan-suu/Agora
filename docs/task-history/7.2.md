# 任务 7.2：离职流程（强制交接文档）

本文件保存历史证据；当前状态、依赖和时间戳只以 [任务索引](../task-status.json) 为准。原文中的 ready/in_progress/done、待合并和延期项状态描述记录时点，不覆盖当前索引或延期台账。后续事件追加到文末，不改写原文区。

## 2026-09-13 原 notes 无损迁移

Source commit: `fa3efafed328bdfeb5b0f575b4e57bbd248a45fd`
Original notes SHA-256: `9229af9dd5c23103e5c5c51868db0c31a5a5f173372406739aed636f18e86471`

~~~~text
<!-- BEGIN ARCHIVED NOTES -->
D12：removeRole 是稳定 actionId 驱动的可恢复离职 saga，而非数组删除。顺序固定为 departing CAS→目标 Worker 当前 step/end drain→从已提交 State 确定性生成 HandoffPacket 并提交责任结果→departed CAS。普通工作只允许 enabled，departing 仅允许离职服务完成受限 drain；Phase 7 不抢跑全局 Preemptor/terminate/fork/resume。存在未完成责任且无 enabled 接任者时，subtask 标 blocked、写 humanGate 并保持 awaiting_replacement，绝不先 departed 或静默丢责；完成后保留最小身份、离职阶段与 handoffRef 供历史恢复/审计。
[2026-09-03 /agora-do-task] Leader 批准启动 7.2；依赖 7.1=done、Phase 6 出口 6.5=done、current_phase=7，跨阶段门禁满足。任务置 in_progress，进入 Explore/Plan；计划确认前不写测试或生产代码。
[2026-09-03 /agora-read-spec→agora-sync-docs] Leader 确认先评审而非机械执行旧规格。复评发现 Phase 7 直接调用 Phase 9 Preemptor、departing 与 enabled-only 执行、暂停后交接主体、空缺即 departed、跨存储恢复及 HandoffPacket 描述六处不一致；已批准按蓝图→详细设计→架构/选型→开发计划→task-status/AGENTS 修订为目标角色 drain + 确定性交接 + 可恢复 saga。
[2026-09-03 implementation] 已按确认计划完成 RoleDeparture 领域状态机、目标 Worker step/end drain、确定性 HandoffPacket/稳定 handoff Message、责任转移或 awaiting_replacement 阻断、D9 /role remove Web 接入及跨存储重试恢复。实现中发现 departing 后 main participants 已移除目标角色会阻断当前在途 Step 提交，已同步规格并收敛为仅绑定活动 handle/role 的 commitWorkerStepMutations 例外；新 Step、普通发言、其他角色与额外 Channel 副作用仍拒绝。G5：tests/integration/phase7/phase7-role-departure.test.ts 使用真实 HarnessExecutor、WorkerRuntime、HTTP、JSON stores、MessageService 与 collaboration CAS，验证在途 CODER Step 先持久化并保存安全点，再转移给 TESTER 并进入 departed；仅外部计费 LLM stream 使用脚本适配器。故障注入验证最终 CAS 失败后同 actionId 从 draining 继续，drain/message/packet 均不重复且沿用首次 requestedTs。
[2026-09-03 /agora-commit] 已创建功能提交 40f7963（Implement recoverable role departure）并推送 feat/role-departure-saga；PR https://github.com/logan-suu/Agora/pull/54，base=dev-1.0.0。交付门禁：typecheck 通过、lint 通过、全量 test 76 files/643 tests 通过、7.2 专属 G5 通过、敏感信息模式扫描通过、git diff --check 通过。任务按合并流保持 in_progress，待人类合并 PR 后由 $agora-pr-merge 标 done。
[2026-09-03 /agora-pr-review·规格复评与修复] 先复评 D9/D12：保留可恢复 saga、目标 drain 与完成前责任校验的总体设计，补齐派生 handoff id 冲突 fail-closed、已有 Leader Message 不得短路未完成 saga、saga 启动后中断不得写伪终态 rejected 三项明确不变式，已按蓝图→详细设计→架构/选型→开发计划→standing decisions 同步，同时修正 Markdown MD038/MD028。TDD 红灯复现内部 handoff msgId 碰撞伪 applied、已有 Leader Message 短路恢复与接任者失效后重放问题；修复为完整验证规范 handoff Message+packet+责任后置条件、从持久 departure 参数续办、启动后失败 fail-fast，并收紧 Worker 完成态/active drain 边界。采纳 CodeRabbit 三条 inline 及两条测试 nit；补充 remove_role 六个拒绝分支、生产 context 接线、ID 冲突、断点续办、中断重试和终态重放回归。门禁：typecheck 0 错；Biome 218 files 0 违规；定向 6 files/75 tests 全绿；除既有非确定 Phase 0 live LRU 用例外的全量 75 files/646 tests 全绿，包含真实 DeepSeek、DockerSandbox 15/15、Web Docker/Harness/MCP 与 7.2 真实 Harness/JSON/HTTP 链。原样 `pnpm test` 两次均只失败既有 live LRU：首次模型返回非法 HandoffPacket，次次模型未产生 TS 文件；领域校验与断言均未放宽，本次改动不触及该模型输出路径。任务保持 in_progress，PR 禁止自动合并。
[2026-09-03 /agora-pr-merge 收尾] PR #54 已由人工合并至 dev-1.0.0（merge commit 0533b00f12c8892c7138ae6b35b1b9658842c500）；任务标 done。G1–G7 证据复核：D9/D12 已先规格复评并同步蓝图、详细设计、架构、选型、开发计划与 standing decisions；typecheck 0 错；Biome 218 files 0 违规；定向 6 files/75 tests 全绿；在合并后的最终代码上原样 `pnpm test` 76 files/647 tests 全绿，包含 live DeepSeek LRU、DockerSandbox 15/15、Web Docker/Harness/MCP 与 7.2 真实 Harness/JSON/HTTP 链；gitleaks、tracking JSON 与 git diff 检查通过，无新增延期项，DEF-016 保持 open 至 Phase 10。级联复核：7.3 已为 ready；7.4 仍等待 7.3，因此保持 pending；Phase 7 保持 in_progress，current_phase 保持 7。
<!-- END ARCHIVED NOTES -->
~~~~
